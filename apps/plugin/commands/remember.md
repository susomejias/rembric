---
description: Save text as a project memory in Rembric.
---

Call `memory.save({type: 'project', title: <concise ≤100-char headline of the text>, content: '$ARGUMENTS'})` and report the new memory id.
