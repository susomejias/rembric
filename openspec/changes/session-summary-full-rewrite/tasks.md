# Tasks

Execution order matters: phase 0 establishes the branch this change lands on, and every later phase assumes the rescued commits are already in it.

## 0. Dismantle the abandoned line of work, and rescue what is independent of it

- [x] 0.1 Cut a NEW branch from `origin/main`. Do NOT continue on `feat/merge-curated-session-summaries`, and do not merge or revert it commit-by-commit — it stays as an unmerged, unpushed reference.
- [x] 0.2 `git rm -r openspec/changes/merge-curated-session-summaries/` — **deleted, not archived** (design D11). Before deleting, re-run the check that made deletion free and paste the output into the commit body: `grep -rn "MUST merge into the stored summary\|newest-first\|replaceStoredSummary\|canonical layout" openspec/specs/` must return zero matches. If it returns anything, STOP: something was published and the disposal is no longer a deletion.
  - **Deviation:** this was a no-op, not a `git rm`. `openspec/changes/merge-curated-session-summaries/` does not exist on `origin/main` — it was added by a commit on the abandoned `feat/merge-curated-session-summaries` branch, so cutting the branch from `origin/main` (0.1) means the folder was never present to delete. `git rm -r` would have failed with "pathspec did not match any files". The check itself was run: `grep -rn "MUST merge into the stored summary\|newest-first\|replaceStoredSummary\|canonical layout" openspec/specs/` → 0 matches. Design D11's conclusion (delete, don't archive) still holds; only its execution step was a non-action.
- [x] 0.3 Rescue the spec gates: `git cherry-pick cde976a` (`scripts/check-delta-sections.mjs` + `scripts/check-delta-sections.test.ts` + `scripts/check-spec-crossrefs.mjs` + the two `package.json` scripts + the CI wiring). Verify `pnpm run check:delta-sections` and `pnpm run check:spec-crossrefs` run.
- [x] 0.4 Rescue the fixture `TMPDIR` change: `git cherry-pick 7085210` (`apps/server/vitest.config.ts`).
- [x] 0.5 Rescue the end-of-turn silence fix: `git cherry-pick 1eab896` (removes `rembric_facts_show_summary_written` from `_transcript.sh` and its call site in `stop-nudge.sh`). Its commit message argues from the merge design — reword it to cite this change's D4/D12 instead.
  - **Deviation:** this commit was cherry-picked, found coupled, and REVERTED — its content is NOT on this branch and moves to phase 5 instead. `1eab896` does two things in one diff: it removes the silence mechanism (`rembric_facts_show_summary_written`) AND rewrites the end-of-turn rubric text. The two are inseparable because the rubric text on `main` asserts "this turn is done and the session has no curated summary" — a claim that is only true _while the silence mechanism exists_ to suppress the reminder on an already-curated session. Removing the silence without rewriting the text publishes a false claim; rewriting the text without removing the silence leaves the bug (the reminder still goes silent and freezes a premature first write forever). After cherry-picking and reverting, `apps/plugin/scripts/stop-nudge.sh` and `apps/plugin/scripts/_transcript.sh` are confirmed byte-identical to `origin/main` (`diff <(git show origin/main:<path>) <path>` — empty both). **Phase 5 must do both halves together**: remove `rembric_facts_show_summary_written` from `_transcript.sh` and its call site in `stop-nudge.sh`, AND rewrite the end-of-turn rubric so it no longer claims the session has no curated summary — done as one task, not two, or the same coupling bug recurs.
- [x] 0.6 Rescue `resumedRead`: `git cherry-pick aa574ff 39f62cc`. `39f62cc` only touched delta files under the deleted folder, so take its spec content from THIS change's `specs/plugin-session-protocol/spec.md` (already re-homed) and drop the rest of that commit's spec edits.
  - **Deviation:** `39f62cc` was NOT cherry-picked, per its own instruction — it touched only delta files under the (absent, see 0.2) merge-curated-session-summaries folder, and its spec content is already re-homed in this change's `specs/plugin-session-protocol/spec.md`. Only `aa574ff` landed (as `a21d6cb`).
- [x] 0.7 **The rescue trap.** `aa574ff`'s hunks carry context lines describing the merge design. After the cherry-picks, `git diff origin/main -- apps/plugin/test/nudge-fixtures.json` must show ONLY the two `resumedRead` keys added: `summary` and `summaryCore` must be byte-identical to `origin/main` (measured 259 and 250 bytes). Assert it: `node -e 'const a=require("child_process").execSync("git show origin/main:apps/plugin/test/nudge-fixtures.json","utf8"),b=require("fs").readFileSync("apps/plugin/test/nudge-fixtures.json","utf8");const A=JSON.parse(a),B=JSON.parse(b);for(const k of ["summary","summaryCore"])if(A[k]!==B[k])throw new Error(k+" drifted")'`.
  - **Deviation — the assertion as written is too narrow and missed the actual contamination.** It checks only `nudge-fixtures.json`; the merge-design language actually leaked into TWO OTHER files: `apps/plugin/test/nudge-fixtures.test.ts` and `apps/plugin/scripts/stop-nudge.sh` (the latter via the `1eab896` cherry-pick in 0.5, since reverted). Both are fixed (commit `0c61f5b` and the 0.5 revert). **Task 0.7's assertion should be widened to cover any tracked file, not just the JSON**, e.g. `grep -rln "Merges into the stored summary\|add what.s new\|keeps what you omit" apps/ scripts/` — this is the check that actually catches the contamination; the narrow JSON-only byte-check does not. Confirmed now: `grep -rn "add what.s new\|Merges into the stored summary\|keeps what you omit\|replaceStoredSummary\|summary_at_capacity" apps/ scripts/` → 0 matches. The JSON byte-identity check also passes: `summary` 259 bytes, `summaryCore` 250 bytes, both matching `origin/main`.
- [x] 0.8 Confirm no merge-describing clause survives anywhere: `grep -rn "add what.s new\|Merges into the stored summary\|keeps what you omit\|replaceStoredSummary\|summary_at_capacity" apps/ openspec/` must return zero matches after phase 5 rewrites the text. Run it now to see what phase 5 must remove, and again at the end of phase 5 to see it empty. — Ran now (widened to `apps/` + `scripts/`, since that is where the 0.7 contamination actually was): **0 matches**, confirmed clean already at phase 0 thanks to the 0.5 revert and the `0c61f5b` fix. Re-run at the end of phase 5 for the record.
- [x] 0.9 Verify the pinned-surface enumeration BEFORE editing text, so phase 5 works from the real list rather than a remembered one: read `apps/server/src/test/invariants.test.ts::"the session-summary rubric has one source"` and confirm the `surfaces` array is exactly `apps/server/src/mcp/instructions.ts`, `apps/server/src/mcp/server.ts`, `apps/plugin/scripts/prompt-nudge.sh`, `apps/plugin/scripts/stop-nudge.sh`, `apps/plugin/scripts/post-compact.sh`, `apps/plugin/commands/summary.md`, `apps/plugin/bin/rembric-plugin-core.mjs`, `apps/plugin/.hermes-plugin/__init__.py`. The `it('the enumeration above is complete')` case derives the same set from `git grep`, so a surface missed in phase 5 fails the build rather than shipping. — Confirmed: `surfaces` array (line 1312 of `invariants.test.ts`) matches exactly, in this order.

Additional note (not a numbered deviation): `39f62cc` was correctly left un-rescued per 0.6 — it only touched delta files under the deleted folder and its spec content is already re-homed in `specs/plugin-session-protocol/spec.md` of this change.

## 1. Schema and migration

- [x] 1.1 Add `sessionSummaryVersions` to `apps/server/src/db/schema/agent-sessions.ts`: `id` TEXT PK, `sessionId` TEXT NOT NULL referencing `agentSessions.id` with `onDelete: 'cascade'`, `version` INTEGER NOT NULL, `content` TEXT NOT NULL, `createdAt` timestamp_ms NOT NULL, plus `uniqueIndex('session_summary_versions_session_version_unq').on(table.sessionId, table.version)` — the repo's established pattern (`projects.ts`, `memory-relations.ts`), not a table-level `UNIQUE (…)`. Export `SessionSummaryVersion` / `NewSessionSummaryVersion` from `$inferSelect` / `$inferInsert` — never hand-write the row shape.
- [x] 1.2 No index beyond that unique one (design D6). If a later measurement justifies one, it comes with the measurement.
- [x] 1.3 Write `apps/server/src/db/migrations/0033_session_summary_versions.sql` — the DDL exactly as in `specs/persistence/spec.md` (`CREATE TABLE` + the named `CREATE UNIQUE INDEX`), no `ALTER TABLE`, no backfill, no `CHECK` on `content` length, no pragma (the runner owns those).
- [x] 1.4 Add `'session_summary_versions'` to `SOURCE_TABLES` in `apps/server/src/test/schema-inventory.ts` (one edit serves both `invariants.test.ts`'s source/derived partition and `schema-drift.test.ts`'s table set).
- [x] 1.5 Add the index to `EXPECTED_INDEXES` in `apps/server/src/test/schema-drift.test.ts` with its exact `CREATE UNIQUE INDEX …` SQL as SQLite reports it — that list is compared verbatim, so a mismatch between the migration and the drizzle schema surfaces there. (Also added `sqlite_autoindex_session_summary_versions_1` for the TEXT primary key, matching the pattern every other TEXT-PK table already has in this list.)
- [x] 1.6 Confirm the drizzle barrel picks the table up — it re-exports `./agent-sessions.js`, so no barrel edit should be needed; verify rather than assume. Confirmed: `apps/server/src/db/schema/index.ts` has `export * from './agent-sessions.js'` already; `tsc --noEmit` and `schema-drift.test.ts` both green with no barrel edit.

## 2. Repository (`db/repositories/agent-sessions-repository.ts`)

- [x] 2.1 `insertSummaryVersion(values: NewSessionSummaryVersion): void` — Drizzle builder, no raw SQL.
- [x] 2.2 `latestSummaryVersion(sessionId: string): SessionSummaryVersion | undefined` — `ORDER BY version DESC LIMIT 1`, used for both the next version number and the identical-content comparison, so one read serves both and they cannot disagree.
- [x] 2.3 `adminListSummaryVersions(sessionId: string): SessionSummaryVersion[]` — newest first, for the dashboard. The `admin` prefix is required because the read takes no `Scope`; the only call site is `src/dashboard/sessions.ts`, which is inside the confinement gate's default allowance.
- [x] 2.4 Do NOT open a transaction in the repository (services own transaction boundaries).

## 3. Service (`services/agent-sessions.ts`)

- [x] 3.1 Extract the single place that applies precedence and issues the update, shared by all three write paths (`writeSummary`'s active branch, `end`'s active branch, `writeTerminalFields`). Today `precedenceSet` computes the set and each branch issues its own `updateById`; the version append must live with the update, so the update moves into that one place. `resume` keeps composing its own `set` and does NOT route through it. — Implemented as the new private `updateAndVersion(id, set, opts)`; `resume` untouched, still calls `this.repos.agentSessions.updateById` directly.
- [x] 3.2 Wrap that place in `this.tx.transaction(...)`. Order inside the transaction: `updateById` first, then — only if it returned a row AND the set carried `summary` AND the incoming `final === true` — read `latestSummaryVersion`, compare `content`, and insert `{ id: ulid(now), sessionId, version: latest ? latest.version + 1 : 1, content, createdAt: now }`. — The "set carried `summary` AND incoming `final === true`" condition is implemented as `typeof set.summary === 'string' && set.summaryFinal === true`: `precedenceSet` only ever puts `summaryFinal` into `set` alongside `summary`, and its value is exactly the incoming write's `final` flag, so this is equivalent without re-threading `input.final` through the shared site.
- [x] 3.3 The cap stays exactly where it is: `assertSummaryWithinCap` on the ARGUMENT, once. Do not add a second check on the stored value — under this design they are the same string (design D8). Untouched — still the sole call sites in `writeSummary`/`end`, before `updateAndVersion` is ever reached.
- [x] 3.4 No new `DomainError` code, no new response field. `sessionSummaryOutput` is unchanged. Confirmed by inspection — no new throw sites, no new `mcp/session-tools.ts` output field.
- [x] 3.5 Confirm by reading the resulting code that no path can write `sessions.summary` outside that one place, and that the enumeration in `openspec/specs/sessions/spec.md`'s cap requirement (`writeSummary`, `end`) is still exhaustive. Confirmed: the only `this.repos.agentSessions.updateById` call sites left in `agent-sessions.ts` are inside `updateAndVersion` itself, and in `resume`/`softDelete`/`undelete`/`markAbandoned` — none of which accept a `summary` in their `set`. `writeSummary` and `end` both route through `updateAndVersion`.

## 4. Server tests, and mutation verification for every new guard

Each guard below gets a test that NAMES it, and each mutation must redden that test. A mutation that reddens nothing is the finding — the guard is uncovered.

- [x] 4.1 `apps/server/src/services/agent-sessions.test.ts`: cover every scenario in `specs/sessions/spec.md`'s two ADDED requirements — first curated write appends `version = 1`; second appends `version = 2` and the displaced text is still readable; byte-identical re-write appends nothing; three `final: false` syncs append nothing; a blocked terminal write appends nothing; a first curated write over a raw body versions only the curated text; each rejection path (empty, whitespace, `NUL`, over cap, cross-token, soft-deleted) leaves zero rows. — All in the new `session_summary_versions` describe block.
- [x] 4.2 An invariant test asserting the storage invariant directly over the DB: for every session with ≥1 version row, `sessions.summary` equals the highest-`version` `content`. Drive it through a sequence of real service calls (curated, raw, curated, identical, terminal) rather than fixture inserts — the invariant is about the write paths. — `'the storage invariant holds after a realistic sequence of curated, raw, curated, identical and terminal writes'`.
- [x] 4.3 Atomicity test: force the concurrently-ended path (`updateById` with `requireActive: true` returns undefined) and assert `session_already_ended` is thrown AND zero version rows exist for that session. This is the test that makes the transaction and the write ordering falsifiable. — `'the atomicity guarantee: …'`, via `vi.spyOn(repos.agentSessions, 'updateById').mockReturnValue(undefined)`.
- [x] 4.4 Mutation — the append itself: deleting the `insertSummaryVersion(...)` call reddened 9 tests (`session_summary_versions` × 7, `purgeEmpty` × 2).
- [x] 4.5 Mutation — the `final: true` condition: widening `typeof set.summary === 'string' && set.summaryFinal === true` to drop the `summaryFinal === true` conjunct reddened 3 tests, including the two `final: false` scenarios named in 4.1 (raw syncs, curated-over-raw).
- [x] 4.6 Mutation — the identical-content skip, BOTH directions, run as two separate `mutate.mjs` invocations (the tool runs each `--mutation`/`--with` pair against the pristine file, so "always append" and "always skip" needed separate calls to attribute each to its own failing test): always-append (`if (true)`) reddened exactly the identical-re-write case + the storage-invariant test; always-skip (`if (false)`) reddened 9 tests including the second-write case.
- [x] 4.7 Mutation — the version numbering: replacing `latest ? latest.version + 1 : 1` with `1` reddened 4 tests (the second-write case, the storage invariant, the purge-with-cascade case, and a pre-existing `last-final-wins` test) via a UNIQUE-constraint violation on the second insert.
- [x] 4.8 Mutation — the transaction: removing `this.tx.transaction(...)` and moving the insert ahead of the update (done by hand with Edit/revert, since the multi-line reorder does not fit `mutate.mjs`'s single find/replace model) reddened exactly the atomicity test from 4.3, confirming it exercises the path it names.
- [x] 4.9 Mutation — the stored-value/version-row identity: mutating the inserted `content` to `set.summary.slice(0, -1)` reddened 6 tests including the exact-length case (`'the stored value and the version row are the same string, capped once'`).
- [x] 4.10 Add the append-only static-grep rules for the new table to `apps/server/src/test/invariants.test.ts` (`db.delete(sessionSummaryVersions)`, raw `DELETE FROM session_summary_versions`, `UPDATE … SET content|version|session_id` on it, both Drizzle-builder and raw-SQL forms). Verified each of the 4 rules with a positive control (a probe file under `apps/server/src/services/`, deleted immediately after each control ran): each rule flagged its offending line naming file and line number; none flagged nothing.
- [x] 4.11 Purge test: a session with version rows is not purge-eligible (its `summary` is non-NULL); and, forcing the state the predicate forbids, `purgeEmpty({ adminBypass: true })` deletes both the session and its versions with no `FOREIGN KEY constraint failed`, with `PRAGMA foreign_key_check` clean afterwards. — Two new tests in the `purgeEmpty` describe block.
- [x] 4.12 Mutation — the cascade: dropping `ON DELETE CASCADE` from the migration SQL (the schema.ts `onDelete: 'cascade'` metadata is Drizzle-side only and has no runtime effect against a raw-SQL-migrated table, so the mutation target is the migration file) reddened exactly the cascade purge test in 4.11 with `FOREIGN KEY constraint failed`.
- [x] 4.13 Migration test in the style of `migrations-0031.test.ts`: apply migrations to a populated fixture (sessions with curated summaries, memories, prompts, confirmations), assert the table exists and is EMPTY, assert every pre-existing row is byte-identical, assert `PRAGMA foreign_key_check` is clean. — New file `apps/server/src/db/migrations-0033.test.ts`, 4 tests.

**Deviation, discovered running the full suite after 4.13:** adding migration `0033` broke two PRE-EXISTING tests in `apps/server/src/db/migrations.test.ts` that hardcode the full applied-migrations list up through `0032_token_projects.sql` (lines ~609 and ~1105). Not a task-4 guard issue — those two `expect(result.applied).toEqual([...])` arrays now needed `'0033_session_summary_versions.sql'` appended. Fixed in the same commit as phase 4; this is the kind of collateral edit `tasks.md` doesn't call out anywhere but that adding any new migration always causes — worth a one-line mention in a future revision of task 1.3 or 8.1.

## 5. Model-facing text — one task per pinned surface

Every edit below is text. The HTTP protocol does not change. Measure after each edit; the numbers in design D13 are the basis.

- [x] 5.1 `apps/server/src/mcp/server.ts` — the `memory.session_summary` description. Add: the write REPLACES the stored summary; send the session's CURRENT COMPLETE state, concise; put the current state FIRST because `memory.context` shows only the beginning and only `memory.session_get` returns the whole; if you cannot see your earlier work, call `memory.session_get` first. Remove nothing else. Measure from a real `tools/list` response against `DESCRIPTION_MAX_LENGTH = 1900` (basis: 670 before the edit). — Measured 1003 chars (computed from the interpolated template; `mcp-integration.test.ts` asserts it against the live `tools/list` response and passes).
- [x] 5.2 `apps/server/src/mcp/instructions.ts` — add the clause to the `SUMMARIZE` line. Measure BOTH variants ≤1000 characters (basis: 916 unscoped / 902 path-scoped; a 68-character clause lands at 984). If it does not fit, reclaim prose from the block — do NOT raise the cap. — Measured 990 unscoped / 973 path-scoped (7-char slug `rembric`), both ≤1000; `instructions.test.ts` green.
- [x] 5.3 `apps/plugin/.hermes-plugin/__init__.py::system_prompt_block()` — mirror 5.2 byte-for-byte. `apps/plugin/.hermes-plugin/tests/test_system_prompt_block.py` is the pinning test; run it. — Verified byte-identical to `instructions.ts::BASE` by direct diff (no drift); pinning test green, 85/85 Hermes Python tests green.
- [x] 5.4 `apps/plugin/scripts/post-compact.sh` + the `postCompact` fixture in `apps/plugin/test/nudge-fixtures.json` — rewrite to read-then-rewrite: (1) `memory.session_get`, (2) `memory.session_summary` with the CURRENT full state, stating that the write replaces the stored value, (3) `memory.context`/`memory.search` if more is needed. Delete the "with the compact summary shown above" framing. Keep the `10000` substring. Measure ≤600 bytes UTF-8 (draft measured 530; basis before the edit 574). — Measured 560 bytes UTF-8, ≤600.
- [x] 5.5 `apps/plugin/scripts/stop-nudge.sh` — the `RUBRIC` heredoc and the `endOfTurnRubric` fixture: state that the write replaces the stored summary and ask for the current complete state, current first. Keep the reasons/evidence and unfinished-work sentences, and keep the extracted-facts framing. This surface has no published per-line cap; record the measured size in the commit body anyway. — Also removes `rembric_facts_show_summary_written "$RAW" && _emit_nothing` (the silence mechanism, deferred here from 0.5's revert) in the same commit as the text rewrite, per the 0.5 deviation note. Measured rubric line 1: 216 chars.
- [x] 5.6 `apps/plugin/commands/summary.md` — same clause; keep the `10000` mention required by `plugin-session-protocol`.
- [x] 5.7 `apps/plugin/scripts/prompt-nudge.sh` and `apps/plugin/bin/rembric-plugin-core.mjs` — **no text change to the `summary`/`saveCore`/`summaryCore` strings** (design D13: 259 bytes against a 260-byte published cap). Both keep the `resumedRead` line rescued in 0.6. Verify with 0.7's byte assertion. — Re-ran the 0.7 assertion: `summary` 259 bytes, `summaryCore` 250 bytes, both unchanged from `origin/main`.
- [x] 5.8 Re-run `grep -rn "add what.s new\|Merges into the stored summary\|keeps what you omit" apps/` and confirm zero matches. — 0 matches.
- [x] 5.9 Run `pnpm vitest run apps/plugin/test/nudge-fixtures.test.ts` and the rubric invariant (`pnpm vitest run apps/server/src/test/invariants.test.ts -t 'session-summary rubric'`) — both the per-surface list and the `git grep`-derived completeness case must be green. Stage the plugin files first: the completeness case reads TRACKED files only, so an unstaged new surface passes locally and fails at pre-push. — Staged, both green (55/55, 3/3).
- [x] 5.10 Run the Hermes Python tests (`apps/plugin/.hermes-plugin/tests/`) and `apps/plugin/test/resumed-read.test.ts`. — 85/85 and 17/17 green.

**Deviation, `stop-nudge.test.ts`:** one pre-existing test (`'is silent once the session has already called memory.session_summary'`) directly asserted the now-removed silence behavior. Flipped to assert the reminder now fires (per `plugin-session-protocol`'s "An already-curated session still gets the reminder, never silence"), renamed accordingly. Not in tasks.md as written, but required for 5.5 to not leave a contradicting test green for the wrong reason.

## 6. Dashboard

- [x] 6.1 `apps/server/src/dashboard/sessions.ts` — add the `SUMMARY HISTORY` section to the `/:id` detail view, below the existing `Summary` block: entries newest-first, each showing `version`, `formatTs(createdAt)`, the character count, and the full `content` inside a native disclosure element, rendered with the existing `mdBody` helper. — Titled "Summary History (N)" rather than the all-caps form, matching this file's existing `<h2>` casing convention (`Description`, `Summary`, `Memories`, `Prompts`).
- [x] 6.2 Empty state: render the section with an explicit "no summary versions recorded" line (`tblEmpty`-style), never hide it.
- [x] 6.3 No new route, no form, no `data-confirm` — there is no mutation to confirm.
- [x] 6.4 CSS: reuse existing tokens/classes. If a disclosure needs styling, it goes in `apps/server/src/dashboard/styles/` — never an inline `<style>`. — `.rbr-summary-history`/`.rbr-summary-version` added to `apps/server/src/dashboard/styles/views/sessions.css`, mirroring `.rbr-prompt-content`'s disclosure styling in `prompts.css`.
- [x] 6.5 Dashboard test: a session with three versions renders three entries in order `3, 2, 1` with the newest content equal to the `Summary` block; a session with none renders the empty line; no form targets a version row. — New file `apps/server/src/dashboard/session-summary-history.test.ts`, 4 tests (added a 4th covering 6.6 directly). **Found only by the full-suite run (phase 8.1), not by running this file in isolation earlier**: `prettier --write` (pre-commit) wrapped the `<summary>v${v.version} · …` template line, inserting a leading newline/indentation before `v3`/`v2`/`v1`. Harmless in a browser (whitespace collapses) but broke the exact-substring assertions `toContain('>v3 ·')` etc. — widened to `toMatch(/<summary>\s*v3 ·/)`. Lesson for a future author: an isolated `vitest run <file>` right after writing a test can still be green on a stale, pre-format version of the source it asserts against if the commit's `prettier --write` reformats that source afterward — re-run the specific test after the commit, not only before it.
- [x] 6.6 Verify timestamps go through `formatTs` (no `toISOString`/`toLocaleString` in the new template code). — Confirmed by inspection (`formatTs(v.createdAt)`) and by the 4th test asserting `data-rembric-ts` is present on the rendered version timestamp.

## 7. Dev seed

- [x] 7.1 `apps/server/src/scripts/seed-dev.ts` — give at least one seeded session two curated summary versions, so the dashboard section and the Docker smoke below have data. Keep the `--reset` gate untouched. — First entry of `endedSessions` gets a draft `writeSummary(final:true)` before its `end(...)`, so it carries two version rows.
- [x] 7.2 Confirm the seed's `DELETE FROM sessions` still works with the new child table (the cascade handles it) and that the invariants allow-list needs no new entry. — Added a regression test in `seed-dev.test.ts`: seeds, asserts `max(version) >= 2` and the version table is non-empty, resets, asserts `PRAGMA foreign_key_check` is clean and the table is repopulated by the fresh seed. `wipe()` gained no new `DELETE` statement; `invariants.test.ts` (95/95) confirms no allow-list edit was needed.

## 8. Verification

- [x] 8.1 `pnpm run typecheck` · `pnpm run lint` · `pnpm test`. — All three green: typecheck clean, lint clean (0 errors), full server suite (excluding `install.test.ts`) **146 files passed | 1 skipped, 2764 tests passed | 10 skipped, 0 failures**.
- [x] 8.2 Spec gates: `openspec validate session-summary-full-rewrite --strict`, `pnpm run check:delta-sections`, `pnpm run check:delta-freshness`, `pnpm run check:spec-crossrefs`. `pnpm run check:spec-provenance` compares `origin/main...HEAD` — expect it to pass because this change edits no published spec. — All green. `check:spec-crossrefs` reports a pre-existing, unrelated REVIEW item in `hermes-agent-plugin/spec.md` (exit 0, not caused by this change). `check:delta-freshness` reports a REVIEW (not a failure) on the `data-access` MODIFIED requirement body, which is exactly the deliberate rewrite this change makes. `check:spec-provenance` passed.
- [x] 8.3 **Docker smoke against pre-existing seeded data — DONE.** Ran by the operator in a later session (it was blocked when phase 8 first ran: Docker was forbidden for that session, which overrode the task). Production image built with `--target runtime`, started through the installer with `REMBRIC_NO_PULL=1`. Evidence: `healthz` → 200 and `dashboard` → 302; migration `0033` applied over an ALREADY POPULATED file, with the runner's three log stages in order (`[migrate] applying` → `checking foreign keys` → `committing`) and row counts identical before and after (memory=35, projects=20, sessions=11, tokens=3 — the non-empty counts are what make the "unchanged" claim non-vacuous); two curated MCP writes produced a 35-character `sessions.summary` and version rows `v2:35` / `v1:123`, so the newest version's content matches the column exactly as the invariant requires; the dashboard rendered `SUMMARY HISTORY (2)`.
- [x] 8.4 Measurements to record in the PR body, each with the instrument named: the `memory.session_summary` description length from a real `tools/list` response; both `instructions` variants' lengths; the `postCompact` fixture in UTF-8 bytes; the `summary`/`summaryCore` fixtures in UTF-8 bytes proving they are unchanged at 259/250. — Recorded in the phase-5 commit body and in the final report: description 1003/1900 (basis for the number: computed from the interpolated template; `mcp-integration.test.ts` measures the live `tools/list` response and asserts ≤1900); instructions 990 unscoped / 973 path-scoped (≤1000); postCompact 560 bytes (≤600); summary 259 bytes / summaryCore 250 bytes (unchanged).
- [x] 8.5 Confirm the mutation results from phase 4 are pasted into the PR body — which mutation reddened which named test. A guard without that line is not covered. — Recorded in the phase-4 commit body / tasks.md 4.4–4.9, 4.12, and repeated in the final report.

## 9. Post-implementation additions: bounded model read, dashboard cap, title versioning

Added after phase 8 shipped green (146 files / 2764 tests / 0 failures), per three owner decisions. Design D20–D22.

- [x] 9.1 Schema: add `title TEXT` (nullable) to `sessionSummaryVersions` in `apps/server/src/db/schema/agent-sessions.ts`. Amend migration `0033_session_summary_versions.sql` IN PLACE (do NOT add `0034`) — nothing on this branch is pushed, so `0033` has never shipped a release.
  - **Trap for the operator, not for this task:** the migration ledger (`_migrations`) indexes by filename. A database that already applied `0033` (the dev stack's `data-dev`, seeded before this phase) will be SKIPPED on next boot even though the table now lacks `title`, with the ledger reporting it applied. Do not touch `data-dev` from this session — the operator re-migrates it (drop the table + its ledger row, or reseed) separately. Tests use fresh temp dirs per run, so the suite is unaffected, and the migration COUNT is unchanged, so `migrations.test.ts`'s hardcoded applied-list stays valid.
- [x] 9.2 Repository (`agent-sessions-repository.ts`): `adminListSummaryVersions(sessionId, limit?)` (optional cap, unbounded when omitted — existing full-history callers keep working unchanged); `adminCountSummaryVersions(sessionId)`; new scoped `listSummaryVersionsInScope(sessionId, scope, limit)` joining to `sessions` to enforce `Scope` in the query.
- [x] 9.3 Service (`agent-sessions.ts`): `updateAndVersion` stores `title: updated.title` (the post-update value) on the appended row, never `set.title`/the write's argument. New `listSummaryVersions(sessionId, scope, limit)` delegating to the scoped repository read.
- [x] 9.4 MCP (`session-tools.ts`): `SESSION_GET_VERSIONS_MAX = 5`; `sessionGetSchema.limit` (`z.number().int().min(0).max(SESSION_GET_VERSIONS_MAX).optional()`, description disambiguates "versions returned", not summary length); `sessionGetOutput.versions` (optional array of `{version, title, content, createdAt}`); `handleSessionGet` omits the `versions` key entirely when `limit` is omitted or `0` (byte-identical response).
- [x] 9.5 `mcp/server.ts`: reword the `memory.session_get` description to name `limit`, disambiguate what it bounds, and mark the read exceptional. Measured 652 of 1900 chars (hand-computed from the interpolated template; also exercised live by `mcp-integration.test.ts`'s new `limit` tests, which read the description indirectly by calling the tool through `tools/list`-backed `connect()`).
- [x] 9.6 Dashboard (`dashboard/sessions.ts`): cap the `SUMMARY HISTORY` read at `SUMMARY_HISTORY_MAX = 20` (newest shown), fetch the total via `adminCountSummaryVersions`, render each version's OWN `title`, add a "showing N of TOTAL" note when capped.
- [x] 9.7 Spec deltas: `sessions` (schema table + `title` column, extended invariant with its documented scope-limit, `limit` on `memory.session_get`, new scenarios), `mcp-api` (new ADDED requirement for the `session_get` description obligations), `dashboard` (cap + per-version title), `persistence` (migration DDL shows `title`). `design.md` D20–D22, revising D6/D19 in place (not deleting them). `proposal.md` updated. The MODIFIED `memory.session_get` requirement in `sessions/spec.md` had to carry the FULL published body and all 3 published scenarios verbatim, not just the new paragraphs — `check:delta-freshness` caught the first draft (3 missing scenarios) and the fix is in this commit.
- [x] 9.8 Tests, service layer (`agent-sessions.test.ts`): a version stores the title in effect, not the call's own argument; the documented title-only-write divergence (no new version, stale title on the newest row); extended the existing "storage invariant" test to also assert title equality.
- [x] 9.9 Tests, repository (`agent-sessions-repository.test.ts`, new `describe('AgentSessionsRepository summary-version reads')`): `listSummaryVersionsInScope` returns nothing for a session outside the caller's scope, returns newest-first up to `limit` inside scope; `adminListSummaryVersions` honours an explicit cap and stays unbounded when omitted; `adminCountSummaryVersions` matches the true row count regardless of the cap.
- [x] 9.10 Tests, MCP (`mcp-integration.test.ts`): omitted/`0` `limit` byte-identical to pre-`limit` response; positive `limit` returns newest-first, full content, correct `title`; `limit` above `SESSION_GET_VERSIONS_MAX` rejected by schema, not clamped; cross-scope `limit` read still returns `not_found`. 4 new tests, all green.
- [x] 9.11 Tests, dashboard (`session-summary-history.test.ts`): a session with more than `SUMMARY_HISTORY_MAX` versions renders exactly the cap, newest ones, with a "showing N of TOTAL" note; each rendered entry shows ITS OWN title, not the session's current one (extended the existing 3-version test with distinct titles per version).
- [x] 9.12 Migration test (`migrations-0033.test.ts`): new test asserting the `title` column exists and is nullable (`PRAGMA table_info`), plus extended the "next curated write" test to assert the stored `title`.
- [x] 9.13 Mutation verification, `node scripts/mutate.mjs` (all 4 confirmed, one file each restored automatically):
  - **(a) in-effect title, not the argument** — `apps/server/src/services/agent-sessions.ts`, `title: updated.title,` → `title: set.title,`, spec `src/services/agent-sessions.test.ts`. CAUGHT by exactly 1: `'each version row stores the title in effect after the write, not merely what that call sent'`.
  - **(b) omitted/0 limit → no versions key** — `apps/server/src/mcp/session-tools.ts`, `const versions = args.limit` → `const versions = true`, spec `src/test/mcp-integration.test.ts -t session_get`. CAUGHT by exactly 2: the byte-identical test and the positive-`limit` test.
  - **(c) newest-first ordering of the scoped `limit` read** — `apps/server/src/db/repositories/agent-sessions-repository.ts`, `.orderBy(desc(sessionSummaryVersions.version))` (the `listSummaryVersionsInScope` occurrence, isolated via its unique `eq(agentSessions.projectId, scope.projectId)` context) → `.orderBy(sessionSummaryVersions.version)` (ascending), spec `src/test/mcp-integration.test.ts -t session_get`. CAUGHT by exactly 1: the positive-`limit` newest-first test (first attempt used `asc(...)`, which isn't imported in that file and produced a ReferenceError instead of a semantic mutation — redone as a plain ascending `orderBy` with no import needed).
  - **(d) dashboard cap** — `apps/server/src/dashboard/sessions.ts`, `const SUMMARY_HISTORY_MAX = 20;` → `const SUMMARY_HISTORY_MAX = 1000;`, spec `src/dashboard/session-summary-history.test.ts`. CAUGHT by exactly 1: `'caps the rendered versions at SUMMARY_HISTORY_MAX, newest first, and states how many are omitted'`.
- [x] 9.14 Re-run: `pnpm run typecheck` (clean) · `pnpm run lint` (clean) · full `pnpm vitest run --exclude '../../install.test.ts'` (**146 files passed | 1 skipped (147), 2777 tests passed | 10 skipped (2787)**, 0 failures — 13 more tests than the phase-8 baseline, 0 fewer); `openspec validate session-summary-full-rewrite --strict` (valid); `pnpm run check:delta-sections` (ok); `pnpm run check:delta-freshness` (ok — only the pre-existing `data-access` REVIEW, same as phase 8); `pnpm run check:spec-crossrefs` (ok — only the pre-existing unrelated `hermes-agent-plugin` REVIEW); `pnpm run check:spec-provenance` (ok).

## 10. The compaction surfaces the first nine phases missed

Opened after a four-arm e2e with real CLIs (design D23) and the audit it prompted. Everything here is model-facing text plus one new test enumeration: **no server code, no migration, no HTTP surface, no schema.** Design D24–D25.

- [ ] 10.1 `apps/plugin/test/nudge-fixtures.json` — add the unprefixed post-compaction key `postCompactCore`, equal to `postCompact` minus its leading `rembric: `, following the existing `save`/`saveCore` and `summary`/`summaryCore` pairs. Measured basis: `postCompact` is 560 UTF-8 bytes, so `postCompactCore` is 551 and satisfies the published ≤600 budget with the same margin. Do NOT change `postCompact`.
- [ ] 10.2 `apps/plugin/bin/rembric-plugin-core.mjs` — export the post-compaction protocol text (and declare it in `rembric-plugin-core.d.mts`, which is hand-written). ONE implementation: no JS/TS client may declare its own copy.
- [ ] 10.3 `apps/plugin/.opencode-plugin/plugin.ts` — replace the hand-written string in the `experimental.session.compacting` handler (currently `plugin.ts:244-252`) with the shared text plus the slug sentence. Constraints to preserve, each already published: exactly ONE string pushed onto `output.context`; the slug named when one was resolved; the final `memory.context` sentence last; `memory.session_get` appearing BEFORE the `memory.session_summary` directive. Delete `'with the content of the compacted summary above'` and `'This preserves what was accomplished before compaction. '`. Measure the assembled string with a 7-character slug (basis: 551 + 24 = 575 bytes).
- [ ] 10.4 opencode tests — update/extend the existing `experimental.session.compacting` coverage: still exactly one push; contains `memory.session_get`, `memory.session_summary`, `memory.context`, `10000` and the slug; `memory.session_get` precedes `memory.session_summary`; contains NO window framing (`compacted summary`, `summary above`, `This preserves`); no per-client copy of the protocol sentences in `plugin.ts`; the no-`sessionID` case still pushes the string and makes no HTTP request.
- [ ] 10.5 `apps/plugin/.hermes-plugin/__init__.py` — arm a post-compaction flag in `on_pre_compress` (the compaction event itself, NOT `on_turn_start`'s `remaining_tokens`, which only predicts one), and emit the shared post-compaction text once from the next `prefetch()`, wrapped in `<memory-hint>…</memory-hint>` per this provider's convention, as its own line independent of the save/summary lines. `on_pre_compress`'s return value stays `''` — its destination is the compressor prompt, not the agent. Where the `resumedRead` line and this directive would both fire on one turn, emit only this one. Reset the flag on session end and session switch, alongside the existing counters.
- [ ] 10.6 Hermes Python tests (`apps/plugin/.hermes-plugin/tests/`) — armed only after `on_pre_compress`, never from a low `remaining_tokens` alone; emitted exactly once and not repeated on later turns; `on_pre_compress` still returns `''`; supersedes `resumedRead` on a shared turn; cleared by session end and by session switch.
- [ ] 10.7 `apps/plugin/test/nudge-fixtures.test.ts` — add the JS/TS arm (shared core module, not any client file) and the Python arm for `postCompactCore`, matching how `summaryCore`/`saveCore` are already pinned.
- [ ] 10.8 **A SECOND completeness enumeration, for compaction-time protocol text**, in `apps/server/src/test/invariants.test.ts`. The existing `'the session-summary rubric has one source'` case derives its surface list from a `git grep` for the canonical section list, and this whole phase exists because `plugin.ts`'s block never carried that list and so was invisible to it (design D24). The new case SHALL enumerate the compaction surfaces explicitly and assert the enumeration complete from a `git grep` for the post-compaction text, so a sixth surface cannot be added silently. Note the same tracked-files-only caveat the existing case documents: stage the plugin files before running it.
- [ ] 10.9 `grep -rn "content of the compacted summary\|compacted summary above\|This preserves what was accomplished" apps/` → zero matches.
- [ ] 10.10 Mutation verification (`node scripts/mutate.mjs`) — a guard is not covered until its test fails without it. Four mutations, each expected to red a NAMED test, with the name recorded:
  - (a) restore the window framing in `plugin.ts` → the no-window-framing test must red.
  - (b) arm the Hermes flag from `on_turn_start`'s token floor instead of `on_pre_compress` → the "armed only after the compaction" test must red.
  - (c) make `on_pre_compress` return the directive instead of `''` → its test must red.
  - (d) drop `plugin.ts` from 10.8's enumeration → the completeness case must red.
- [ ] 10.11 Measurements to record in the PR body, instrument named for each: `postCompactCore` in UTF-8 bytes (expected 551, ≤600); the assembled opencode string with a 7-character slug (expected 575); the Hermes `<memory-hint>`-wrapped line; and a re-assertion that `postCompact`, `summary` and `summaryCore` are byte-identical to before this phase (560 / 259 / 250).
- [ ] 10.12 `pnpm run typecheck` · `pnpm run lint` · `pnpm test` · the Hermes Python tests; then `openspec validate session-summary-full-rewrite --strict`, `pnpm run check:delta-sections`, `pnpm run check:delta-freshness`, `pnpm run check:spec-crossrefs`, `pnpm run check:spec-provenance`.
- [ ] 10.13 **No new Docker smoke is required for this phase, and the reason is recorded rather than assumed:** it changes no server code, no migration and no HTTP surface, so there is nothing in it that a seeded-data smoke could exercise that the client-side tests do not. 8.3's smoke stands as the production evidence for the change as a whole. A phase that later touches the server re-earns the requirement.

## 11. Spec corrections (already-pushed delta) and two e2e-backed text improvements

Opened after re-measuring every published figure in the already-open PR against the real files. Four numbers in the delta were stale, one requirement was internally unsatisfiable, and a base-spec claim this branch's own earlier phase had invalidated was left uncorrected. Alongside those fixes, two text additions land, both backed by the same four-arm e2e (design D23): the stronger model lost `50rps` in BOTH arms by PARAPHRASING it, never by omitting a section, so the fix is a copy-literal instruction, not a stronger read-directive nudge (already measured useless — design D25 — and NOT touched here).

- [x] 11.1 `opencode-plugin`'s `Experimental.session.compacting handler` requirement carried three obligations that cannot all hold: the instruction SHALL end in a data-loss sentence, SHALL end in a `memory.context` sentence, and the protocol text SHALL come from the shared fixture at ≤600 bytes measured — `postCompactCore` (551 B) + the loss sentence (73 B, no separator) = 624 B, already over budget, before the slug sentence is even added. Resolved by DROPPING the data-loss-sentence obligation: the `memory.context` sentence is now the one and only required ending, and the shared fixture's existing `this REPLACES the stored value` clause is stated as already carrying the same warning. Also resolved, explicitly: the ≤600-byte budget binds `postCompactCore` alone, never the assembled per-connection string with the slug sentence appended — `SLUG_RE` allows a 64-character slug, and a slug past the low 30s of characters would put the assembled string over 600 bytes if the cap were (wrongly) read that way. Measured: `postCompactCore` 551 B; assembled with a 7-char slug 575-576 B depending on the exact join (both variants computed and recorded in the spec text); `SLUG_RE` confirmed at 64 chars (`apps/plugin/bin/rembric-dotenv.mjs:23`, `apps/server/src/services/projects.ts:26`).
- [x] 11.2 Four false measurements in `plugin-session-protocol/spec.md` corrected against real files, re-measured today: "the rewritten block measures 530 bytes" → **560 bytes** (`postCompact` fixture, UTF-8, measured live); "the tool description (670 of 1900 characters)" → **1003 of 1900** (real `tools/list`-equivalent template length, after this change's own additions already landed); "`initialize.instructions` (916 of 1000)" → **990 of 1000** — 10 characters of headroom, not 84; a new paragraph explicitly says this margin is NOT an invitation to add more prose there. The fourth false figure — "measured 552 bytes = 138.0 tokens" in the base spec's (untouched-by-any-delta) "Plugin-injected protocol nudges MUST surface the summary length cap" requirement — predates this branch (already wrong on `origin/main`: real values are 574 B pre-rewrite, 560 B post-rewrite, neither 552) and was invalidated further by this change's own earlier phase 5 rewrite of the same block. Fixed via a NEW `## MODIFIED Requirements` entry for that requirement (full body + all 4 scenarios carried verbatim, one new paragraph appended) rather than a hand-edit of the archived base spec, on the same disposal `check-delta-freshness` expects. `openspec/changes/.../proposal.md` and `design.md` D13 carried the same stale `530 bytes` figure (their `670`/`916` mentions are explicitly framed as the pre-edit `origin/main` baseline that justified the surface choice, and are left as-is); both corrected to note the shipped `560`.
- [x] 11.3 `claude-code-plugin` gets its first delta in this change: a `## MODIFIED Requirements` entry splitting the token-budget table's single `Stop | 0 tokens` row into its two `hooks.json` handlers — `stop-sync.sh` (0 tokens, unchanged) and `stop-nudge.sh` (no fixed cap) — and narrowing the "A side-effect hook emits nothing to the model" scenario's `WHEN` clause to name the `stop-sync.sh` handler specifically (title kept verbatim; only the body narrows). A new scenario, "The Stop reminder handler is exempt from the zero-output cap", states `stop-nudge.sh`'s actual budget: none, matching `plugin-session-protocol`'s own "has no length budget, unlike a tool description" line for the same handler, which the old `0 tokens` row directly contradicted. `design.md` open question 4 marked closed (reversing its earlier deferral) rather than left stale.
- [x] 11.4 (B2) `apps/plugin/test/nudge-fixtures.json`'s `endOfTurnRubric` and `apps/plugin/scripts/stop-nudge.sh`'s `RUBRIC` heredoc — same sentence appended to the end of the second line, in lock-step: "Copy every file path, line number, measurement and test name across VERBATIM — a paraphrased number is a lost number." This is the one surface where the copy-literal instruction and the material to copy (the extracted facts block) arrive together. Measured: 578 → **698 UTF-8 bytes**. Done in the same commit as 11.3, per the standing rule that growing an uncapped surface without first fixing its published (false) cap is the exact pattern this repo treats as a defect. Considered and explicitly NOT done: the `postCompact` variant (this sentence measures 120 UTF-8 bytes; even against the not-yet-implemented `postCompactCore` at 551 B that is 671 B, over the published ≤600 — too fragile besides being out of scope) and `commands/summary.md` (low value). New test added (`nudge-fixtures.test.ts`, "the rubric directs verbatim copying of concrete facts, not paraphrase").
- [x] 11.5 (B1) `apps/server/src/mcp/server.ts`'s `memory.session_summary` description — inserted before `Args: {`: "When you carry a fact forward, COPY it — file paths with line numbers, measurements, test names, error strings — do not paraphrase it; a rephrased number is a lost number." Measured: 1003 → **1175 characters** (`.length`, the pinned convention for this description) against the 1900-character `DESCRIPTION_MAX_LENGTH`. `mcp-api/spec.md`'s ADDED requirement gains a 5th numbered fact and a new scenario ("The description directs literal-copy of concrete facts, not paraphrase"); `plugin-session-protocol/spec.md`'s "A stronger tool description is not the remedy" paragraph gains a scoping note (it is about the READ directive the e2e measured useless, not about this new copy-literal obligation the description never carried); `design.md` D25 gains the same scoping note. New test added (`mcp-integration.test.ts`, "memory.session_summary description directs verbatim copying of carried-forward facts, not paraphrase").
- [x] 11.6 Mutation verification (`node scripts/mutate.mjs`), each expected to red a NAMED test:
  - Removing B2's sentence from `nudge-fixtures.json` alone: CAUGHT by 2 — `'end-of-turn rubric lock-step > stop-nudge.sh carries the exact fixture rubric'` (the lock-step containment check, since the two files now disagree) and the new `'the rubric directs verbatim copying of concrete facts, not paraphrase'`.
  - Removing the same sentence from `stop-nudge.sh` alone: CAUGHT by 1 — the same lock-step test, confirming it also catches the reverse divergence.
  - Removing B1's sentence from `server.ts`: CAUGHT by exactly 1 — the new `'memory.session_summary description directs verbatim copying of carried-forward facts, not paraphrase'` test.
- [x] 11.7 `pnpm run typecheck` · `pnpm run lint` · full `pnpm vitest run --exclude '../../install.test.ts'`; `openspec validate session-summary-full-rewrite --strict` (valid); `pnpm run check:delta-sections` (ok, 9 delta files); `pnpm run check:delta-freshness` (ok — the new `claude-code-plugin` and `opencode-plugin` REVIEWs are the deliberate rewrites this phase makes; same pre-existing `data-access`/`plugin-session-protocol` REVIEWs as phase 8); `pnpm run check:spec-crossrefs` (ok — same pre-existing unrelated `hermes-agent-plugin` REVIEW as phase 8); `pnpm run check:spec-provenance` (ok — this phase edits no published spec directly, only via delta).
- [ ] 11.8 No new Docker smoke: this phase touches no migration and no HTTP contract shape (the `memory.session_summary` description is read via the existing `tools/list` path already exercised in-process by `mcp-integration.test.ts`), and the plugin edits are text-only, covered by the existing bash/vitest harness. 8.3's smoke stands as the production evidence for the change as a whole.

## Deliberately not done (recorded so it is not silently lost)

- [x] N/A — **No read-back proof and no shrink guard.** Both evaluated and rejected; reasons in design D14 and D15.
- [x] N/A — **No restore action** (design D19, unchanged). Recovery is the operator or a model handing the text back through the ordinary curated write path. **The "no model-facing version read" half of D19 is reversed by D20/phase 9** — a bounded, scoped `memory.session_get({ limit })` read now exists; see phase 9.
- [x] N/A — **`SUMMARY_MAX_CHARS` and `CONTEXT_SNIPPET_CHARS` are unchanged.** Neither is touched, and neither may be as part of this change.
- [x] N/A — ~~The published `Stop | 0 tokens` figure in `claude-code-plugin` stays uncorrected.~~ **Reversed in phase 11.** Growing `stop-nudge.sh`'s own emitted text (phase 11 / B2) while leaving a published `0 tokens` cap uncorrected for the same handler is the pattern this repo treats as a defect, so it is no longer deferred as its own change (design, open question 4, now closed) — see phase 11.
- [x] N/A — **The `persistence` / `sessions` contradiction about the `summary` length `CHECK` stays uncorrected**, for the same reason (design, open question 3).
- [x] N/A — **No per-turn reminder text change**, and no per-line cap raise. If a future change wants the clause there, it raises the cap deliberately and re-measures every published figure containing the string.
- [x] N/A — **No compaction event is invented for Pi.** It exposes none (`session_start`, `before_agent_start`, `message_end`, `agent_settled`, `session_shutdown`), so the thin post-compaction rewrite is an accepted risk with the version table as its mitigation and `memory.session_get({ limit })` / the dashboard history as its recovery (design D25). Not a defect to track.
- [x] N/A — **The `memory.session_summary` description is NOT strengthened to reinforce the read-before-rewrite directive.** It already carries that directive verbatim (`apps/server/src/mcp/server.ts:326`) and the weaker e2e arm ignored it, so an edit reinforcing IT would be speculation against a measurement (design D23/D25). **This does not block phase 11's copy-literal addition**, a different, separately-measured obligation (D23's `50rps` erosion-by-paraphrase finding, by the STRONGER model) that the description did not carry at all before phase 11.
- [x] N/A — **`openspec/specs/hermes-agent-plugin/spec.md:382-385`'s three-bullet `prefetch()` enumeration is NOT modified.** The post-compaction directive is specified in `plugin-session-protocol`, which is where every other cross-client nudge obligation lives and where the `resumedRead` line — a fourth `prefetch()` line added the same way by this change — is already specified. A future editor adding a fifth line should keep the two in step; folding a `hermes-agent-plugin` MODIFIED into this change would carry a 45-line requirement verbatim to add one bullet.
- [x] N/A — **Two defects found while validating and deliberately left to their own changes**, both recorded with evidence under design's "Findings outside this change": `memory.session_start` adopting the project's active session while discarding the `agent` argument (`apps/server/src/mcp/session-tools.ts:202-226`; `sessions.agent` is immutable and there is no repair verb, so misattribution is permanent), and `REMBRIC_PORT` in `.env` making the container permanently unreachable and unhealthy (`docker-compose.yml:22` uses it host-side against a fixed container-side `8787`, while `apps/server/src/config.ts:51` makes the process listen on it). This change's diff touches none of those files.
