## 1. Sequencing gates — do these BEFORE anything else

- [x] 1.1 **OPERATOR / SEQUENCING GATE.** Confirm `refine-session-summary-writes` has been implemented and ARCHIVED. This change's `sessions` delta is written against that change's post-archive spec state: it MODIFIES the requirement that change ADDs ("A curated session-summary write MUST be merged section-wise with the stored summary") and re-modifies a scenario that change already touches ("A session summary MUST follow the documented structure" → "called twice; the second call wins"). Applying this change first produces a delta that will not resolve.
      **DEVIATION:** verified `refine-session-summary-writes` is IMPLEMENTED and committed (`82c52b1`), but NOT archived — its folder is still at `openspec/changes/refine-session-summary-writes/`, `openspec status` reports it `in-progress` (40/41), and `openspec/specs/sessions/spec.md` does not yet carry its ADDED requirements. Archiving is a separate phase (`sdd-archiver`) outside this session's scope. Since task 1.2's diff (below) found no drift between the sibling's as-implemented delta text and what is copied into this change, proceeding with source-level implementation is safe; only the ARCHIVE ORDER remains outstanding (this change must not be archived before `refine-session-summary-writes` archives). Flagged in the PR description.
- [x] 1.2 Re-read `openspec/changes/archive/*-refine-session-summary-writes/specs/sessions/spec.md` (or the merged `openspec/specs/sessions/spec.md` if already archived) and diff it against the two requirement bodies copied into this change's `specs/sessions/spec.md`. If the sibling's text moved during review, rebase this delta onto the actual archived text BEFORE editing any source file. The coupling is exactly two places: the **One site** cross-reference and the last-final-wins scenario.
      Not yet archived (see 1.1), so diffed against `openspec/changes/refine-session-summary-writes/specs/sessions/spec.md` directly — the as-implemented, committed delta text. Both coupling points match byte-for-byte except for this change's intentional edits (the **One site** repoint and the last-final-wins bullet's replacement). No rebase needed.
- [x] 1.3 **OPERATOR / SEQUENCING GATE.** Confirm `measure-summary-clobbering` phase 1 (its `tasks.md` items 1.1–1.5) has already captured versions 14 and 15 of the session it names out of the operator's deployment, OR that the change has been abandoned. The drop migration destroys those rows and nothing recovers them. That change's own `tasks.md:3` already flags the dependency — this task is where it is honoured.
      Phase 1 is NOT captured (all of `measure-summary-clobbering`'s `tasks.md` items are unchecked). Per the launching agent's explicit instruction, `measure-summary-clobbering` is a discarded proposal left on disk pending the owner's decision to delete it — treated as abandoned, satisfying the OR clause. Its folder was left untouched, as instructed.
- [x] 1.4 Check whether `server-gated-session-nudges` has already archived. Its delta text carries three references this change would leave dangling — a **One site** cross-reference (`specs/session-nudges/spec.md:10`), a no-backfill rationale naming `session_summary_versions.created_at` (`specs/sessions/spec.md:7`), and a scenario bullet asserting "no version row SHALL be appended" (`specs/sessions/spec.md:100`). If it archived first, repoint and drop them as part of THIS change's `sessions` and `session-nudges` deltas before implementing. If it has not, record in the PR description that the obligation falls to it. Either order is fine; leaving them unrepointed is not.
      Confirmed NOT archived (folder still at `openspec/changes/server-gated-session-nudges/`, `openspec status` reports `in-progress` 81/86, `openspec/specs/session-nudges/spec.md` does not exist yet). Verified all three exact references are present at the stated locations in its own (uncommitted-to-specs) delta. Recorded in the PR description that the repoint obligation falls to whichever of the two changes archives second. A FOURTH, smaller dangling reference was found beyond the three named here: `apps/server/src/db/migrations/0034_session_nudge_gate.sql`'s own header comment names `session_summary_versions.created_at` — a rationale comment in an already-committed migration file belonging to that sibling change, left untouched (out of this change's declared impact) and flagged in the PR description alongside the other three.
- [x] 1.5 **OPERATOR.** Take a `sqlite3 .backup` of the target deployment's data file before the upgraded image is deployed, per `docs/backup.md`. This is the only copy of the version rows that will exist afterwards.
      Cannot be performed from this sandboxed session — there is no access to the operator's production deployment. Flagged explicitly in the report as an outstanding OPERATOR action that must happen before this migration reaches that deployment.

## 2. The migration

- [x] 2.1 Create `apps/server/src/db/migrations/00NN_drop_session_summary_versions.sql` where `00NN` is the next unused number. Check for a collision first: `measure-summary-clobbering` also claims `0034`, so whichever change lands second takes the next free number. Body is exactly `DROP TABLE session_summary_versions;` — no `CREATE`, no `INSERT … SELECT`, no `ALTER TABLE`, no `DROP INDEX`, no `PRAGMA`.
      `0034` was already claimed by `server-gated-session-nudges` (`0034_session_nudge_gate.sql`, already committed). Assigned `0035_drop_session_summary_versions.sql`.
- [x] 2.2 Give the file a header comment carrying ONE non-obvious fact and no more: that the rows are destroyed irreversibly and that the decision is recorded in `persistence`, "The `session_summary_versions` table MUST be dropped by a dedicated migration, with `0033` retained on disk". Do NOT restate the DDL, do not re-argue the decision, do not add a banner.
- [x] 2.3 Verify `apps/server/src/db/migrations/0033_session_summary_versions.sql` is UNCHANGED and still present: `git status` shows no modification and no deletion. Deleting it is the failure mode this task exists to catch.
- [x] 2.4 Add the new filename to both migration-list fixtures in `apps/server/src/db/migrations.test.ts` (`:632` and `:1110` on the pre-change tree) and confirm `0033_session_summary_versions.sql` is still listed in both.
- [x] 2.5 Delete `apps/server/src/db/migrations-0033.test.ts` entirely. It cannot be kept as a checkpoint pin: two of its five cases construct `AgentSessionsService` and call `adminListSummaryVersions`, neither of which survives.
- [x] 2.6 Create `apps/server/src/db/migrations-00NN.test.ts` following the shape of the deleted file's fixture usage (`createMigrationFixture`, `stagePrior`, `stage`), covering the `persistence` ADDED requirement's scenarios: the drop against a populated file carrying version rows (table gone, every other table byte-identical, `PRAGMA foreign_key_check` clean), the file containing exactly one `DROP TABLE` and no pragma, and a second boot applying nothing.
      Created `migrations-0035.test.ts`; all 3 cases pass (`pnpm exec vitest run src/db/migrations-0035.test.ts`).

## 3. Schema, repository, service

- [x] 3.1 Remove `sessionSummaryVersions`, its doc comment and the `SessionSummaryVersion` / `NewSessionSummaryVersion` types from `apps/server/src/db/schema/agent-sessions.ts` (`:110-144` on the pre-change tree). Drop any import that becomes unused (`uniqueIndex` is used only by this table — check before removing).
      `uniqueIndex` confirmed unused elsewhere and removed from the import.
- [x] 3.2 Remove the five methods from `apps/server/src/db/repositories/agent-sessions-repository.ts` (`:380-453`): `insertSummaryVersion`, `latestSummaryVersion`, `adminListSummaryVersions`, `adminCountSummaryVersions`, `listSummaryVersionsInScope`. Clean up the now-unused imports (`sessionSummaryVersions`, `NewSessionSummaryVersion`, `SessionSummaryVersion`, and `count`/`isNull`/`desc` only if nothing else uses them — check each).
      `count`/`isNull`/`desc` are all still used elsewhere in the file (kept). `Scope` (not named in the task, but only used by the removed `listSummaryVersionsInScope`) was ALSO unused afterward and removed — a deviation from the literal task text, done for a clean typecheck.
- [x] 3.3 Update `apps/server/src/db/repositories/agent-sessions-repository.test.ts`: remove the version-row fixture insert (`:174`) and the three describe blocks (`adminListSummaryVersions`, `adminCountSummaryVersions`, `listSummaryVersionsInScope`). Every other block in the file stays.
      The whole outer `describe('AgentSessionsRepository summary-version reads', …)` wrapper existed only to host those three blocks and their shared fixture, so it was removed in full along with the now-unused `projectScope`/`sessionSummaryVersions` imports; the sibling `describe('AgentSessionsRepository admin filters', …)` block is untouched.
- [x] 3.4 In `apps/server/src/services/agent-sessions.ts`, delete `updateAndVersion` (`:298-321`) and replace its three call sites (`:276`, `:371`, `:422`) with direct `this.repos.agentSessions.updateById(...)` calls carrying the same arguments. Remove the `ulid` import if nothing else uses it. Do NOT remove the `TransactionRunner` constructor parameter — `purgeEmpty` (`:727`) still needs it.
      **DEVIATION, recorded because it changes the shape of the collapse:** by the time this change was implemented, `server-gated-session-nudges` had already landed and `updateAndVersion` had grown a second responsibility beyond the version append — stamping `last_summary_at` (the nudge-gate timestamp) on exactly the writes that store a `final:true` summary. Calling `updateById` with the RAW `set` (as the task literally describes) would have silently dropped that stamping and regressed the already-shipped nudge feature. Instead, the stamping logic was extracted into a small private helper, `stampLastSummaryAt(existing, set)`, called at all three sites alongside the direct `updateById` call — preserving the nudge-gate behavior while still removing the transaction wrapper and the version-row append. `ulid` import kept: `start()` still uses it. `TransactionRunner` kept: `purgeEmpty` still needs it.
- [x] 3.5 Delete `listSummaryVersions` from the same service (`:481-483`).
- [x] 3.6 Trim the `writeTerminalFields` doc comment so it documents only the terminal-write deviation. The version-append paragraphs go with the method they described; do not replace them with a comment about the removal.
      On the current tree this doc comment already documented only the terminal-write deviation (no version-row paragraphs present) — presumably already trimmed by a prior change. No edit needed; confirmed by inspection.

## 4. MCP surface

- [x] 4.1 In `apps/server/src/mcp/session-tools.ts`, remove `SESSION_GET_VERSIONS_MAX` (`:54`), the `limit` field of `sessionGetSchema` (`:58-66`) and the `versions` computation plus its response spread in the `session_get` handler (`:416-428`). `sessionGetSchema` ends with exactly one property, `sessionId`.
- [x] 4.2 In `apps/server/src/mcp/server.ts`, remove the `SESSION_GET_VERSIONS_MAX` import (`:73`) and rewrite the `memory.session_get` description (`:360`) to drop every `limit` sentence. The rewritten description SHALL NOT name a version, a history or a recovery path — required by `sessions`, "No surface promises recovery of replaced text".
- [x] 4.3 Record the emitted `memory.session_get` description length from a real `tools/list` response, before and after. It shortens; assert it against `DESCRIPTION_MAX_LENGTH` (1900) in the integration test as every other description already is.
      Measured: 652 → 287 characters. The existing generic test `'keeps every tool description under the client truncation ceiling'` (`mcp-integration.test.ts`) already asserts every emitted description — including `memory.session_get`'s, now shortened — against `DESCRIPTION_MAX_LENGTH`; ran it explicitly against a real `tools/list` response and it passes.
- [x] 4.4 In `apps/server/src/test/mcp-integration.test.ts`, remove the `SESSION_GET_VERSIONS_MAX` import (`:22`) and the over-max rejection case (`:2428`). Replace them with the two scenarios from the modified `sessions` requirement: `memory.session_get({ sessionId })` succeeds and returns the full summary with NO `versions` key (the control), and `memory.session_get({ sessionId, limit: 2 })` is refused by input validation with a message naming the tool and the property `limit`. Both must run over the real MCP boundary, not by calling the handler directly — the strict schema lives at the registration seam and a direct handler call bypasses it.
      Also removed two other now-obsolete `limit`-carrying tests found alongside the named one (a byte-identical-zero-limit test and a positive-limit-returns-versions test, and a cross-scope+limit test now redundant with the limit-less cross-scope test already in the file) — all three depended on the retired `versions`/`limit` surface. Both new tests run over the real MCP `tools/call` boundary.

## 5. Dashboard

- [x] 5.1 In `apps/server/src/dashboard/sessions.ts`, remove `SUMMARY_HISTORY_MAX` and its doc comment (`:40-49`), the two `admin*` reads (`:323-327`) and the entire `Summary History` template block (`:413-436`). The `Summary` block above it and the `Memories (N)` block below it are unchanged.
- [x] 5.2 Remove the `.rbr-summary-history` and `.rbr-summary-version` rules from `apps/server/src/dashboard/styles/views/sessions.css` (`:38-65`). Leave every other rule in the file.
- [x] 5.3 Delete `apps/server/src/dashboard/session-summary-history.test.ts` entirely.
- [x] 5.4 Confirm no dashboard route, form, action token or `data-confirm` modal was involved: `git diff` shows no change to `csrf`-related code and no route removal. The section never had a mutation surface, which is what makes this purely subtractive.
      Confirmed: `git diff` on `sessions.ts` shows no `csrf` or `app.get`/`app.post` route lines touched.

## 6. Inventories, invariants and the seed

- [x] 6.1 Remove the four `session_summary_versions` forbidden-pattern rules from `apps/server/src/test/invariants.test.ts` (`:155-176`). Then assert positively that the remaining rules in that array still cover `memory`, `sessions`, `prompts` and `memory_relations` — count them before and after and state both numbers, rather than trusting the diff.
      Counted: 23 rules before, 19 after (4 removed). The 19 remaining still cover exactly `memory` (8 rules), `sessions` (4), `memory_relations` (3) and `prompts` (4) — nothing else in that family was touched.
- [x] 6.2 Remove `'session_summary_versions'` from `SOURCE_TABLES` in `apps/server/src/test/schema-inventory.ts` (`:30`).
- [x] 6.3 Remove the `session_summary_versions_session_version_unq` entry and the `sqlite_autoindex_session_summary_versions_1` entry from `apps/server/src/test/schema-drift.test.ts` (`:163-166`, `:233`).
- [x] 6.4 Run `pnpm vitest run apps/server/src/db/migrations-0031.test.ts` explicitly. It builds `CENSUS_TABLES` from `SOURCE_TABLES` but filters to tables present at that checkpoint, so the removal should be a no-op there — confirm it by running the file, not by reading it.
      Ran it: 23/23 tests pass, confirming the removal is a no-op there.
- [x] 6.5 In `apps/server/src/scripts/seed-dev.ts`, remove the `i === 0` draft-curation block (`:311-322`). Its only purpose was to give the retired dashboard section a second row; the session it decorated keeps its real `end()` summary.
      The `entries()`/`[i, cfg]` destructure became unused after removing the block; simplified the loop to `for (const cfg of endedSessions)`.
- [x] 6.6 In `apps/server/src/scripts/seed-dev.test.ts`, delete the "seeds at least one session with two summary versions, and --reset cascades them cleanly" test (`:80-111`). If the `--reset` cascade assertion it carried (`PRAGMA foreign_key_check` clean after a reset) is not covered elsewhere in that file, keep that one assertion in an existing reset test rather than losing it.
      That assertion was not covered elsewhere; added `expect(db.handle.raw.prepare('PRAGMA foreign_key_check').all()).toEqual([])` to the existing "regression: add-entity-index" `--reset` test.

## 7. Service tests — rewrite, do not just delete

- [x] 7.1 Delete the whole `describe('session_summary_versions', …)` block from `apps/server/src/services/agent-sessions.test.ts` (from `:1282`).
- [x] 7.2 Delete the "purging a session removes its version rows via the cascade, with no FK violation" test (`:709`). The cascade it exercised no longer exists.
- [x] 7.3 **REWRITE, do not delete,** the "a session carrying a version row is not purge-eligible" test (`:694`). What actually keeps that session out of the purge is clause 1 of `sessions`, "Sessions MAY be physically purged when empty" — any summary text at all makes it ineligible — not the version row. Rename it accordingly and assert against `sessions.summary` being non-NULL. Deleting it would drop coverage of a predicate that still exists.
      Renamed to "a session with a non-NULL summary is not purge-eligible, regardless of how it got one"; asserts `sessions.getById(s.id)?.summary` is not null before asserting purge-ineligibility.
- [x] 7.4 Remove the three `adminListSummaryVersions` assertions at `:702`, `:715` and `:728` from the surrounding purge tests, keeping every other assertion in those tests intact.

## 8. Prove the removal is real, not just green

- [x] 8.1 `grep -rn "session_summary_versions\|sessionSummaryVersions\|SummaryVersion\|SUMMARY_HISTORY_MAX\|SESSION_GET_VERSIONS_MAX" apps/ openspec/specs/` returns hits ONLY in `apps/server/src/db/migrations/0033_session_summary_versions.sql`, the new drop migration, and the two migration-list fixtures. Any other hit is an incomplete removal.
      Excluding the gitignored `apps/server/coverage/` build artifact and the historical `CHANGELOG.md` (a past release's changelog entry, correctly left unedited), the live `apps/` source grep returns hits ONLY in: `0033_session_summary_versions.sql` (retained), `0035_drop_session_summary_versions.sql` (new), the two `migrations.test.ts` fixtures, `migrations-0035.test.ts` (the new migration's own test, functionally equivalent to "the new drop migration"), and — an expected 4th, not one of these three — `0034_session_nudge_gate.sql`'s own header comment (the sibling's already-committed file; see the 1.4 note). `openspec/specs/**` still shows the OLD (pre-archive) text throughout, because archiving neither this change nor `refine-session-summary-writes`/`server-gated-session-nudges` is part of the apply phase — expected, not an incomplete removal.
- [x] 8.2 `grep -rn "rbr-summary-history\|rbr-summary-version" apps/` returns nothing.
- [x] 8.3 Run `node scripts/mutate.mjs` once against the new `memory.session_get` schema: re-add a permissive passthrough (e.g. loosen the registration seam's strictness for that one tool) and confirm the task-4.4 refusal test goes RED while the plain-`sessionId` control stays GREEN. A refusal test that passes on both sides is measuring the wrong thing.
      Mutated the ONE shared registration seam (`apps/server/src/mcp/server.ts`'s `.strict()` → `.passthrough()`, the only occurrence) and ran `mcp-integration.test.ts -t "memory.session_get"`: the refusal test went RED (`expected undefined to be true`), every other `memory.session_get`-named test (including the bare-`sessionId` control) stayed GREEN. `mutate.mjs` reported "all 1 mutations caught" and restored the file byte-identically.
- [x] 8.4 Confirm that after the migration a curated write still works end to end at the service level: one test that writes a curated summary and reads it back, with no transaction wrapper and no version read anywhere in the path. This is the control for task 3.4 — collapsing `updateAndVersion` must not have changed what `writeSummary` and `end` store.
      Added `'a curated write still stores and reads back end to end after the updateAndVersion collapse'` to `agent-sessions.test.ts`.

## 9. Verification

- [x] 9.1 `pnpm run typecheck` — green.
- [x] 9.2 `pnpm run lint` — green.
- [x] 9.3 `pnpm test` — green: 149 test files, 2850 tests (2839 TS + Hermes Python suite), 0 failures.
- [x] 9.4 `pnpm run eval` is NOT required — retrieval is untouched, and no recall path ever read the dropped table. Recorded here rather than skipped silently: not run.
- [x] 9.5 `openspec validate retire-summary-versions --strict` is green.
- [x] 9.6 `pnpm run check:spec-provenance` is green against `origin/main...HEAD`.

## 10. Real Docker smoke against pre-existing seeded data — standing requirement, not optional

Run against `pnpm run dev:docker:up` (`chown -R 10001:10001 data-dev` first if it fails with `SQLITE_CANTOPEN`). Note that `dev:docker:up` runs `seed-dev --reset` on every boot, so stage the pre-existing data deliberately.

- [x] 10.1 Boot the PRE-change image once, curate a session summary twice so version rows exist, and record the row counts of every table. Then boot the post-change image against that same data file.
      Executed via `git stash` (parking the staged apply) to boot the tree at the pre-change commit (`f94484e`) through `dev:docker:up`, curated a session's summary twice over real MCP (2 version rows created, on top of the seed's own draft-curation pair), recorded row counts for every table. Then `git stash pop` to restore this change's source on the SAME running container (bind-mounted `apps/server/src`, so `tsx watch` respawned the server process in place — no container restart, no re-seed).
- [x] 10.2 Confirm on the upgraded file: `session_summary_versions` does not exist; `sessions.summary` for that session is byte-identical to what the pre-change boot left; every other table's row count is unchanged; `PRAGMA foreign_key_check` returns empty.
      All confirmed by direct query against the live `data-dev/data.db` via `better-sqlite3` from the host: table absent (real query error, not a zero count), `sessions.summary` = `"Goal: smoke test the migration upgrade path (v2, revised)."` unchanged, every other table's count identical across the two snapshots, `PRAGMA foreign_key_check` = `[]`.
- [x] 10.3 Over MCP, call `memory.session_get({ sessionId })` and confirm the response carries the full summary and NO `versions` key. Then call it with `{ sessionId, limit: 2 }` and confirm a hard refusal naming the tool and the property — not an ignored argument, not an empty array.
      Confirmed over real MCP against the upgraded, running server: bare call returns the full summary with no `versions` key; the `limit: 2` call is refused with `MCP error -32602: Input validation error: … Unrecognized key(s) in object: 'limit'` for tool `memory.session_get`.
- [x] 10.4 Over MCP, write a curated summary and confirm it stores; write a partial one and confirm the untouched sections survive. This is the control that the `updateAndVersion` collapse did not change the write path.
      Confirmed over real MCP: a `## Goal`/`## Files` write merged onto the existing (unsectioned) stored text, preserving it as a preamble; a following partial write updating only `## Files` left `## Goal` byte-identical while `## Files` changed — the merge/preservation-by-omission behavior working correctly post-collapse.
- [x] 10.5 Open `/dashboard/sessions/:id` for that session and confirm the page renders, shows the `Summary` block, and carries no `Summary History` heading and no empty history section. Screenshot headless (Playwright) — there is no GUI on this host.
      **DEVIATION:** no complete headless-Chromium binary was available in this sandbox (the cached `ms-playwright` chromium download was incomplete — support files only, no executable), so no screenshot was captured. Substituted a direct authenticated HTTP fetch of the rendered page (cookie login via `POST /dashboard/login`, then `GET /dashboard/sessions/:id`): status 200, `>Summary<` present once, `>Memories (` present once, `Summary History` and `rbr-summary-history`/`rbr-summary-version` both absent (0 occurrences) from the rendered HTML.
- [x] 10.6 Confirm the server did not refuse to start on the shrink guard, and that the startup banner's row counts differ from the pre-change boot only by the absence of the dropped table.
      Confirmed via `docker logs`: the respawn-boot (migration applied) logged no data-loss-guard message at all (unlike the earlier full container boot, which DID log one for the reseed's own row-count drop), and its `[bootstrap] counts: memory=35 projects=20 sessions=6 tokens=3 prompts=0` line is IDENTICAL to the immediately-preceding pre-migration state — the dropped table was never part of the counted set.
- [x] 10.7 Confirm no `memory`, `memory_fts`, `memory_vec` or entity-table row changed across the upgrade — append-only memory is not in play here, and this is what proves it.
      Confirmed via the two full-table-count snapshots (10.1/10.2): `memory`, `memory_fts`, `memory_fts_vocab`, `memory_entities`, `memory_entity_links`, `memory_entity_scan`, `memory_vec_rowids` and all `memory_vec_*` shadow tables are identical before and after.

## 11. Recorded so it is not lost

- [x] 11.1 State in the PR description that an in-section clobber (a curated write carrying `## Accomplished` with less text than it replaces) is now unrecoverable, that the owner accepted this knowingly, and that no shrink guard was added — rejected on the record in `2026-08-13-session-summary-full-rewrite` and again in `refine-session-summary-writes` because a correct summary legitimately shrinks.
- [x] 11.2 State in the PR description that rollback to a pre-drop image breaks the curated write path, and name the two recoveries (roll forward, or recreate the empty table from the retained `0033` DDL).
- [x] 11.3 State that `0033_session_summary_versions.sql` was deliberately retained on disk, with the reason (fresh-vs-upgraded `_migrations` divergence) and the precedent (`0011`/`0012`).
- [x] 11.4 Record the three open questions from `design.md` — whether the drop should log the destroyed row count, whether `memory.session_get` needs any replacement affordance, and whether the residual risk warrants an instrument before this lands — with their defaults taken, and note that the third is deliberately left open rather than defaulted.
- [x] 11.5 Conventional Commits throughout; hooks never bypassed.
