# Tasks

## 1. The description

- [x] 1.1 `apps/server/src/mcp/server.ts` — `memory.save_prompt`'s description constrains WHEN to call: reusable-only, no routine calls, explicit request or plainly reusable artifact, a stated goal ruled out by name, the adjacent intents redirected to `memory.save` / `memory.session_summary`, and "when in doubt, do not call this".
- [x] 1.2 The description still contains `sessionId` with guidance not to invent one, so `mcp-api`'s `Scenario: Tool descriptions mention sessionId explicitly` (`spec.md:1211-1215`) keeps passing. Verified by running the suite, not by reading the diff.
- [x] 1.3 Still inside `DESCRIPTION_MAX_LENGTH` — asserted from a live `tools/list` by `mcp-integration.test.ts`, which is what confirms it. `memory.save_prompt` is not one of the seven tools whose exact length is pinned, so no expected number needed updating.

## 2. Coverage

- [x] 2.1 New test `memory.save_prompt restrains WHEN it is called, not only what to pass` in `mcp-integration.test.ts`, one assertion per bullet of the new scenario.
- [x] 2.2 Mutation-verify each condition separately.

  | Mutation                                                                                   | Result                  |
  | ------------------------------------------------------------------------------------------ | ----------------------- |
  | Whole new guidance block reverted to the old one-line trigger                              | caught                  |
  | Only "do NOT call it routinely, and never once per session as a matter of course" weakened | caught                  |
  | Only the `memory.save` / `memory.session_summary` redirect removed                         | caught                  |
  | Only the word `REUSABLE` removed from the opening sentence                                 | **NOT caught at first** |

  The fourth escape was a defect in the test, not in the code: `expect(desc).toMatch(/reusable/i)` was also satisfied by "reusable artifact" later in the same string, so the opening claim was uncovered while appearing covered. The assertion is now anchored (`/^Persist a REUSABLE prompt/`) and the mutation is caught. This is the vacuity trap the repo's guidance warns about — an assertion that passes for a reason other than the one it names.

## 3. Spec

- [x] 3.1 `mcp-api` delta — the `memory.save_prompt` requirement gains the calling discipline, so the trigger stops being unspecified prose in code, plus the paragraph recording why the nudge cadence is NOT the cause and must not be "fixed".
- [x] 3.2 New scenario `The description restrains when the tool is called`, with one bullet per constraint the test asserts.

## 4. Verification

- [x] 4.1 `openspec validate tighten-save-prompt-trigger --strict`.
- [x] 4.2 `check:delta-freshness` — the delta was generated by extracting the requirement verbatim and inserting the new paragraph and scenario, so the only reported differences should be the insertions.
- [x] 4.3 `typecheck`, `lint`, full `pnpm test`.

## 5. Deliberately not done

- [ ] 5.1 The sessionId nudge keeps its imperative shape. It is the line that puts this tool in front of the model on turn 1, but it only names the tool — the description is what licensed the call. Rewording it means three language copies, fixtures and an invariant test, and it would not have prevented this. Left for its own change if the shape proves to cause other misfires.
- [ ] 5.2 Prompt rows already saved under the old trigger are left alone. Which of them are worth keeping is the operator's judgement, and the dashboard already soft-deletes.
