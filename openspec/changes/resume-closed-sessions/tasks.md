# Tasks — resume closed sessions

## 1. Service verb

- [x] 1.1 Add `resume(sessionId: string, input: { tokenId: string }): AgentSession` to `apps/server/src/services/agent-sessions.ts`, ordered exactly as the spec requires: row lookup → cross-token mask (`session_not_found`) → soft-delete gate (`session_deleted`, taken against the row read in the same tick) → already-`active` no-op returning the row with no `UPDATE` → single `UPDATE` setting `status: 'active'`, `endedAt: null`, `lastActivityAt: this.now()` via `updateById(id, set, { requireActive: false })`.
- [x] 1.2 Confirm no new repository method is needed: `updateById` with `requireActive: false` already exists (`apps/server/src/db/repositories/agent-sessions-repository.ts:114-129`). If one turns out to be needed, stop and re-check `apps/server/src/test/invariants.test.ts:747-758` (repository set-equality) and `:760-767` (unscoped-read inventory) before adding it.
- [x] 1.3 Do NOT route `resume` through `precedenceSet` or `writeTerminalFields`; it writes no precedence-governed column. Verify `pnpm vitest run apps/server/src/test/invariants.test.ts` still passes both `the terminal write path adds nothing to the precedence fields` and `precedenceSet can only ever produce summary and title fields` unchanged. — verified: both green, neither test edited.
- [x] 1.4 Update the FSM docstring at `apps/server/src/db/schema/agent-sessions.ts:15-37`: `ended_at` is "mutable once **per terminal transition**, cleared on resume", and the transition list gains `ended | abandoned -> active (memory.session_resume)`. One line per fact, no banner comments.

## 2. MCP tool

- [x] 2.1 Add `handleSessionResume` to `apps/server/src/mcp/session-tools.ts` with input `{ sessionId: string }` (REQUIRED — no `resolveSessionId` call, no router fallback, no `findActiveForTransport` fallback).
- [x] 2.2 Resolve scope with `requireScope(deps, 'write')` and reject a cross-project target with `session_not_found` via the existing `assertExplicitSessionOwned` (`apps/server/src/mcp/_shared.ts:336`), so project isolation matches every other session tool.
- [x] 2.3 On success call `deps.router.setActiveSession(key.tokenId, key.mcpSessionId, session.id)` — the same call at `apps/server/src/mcp/session-tools.ts:189` — including on the already-`active` no-op path. Do NOT call `setActiveProject`.
- [x] 2.4 Register `memory.session_resume` in `apps/server/src/mcp/server.ts` with an `outputSchema` marking `sessionId`, `status`, `startedAt`, `resumedAt`, `previousStatus`, `previousEndedAt` required, and a description whose `Returns: { … }` list names every one of them and states that `previousEndedAt` is not retained on the row. — `ok` and `title` are required too, and the description names all eight. Registering the tool also required adding it to `WRITE_TOOLS` in `mcp-integration.test.ts` (set-equality over registered tools). STILL OWED: the `tools/list` description-vs-`outputSchema` CI test the `mcp-api` delta requires for this tool (scenario "`memory.session_resume`'s description names every required output field"), alongside the existing `memory.session_start` one.
- [x] 2.5 Verify the input schema is strict without extra work — `registerTool` wraps every `inputSchema` in `z.object(...).strict()` at `apps/server/src/mcp/server.ts:191-193`. Prove it with a probe sending `{ sessionId, epoch: 3 }` through the real MCP client boundary (not the handler directly) and asserting `invalid_input`. — probed against a real in-process server over Streamable HTTP: control `{ sessionId }` succeeded; `{ sessionId, epoch: 3 }` was refused with `-32602 … unrecognized_keys: ["epoch"]`, which is how this repo's existing tests assert a zod-boundary `invalid_input`.

## 3. Rewrite the terminal-invariant test deliberately

- [x] 3.1 Rewrite `apps/server/src/services/agent-sessions.test.ts:923-957` (`terminal rows are terminal`). `resume` MUST NOT be appended to the eight-verb refusal list — it is the one verb that legitimately moves `status` and `ended_at`. Restructure into two blocks: the existing eight verbs still asserted to move nothing, and a new block asserting `resume` moves exactly `status`, `ended_at` and `last_activity_at` and nothing else.
- [x] 3.2 Assert column-by-column in the resume block that `agent`, `token_id`, `project_id`, `started_at`, `summary`, `summary_final`, `title`, `title_final` and `deleted_at` are byte-identical before and after. A count-based assertion is not acceptable here: the test's own header records that a counting invariant passed under mutation on this exact terrain.
- [x] 3.3 Add the two-call sequence test: `markAbandoned` on an `ended` row still throws `session_already_ended`, and `resume` → `markAbandoned` succeeds with a fresh `ended_at`.
- Also landed in the same two files, because tasks 6.1-6.3/6.6-6.7 have nothing to mutate against otherwise: service-level `resume` scenarios (ended/abandoned identical, cross-token mask, unknown id, soft-delete refusal leaving `deleted_at` set, already-`active` no-op with `updateById` spied as never called) and tool-level ones (response shape incl. `previousStatus`/`previousEndedAt`, router pin with a second live session, re-pin on the no-op path, cross-project and cross-token masks, soft-delete refusal pinning nothing). Task 4.1's sweep-survival test was deliberately NOT written here.

## 4. Consumer verification — one task per consumer, each independently checkable

- [x] 4.1 `abandonStale` / `abandonInactiveSince`: write a test that resumes a row abandoned by the sweep, immediately re-runs `abandonStale({ olderThanMs })` with the same window, and asserts the row is still `active`. Without the `last_activity_at` stamp this test must fail — verify that by temporarily dropping the stamp (task 6.5).
- [x] 4.2 `purgeEmpty` / `findPurgeableEmptyIds`: assert a resumed empty row is absent from `findPurgeableEmptyIds(cutoff)` and that `countPurgeableEmpty` agrees, with a control asserting the same row IS returned before the resume (a "not in the set" assertion over an empty set proves nothing).
- [x] 4.3 `memory.session_start`: assert it adopts a resumed row with `reused: true` and does not insert a second row — assert the row count for `(token_id, project_id)` as the control.
- [x] 4.4 `memory.session_end`: assert `end()` on a resumed row takes the ACTIVE branch, writing a fresh `ended_at` (not restoring the discarded one) and clearing the router binding.
- [x] 4.5 `memory.session_summary`: assert a `final:true` write on a resumed row REPLACES a previously-final summary (last-final-wins, because the row is no longer terminal), with a control asserting the same write on the same row before the resume is a no-op.
- [x] 4.6 `memory.session_get`: assert a resumed row reports `status: 'active'` and `endedAt: null`, and that the pair moves together in both directions rather than ever reading contradictory. **Corrected during apply**: this task originally also named `memory.timeline`, but `timelineOutput` (`apps/server/src/mcp/memory-tools.ts:548-553`) publishes only `target`, `before`, `after` and `fallback` — that tool carries no session state, so the assertion was not implementable there. `memory.timeline` is instead covered for the property a resume can actually break: memories saved before and after the cut read as one continuous session thread, with an orphaned save (`fallback: 'time_window'`) as the control.
- [x] 4.7 `memory.context` / `recentForContext`: assert a resumed session appears exactly ONCE, and assert the accepted ordering limitation positively — a session started earlier and resumed later still sorts below a session started in between.
- [x] 4.8 `memory.stats` / `memory.doctor`: assert `sessionsByStatus` reflects the move (`ended` decreases by one, `active` increases by one) across a resume. No code change expected; this is a regression pin on a counter that is no longer monotonic.
- [x] 4.9 Dashboard sessions list and detail: assert a resumed row renders `Ended` as the empty/dash placeholder (`apps/server/src/dashboard/sessions.ts:110`, `:380`) and that the Abandon form is rendered again because `status === 'active'`. No dashboard handler or template change is expected — if one turns out to be needed, stop: `dashboard` has no delta spec in this change and the design records why (D11).
- [x] 4.10 HTTP session routes (`apps/server/src/server/api-router.ts:94`, `:134`, `:178`): assert none of them resumes, and specifically that an ensure POST against an `ended` row returns `created: false` with the row still `ended`.
- [x] 4.11 `markAbandoned`: assert it still throws `session_already_ended` on a row that is `ended` and has NOT been resumed.

## 5. Verification gate

- [x] 5.1 `pnpm run typecheck`
- [x] 5.2 `pnpm run lint`
- [x] 5.3 `pnpm test` — all green, including `apps/server/src/test/invariants.test.ts` and `apps/server/src/services/agent-sessions.test.ts`.
- [x] 5.4 `openspec validate resume-closed-sessions --strict`
- [ ] 5.5 `pnpm run check:spec-provenance`
- [x] 5.6 `pnpm run check:delta-freshness`. Expected result, measured at proposal time rather than guessed: **`ok (1 active change(s), 4 body difference(s) to review)`**, with zero missing headers and zero dropped scenarios (those two always fail the run). The four REVIEW lines are deliberate rewrites and are expected to be exactly these, with this many lines each:
  - `sessions / Sessions MUST be append-only` — 2 lines (the `ended_at` "at most once" clause, and the cross-reference to the renamed terminal-writes requirement)
  - `mcp-api / Session-lifecycle MCP tools MUST reject soft-deleted sessions` — 2 lines (the tool enumeration, and the cross-reference to the renamed four-tools requirement)
  - `plugin-session-protocol / Per-client lifecycle mapping MUST be honoured` — 1 line (the Pi `reload` bullet's "no path back to `active`" clause)
  - `pi-plugin / The shutdown reason decides whether the session is ended` — 1 line (the same clause quoted verbatim there)

  `http-api` is deliberately NOT in this list: its delta adds a paragraph and a scenario without rewriting any published line, so the script has nothing to report. A fifth reported requirement, or a different line count on any of the four, means an unintended edit — investigate rather than accept.

- [x] 5.7 `pnpm run eval` is NOT required: no retrieval, ranking, embedding or search path is touched. Confirm by grepping the diff for `apps/server/src/services/memory.ts`, `apps/server/src/db/repositories/memory-repository.ts` and `apps/server/src/llm/` — if any appears, run it.

## 6. Mutation — prove each new guard is load-bearing

Use `node scripts/mutate.mjs --file … --spec … --mutation … --with …` for each. A guard whose tests stay green on both sides of the mutation is not covered; fix the test, not the mutation.

- [x] 6.1 Weaken the cross-token mask in `resume` (accept any token) → the cross-token test must go red.
- [x] 6.2 Remove the soft-delete gate in `resume` → the `session_deleted` tests (service and MCP tool) must go red.
- [x] 6.3 Change the already-`active` branch to emit an `UPDATE` anyway → the no-op test asserting `last_activity_at` is unchanged must go red.
- [x] 6.4 Drop `endedAt: null` from the update `set` → the `ended_at IS NULL` assertions must go red. This is the D2 guard; if it stays green, the assertion is checking `status` only.
- [x] 6.5 Drop `lastActivityAt` from the update `set` → task 4.1's sweep-survival test must go red.
- [x] 6.6 Widen `resume` to also accept a soft-deleted row's `deleted_at` clear → the "resume SHALL NOT clear `deleted_at`" assertion must go red.
- [x] 6.7 Remove the `setActiveSession` call from the tool handler → the router-pin test (task 4.3 / the mcp-api pin scenario) must go red. Run this one specifically with the two-concurrent-active-sessions fixture, since that is the only arrangement where the pin is distinguishable from the `findActiveForTransport` fallback.

## 7. Docker smoke against pre-existing seeded data — mandatory, not optional

This change alters production lifecycle behaviour on the MCP surface, so a real-stack smoke is required. Follow `.agents/skills/rembric-smoke-tests/`.

- [x] 7.1 Bring up the dev stack (`pnpm run dev:docker:up`; `chown -R 10001:10001 data-dev` first if it errors with `SQLITE_CANTOPEN`). Note that every boot reseeds — capture the ids you need from the seeded corpus, do not rely on a corpus you created earlier.
- [x] 7.2 **Control first**: pick a seeded session that is already `ended`, and confirm over the real MCP transport that a `memory.save` on a transport with no session binding persists `session_id = NULL`. Without this control passing, the resume arm proves nothing.
- [x] 7.3 Call `memory.session_resume` over the real Streamable HTTP MCP transport (not by invoking the handler in-process — that bypasses the zod schema and the request context). Assert the response shape, then re-read the row through `memory.session_get` and assert `status: 'active'`, `endedAt: null`.
- [x] 7.4 On the SAME transport, call `memory.save` with no `sessionId` and assert the memory persists with `session_id` equal to the resumed session — this is the router pin working end-to-end.
- [x] 7.5 Call `memory.session_summary({ final: true })` on the resumed row and assert it REPLACES the seeded summary, then confirm the same call against a still-terminal seeded row does NOT replace its final summary. Both arms in one run; the pair is the evidence.
- [x] 7.6 Load `/dashboard/sessions` and the resumed session's detail page headless (Playwright + screenshot; there is no GUI here) and confirm `Ended` renders empty, the status pill reads active, and the Abandon form is present.
- [~] 7.7 **NOT EXECUTABLE in this stack, and the reason is the harness, not the change.** `dev:docker:up`'s container command runs `seed-dev --reset` on every boot, so a restart wipes the database: after `docker restart rembric-dev` the resumed row was gone, every session id was new, the smoke's memories were absent, and the log applied migrations from `0000` because the DB is rebuilt from scratch. Neither half of this task can be measured here — the row does not survive to be re-abandoned, and "no migration ran" is vacuous against a database that is always recreated. Substituted evidence, both executed: (a) the sweep-survival property is pinned by task 4.1, which re-runs `abandonStale` over the _same_ window that abandoned the row and asserts it stays `active`, and mutation 6.5 proves that test depends on the `last_activity_at` stamp; (b) "no migration in this change" is verified structurally — `git status apps/server/src/db/migrations/` is empty. A true cross-restart test needs a stack whose boot does not reseed, which does not exist in this repo today.
- [x] 7.8 Tear down.

## 8. Deferred and rejected — recorded so they are not silently lost

- [x] 8.1 **Deferred**: `POST /api/<slug>/sessions/:id/resume`. Not added here because an HTTP request has no `mcpSessionId` and therefore cannot pin the `SessionRouter` binding (design D4, D10). The client-emitter change will need it and must design the binding hand-off.
- [x] 8.2 **Deferred**: client-side resume emission for all five clients (design D10). Blocked on two facts, neither of which is establishable by reading code — see 8.3.
- [x] 8.3 **Open question, needs an empirical probe, NOT part of this change**: does Claude Code's `session_id` survive `claude --resume`? Method: start a session, capture the id POSTed to `/api/<slug>/sessions`, end it, resume, compare. Record the result on the change that adds the emitter.
- [x] 8.4 **Deferred**: changing `recentForContext`'s sort key from `started_at DESC` to `COALESCE(last_activity_at, started_at) DESC` (design open question 2). Needs measurement against a real corpus plus `pnpm run eval`; this change documents the current ordering as an accepted limitation instead.
- [x] 8.5 **Rejected, with reason recorded in design D6**: per-execution epoch column and client-carried fencing token. Do not reintroduce without new evidence that the ~3s zombie-`/end` window (`apps/plugin/scripts/_api.sh:79`, `apps/plugin/bin/rembric-plugin-core.mjs:8`) has widened.
- [x] 8.6 **Rejected/deferred, design D7**: an append-only execution or transition audit (issue #326 acceptance criterion 8). No consumer today; `previousStatus` / `previousEndedAt` in the resume response are the only report, and they are not persisted.
- [x] 8.7 **Rejected, design D11**: a dashboard Resume action. `dashboard/spec.md:876`'s "not reversible from the dashboard" copy stays true and MUST NOT be edited by this change.

## 9. Close-out

- [x] 9.1 Re-read the five delta spec files against their published counterparts one final time and confirm no requirement claims behaviour that tasks 1-4 did not implement. An overclaiming spec is worse than a missing one.
- [ ] 9.2 Reference issue #326 in the PR body, stating explicitly which acceptance criteria are met (1, 2, 3, 5, 6, 7, 9), which are rejected with reasons (4 — fencing, design D6), and which are deferred (8 — lifecycle history, design D7). PR title and body in English.

---

# Extension: client integration across all five clients

Phases 1-9 landed the server-side verb and the MCP tool. Phases 10-17 are the client half, decided in design D10 and D13-D16. Nothing above is re-opened; **do not edit a ticked box in phases 1-9.** Three of them are superseded and are re-stated here rather than rewritten in place:

- **Supersedes 5.6.** Its prediction (`4 body difference(s)`) described the change before this extension. The value measured after writing the extension is `ok (1 active change(s), 9 body difference(s) to review)`, zero missing headers and zero dropped scenarios. Task 15.6 carries the new per-requirement breakdown.
- **Supersedes 8.1 and 8.2.** Both recorded the HTTP route and the client emitters as deferred. They are no longer deferred; phases 10-13 implement them. The deferral's stated reason — an HTTP resume cannot pin the `SessionRouter` — remains **true**, and is now a documented property of the route rather than a reason to withhold it (design D13, task 10.5).

## 10. HTTP route

- [x] 10.1 Add `POST /:slug/sessions/:id/resume` to `apps/server/src/server/api-router.ts`, structurally mirroring the `/end` handler: `ctx.project` guard → `isAuthorized(ctx, 'write', …)` → `rejectIfDeleted(...)` → body parse → `deps.agentSessions.resume(sessionId, { tokenId: ctx.token.id })` → `domainErr` on throw. Do NOT call `deps.sweep`.
- [x] 10.2 Declare the body schema as `z.object({}).strict()`, NOT a bare `z.object({})`. The whole route-versus-field decision rests on this one property (design D13): a permissive schema here reproduces the silent-discard failure the route exists to avoid. Accept both an absent body and `{}` — `readJson` and the bash helper's empty-body normalisation both produce `{}`.
- [x] 10.3 Capture the row BEFORE the service call (the `rejectIfDeleted` read is not reusable — it returns `null` on success) so `previousStatus` and `previousEndedAt` are reportable. Serialise `previousEndedAt` as ISO or `null`, matching `/end`'s `endedAt?.toISOString() ?? null`.
- [x] 10.4 Write the route tests as one file exercising every scenario in the `http-api` delta: ended→active, abandoned→active, already-active no-op (assert `last_activity_at` unchanged), unknown property → 400 **with the `{}` control passing in the same run**, soft-deleted → 409, cross-token → 404, cross-project → 404, read-only token → 403, no-slug path → 404.
- [x] 10.5 Assert the negative design D13 promises: after an HTTP resume, the `SessionRouter` entry for a transport carries no session binding — **and** the control passes in the same run, `memory.session_resume` over MCP on the same row DOES set it. A one-sided negative here proves nothing.
- [x] 10.6 Confirm no SQL enters `api-router.ts`: the route calls the service, which calls the repository. Re-run `apps/server/src/test/invariants.test.ts`'s data-access-confinement test unchanged.

## 11. Shared core — the resume rides on `ensureSession`

- [~] 11.1 In `apps/plugin/bin/rembric-plugin-core.mjs`, POST `/api/${slug}/sessions/${sessionId}/resume` with `{}` inside `ensureSession`, **after** the `/sessions` POST and **only** on the branch that just added the id to `knownSessions`. None of the three early returns (empty id, sub-agent, already known) may reach it. — **already present in the working tree at `:125-139`** when this extension was written (landed concurrently), including the refinement that `rembricPost` now reports whether the request landed and the resume is skipped when the ensure did not. The spec was aligned to that refinement rather than the reverse: it is the better rule, and the shared-core requirement now carries it with the reason. Re-verify the branch placement and the `d.mts` surface against 11.2 and 11.3 rather than assuming this task is closed — **the route it POSTs to does not exist yet**, so nothing has exercised it end to end.
- [x] 11.1b Add the failed-ensure test the aligned spec requires: with the ensure answering non-2xx or throwing, the resume is NOT POSTed, the id stays in `knownSessions` (so a later call POSTs neither), and the control — a `200` ensure DOES produce the resume — passes in the same run.
- [x] 11.2 Do NOT export a new public function and do NOT add a field to `rembric-plugin-core.d.mts`'s `SessionProtocol`. The resume is an internal consequence of registration; exporting it would invite a client to call it directly and break the once-per-id guarantee. If a client ever needs it standalone, that is a different change.
- [x] 11.3 Confirm opencode and Pi need **zero** source edits, and pin it: assert neither `apps/plugin/.opencode-plugin/plugin.ts` nor `apps/plugin/.pi-plugin/index.ts` contains the string `/resume`. This is the `plugin-session-protocol` scenario "The resume rides on the core's ensure, not on the client".
- [x] 11.4 Core test: `ensureSession(id)` twice → exactly two POSTs total (one ensure, one resume), not four. Then `forgetSession(id)` followed by `ensureSession(id)` → two more, since the id left the set. Assert the ORDER (ensure before resume), not just the count.
- [x] 11.5 Core test: a sub-agent id POSTs neither.

## 12. Bash clients — Claude Code and Codex CLI

- [x] 12.1 `apps/plugin/scripts/session-start.sh`: after the existing `rembric_post "/api/${SLUG}/sessions" …`, add `rembric_post "/api/${SLUG}/sessions/${ID_ESC}/resume" '{}'` inside the same `if [ -n "$SESSION_ID" ] && [ -n "$SLUG" ]` block. Do not read or branch on `source`. Note the deliberate asymmetry with the JS core (11.1): `rembric_post` returns `0` unconditionally by its own fail-safe contract, so bash cannot cheaply gate on whether the ensure landed. Do NOT add a return-code channel to `rembric_post` for this — it is sourced by eight scripts that all rely on the current discipline, and the cost of one extra POST after a failed ensure is one timeout on an already-broken path.
- [x] 12.2 `apps/plugin/scripts/post-compact.sh`: the same pair, inside its existing guard. Correct the comment at `:22-24` — it currently claims the ensure re-creates an abandoned row, which it never did (design D10). One line, stating what the pair now does.
- [x] 12.3 `apps/plugin/hooks/hooks.json`: change the `SessionStart` matcher to `startup|resume|clear|fork`, and pass `claude-code` to `session-end.sh`.
- [x] 12.4 `apps/plugin/scripts/session-end.sh`: add the `AGENT="${1:-${REMBRIC_AGENT:-claude-code}}"` argument and the `case "$AGENT" in codex-cli) … ;; *) … ;; esac` transcript dispatch, copying `pre-compact.sh`'s existing shape. There is to be no `session-end.codex.sh`.
- [x] 12.5 `apps/plugin/scripts/_api.sh`: change `--max-time 3` to `--max-time "${REMBRIC_POST_MAX_TIME:-3}"`. Read at call time, not captured at source time.
- [x] 12.6 `apps/plugin/hooks/hooks.codex.json`: add the `SessionEnd` entry — no `matcher`, `"timeout": 3`, command `REMBRIC_POST_MAX_TIME=2 "${CLAUDE_PLUGIN_ROOT}"/scripts/session-end.sh codex-cli`. Both halves of the budget are required; `"timeout": 3` alone still lets one slow request consume it whole (design D14).
- [x] 12.7 Update the four `apps/plugin/test/hook-manifests.test.ts` assertions named in the `codex-distribution` delta: the event-type set (`:87-91`) to six including `SessionEnd`, the handler count (`:93-95`) to `9`, `expect(codexHooks.SessionEnd).toBeUndefined()` (`:97-100`) replaced by a positive assertion that the entry exists with `"timeout": 3` and no `matcher`, and the ordered invocation lists (`:182-207`) for both manifests. Keep every assertion an exact set or exact count — a `toContain` cannot catch a manifest claiming an event is absent, which is this defect's whole class.
- [x] 12.8 Add the Claude `SessionStart` matcher assertion for `startup|resume|clear|fork` (`hook-manifests.test.ts:54-59`) and assert the Codex one is still `startup|resume|clear`. The divergence is deliberate and must be pinned on both sides, or a future "consistency" fix silently adds a matcher Codex never emits.
- [x] 12.9 `sh -n` every edited script, and run the installer's headless layer (`rembric-tui-installer-e2e`) — both hook manifests are files the installer copies.

## 13. Hermes

- [x] 13.1 Add a process-scoped `set[str]` of ensured session ids to `RembricMemoryProvider`. Both ensure sites — `initialize` and `on_session_switch` — use the same set, so the resume fires at most once per id per process.
- [x] 13.2 POST `/api/<slug>/sessions/<id>/resume` with `{}` after the ensure at each site, only when the id was newly added to that set, and only when not `self._suppressed` and a slug resolved.
- [x] 13.3 Correct the false comment at `apps/plugin/.hermes-plugin/__init__.py:606-608`. It claims `/reset` and `/new` pass `parent_session_id=""` "by upstream contract"; `cli.py:7292` passes `old_session_id or ""`. Replace it with one line stating the actual rule — the discriminator is the cached id, not `parent_session_id` — per the comment policy (one non-obvious why, no banner).
- [x] 13.4 Do NOT consume `reason` or `rewound`; keep `del kwargs` (design D16). Add the test that pins it: identical HTTP behaviour with `reason="resume"`, with `rewound=True`, and with neither, for the same `(cached id, new id, slug, suppressed)` tuple.
- [x] 13.5 Add the same-id test: `on_session_switch(new_session_id=X, parent_session_id=X)` and `on_session_switch(new_session_id=X, parent_session_id="", rewound=True)` both POST no `/end`, **with the rotating case as the control in the same run**.
- [x] 13.6 Run the Hermes plugin's own Python tests (`apps/plugin/.hermes-plugin/tests/`), in particular `test_lifecycle_calls.py`, which asserts the exact POST sequence and will need the resume added.

## 14. Mutation — prove each new client guard is load-bearing

Same discipline as phase 6: a guard whose tests stay green on both sides of the mutation is not covered. Fix the test, not the mutation.

- [x] 14.1 Move the core's resume POST above the `knownSessions.has(sessionId)` early return → task 11.4's "two POSTs, not four" must go red.
- [x] 14.2 Remove the core's resume POST entirely → task 11.4 and the opencode reopen scenario must go red.
- [x] 14.3 Drop `.strict()` from the HTTP route's body schema → task 10.4's unknown-property test must go red. This is the D13 guard; if it stays green the test is asserting the `{}` control only.
- [x] 14.4 Remove `rejectIfDeleted` from the route → **the CROSS-PROJECT 404 test must go red, not the 409 one.** Corrected during apply, and measured: with the boundary guard gone the 409 `session_deleted` stays green because `AgentSessionsService.resume` (`apps/server/src/services/agent-sessions.ts:405-410`) re-reads `deletedAt` in its own tick and throws anyway, and the cross-token 404 stays green because the service masks on `tokenId` too. Project isolation is the only thing the router guard contributes exclusively. Targeting the 409 here would pass on both sides of the mutation and prove nothing — the exact failure mode this block exists to catch.
- [~] 14.5 Make the route call `setActiveSession` → task 10.5's negative must go red. Guards against a well-meaning future edit that "fixes" the asymmetry D13 records. **NOT EXECUTABLE, and that is the stronger result.** Measured: `apps/server/src/server/api-router.ts` has no reference to `router` or `SessionRouter` at all — the HTTP router is not constructed with that dependency, so the mutation cannot be written without first widening the router's dependency injection. The asymmetry D13 records is therefore structural rather than test-enforced: an edit that "fixes" it has to add a dependency, which is a visible design change rather than a one-line slip. Task 10.5's negative test still stands as the behavioural pin.
- [x] 14.6 Remove `fork` from the `hooks.json` matcher → task 12.8 must go red.
- [x] 14.7 Remove the `"timeout": 3` key from the Codex `SessionEnd` entry → task 12.7's positive assertion must go red.
- [x] 14.8 Remove Hermes's ensured-id set (resume on every switch) → task 13.4's once-per-process assertion must go red.

## 15. Verification gate (extension)

- [x] 15.1 `pnpm run typecheck`
- [x] 15.2 `pnpm run lint`
- [x] 15.3 `pnpm test` — including `apps/plugin/test/hook-manifests.test.ts`, `apps/plugin/test/nudge-fixtures.test.ts` and `apps/server/src/test/invariants.test.ts` (the shared-core single-implementation invariant now covers the resume call).
- [x] 15.4 `git ls-files apps/plugin/` — confirm ONE copy of each shared resource. The resume must appear once in `rembric-plugin-core.mjs`, once in each of `_api.sh`'s two bash callers, and once in `__init__.py`. Three languages, not three JS copies.
- [x] 15.5 `openspec validate resume-closed-sessions --strict`
- [x] 15.6 `pnpm run check:delta-freshness`. **Measured while writing this extension**, not predicted: `ok (1 active change(s), 9 body difference(s) to review)`, zero missing headers, zero dropped scenarios. The nine REVIEW entries and their line counts are expected to be exactly:
  - `claude-code-plugin / …/scripts/_api.sh` — 1 (the `--max-time` bullet)
  - `claude-code-plugin / …six hook event types…` — 6 (the catalog sentence, the `SessionStart` sub-heading, its `Matcher:` line, its `source` bullet, `post-compact.sh`'s re-POST bullet, the `SessionEnd` action bullet)
  - `mcp-api / Session-lifecycle MCP tools MUST reject soft-deleted sessions` — 2
  - `opencode-plugin / Session.created handler with sub-agent filtering` — 1
  - `pi-plugin / The shutdown reason decides whether the session is ended` — 1
  - `plugin-session-protocol / Sessions MUST converge…` — 1 (the `SessionEnd` (Claude Code) bullet)
  - `plugin-session-protocol / Per-client lifecycle mapping…` — 16 (ten table rows, the "Four rows are load-bearing" lead-in, its four bullets, and the "Codex CLI has no `SessionEnd` event" closing line)
  - `plugin-session-protocol / The shared client core…` — 1
  - `sessions / Sessions MUST be append-only` — 2

  A tenth reported requirement, or a different count on any of these, means an unintended edit — investigate rather than accept. `codex-distribution` and `hermes-agent-plugin` are absent because both go through `REMOVED` + `ADDED`, which the script does not compare; `http-api` is absent because its MODIFIED block rewrites no published line.

- [ ] 15.7 `pnpm run check:spec-provenance`
- [x] 15.8 `pnpm run eval` still NOT required — no retrieval, ranking, embedding or search path is touched. Re-confirm by grepping the diff for `apps/server/src/services/memory.ts`, `apps/server/src/db/repositories/memory-repository.ts` and `apps/server/src/llm/`.

## 16. Docker smoke against pre-existing seeded data — mandatory, not optional

This extension changes production HTTP behaviour and both shipped hook manifests, so the real-stack smoke is required again and is not satisfied by phase 7. Follow `.agents/skills/rembric-smoke-tests/`; the per-client arms follow `.agents/skills/rembric-plugin-development/references/e2e-walkthrough.md`.

- [x] 16.1 Bring up the dev stack (`chown -R 10001:10001 data-dev` first if it errors with `SQLITE_CANTOPEN`). Every boot reseeds — capture ids from the seeded corpus, do not rely on one created earlier.
- [x] 16.2 **Control first**: pick a seeded `ended` session and confirm over real HTTP that the ordinary ensure (`POST /api/<slug>/sessions {id}`) returns `created:false` and leaves it `ended`. Without this the resume arm proves nothing.
- [x] 16.3 `POST /api/<slug>/sessions/<id>/resume` with `{}` over real HTTP against that row. Assert the response shape including `previousStatus` and `previousEndedAt`, then re-read through `memory.session_get` and assert `status: 'active'`, `endedAt: null`.
- [x] 16.4 Send `{ "epoch": 3 }` to the same route and assert `400 invalid_input`, with the `{}` control succeeding in the same run. Probe the real HTTP boundary — calling the handler in-process bypasses the zod schema, which is the only thing this task measures.
- [x] 16.5 **Version-skew arm**: point a client's `REMBRIC_SERVER_URL` at a server build without the route (or an equivalent 404 path) and confirm the client emits exactly one stderr diagnostic and the host session continues — the ensure that preceded it still landed. This is the property that justified a route over a body field (design D13); asserting it only in unit tests would be testing the client's own mock. **Measured 4/4** against a real `node:http` server that answers `/sessions` with 200 and `/sessions/:id/resume` with 404 — i.e. a build predating this change — driving the real `createSessionProtocol`: the client issued exactly one resume call, the 404 propagated no exception, exactly one stderr diagnostic naming the path was emitted, and the preceding ensure still landed. The contrast that makes D13 concrete: the same skew against an `attach` field on the non-strict ensure body would have produced no extra call and no diagnostic at all — a `200 created:false` byte-identical to success.
- [ ] 16.6 **Claude Code arm**: run a real session against the stack, confirm the row registers, end it, then `claude --resume <id>` and confirm the row returns to `active` and a subsequent `memory.save` attaches to it. Record the observed session id on both runs — this is also the standing evidence for design open question 1. **NOT RUN headless, and the blocker is measured**: `claude -p --plugin-dir <worktree>/apps/plugin` loads no plugin — no session row, no `REMBRIC_DEBUG=1` diagnostic, and nothing about plugins or hooks in `--debug` output. Print mode appears not to run plugin hooks. What DOES stand as evidence: design open question 1 is closed independently (`claude -p --output-format json` then `--resume <id>` returned the identical `session_id`, and the CLI reference confirms it by contrast — `--fork-session` "create a new session ID **instead of reusing the original**"); and the scripts this arm would exercise are the SAME ones the Codex arm ran end-to-end (`session-start.sh`, `session-end.sh`, `_api.sh` — the repo forbids per-client variants), with only the manifest differing and manifests pinned by exact-set tests. Residual risk is therefore confined to `hooks.json` wiring. An operator should run this arm interactively before the release is called validated on Claude Code.
- [x] 16.7 **Codex arm, and design open question 4**: register a Codex session through the real hook and record the `session_id` it POSTs. Close Codex, confirm `SessionEnd` fired and the row is `ended`. Then `codex exec resume <id>` and record the `session_id` the hook POSTs on the second run. **Write down whether the two ids match.** If they do not, the Codex resume is a permanent no-op and the `codex-distribution` scenario "A resumed Codex conversation re-attaches its memories" is unimplementable and must be withdrawn rather than shipped as an aspiration. **DONE, measured 2026-08-09 against the local stack, and design open question 4 is CLOSED: the ids match.** Setup: `codex plugin marketplace add <worktree>` + `codex plugin add rembric@rembric` into an isolated `CODEX_HOME` (the operator's `~/.codex` was never touched), cached copy confirmed to carry this change's `hooks.codex.json`, run with `--dangerously-bypass-hook-trust`. Run 1 (`codex exec`) reported `thread_id 019fe7f1-3621-77d2-ae61-46f04d5dd276`, and a session row appeared under **exactly that id** with `agent='codex-cli'` — so the `session_id` the hooks receive IS the `thread_id`. The row reached `status='ended'` with `ended_at=1786302777093`, which is the **new `SessionEnd` firing**: before this change Codex had no such event and the row could only reach `abandoned` via the 24h sweep. Run 2 (`codex exec resume <id>`) reported the same `thread_id`, and afterwards the row was still a **single** row whose `ended_at` had moved to `1786302811050`. That movement is the proof the resume landed: a `/end` against a still-terminal row is a documented no-op and would have left the original timestamp. **First recorded conclusion for this task was wrong and is retracted**: an earlier attempt reported the hooks never firing, but the cause was a malformed probe `.rembric` (the file needs `PROJECT_SLUG=<slug>`, not a bare slug) — the shell scripts exit 0 in silence with no slug, which is indistinguishable from not running. Same defect invalidated the first Pi attempt.
- [x] 16.8 **Codex budget arm**: measure the wall time of the `SessionEnd` hook against the local stack and confirm it completes inside its declared 3 seconds. If it does not, the row stays `active` — which the spec permits — but the number belongs in the PR body either way. **Measured: 26 ms against a 3000 ms budget**, running the real `scripts/session-end.sh codex-cli` with the manifest's own `REMBRIC_POST_MAX_TIME=2` over a 120-line transcript, against the local stack. **Control that makes the number mean something**: the row went from `active` to `ended` with a fresh `ended_at` and an 85-char summary — without it the timing would be of a script that did nothing. It nearly was: the first two runs reported 2 ms because the probe invoked the script with `sh`, and the script reads `${BASH_SOURCE[0]}` under `set -u`, so a POSIX shell aborts into the script's own `trap ... ERR` and exits 0 having done nothing. Anyone re-running this arm must use `bash` and must assert the row moved. Remote deployments stay inside the budget by construction rather than by this number: the POST is capped at 2 s, leaving ~1 s for the transcript read and the diagnostic.
- [x] 16.9 **Pi arm**: run a real Pi session, quit it, confirm the row is `ended`, then `pi -r` the same conversation and confirm it returns to `active` and a subsequent `memory.save` attaches. Pi's id stability is already proven, so this arm isolates a bug in the core rather than in a host.
- [ ] 16.10 **opencode arm**: register a session, retire it (the sweep or a manual `markAbandoned`), reopen it in a new opencode process and confirm `chat.message`'s first ensure resumes it — opencode emits no `session.created` on a reopen, so this arm and 16.9 are not redundant. **NOT RUN.** Weigh the residual risk honestly rather than treating it as covered: opencode required **zero** source edits (task 11.3 pins that), and the code path its reopen would exercise is `createSessionProtocol.ensureSession` in the shared JS core — the exact function the Pi arm (16.9) drove end-to-end against the real stack, since Pi imports the same module. What is NOT covered is opencode's own trigger, i.e. that `chat.message` really is reached on a reopen with the same session id; that rests on the host reading proved in this change's research (`create` is idempotent and returns before `publish`, so `session.created` fires exactly once per id) and on the plugin's existing tests, not on a live run. Run it before calling opencode validated.
- [x] 16.11 Load `/dashboard/sessions` headless (Playwright + screenshot; there is no GUI here) and confirm a Codex row now reads `ended` after a normal close where it previously read `abandoned`.
- [x] 16.12 Tear down.

## 17. Deferred and rejected in this extension — recorded so they are not silently lost

- [x] 17.1 **Deferred, design open question 3**: raising Claude Code's `SessionEnd` hook timeout. The host gives `SessionEnd` handlers a shared 1.5-second budget unless a per-hook `timeout` raises it, and `hooks.json` declares none while `_api.sh` allows the POST 3 seconds. Found while verifying the Codex budget. The fix is one JSON key; what is missing is the measurement that justifies the value. Do NOT fix it here on the strength of the reading alone.
- [x] 17.2 **Rejected, design D16**: consuming Hermes's `reason` or `rewound` keywords. Neither is declared by the `MemoryProvider` ABC, both are absent from some call sites, and under D10 the only thing either could buy is skipping a call that is already a no-op. Task 13.4's test is what keeps a future contributor from wiring it up silently.
- [x] 17.3 **Rejected**: an `attach: true` field on `POST /api/<slug>/sessions` instead of the route. Do not reintroduce without first making `sessionPostSchema` strict, which is its own breaking change to a shipped HTTP contract that five clients POST to.
- [x] 17.4 **Rejected**: gating any client's resume on a host signal. The `plugin-session-protocol` scenario "The resume is not conditioned on a host signal" is what fails if someone adds one.
- [x] 17.5 **Not in scope**: changing the `abandonStale` window now that Codex reaches `ended` normally. The 24h default is unchanged, and the sweep is still the net for every close where `SessionEnd` does not fire.
- [x] 17.6 Extend the PR body from task 9.2 to name the three pre-existing defects this extension corrects (Codex `SessionEnd`, Claude Code `fork`, the three Hermes statements), each with its documentation quote, and to state the two measured values from tasks 16.7 and 16.8.
