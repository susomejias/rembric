# Tasks — tell the truth about the relevance gate

Read `design.md` first: D1–D8 are cited by number below. No migration, no schema change, no load-bearing invariant crossed.

## 1. The empty-pool abstention (D2, D3)

- [x] 1.1 Add a second reason constant beside `ABSTAIN_REASON` in `apps/server/src/services/hybrid-search.ts:102` — proposed value `'no candidate matched the query in this scope'`. Do NOT reuse or reword `ABSTAIN_REASON`; the floor's string is asserted elsewhere and must stay byte-identical.
- [x] 1.2 In `hybridSearch`, return `{ ids: [], abstained: true, reason: <new constant> }` when `fused.length === 0`, placed BEFORE the `if (gatesEnabled || opts.onGateWindow)` block (`:139`) so the verdict holds with both gates `null` and adds no level computation and no DB read (`memory/spec.md` "While BOTH gates are disabled the branch SHALL perform no gate-related work at all").
- [x] 1.3 Confirm by reading the surrounding code, then by running, that the early return cannot swallow the sweep sink: `opts.onGateWindow` is supplied only by the calibration sweep, and `poolLevels` already special-cases an empty pool (`:172-184`). Decide and record whether the sweep still needs its `onGateWindow` callback on an empty pool; if it does, order the early return after the callback.
      **DECIDED: the sweep keeps its callback on an empty pool.** `poolLevels`' own comment ("Only the sweep sink wants term statistics for a pool with nothing to score") is the evidence that the read was deliberately kept for it, and the sweep prints the df list precisely to show which query terms the index does not hold — which is exactly why a pool would be empty. So the early return notifies the sink first (level/coverage/cosine/poolSize all 0, real `documentCount` and df map) and then returns. Two consequences, both deliberate: `poolLevels`' `pool.length === 0 && !opts.onGateWindow` early return became dead (the only empty-pool caller now always has a sink) and was deleted; the floor's `leveled.length === 0` disjunct also became dead (an empty pool can no longer reach the floor) and was deleted, which is what makes the empty-pool reason win over the floor's on an empty pool — the truthful attribution per the delta. No committed corpus query has an empty pool, so the sweep's output is byte-identical either way.
- [x] 1.4 Verify the four existing `abstained === false` assertions predicted green in D8's table actually are, by running `pnpm vitest run apps/server/src/services/hybrid-search.test.ts` — in particular `:898-905` (empty page 2 over a 5-row pool), which is the control for scoping the new state to the POOL rather than the page. A failure there means (A) was scoped to the page and must be corrected, not that the test is wrong.
- [x] 1.5 Tighten the name of `hybrid-search.test.ts:658` — "with the gates disabled (the default), hybridSearch never abstains" is now an overclaim; it holds only because its pool is non-empty. Rename to say so and assert the pool's non-emptiness in the test rather than leaving it implicit.
- [x] 1.6 Extend the existing empty-pool probe at `apps/server/src/test/retrieval/queries.test.ts:59-62` (`zzqqwx vvbbnm ppllkk` through the lexical-only retriever, which currently asserts `ids` only) to also assert `abstained === true` and that the reason is the new constant, not the floor's.
- [x] 1.7 Add a test for each additional reachable path named in D2, since one reachable path is not evidence for a rule: a `type` filter matching no row in a populated scope, and an empty scope. Both must abstain with the new reason.

## 2. `gateShortened` in the service layer (D4)

- [x] 2.1 Add `gateShortened?: true` to `HybridSearchResult` (`hybrid-search.ts:96-100`) and set it when ALL THREE conditions hold: `gated.length < leveled.length` (the filter removed at least one row) AND the returned page holds fewer rows than `opts.limit` AND `opts.offset < fused.length`. Compute the second against the sliced page, after the boost and diversity cap, not against the gated pool.
      The third condition landed after the rest of the section: at `offset >= fused.length` the ungated page is empty too, so the flag was naming the gate as the cause of an emptiness the caller's own paging produced. Measured at the MCP wire on a five-row pool with one survivor — truthful at offsets 0/1/4, false at 5/6/50 — with a sibling project whose filter removes nothing as the control.
- [x] 2.2 Forward it through `MemoryService.searchWithAbstention` (`apps/server/src/services/memory.ts:445-455` signature, `:563` final return) AND through the `if (ids.length === 0) return { memories: [], abstained, reason };` early return at `:550` — an empty page CAN be gate-shortened (pool 5, gate keeps 2, `offset: 2`), so dropping the field on that path would lose exactly the case D4 argues for.
- [x] 2.3 Correct the `searchWithAbstention` docstring at `memory.ts:440-443`. It currently reads "Same as `search`, plus the `ABSTENTION_FLOOR`'s verdict alone: a page shortened by `RELATIVE_LEVEL_RATIO` reports `false`, so a caller can tell 'nothing relevant exists' from 'fewer than `limit` rows were relevant'" — a promise that was false as shipped and is what this change makes true. One line, non-obvious why only; no banner, no restatement of the diff.
- [x] 2.4 Unit-test the four-way truth table on `hybridSearch` directly, each case constructed rather than incidental: filter removed rows + short page (flag), filter removed rows + full page (no flag), no removal + short page (no flag), no removal + full page (no flag). The two no-flag cases with a short page are the controls that make the first case a finding.
- [x] 2.5 Assert D4's disjointness claim rather than trusting it: no single response carries both `abstained: true` and `gateShortened`. Test it at `relativeLevelRatio: 1` over a non-empty pool (the filter keeps only the leader, so the pool cannot empty — the existing test at `hybrid-search.test.ts:865-874` already pins that half).

## 3. The MCP search surface (D4)

- [x] 3.1 Add `gateShortened: z.literal(true).optional()` to `memorySearchOutput` (`apps/server/src/mcp/memory-tools.ts:363-378`), documented with one line on the condition — the field's meaning is not derivable from its name alone. Narrowed from the `z.boolean().optional()` this task first specified, so "omitted, never `false`" is enforced instead of documented: emitting `false` is rejected server-side with `-32602 Output validation error … "Invalid literal value, expected true"` (measured by forcing the emission).
- [x] 3.2 Emit it conditionally in the search response (`memory-tools.ts:1051-1054`) as `...(gateShortened ? { gateShortened } : {})`, matching the three conditional fields already there. Never emit `false`.
- [x] 3.3 Confirm `structuredContent` and the `text` block stay equal in meaning (`mcp-api/spec.md:1484` "Structured content matches the text payload") — both are built from the same `ok()` object, so this is a check, not a change.
      CONFIRMED, no change: `ok()` (`apps/server/src/mcp/result.ts:6-12`) serialises ONE payload to `text` and then `JSON.parse`es that same string into `structuredContent`, so the two cannot diverge by construction.

## 4. The MCP context surface (D6)

- [x] 4.1 Switch the context relevance channel's ranked pass (`memory-tools.ts:1411`) from `deps.memory.search` to `deps.memory.searchWithAbstention`, capturing `abstained`, `abstainReason` and `gateShortened`. Keep the same `{ query: focusText, limit: RELEVANCE_LIMIT }` input — the pass's limit does not change (D6).
- [x] 4.2 Add the verdict object to the context output schema (near `memory-tools.ts:480`) as `rankedPass: <verdict>.optional()` and emit it in the response (`return ok({…})` at `:1476-1487`) ONLY when the ranked pass executed. The field names inside it are `memory.search`'s, per the delta (`specs/mcp-api/spec.md:100`, "the flag names SHALL match `memory.search`'s") and D6 — so `abstainReason`, not `reason`.
      **CORRECTED AFTER REVIEW.** This task originally prescribed `reason: z.string().optional()` literally, contradicting both its own design (D6) and the delta sentence above; the first implementation followed the task and shipped `rankedPass.reason` beside `memory.search`'s `abstainReason`. The spec and design are right and the task was wrong: `abstainReason` is the already-published wire name on `memory.search` (`openspec/specs/mcp-api/spec.md`), so the rename ran INWARD instead — `HybridSearchResult.reason` → `abstainReason`, through `MemoryService.searchWithAbstention`, which also removed `handleSearch`'s `reason` → `abstainReason` remap. Both surfaces are now built from one exported `SearchVerdict` type (`hybrid-search.ts`) and one zod fragment (`memory-tools.ts::searchVerdict`), so they cannot drift again. Nothing published changed name on the wire.
- [x] 4.3 Test the two skip paths explicitly, because their absence is the requirement: no derivable `focusText` (`:1370`) and an entity pre-pass that filled the channel to `RELEVANCE_LIMIT` (`:1410`). In both, `rankedPass` must be ABSENT from the response — not `{ abstained: false }`.
- [x] 4.4 Test the two reporting paths: a focus whose pool is empty (`rankedPass.abstained === true` with the new reason) and a focus the filter shortens (`rankedPass.gateShortened === true`, `abstained === false`).
- [x] 4.5 Add the D6 case that looks like a contradiction and is not: the channel full (entity rows plus ranked rows reaching `RELEVANCE_LIMIT`) while `rankedPass.gateShortened` is `true`, because the flag describes the ranked pass's own page against its own limit. Assert it so the behaviour is pinned rather than discovered later as a bug report.

## 5. The description reword (D5) — the character budget is the constraint

- [x] 5.1 Apply the three edits in D5 to `SEARCH_DESCRIPTION` (`apps/server/src/mcp/server.ts:130`) exactly as quoted: the offset parenthetical (−25), the default-page clause (−39), and the tail swap (+121).
      Applied verbatim from D5. **Noted deviation from the Risks section, not from D5:** the risk bullet claims "the widen affordance stays attached to 'if every result looks relevant' as before", but D5's edit 2 is exactly what removes that phrase (it is 39 of the 64 reclaimed chars). D5 is the operative decision and the arithmetic depends on it, so the phrase is gone; the widen affordance itself survives ("Returns a small default page (8); need more? Prefer raising `limit` (up to 200).") and `mcp-api/spec.md:413` names only the affordance, not the condition attached to it.
- [x] 5.2 Measure the result from a REAL `tools/list` response, not from the constant — that is the boundary `mcp-integration.test.ts:379` asserts over and the only measurement that counts. Record the length and the remaining headroom in the task line. Expected: 1874 and 26. If it differs, the wording drifted; fix the wording, do not adjust the expectation.
      MEASURED at the boundary: **1874 chars, 26 of headroom** under `DESCRIPTION_MAX_LENGTH = 1900`. Asserted as `expect(desc.length).toBe(1874)` over the `tools/list` response in `mcp-integration.test.ts`, so a drift reds CI. Per-edit deltas matched D5 exactly: −25, −39, +121.
- [x] 5.3 Confirm every mandated clause survived, by substring assertion against the same `tools/list` string, one assertion per requirement rather than one combined check: the recall trigger (`mcp-api/spec.md:413`), the hybrid-ranking clause, the widen affordance (`limit` AND `offset` both still named), and `not as a signal to invent or assume context` verbatim (`mcp-api/spec.md:254`).
- [x] 5.4 Add the negative assertion that would have caught this defect: while `ABSTENTION_FLOOR` is `null`, the `memory.search` description SHALL NOT attribute abstention to the relevance floor. Assert against the live `tools/list` string. This closes the coverage gap found by `grep -rln SEARCH_DESCRIPTION apps/server/src` returning only `server.ts` — no test asserted any of this text before.
- [x] 5.5 Add the substring assertions for the new content: the description names `gateShortened`, states that a short page is not corpus exhaustion, and states that a full page is not proof of relevance.
- [x] 5.6 Confirm no OTHER tool description moved. `pnpm vitest run apps/server/src/test/mcp-integration.test.ts` covers the cap over every registered tool; record the count asserted so a silently-skipped tool is visible.
      **23 tools** measured from the live `tools/list` (`grep -c 'registerTool(' src/mcp/server.ts` = 23, and the cap test's floor is `>= 23`); only `memory.search`'s description changed in the diff.

## 6. The eval harness (D1, D7)

- [x] 6.1 Narrow `checkAbstentionFlags` (`apps/server/src/test/retrieval/run-eval.ts:74-85`) to fail only when `o.reportedAbstained === true && o.retrieved.length > 0`. Update its docstring: it currently says "An explicit abstention flag that disagrees with emptiness fails the run", which becomes false. State which direction is enforced and why the other is not (one line, pointing at the spec — not a rationale essay in a comment).
- [x] 6.2 Add a direct test of `checkAbstentionFlags` over constructed `RawOutcome`s: `abstained: true` with 3 results must produce a failure naming the query; `abstained: false` with 0 results must produce none. This is the coverage the committed corpus provably cannot supply (D7) and it is what makes 6.1 a verified behaviour change rather than a text edit.
- [x] 6.3 Run `pnpm run eval` and record the shortened-page distribution — how many of the 24 committed queries return fewer than 8 rows at `k=8`, and the `gated` vs `ungated` counts. Assert on the DISTRIBUTION, not on one query: the headline claim is "18 of 24 shortened, 0 of 24 empty" and it regenerates. If the figure has moved, record the new one and say what moved it; the claim is motivating evidence, not a contract, so a drift is a note rather than a blocker.
      MEASURED at this commit's tree (hybrid retriever, `k=8`, shipped `RELATIVE_LEVEL_RATIO = 0.4`, every query run twice — once gated, once with `relativeLevelRatio: null`): **24 queries, 18 shortened (<8 rows), 0 empty, 6 full**. The headline figure holds. One sub-figure drifted: the proposal says "six of them `gated=2` against `ungated=8`"; the measurement is **8** such queries (`q-atlas-orm`, `q-nimbus-pg-pin`, `q-pref-pr-review`, `q-pref-nimbus-docstrings`, `q-nimbus-dup-processing-reason`, `q-es-atlas-validation`, `q-es-commit-language`, `q-es-nimbus-cron-migration`). Recorded as a note, not a blocker: it is motivating evidence and no spec text depends on it. Every query reports `abstained: false`, which is why the harness guard has nothing to judge (D7).
- [x] 6.4 Confirm every committed baseline metric is unchanged by this change. Nothing here alters which rows a query returns, so `precisionAtK`, `recallAtK`, `mrr`, `avgTokensReturned`, `abstentionFalsePositiveRate` and `overAbstentionRate` must all be identical at every committed `k`. A moved metric means a behaviour change slipped in and must be explained before landing. Do NOT regenerate baselines.
      CONFIRMED: `pnpm run eval` passed with `hybrid P@8=0.156 R@8=1.000 MRR@8=0.797 tokens=318 abstainFP=1.00 overAbstain=0.00`, `grep P@8=0.148 R@8=0.938 MRR@8=0.710 tokens=490`, `memory-md-dump P@8=0.094 R@8=0.656 MRR@8=0.295 tokens=516` — the same figures the committed baselines carry, and `checkFloors`/`checkBounds` raised nothing. `git status` shows no file under `src/test/retrieval/baselines/` touched.

## 7. Mutation — every new condition, one at a time (CLAUDE.md)

Per CLAUDE.md, a new guard is not covered until its test fails without it. `node scripts/mutate.mjs --file … --spec … --mutation … --with …` does the backup/mutate/run/restore loop. Weaken each condition SEPARATELY; a combined mutation cannot tell you which test carried which condition.

- [x] 7.1 Mutate `fused.length === 0` → `false` (the empty-pool abstention never fires). The tests from 1.6 and 1.7 must go red, naming the abstention.
      RED as required. `node scripts/mutate.mjs --file apps/server/src/services/hybrid-search.ts --spec src/services/hybrid-search.test.ts --mutation 'if (fused.length === 0) {' --with 'if (false) {'` → `CAUGHT by 7`: the five new empty-pool tests plus `never reports both an abstention and a shortening, even at ratio 1` and `names each abstention cause with its own reason` (first failure: `expected false to be true` at `hybrid-search.test.ts:1129`). The 1.6 probe in `queries.test.ts` names it too (verified separately by the same mutation against that spec).
- [x] 7.2 Mutate `fused.length === 0` → `true` (always abstains). The full-page tests and the eval must go red — the control that proves the condition, not just its presence, is under test.
      RED on both instruments. vitest: same mutation `--with 'if (true) {'` → `CAUGHT by` 22 tests, including all four truth-table cases, `a page shortened by the relative filter reports abstained:false` and the three `disabled path does no gate work` tests. Eval (run by hand — `mutate.mjs` drives vitest only): `pnpm run eval` exited 1 with `hybrid@5 precisionAtK regressed: 0.000 < committed floor 0.188`, `hybrid@8 recallAtK regressed: 0.000 < committed floor 0.950`, `hybrid@{5,8} overAbstentionRate regressed: 1.000 > committed cap 0.063`. File byte-verified restored.
- [x] 7.3 Mutate the removal half of `gateShortened` (the `gated.length < leveled.length` comparison) to a constant `true`. The "no removal + short page" test from 2.4 must go red.
      RED as required, and by exactly the intended control. `--mutation 'gated.length < leveled.length' --with 'true'` → `CAUGHT by 1`: `the page is short but the filter removed nothing — no flag`.
- [x] 7.4 Mutate the shortness half (page length vs `limit`) to a constant `true`. The "filter removed rows + full page" test from 2.4 must go red.
      RED as required. `--mutation 'page.length < opts.limit' --with 'true'` → `CAUGHT by 2`: `the filter removed rows but the page is full — no flag` and `survives the service layer, including on an empty page past the survivors`.
- [x] 7.5 Mutate the `rankedPass` presence condition so the field is always emitted. The two skip-path tests from 4.3 must go red.
      RED as required. `--file apps/server/src/mcp/memory-tools.ts --spec src/mcp/context-relevance-under-gates.test.ts --mutation '...(rankedPass ? { rankedPass } : {}),' --with 'rankedPass: rankedPass ?? { abstained: false },'` (a bare always-spread would NOT be observable: `JSON.stringify` drops an `undefined` value, so the field would stay absent in the payload) → `CAUGHT by 2`: `omits the ranked pass when no seed can be derived` and `omits the ranked pass when the entity pre-pass already filled the channel`.
- [x] 7.6 Mutate `checkAbstentionFlags`' narrowed predicate to `if (false)`. **Expected: `pnpm run eval` still passes, unchanged.** Record that result and its interpretation (D7): a guard passes when there is no defect, and no committed outcome exercises it — `archive/2026-08-03-weight-relevance-levels-by-idf/measurements/sweep-after-amendment.txt:56` records "fused pool per query: min 10, max 26". Do NOT read a passing run as a regression, and do NOT add a corpus query to force it (deferred — task 10.1).
      **As predicted by D7: the eval still PASSED, byte-identically.** `if (false)` in `checkAbstentionFlags` → exit 0, `hybrid P@8=0.156 R@8=1.000 MRR@8=0.797 tokens=318 abstainFP=1.00 overAbstain=0.00` and the two other retrievers unchanged, `rembric retrieval eval passed.` Interpretation, recorded rather than mistaken for a regression: a guard against a defect passes when there is no defect, so `if (false)` cannot distinguish "vacuous" from "correct" — and no committed outcome exercises it in either direction (measured: all 24 queries report `abstained: false` with 2–8 rows; pool never empty, `sweep-after-amendment.txt:56`). The evidence is in 7.7. Note: the predicate now lives in `abstention-flags.ts` (extracted so it can be imported without executing `run-eval.ts`'s top-level `main()`).
- [x] 7.7 Mutate the same predicate by INVERTING it (`===` instead of the narrowed comparison). `pnpm run eval` must FAIL, naming at least one query. This is what proves the check evaluates 24 live outcomes rather than being skipped, and it is the mutation that carries the evidence 7.6 cannot.
      **RED, as required.** Predicate inverted to `if (o.reportedAbstained !== true && o.retrieved.length > 0)` → `pnpm run eval` exited 1 with `rembric retrieval eval FAILED:` and **72 named outcomes**, e.g. `hybrid 'q-atlas-orm' reported abstained=false while returning 2 result(s) — the flag disagrees with the behaviour it describes`. 72 = 24 hybrid queries plus the 48 flagless `grep` / `memory-md-dump` outcomes, whose `undefined` also satisfies `!== true`. This is what proves the check evaluates live outcomes rather than being skipped. File byte-verified restored.
      Additionally, the direct test from 6.2 was mutation-checked three ways against `abstention-flags.test.ts`: `if (…) {` → `if (false) {` → CAUGHT by 2 (`fails an outcome that claims abstention while returning rows, naming the query`, `names every offending outcome, not just the first`); `o.retrieved.length > 0` → `true` → CAUGHT by 1 (`permits the two coherent combinations`); `o.reportedAbstained === true` → `true` → CAUGHT by 2 (that test plus `ignores a retriever with no explicit flag, in both result shapes`).
- [x] 7.8 For any guard that turns out to be TYPE-level rather than runtime, note it here and verify it with a hand `tsc` widen/restore loop instead — `mutate.mjs` drives vitest, which strips types and ignores `@ts-expect-error`, so it reports a type-level guard as uncovered whether or not it is. Expected candidate: the optionality of `gateShortened` in `HybridSearchResult`.
      CONFIRMED type-level, verified by a hand `tsc` loop rather than by `mutate.mjs`. Appending `const _probe: HybridSearchResult = { ids: [], abstained: false, gateShortened: false };` to `hybrid-search.ts` yields `error TS2322: Type 'false' is not assignable to type 'true'` — so `gateShortened?: true` is what makes "omitted, never `false`" unrepresentable rather than merely untested. Probe removed; `tsc --noEmit` clean afterwards.
      Two further mutations run beyond the list above, because each forwarding hop is its own condition: dropping the spread from `memory.ts`'s empty-page return → CAUGHT by `survives the service layer, including on an empty page past the survivors`; deleting `...(gateShortened ? { gateShortened } : {}),` from the MCP search response → CAUGHT by the MCP-boundary gate-shortening test.
- [x] 7.9 Mutate the pool-boundary conjunct added in 2.1, both by dropping it and by weakening it off-by-one, against `src/test/mcp-integration.test.ts -t 'gate-shortened'`.
      Dropping it (`--mutation 'gateRemovedRows && page.length < opts.limit && opts.offset < fused.length' --with 'gateRemovedRows && page.length < opts.limit'`) → `CAUGHT by 1`, failing on `offset 5: expected { count: +0, memories: [], …(2) } to not have property "gateShortened"` / `Received: true`. Weakening it (`--mutation 'opts.offset < fused.length' --with 'opts.offset <= fused.length'`) → `CAUGHT by 1`, failing on the same `offset 5` case and not on `offset 50`, which is what shows the boundary itself is pinned rather than just the far region. The two pre-existing conjuncts still red their own unit tests against the three-conjunct form: dropping `gateRemovedRows` → `CAUGHT by 2` (`the page is short but the filter removed nothing — no flag`, `the filter disabled removes nothing, so a short page carries no flag`); dropping `page.length < opts.limit` → `CAUGHT by 2` (`the filter removed rows but the page is full — no flag`, `survives the service layer, including on an empty page past the survivors`).
- [x] 7.10 Close two pre-existing coverage gaps on the lines this change rewrote — both mutants were green at `a1a29a0^` too, but one is the field this change renamed. Each mutation run against the WHOLE server suite (`--spec src`), not just its own file, so "nothing else pins it" is measured rather than assumed.
      `--file apps/server/src/services/memory.ts --mutation 'abstained: ranked?.abstained ?? false,' --with 'abstained: ranked?.abstained,'` (the narrow form: it changes only the no-query listing branch, where `ranked` is `undefined`) → `CAUGHT by 1`, the new `publishes the verdict on both search branches` test, and by nothing else in the suite. The broad form `--with 'abstained: undefined,'` is caught by 18 tests because it also kills the ranked branch, where the required field is already exercised — which is why the narrow form is the one that isolates the gap.
      `--file apps/server/src/mcp/memory-tools.ts --mutation '...(abstainReason ? { abstainReason } : {}),' --with '...{},'` → `CAUGHT by 1`, the same new test, and by nothing else in the suite: before it, only the ABSENCE of `abstainReason` was asserted at the wire, never its emission.

## 8. Verification

- [x] 8.1 `pnpm run typecheck` — clean (`tsc --noEmit`, 3 workspace projects).
- [x] 8.2 `pnpm run lint` — clean (`eslint .`, no output).
- [x] 8.3 `pnpm test` — **129 test files passed | 1 skipped (130); 2275 tests passed | 10 skipped (2285)**, exit 0 (2274/2284 before 7.10 added one test). The Hermes plugin suite that runs after it: `Ran 72 tests … OK`.
- [x] 8.4 `pnpm run eval` — retrieval is touched, so this is mandatory, not optional. Baselines unchanged (6.4).
- [x] 8.5 `pnpm run check:spec-provenance`
- [x] 8.6 Confirm no plugin change is needed rather than asserting it: `grep -rl abstain apps/plugin/` must return ZERO files, `git ls-files apps/plugin/ | wc -l` unchanged, and no file under `apps/plugin/` modified in the diff. The description is server-side and all four clients read it from `tools/list`; an optional output-schema field is additive for the bridge, which does not interpret response bodies.
      MEASURED: `grep -rl abstain apps/plugin/` → **0** files; `git ls-files apps/plugin/ | wc -l` → **68** (unchanged); `git status --porcelain apps/plugin/` → **0** lines.
- [x] 8.7 Confirm the invariant suite is untouched in substance: `pnpm vitest run apps/server/src/test/invariants.test.ts`. No SQL is added, so the data-access-confinement grep must be unaffected — if it moved, SQL leaked out of `db/`.

## 9. Docker smoke against pre-existing seeded data (operator-run — MCP surface changed)

Standing requirement for anything touching MCP. `dev:docker:up` reseeds with `--reset` on every boot, so the "pre-existing data" half must be established deliberately: boot once to seed, then probe WITHOUT rebooting.

- [x] 9.1 **OPERATOR:** `chown -R 10001:10001 data-dev` then `pnpm run dev:docker:up` (foreground; see `docs/docker.md`). Wait for the seed to complete.
      DONE (applier-run, not deferred to the operator): `chown -R 10001:10001 data-dev` then `pnpm run dev:docker:up`. Booted healthy, mounts verified as this worktree (`/root/rembric/apps/server/src -> /app/apps/server/src`, `/root/rembric/data-dev -> /data`), seed complete: `[bootstrap] counts: memory=35 projects=1 sessions=5 tokens=3 prompts=0`. Probes then ran WITHOUT rebooting.
- [x] 9.2 **OPERATOR:** Against the running stack, probe the four states through the real MCP boundary at `/mcp/<slug>` with an official SDK `Client` over `StreamableHTTPClientTransport` — not by calling handlers directly, which bypasses the zod schemas:
      DONE, through an official SDK `Client` over `StreamableHTTPClientTransport` at `/mcp/demo` and `/mcp` (production embedder `onnx-community/gte-multilingual-base` wired):
      (a) `{query:'nimbus scheduler', limit:8}` → `count=3 abstained=false gateShortened=true` — 3 of ~26 pooled rows survived the 0.4 ratio;
      (b) `{query:'zerocopy hotpath deploy pin', limit:8}` → `count=2 abstained=false gateShortened=true`;
      (c) `{query:'nimbus', type:'procedural'}` (no seeded row has that type) → `count=0 abstained=true abstainReason="no candidate matched the query in this scope"`, `gateShortened` ABSENT. The empty-scope variant agrees: `scope:'global'` on the unscoped connection (the seed leaves global empty) → same verdict, same reason;
      (d1) CONTROL, full page: `{query:'nimbus scheduler', limit:2}` → `count=2 abstained=false`, `gateShortened` **ABSENT** — the flag is conditional, not hardcoded;
      (d2) `{query:'nimbus scheduler', limit:2, offset:60}` → `count=0 abstained=false gateShortened=true`. **SUPERSEDED — this measurement is what exposed the defect, not the specified behaviour.** `offset:60` sits past the whole ~26-row pool, so the ungated page there is empty too and the flag was blaming the gate for the caller's own paging. The pool-boundary conjunct added in 2.1 removes exactly this region; the region and its boundary are now measured at the MCP wire in `mcp-integration.test.ts` and mutation-pinned in 7.9. What survives from this probe is the truthful half: an `offset` past the SURVIVORS but inside the pool still carries the flag.
      (d3) CONTROL, the case that clause actually describes: three identical global rows (all at the pool leader's level, so the filter removes nothing) → `limit:3` gives `count=3` with `gateShortened` ABSENT, and `limit:2, offset:5` gives `count=0 abstained=false` with `gateShortened` **ABSENT**. Without (d1)+(d3) a probe stuck on `true` would look like a finding.
      (a) a normal query → full page, no `gateShortened`, `abstained: false`;
      (b) a query the filter shortens → `gateShortened: true`, `abstained: false`;
      (c) `memory.search` with a `type` filter matching no seeded row → `abstained: true` with the new reason;
      (d) CONTROL — a deep `offset` past a non-empty pool → empty page, `abstained: false`, NO `gateShortened`. Without (d) a broken probe is indistinguishable from a real finding.
      This control's literal wording turned out to be right and (d2)'s reading of it wrong: past the whole POOL the flag must stay absent whether or not the filter removed anything, and only the region between the survivors and the pool's end carries it.
- [x] 9.3 **OPERATOR:** Probe `memory.context` on the same stack: with a focus that shortens (`rankedPass.gateShortened`), with a focus whose pool is empty (`rankedPass.abstained`), and CONTROL with no focus on a connection with no session or prompts (`rankedPass` ABSENT).
      DONE: focus that shortens (`'zerocopy hotpath deploy pin'`) → `rankedPass={"abstained":false,"gateShortened":true}` with 2 rows in the channel; focus over an empty ACTIVE pool (the three control rows archived first, so both branches' status predicate empties the pool even with the embedder wired) → `rankedPass={"abstained":true,"reason":"no candidate matched the query in this scope"}` with `relevantMemories=0` (that `reason` key is the payload as measured, BEFORE 4.2's correction renamed it to `abstainReason` later in this same unreleased change — the recorded probe is not re-run, so it is annotated rather than rewritten); CONTROL, unscoped connection with no session and no prompts and no `focus` → `relevantMemories=0` and `rankedPass` **ABSENT**.
      Worth recording because it is the confabulation this change does NOT fix (out of scope by design): a focus of pure nonsense (`'zzqqwx vvbbnm ppllkk'`) against the populated project still returns 2 ranked rows with `rankedPass={"abstained":false,"gateShortened":true}` — the dense branch has no distance floor, so the pool is not empty and the honest report is "the gate shortened this", not "nothing matched".
- [x] 9.4 **OPERATOR:** Read the `memory.search` description from that stack's `tools/list` and record `String.length`. It must match the figure from 5.2 exactly — this is the same measurement at the real boundary, and a mismatch means the constant and the served description diverge.
      MEASURED from the running stack's `tools/list`: **`String.length` = 1874** — identical to 5.2's figure, so the constant and the served description do not diverge. Same string: `names gateShortened: true`, `mentions "relevance floor": false`.
- [x] 9.5 **OPERATOR:** Confirm the upgrade story on populated data: no migration ran, `memory_fts` / `memory_vec` / the three entity tables were not rebuilt, and the row counts are unchanged from before the probes. Record the counts on both sides — a digest match over two empty sets proves nothing.
      CONFIRMED. `_migrations` = **31 rows before and after**, last row identical (`0030_memory_fts_vocab.sql`, `applied_at=1785710129139`); no "applying migration" line in `docker logs`. Row counts before → after: `memory 35 → 38`, `memory_fts 35 → 38`, `memory_vec_rowids 35 → 38`, `memory_entity_scan 35 → 38`, `memory_relations 21 → 24`, `memory_entities 1 → 1`, `memory_entity_links 1 → 1`, `confirmations 0 → 0`. Every delta is exactly the 3 control rows the probe itself saved (plus the 3 candidate judgments their identical content legitimately created) — the derived tables tracked them incrementally, none was rebuilt. Counts are non-empty on both sides, so the match is not vacuous.
- [x] 9.6 **OPERATOR:** Tear down (`docs/docker.md`) and confirm no stray container or volume remains.
      DONE: `docker compose -f docker-compose.yml -f docker-compose.dev.yml down --remove-orphans` removed the container and the network; `docker ps -a | grep -c rembric` → 0, `docker volume ls | grep -c rembric` → 0.

## 10. Deferred and explicitly rejected — recorded so they are not lost

- [x] 10.1 **DEFERRED, coupled, do together:** add an empty-gold query to the committed eval set whose fused pool is genuinely empty. It would make BOTH inert gates in this area fireable at once — `checkAbstentionFlags` would gain a live outcome (D7), and `abstentionFalsePositiveRate` would drop below `1.000`, at which point its cap of `1` (`baselines/{hybrid,grep,memory-md-dump}.json:31,35`) stops being arithmetically unreachable. Not done here: it moves committed baselines, and it belongs with the corpus expansion that `ABSTENTION_FLOOR` calibration also waits on.
      Recorded, not done. Unchanged by this change.
- [x] 10.2 **DEFERRED:** the `abstentionFalsePositiveRate` cap is inert as committed — measured `1.000` against a cap of `1`, and `ratchetCaps` clamps every proposal with `Math.min(1, …)` (`apps/server/src/test/retrieval/floor-ratchet.ts:136`), so `measured > cap` can never hold. Recorded in `design.md` D7 as known-inert; NOT specified in this change's delta, because writing a requirement we do not implement is the overclaim this change exists to remove. Fix with 10.1.
      Recorded, not done. Re-verified this session: `pnpm run eval` still reports `abstainFP=1.00` for all three retrievers against a committed cap of `1`.
- [x] 10.3 **REJECTED for this change:** a distance floor on the dense kNN branch — the actual cure for the measured garbage query (`zzqqwx vvbbnm ppllkk xhtqrv` → 8 of 8 rows). Recall risk; needs its own sweep, its own baselines and its own change.
      Recorded, not done — and re-measured on the live stack as evidence it is still live: a nonsense focus returns ranked rows rather than nothing (see 9.3).
- [x] 10.4 **REJECTED for this change:** enabling or calibrating `ABSTENTION_FLOOR`. Unachievable on the committed corpus — the level distributions overlap on `[0.296, 0.307]` (`archive/2026-08-03-weight-relevance-levels-by-idf/measurements/sweep-after-amendment.txt:61`), so criteria 2 and 3 of `memory/spec.md:1361` cannot both be met at any value.
      Recorded, not done. `ABSTENTION_FLOOR` is still `null`, and the new description-content test asserts that the description does not name it while it is.
- [x] 10.5 **DEFERRED, separate reconciliation** (precedent: commit `4f7adff`): `memory/spec.md:1361` criterion 5 requires a sweep grid step no coarser than 0.05, and the shipped `SWEEP_RATIOS` is a 0.10 grid (`run-eval.ts:329`); `run-eval.ts:318`'s docstring still quotes "two grid steps wide", a bar rewritten in absolute level units. Both are spec-vs-code drift in this area, neither is this change's subject.
      Recorded, not done.
- [x] 10.6 **OPEN, default taken** (`design.md` Open Question 2): the entity branch keeps reporting `abstained: false` on a miss. It has no gate and no relevance level, so "abstention" may be the wrong word for it, and `mcp-api/spec.md:1788` already governs its empty-result contract via `entityIndexDraining`. Stated in the delta so the silence is a decision, not an oversight.
      Recorded: the entity branch still returns `abstained: false` on a miss (`memory.ts`'s entity return is untouched), and `memory.test.ts:282` confirms it.

## 11. Deviations found while applying — artifact edits recommended, NOT taken unilaterally

- **~~`mcp-api/spec.md` delta, scenario "A gate-shortened page is marked as such over the MCP boundary", the AND clause, contradicts D4.~~ RETRACTED — there was no contradiction, and acting on this report is what shipped the defect.** The clause as the proposer wrote it read "an `offset` past the end of a **non-empty pool** SHALL receive `abstained: false` with no `gateShortened` field" — that is `offset >= poolSize`, and it was correct. D4's worked example is `pool 5, gate keeps 2, limit: 2, offset: 2`, i.e. `offset < poolSize`, INSIDE the pool. Two different regions, never in conflict. Reading "past the pool" as "past the survivors" manufactured the contradiction, and commit `ee76b22` then rewrote a correct requirement into a false one and added a test pinning the false behaviour at `offset: 5` on a five-row pool. Both are now reverted in substance: the scenario states BOTH regions explicitly so the misreading is unavailable, D4 and the `memory` delta carry the third conjunct, and `hybrid-search.ts` implements it. Lesson worth keeping: a "contradiction between two artifacts" claim needs the same evidence bar as a runtime claim — here, checking which region each sentence quantified over would have cost one reading.
- **`design.md` Risks vs D5, on the widen affordance.** Recorded inline at 5.1: the Risks bullet says the affordance "stays attached to 'if every result looks relevant' as before", but D5's edit 2 is what deletes that phrase. D5's exact strings and its character arithmetic were followed; the Risks sentence is stale.
- **The floor's reason string is no longer reachable on an empty pool.** Deliberate and required by the delta ("attributing the verdict to a gate that did not run is a false statement about the search"), but it means the existing test formerly named "abstains when the candidate set is empty and a floor is enabled" no longer tests the floor. It was renamed and now asserts the empty-pool reason; the floor's own reason is asserted separately over a NON-empty pool ("names each abstention cause with its own reason"), which is the coverage the delta's "two causes carry different reasons" scenario needs.
