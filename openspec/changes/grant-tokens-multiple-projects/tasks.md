## 0. Settle the open questions and the ordering before writing code

**DONE FIRST, before any task below: the two MODIFIED blocks were rebased onto the text `retire-the-global-scope` published on 2026-08-05.** Both were written before that change archived, so both described a world with a global scope.

- **`auth` / "Tokens MUST support scope and expiration" had a real silent revert.** The block omitted the published paragraph _"Because every connection now resolves to exactly one project, `resolvedScope` is always a project scope. A `*` or `read:*` token is **unbound** rather than global-scoped…"_ entirely, so archiving would have deleted it. Restored, with this change's own set-arm paragraph kept after it. Also dropped the stale parenthetical _"(or the tool's requested/target scope where the tool takes one)"_ — no tool takes one now, `memory.save`'s `scope` argument was removed.
- **`dashboard` / "Tokens MUST be manageable from the dashboard" needed no change.** All 12 body lines the freshness gate reports are deliberate rewrites of this change: the grammar table gains a `token_projects` column and the two set rows, state precedence gains the empty-set level, the one-time view enumerates member slugs, and the design-token sentence names the multi-selection control. Verified line by line against `openspec/specs/dashboard/spec.md` rather than assumed.

**A gate weakness found while doing this, worth its own follow-up.** `scripts/check-delta-freshness.mjs:44-46` defines `bodyLines` as _"everything before the first scenario"_ (`const cut = slice.indexOf('\n#### Scenario:')`), so **scenario bodies are never compared** — only scenario titles, which hard-fail only on removal. The `auth` scenario "Project-restricted token calls a read tool outside its project" still said _"project B or global"_ and omitted the published `**AND**` line naming the pinned project and `project.use`; both would have reverted **with no report at all**, not even the advisory the prose gets. Fixed here by hand. The gate cannot be relied on for scenario bodies, so a rebase must read the published requirement whole.

- [x] 0.1 **Ordering gate (design D1).** Confirm `retire-the-global-scope` has landed on this branch's base. If it has not, **stop**: hazard 2 (both changes rewrite `isAuthorized` in different dimensions) and hazard 3 (the `Scope`/`TokenScope` 1:1 breaks here while the predecessor changes `Scope`'s shape) make the merge order load-bearing. Record the base commit in the commit body. **DONE — the predecessor is merged, not merely present on the base.** `retire-the-global-scope` archived as `openspec/changes/archive/2026-08-05-retire-the-global-scope/` and merged to `main` as **`c26ecfb`** (PR #311, five CI checks green). Verified at the strongest reading rather than by the branch tip: `openspec/specs/auth/spec.md` carries the published paragraph _"Because every connection now resolves to exactly one project, `resolvedScope` is always a project scope"_. Hazards 2 and 3 are therefore both retired — `isAuthorized` has settled and `Scope` has its final release-N shape.
- [x] 0.2 **Assign the migration number last, not first (design D1).** `db/migrate.ts:55-57` is `readdirSync().filter(…).sort()` keyed on filename with **no duplicate-prefix detection** — measured, two `0030_*` files both applied, ordered by the alphabetical suffix. Take the next unused prefix after the predecessor's migration exists on the base. Verify by listing `apps/server/src/db/migrations/` and confirming no other file shares the chosen prefix. **DEFERRED BY DESIGN, as this task instructs — not assigned yet.** `apps/server/src/db/migrations/` ends at `0031_default_project.sql`, so the next free prefix is **`0032`**, and it MUST be re-confirmed against the directory at the moment the migration is written: `db/migrate.ts:55-57` is `readdirSync().filter('.sql').sort()` with no duplicate-prefix detection, so two files sharing a prefix both apply, ordered by the alphabetical suffix.
- [x] 0.3 **Operator decision — OQ1, the shape.** `design.md` D3 records **option 1** (new `projects` / `read:projects` literal; `auth/spec.md:41`'s enumeration is amended). Default if unanswered: **option 1 as specified.** If the owner picks option 1b (keep `scope = project:<primary-id>`, extras in the join table), delete the `auth` delta's `MODIFIED` requirement, keep every `ADDED` one, and add a scenario stating that the `project` column under-reports reach — do not leave the column silently misreporting. **DEFAULT TAKEN: option 1** — the new `projects` / `read:projects` literals, with `auth/spec.md`'s enumeration amended (already carried in the rebased delta). **Alternative not taken (1b):** keep `scope = project:<primary-id>` and put the extras in the join table. It would leave the `project` column under-reporting a token's real reach, which is a truthfulness defect of exactly the class the predecessor spent five waves removing. If the owner reverses this, delete the `auth` delta's MODIFIED requirement, keep every ADDED one, and add a scenario stating the under-report explicitly rather than letting the column lie.
- [x] 0.4 **Operator decision — OQ2, does `auth/spec.md:69` cover "which projects".** Default if unanswered: **yes** — the membership set is authorization state and is re-read per request (design D7). If the owner reads `:69` as an exhaustive list and permits caching, amend `auth/spec.md:69` in the same change to say so explicitly, and delete the `auth` delta requirement "A token's project membership set MUST be authorization state…" rather than leaving it unimplemented. **DEFAULT TAKEN: yes** — the membership set is authorization state and is re-read per authenticated request (design D7). **Alternative not taken:** read `auth/spec.md:69` as an exhaustive list and permit caching the set. That would make a removed member keep working until eviction, so revocation of reach would not be immediate — the property `auth`'s own revocation requirement exists to guarantee. If reversed, amend `:69` in this change to say so explicitly and delete the delta's membership-freshness requirement rather than shipping it unimplemented.
- [x] 0.5 **Operator decision — OQ3, one verb or per-project verbs.** Default if unanswered: **one verb** (design D5). If per-project verbs are wanted now, the `dashboard` delta's 2×N table is replaced rather than extended and needs its own scenarios for the split refusal (`project.use` gates only on `'read'` at `project-tools.ts:128`, so a read-only member would open a connection and be refused later at `memory.save`). **DEFAULT TAKEN: one verb for the whole set** (design D5). **Alternative not taken:** per-project verbs. It is not merely more work — `project.use` gates only on `'read'` (`project-tools.ts:128`), so a read-only member would open a connection successfully and be refused later at `memory.save`, which splits one refusal across two turns. Deferred cleanly: `ALTER TABLE token_projects ADD COLUMN access TEXT NOT NULL DEFAULT 'write'` is measured rebuild-free, so this is reversible without a table rebuild.
- [x] 0.6 **Operator decisions — OQ4 (autocreate) and OQ5 (multi-project OAuth).** Defaults: autocreate **refused** for a set token, multi-project OAuth an explicit **non-goal**. Both are already what the code does; the deltas record them so they are not rediscovered. **DEFAULTS TAKEN: autocreate refused for a set token, and multi-project OAuth an explicit non-goal.** Both are already what the code does; the deltas record them so a future reader does not rediscover them as bugs. The OAuth non-goal is load-bearing in one direction worth naming: the predecessor established that a path-less `/mcp` grant stays **unbound** (`mcp-oauth` delta `:9`), so there is no consent screen that could express a set, and inventing one is a separate change.
- [x] 0.7 Pick the state label for a memberless set token. It MUST be distinct in the rendered HTML from `active`, `revoked` and the existing unresolvable label (`inert`), per the `dashboard` delta, and MUST reuse an existing pill class so no design token is added. **Do not reuse `inert`** — that label carries `auth/spec.md:268`'s never-repair contract, and an empty set is repairable. **DECIDED: label `no projects`, class `pending`.** Distinct in the rendered HTML from `active` (class `active`), `revoked`/`expired` (both class `archived`) and the unresolvable `inert` (class `legacy`) — verified against `stateOf` at `dashboard/tokens.ts:42-47`. `pending` already exists in `styles/core/atoms.css:66-71`, so **no design token is added**. `inert` is deliberately NOT reused: it carries `auth/spec.md`'s never-repair contract, whereas an empty set is repairable by the operator, and `pending` is the only existing class whose tone says "awaiting your action" rather than "dead".
- [x] 0.8 Confirm OQ6 stays out: `dashboard/spec.md:710`'s project-scope dashboard session is vacuous today (login requires `*`, `dashboard-router.ts:156`) for reasons predating this change. It is **not** reconciled here. Record it as a follow-up so it is not silently lost. **CONFIRMED OUT, and recorded as a follow-up so it is not silently lost.** `dashboard/spec.md`'s project-scope dashboard session is vacuous today for a reason that predates this change: login requires `*` (`server/dashboard-router.ts:156`), so no project-scoped token can ever hold a dashboard session. Not reconciled here. It joins the spec-gate follow-up list this work has accumulated — see the note under §0 above on `check-delta-freshness` never comparing scenario bodies.

## 1. Commit the pre-change authorization baseline — before touching a line

The whole change rests on "no existing token's behaviour moves". That is only provable against a baseline captured on the real boundaries **before** the change. Committed as a measurement fixture under `openspec/changes/grant-tokens-multiple-projects/measurements/`.

Instruments, each named — do **not** mix them in one table:

- the dashboard mint form (`POST /dashboard/tokens`) for minting, so the scope string is the one the real producer writes;
- `POST /dashboard/login` for the admin gate;
- the MCP SDK `StreamableHTTPClientTransport` against `/mcp` and `/mcp/<slug>` — **not** a direct handler call, which bypasses the zod schema and proves nothing about the tool;
- `POST /api/<slug>/sessions` for the HTTP surface.

- [x] 1.1 Seed two projects, `alpha` and `home`, and mint one token of each existing shape. Record the exact scope string persisted for each. **DONE** — `measurements/authorization-baseline.mjs`. Both projects are seeded through the operator's own `POST /dashboard/projects/create` form and both tokens through `POST /dashboard/tokens`, so nothing in the script depends on a service shape it could get wrong. The persisted scope strings, read back from the rendered `/dashboard/tokens` page (same side of the boundary as the mint that produced them): `*`, `read:*`, `project:01KZ8QXJZ3KKHXJSE28JKK4MS3` (project cell `alpha`), `read:project:01KZ8QXJZ3KKHXJSE28JKK4MS3` (`alpha`).
- [x] 1.2 Capture the matrix. The expected pre-change values, to be reproduced not assumed: **DONE — all five rows reproduce the expected table cell for cell**, so nothing in it needed correcting. Full matrix in `measurements/authorization-baseline.md`, raw cells in `authorization-baseline.json`. **One reading moved under an unchanged verdict, and §8 must not diff on code alone because of it:** a `project:<alpha>` token's path-less `/mcp` cells are still `forbidden`, but the cause changed from "no project is active on this connection" to "this token is not authorized for the default project", and the refusal message now names the default project. A code-only diff cannot see that; the message text has to be read.

| token                        | `POST /dashboard/login` | `/mcp` (path-less) read / write | `/mcp/alpha` read / write | `/mcp/home` read / write | `POST /api/alpha/sessions` | `POST /api/home/sessions` |
| ---------------------------- | ----------------------- | ------------------------------- | ------------------------- | ------------------------ | -------------------------- | ------------------------- |
| `*`                          | 302 to the dashboard    | OK / OK                         | OK / OK                   | OK / OK                  | 200                        | 200                       |
| `read:*`                     | 401                     | OK / forbidden                  | OK / forbidden            | OK / forbidden           | 403                        | 403                       |
| `project:<id of alpha>`      | 401                     | forbidden / forbidden           | OK / OK                   | forbidden / forbidden    | 200                        | 403                       |
| `read:project:<id of alpha>` | 401                     | forbidden / forbidden           | OK / forbidden            | forbidden / forbidden    | 403                        | 403                       |
| invalid bearer               | 401                     | 401                             | 401                       | 401                      | 401                        | 401                       |

- [x] 1.3 Record **both** the HTTP status and the structured error `code` for every cell. A change that turns `forbidden` into `project_required` is a behaviour change even at the same status. **DONE** — every cell carries both. Where a surface answers over HTTP the cell is `{status, code}`; where it answers as an MCP tool result the cell is `{ok}` or `{ok:false, code}`; where authentication refuses before tool dispatch the cell records the transport refusal and its `token_invalid` body, which is a materially different shape from a tool-level denial and is stored as such rather than flattened to `401`.
- [x] 1.4 Assert the non-vacuity of the baseline itself: count the cells that are a **success**, and record the count. A later "identical" diff over an all-refused matrix would prove nothing. **DONE: 16 of 45 cells are a success**, recorded in `authorization-baseline.json` as `nonVacuity` and in the markdown. **The control this task exists for earned itself on the first run.** The probe sent `type: 'note'`, which is not in `MEMORY_TYPES`, so the now-strict tool schema refused all six write cells with `-32602 Invalid arguments` and the matrix read as "writes are denied" — a malformed write probe is indistinguishable from a real denial. The script now refuses to emit a matrix at all unless the `*` token succeeds on all three writes and both session posts, so that failure mode cannot be recorded as a baseline again.
- [x] 1.5 Commit the matrix and the script that produced it, so the after-run is a re-run rather than a re-derivation. **DONE** — script, raw JSON and rendered matrix all committed under `measurements/`, so task 8.1 is a re-run of the same file rather than a re-derivation. Usage is in the script header; note it must be invoked with an **absolute** path (`pnpm --filter @rembric/server exec tsx` runs from `apps/server`, so a repo-relative path resolves wrong) and that the SDK is imported through `apps/server/node_modules` because a bare specifier cannot resolve from outside the package.

## 2. Failing tests first

Every test in this group MUST be observed **failing** before any production line changes, and the three mandatory controls MUST be observed **green** at the same moment. A control that is red here means the harness is wrong, not the code.

- [x] 2.1 **Control (MUST be green before and after):** the admin `*` token succeeds on `/mcp/alpha` read and write, on `POST /api/alpha/sessions`, and on `POST /dashboard/login`. **GREEN pre-change**, `test/token-project-sets.test.ts` — "an admin token reaches a project over MCP and HTTP and opens a dashboard session". The `*` token is minted through the dashboard form rather than reusing the bootstrap admin row, so the control and the subjects share one producer; the persisted scope is asserted to be `*` before the four probes.
- [x] 2.2 **Control (MUST be green before and after):** a token minted for `alpha` is **denied** on `/mcp/home` and `POST /api/home/sessions`. Without this denial control, an `isAuthorized` rewritten to return `true` unconditionally passes every other test in this file. **GREEN pre-change** — "a project-scoped token is denied another project, and authorized on its own". The three `alpha` successes are asserted in the same test as the three `home` refusals: without them a malformed probe is indistinguishable from a denial. **The spine is proven, not asserted:** `node scripts/mutate.mjs --file apps/server/src/services/tokens.ts --spec src/test/token-project-sets.test.ts -t 'controls that must hold on both sides of the change' --mutation "if (scope === '*') return true;" --with 'return true;'` — an `isAuthorized` that returns `true` unconditionally is **CAUGHT by exactly this control** (`expected { ok: true } to deeply equal { ok: false, code: 'forbidden' }`), while 2.1 and 2.3 stay green. The same mutation under `-t 'authorizes nothing while its set is empty'` is caught by 2.10, so §7.3 has two independent catchers. `tokens.ts` restored byte-identical both times.
- [x] 2.3 **Control (MUST be green before and after):** an invalid bearer is 401 on every surface in 1.2. **GREEN pre-change** — "an invalid bearer is refused on every surface". **The baseline over-reports one cell and this control corrects it:** `authorization-baseline.md`'s matrix prints `401 token_invalid` for the six MCP cells, but `authorization-baseline.json` recorded `{transport:'refused', status:null}` — the SDK throw carries the JSON body and no status, so the `401` in the rendered table was carried over from the expected table rather than measured. The test therefore measures the status with a raw `initialize` POST (401, `token_invalid`) **and** asserts the SDK transport refusal separately, keeping the two shapes distinct as 1.3 requires.
- [x] 2.4 **Failing:** minting through the form with two projects selected persists `scope = 'projects'`, `project_id IS NULL`, and exactly two `token_projects` rows. Observe it fail — the form has a single `<select name="project">` today (`dashboard/tokens.ts:155`) and `TokenScope` has no set arm (`services/tokens.ts:52`). **OBSERVED FAILING** — "persists the set literal, no single-project binding, and one membership row per project": `AssertionError: expected 'project:01KZ8T3XPYCW3WMTDC59VZRAHC' to be 'projects'`. Today's handler reads `form.get('project')`, so the second submitted value is silently dropped and the first mints the single-project arm.
- [x] 2.5 **Failing:** that token is authorized on `/mcp/alpha` and `/mcp/gamma` for read and write, and on `POST /api/alpha/sessions`. **OBSERVED FAILING** — "reaches every member and is denied every non-member, including the path-less connection": `AssertionError: gamma read: expected { ok: false, code: 'forbidden' } to deeply equal { ok: true }`. The `alpha` cells pass today (the dropped second value leaves an `alpha`-pinned token), which is what makes the `gamma` failure the reach failure rather than a broken probe.
- [x] 2.6 **New control specific to this change (b):** the same set token, naming `{alpha, gamma}`, is **denied** on `/mcp/beta`, on the path-less `/mcp`, and on `POST /api/beta/sessions`. Assert it in the same test as 2.5, not a separate one that could be skipped. **DONE, in the same `it` as 2.5** — one token, reach asserted first and the five refusals after, so the denials cannot be skipped and the successes above them are what prove the probes well-formed. The refusal cells are unreached in the pre-change run because 2.5's `gamma` assertion aborts the test first; they are separately measured as pre-change `forbidden` in the committed baseline (`project:<alpha>` row: `/mcp` read/write and `/mcp/home` read/write all `forbidden`).
- [x] 2.7 **New control specific to this change (a) — the escalation control:** a set token naming **every** existing project is **denied** `POST /dashboard/login` and every `/admin/*` route, while the `*` control succeeds on both. Without this, reach has silently become admin and nothing else in the suite notices. (`auth` delta, "A token reaching every project MUST NOT be an admin token".) **OBSERVED FAILING, with all four gate cells measured green first** — "is not an admin token even when its set names every project". Ordered so the gates are exercised before the shape: `*` → login 302 and `POST /admin/consolidation/run` 200; subject → login 401 and `/admin/consolidation/run` 403 `forbidden`. The failure is the shape, `AssertionError: expected 'project:1bdb120d90305f617413e48e37' to be 'projects'` (the default project's id, first in the submitted list) — breadth is what the refusals must be about, so the subject has to actually be a set. `/admin/*` has exactly one route today (`server/http.ts:137`, registered only when `triggerConsolidation` is wired), so "every `/admin/*` route" is that one.
- [x] 2.8 **Failing:** a `read:projects` token over `{alpha}` is authorized for a read-classified tool on `alpha` and refused a write-classified one, with nothing persisted. **OBSERVED FAILING** — "reads but does not write its members when the set is read-only": `AssertionError: expected 'read:project:01KZ8T8GDWWSR10HTEZ7FEVBBQ' to be 'read:projects'`. **Deviation, recorded:** the form cannot mint a one-member set (task 2.14 requires a single selection to mint the single-project arm), so the token is minted over `{alpha, gamma}` with `access = read`, asserted on member `alpha`, and then reduced to exactly `{alpha}` by removing the `gamma` membership row — so the requirement's literal one-member state is exercised too. "Nothing persisted" is the `memory` row count for `alpha` before and after the refused write, not the absence of a response.
- [x] 2.9 **Failing:** creating a set token then removing one member makes the next request against that member `forbidden`, while the remaining member still succeeds — asserted with the credential-lookup cache **warm** (the same plaintext already verified once), which is the case `auth/spec.md:69` exists to cover. (`auth` delta, both membership-freshness scenarios.) **OBSERVED FAILING** — "loses a removed member on the next request with the credential cache already warm": `AssertionError: expected { ok: false, code: 'forbidden' } to deeply equal { ok: true }` on the pre-removal read of `gamma`. The warmth is structural rather than asserted: that first successful read is what puts the plaintext in `verifiedCache`, so every probe after the removal is answered on the cached-lookup path. The cache is not observable from outside the process, and a timing assertion over a ~23 ms scrypt would be an instrument, not a contract.
- [x] 2.10 **GREEN pre-change, not failing — and the task's label is what was wrong.** Written and run: "authorizes nothing while its set is empty" passes today, on all nine refusal cells and all nine `*` control cells. The reason is the measurement D6 already recorded — `isAuthorized` ends in `return false`, so an unrecognised scope string authorizes nothing by string alone, and `projects` is unrecognised today. So this is a **regression guard on a property that already holds**, not a test of new behaviour, and it is exactly the assertion mutation 7.5 (treat `'projects'` as `'*'`) and 7.3 (membership unconditional) must red. The subject is built by minting a `*` token through the form and re-labelling its scope to `projects` with a NULL binding: the form cannot mint a memberless set (zero selections mint `*`), and the row is never given a member, so its whole reach would have to come from the scope string.
- [x] 2.11 **Failing:** `project.use({ slug: 'brand-new', autocreate: true })` from a set token is refused with `forbidden` and creates no `projects` row, while the `*` control creates it. **OBSERVED FAILING** — "cannot create the project it would need to reach": `AssertionError: expected 'project:01KZ8T926P9N0W649KADMJ2CCA' to be 'projects'`. The refusal and the `*` control are asserted **before** the shape, and in that order, because the control creates `brand-new` and would turn the subject's call into a resolve rather than an autocreate. Both measured pre-change: subject refused `forbidden` with no row, control `ok` with the row created. Probed on the path-less `/mcp` — a path-scoped connection refuses `project.use` with `scope_locked` at `project-tools.ts:92` before the autocreate gate is reached.
- [x] 2.12 **Failing (dashboard):** the tokens list renders every member slug of a set token in slug-ascending order, no ULID, and state `active`; a memberless set token renders the label from 0.7, distinct from `inert`; a revoked memberless set token renders `revoked`. **OBSERVED FAILING** — "lists every member slug, and marks a memberless set apart from an unresolvable row": `AssertionError: expected 'gamma' to contain 'alpha'`. The slugs are submitted out of order (`gamma`, then `alpha`) so the ascending assertion is about the render rather than the submission; today the cell holds only `gamma`, the first submitted value. The memberless and revoked-memberless rows are inserted through the repository (`scope = 'projects'`, `project_id NULL`) beside an unresolvable `project:<slug>` row, and the two labels are asserted to be different values — `no projects` against `inert`.
- [x] 2.13 **Failing (dashboard):** the one-time-view panel after minting over `{alpha, gamma}` contains `projects` and both slugs — not a count. **OBSERVED FAILING** — "names the minted scope and every project in the one-time view": `AssertionError: expected '<div class="one-shot"><strong>New tok…' to contain '<code>projects</code>'`. Asserted on the extracted `one-shot` panel, not on the page: every project slug also appears in the create form's option list, so a whole-page assertion would pass without the panel naming anything.
- [x] 2.14 **Failing (dashboard):** a single-project selection still mints `project:<id>` with `project_id` set and **zero** `token_projects` rows. This is the "a single selection does not become a one-member set" scenario and it is what keeps the FK binding on the common case. **OBSERVED FAILING, on the schema and nothing else** — "keeps a single selection on the single-project arm rather than a one-member set": `AssertionError: token_projects does not exist: expected false to be true`. The two scope assertions above it pass today, which is the point: this test preserves current behaviour and its only new requirement is the zero-row read. The table-existence expectation is asserted explicitly rather than left to throw a bare `SQLiteError`, and **deliberately not tolerated** — a helper answering `0` for a missing table would let this test pass vacuously if §3's migration never landed.
- [x] 2.15 **Failing (dashboard):** a multi-project submission containing one regex-violating value is refused with a flash error, and **both** the `tokens` and `token_projects` row counts are unchanged. Assert the counts, not just the absence of the new row. **OBSERVED FAILING** — "creates neither a token nor a membership row when one selected slug is invalid": `AssertionError: token_projects does not exist: expected false to be true`. **The refusal assertion is unreached in the pre-change run** and this is a deliberate ordering choice: "unchanged" needs a before-count, so both counts are read before the submission. Today the handler reads `form.get('project')`, takes `alpha`, ignores `INVALID Slug!` and answers 302 — so the refusal assertion will red on its own once the table exists. Refusals from this form are `400` with the flash error in the body, not a redirect (`flashErrorPage`, `dashboard/components.ts:59`).
- [x] 2.16 Record in the commit body which of 2.4-2.15 failed and with what, and confirm 2.1, 2.2, 2.3 were green before any production change. **DONE.** One run of `apps/server/src/test/token-project-sets.test.ts` at this section's end: **14 tests, 4 passed, 10 failed.** The three controls (2.1, 2.2, 2.3) are among the four green, measured in the same run as every failure, so no failure below can be a broken harness. The fourth green is 2.10, whose label was wrong (see its entry).

Verbatim first failure per test, in file order:

| task       | failure                                                                                |
| ---------- | -------------------------------------------------------------------------------------- |
| 2.4        | `expected 'project:01KZ8TCW7K2MDXS6WK1ANEG3V5' to be 'projects'`                       |
| 2.5 (+2.6) | `gamma read: expected { ok: false, code: 'forbidden' } to deeply equal { ok: true }`   |
| 2.7        | `expected 'project:34562bcd0f5a36f3f515e02e5f' to be 'projects'`                       |
| 2.8        | `expected 'read:project:01KZ8TCW7K2MDXS6WK1ANEG3V5' to be 'read:projects'`             |
| 2.9        | `expected { ok: false, code: 'forbidden' } to deeply equal { ok: true }`               |
| 2.11       | `expected 'project:01KZ8TCW7K2MDXS6WK1ANEG3V5' to be 'projects'`                       |
| 2.12       | `expected 'gamma' to contain 'alpha'`                                                  |
| 2.13       | `expected '<div class="one-shot"><strong>New tok…' to contain '<code>projects</code>'` |
| 2.14       | `token_projects does not exist: expected false to be true`                             |
| 2.15       | `token_projects does not exist: expected false to be true`                             |

**No test was stubbed, skipped or `it.fails`-marked, and none needed to be.** `tokens.scope` is a `text` column, so `NewToken['scope']` is `string` and a fixture can persist a shape the typed producer cannot yet compose — no fake `TokenScope` arm was invented to make the file compile. Two consequences of that, both recorded rather than worked around:

- Nothing in §2 depends on §5.1/§5.2's types, so §5 unblocks no test here. What §3.1/§3.2 unblocks is 2.14 and 2.15 (the two failing purely on the missing table) and the last leg of 2.8 and 2.9 (the membership `DELETE`, currently unreached).
- 2.10 needs no production code at all and is green today.

**What §1 and the baseline got wrong, found while writing the controls.** `measurements/authorization-baseline.md`'s matrix prints `401 token_invalid` for the six invalid-bearer MCP cells, but `authorization-baseline.json` recorded `{"transport":"refused","status":null}` for each: the SDK throw carries the JSON body and no status, and its own `\b(\d{3})\b` extraction returned null. The `401` in the rendered table was carried over from the expected table in 1.2 rather than measured — the one cell in that matrix that is not evidence. 2.3 measures it properly with a raw `initialize` POST (401, `token_invalid`) and keeps the SDK transport refusal as a separate shape. §8.1's re-run will reproduce `status: null` there; it is not a regression.

## 3. Schema and migration

- [x] 3.1 Add `apps/server/src/db/schema/token-projects.ts`: `token_projects` with `token_id` / `project_id`, both real FKs, `PRIMARY KEY (token_id, project_id)`, `WITHOUT ROWID`. No secondary index — every read is keyed by the leading primary-key column. **DONE.** `WITHOUT ROWID` is inexpressible in Drizzle (the same category as `memory_entity_links` / `memory_entity_scan`, `db/schema/entities.ts:65-66`), so the schema file says so and `schema-drift.test.ts::EXPECTED_WITHOUT_ROWID_TABLES` is what pins it against `sqlite_master`. Exported from `db/schema/index.ts` because `schema-drift`'s "every Drizzle table declares the primary key the DB actually has" iterates the barrel — an unexported table would not be checked at all.
- [x] 3.2 Add the migration at the prefix from 0.2: **exactly one `CREATE TABLE`**. No `_new` table, no `INSERT … SELECT`, no `DROP TABLE`, no index or trigger recreation, no pragma. `tokens` is **not** rebuilt — `tokens_project_scope_check`'s `project_id IS NULL` disjunct (`db/schema/tokens.ts:40`) already admits `scope = 'projects'` with a NULL binding, measured. **DONE as `0032_token_projects.sql`.** The prefix was re-confirmed free at the moment of writing, as 0.2 requires: `ls apps/server/src/db/migrations/ | grep -E '^0032'` returned nothing, and the directory holds 32 `.sql` files (`0000`–`0031`, no gap and no duplicate prefix). `meta/_journal.json` is empty and unread by `db/migrate.ts`, so nothing was added there.
- [x] 3.3 Run the suite and **observe `test/schema-drift.test.ts:377` go red first** (`expect(tables).toEqual([...EXPECTED_TABLES].sort())`). Its going red is the proof the migration created the table. Record the failure output in the commit body. **OBSERVED RED, and by TWO tests rather than the one the task predicted:**

  ```
  FAIL  src/test/schema-drift.test.ts > … > runs all migrations forward against a fresh DB
  AssertionError: expected [ '_migrations', …(38) ] to deeply equal [ '_migrations', …(37) ]
        "sessions",
  +     "token_projects",
        "tokens",

  FAIL  src/test/schema-drift.test.ts > … > keeps WITHOUT ROWID on the tables that declare it
  AssertionError: expected [ 'memory_entity_links', …(3) ] to deeply equal [ 'memory_entity_links', …(2) ]
        "memory_replaces",
  +     "token_projects",
  ```

  The second failure is the stronger evidence of the two: it proves the created table is `WITHOUT ROWID`, which the table-set assertion cannot see. `EXPECTED_INDEXES` did **not** red, and that is correct rather than a gap — a `WITHOUT ROWID` table's primary key creates no `sqlite_autoindex_*` row, which is why `memory_replaces` and `memory_entity_links` have no entry there either.

- [x] 3.4 Add `token_projects` to the pinned inventory so 3.3 goes green again, **in the same commit** as 3.2. **DONE, three pins in the same commit:** `test/schema-inventory.ts::SOURCE_TABLES` (it is operator-supplied authorization state, not derived — `invariants.test.ts`'s source/derived partition is a partition, so an unclassified table fails there too), `schema-drift.test.ts::EXPECTED_WITHOUT_ROWID_TABLES`, and `EXPECTED_COLUMNS` (which pins both columns NOT NULL and the composite-PK positions `token_id` = 1, `project_id` = 2 — the ordering the `token_id`-keyed read depends on).
- [x] 3.5 Update `apps/server/src/db/schema/tokens.ts:6-19`'s scope-semantics docstring for the two new arms. One line per non-obvious fact; no banner, no restatement of the type. **DONE** — two arms added to the grammar list, plus the two non-obvious facts: `project_id` is NULL for them and the CHECK's first disjunct is why that needed no rebuild, and their reach lives entirely in `token_projects` so a reader ignorant of that table under-authorizes.

- [x] 3.6 **Added in review — a new table is also a new parent, and `seed-dev --reset` did not know about it.** `scripts/seed-dev.ts::wipe()` deletes `dashboard_sessions`, `tokens` and `projects` under `PRAGMA defer_foreign_keys = ON`, so the check fires at COMMIT: one `token_projects` row dangles on **both** FKs and aborts the whole reset, and `main()` does not catch. Measured with a control, before the fix: 0 membership rows → reset succeeds (`tokens=3 projects=1`, `foreign_key_check` empty); 2 membership rows → `SqliteError: FOREIGN KEY constraint failed`. That matters more than its severity label because the dev container's `CMD` runs `--reset` on every boot, so one set token minted in the dev stack bricks it until someone deletes the row by hand — and minting one is what the plugin skill's mandatory e2e walkthrough drives. Fixed by deleting `token_projects` before either parent. **Mutation, one run:** removing that one line → `CAUGHT by 1`, exactly the new regression test (`--reset does not violate FK constraints when a token names a set of projects`), the other seven tests in the file green including the pre-existing zero-membership reset control; `seed-dev.ts` restored. `invariants.test.ts`'s `DELETE FROM` allow-list needed **no** edit: it enumerates the append-only tables, and `token_projects` is not one — `DELETE FROM tokens` and `DELETE FROM projects` sit unguarded on the lines above for the same reason.

## 4. Repository — all new SQL lives under `db/`

- [x] 4.1 Add the membership reads/writes to the tokens repository under `apps/server/src/db/repositories/`. Per `data-access/spec.md:23` and `:37`: the authorization-path read is keyed by token id and is not scope-parameterised (it _produces_ reach rather than filtering by it); the dashboard's "which slugs does this token reach" read carries the `admin*` prefix and is callable only from `src/dashboard/`. **DONE** — three methods on `TokensRepository` (the tokens aggregate owns membership): `listProjectIds(tokenId)` for the authorization path, `insertProjects(rows)` for the write, and `adminListProjectSlugs()` for the dashboard. The admin read resolves slugs in SQL and orders by `(token_id, slug)` so §6.5's slug-ascending cell needs no sort in the template, and it returns one flat row per membership rather than a grouped shape — a `Map` is a presentation decision, not a row. Its DTO is derived, not hand-written: `Pick<TokenProject, 'tokenId'> & Pick<Project, 'slug'>`. `adminListProjectSlugs` has no call site until §6.1-6.5; that is why it is not yet in `invariants.test.ts::ADMIN_CALL_SITES`, whose allow-list keys on call sites.
- [x] 4.2 Confirm `test/invariants.test.ts`'s data-access confinement grep passes — no SQL outside `db/`, no `admin*` call from outside `src/dashboard/`. **CONFIRMED** — 73 tests green, including data-access confinement, admin-method confinement and the unscoped-read inventory. The inventory needed no edit for either read: `tokens-repository.ts` is classified control-plane, and `listProjectIds` would in any case be exempt as an id-keyed read.
- [x] 4.3 Insert membership rows inside the same `db.transaction()` as the `tokens` insert, opened by the **service** (`data-access/spec.md:105`); the repository opens no transaction. **DONE** — `TokensService.create` wraps both inserts in one `this.tx.transaction(...)`, unconditionally rather than only for the set arm (one path, so a future arm cannot miss it). That needed a `TransactionRunner` on the service, which it did not have: added as the **second** constructor parameter, following the `(repos, tx, now)` order every other transactional service already uses (`memory.ts:184-190`, `agent-sessions.ts:166-170`, `prompts.ts:68-72`, `relations.ts:192-196`). The cost is 34 mechanical call-site edits, all caught by `tsc`; no assertion in any of them changed.

## 5. Service layer — the union point

- [x] 5.1 Extend `TokenScope` (`services/tokens.ts:52`) with `'projects' | 'read:projects'`. Verify by compiling that `pinnedProjectId('projects')` returns `null` and `projectScopedGrant` is unreachable for the new arms. **DONE.** `pinnedProjectId` is asserted at runtime rather than only by compiling (`tokens.test.ts` "the set arms are pinned to no project"): both new literals return `null`. `projectScopedGrant` is unreachable for them because its only two callers are `singleProject` (the one-project composer) and the OAuth path, whose `resolveGrantedScope` returns only `'*'` / `'read:*'` (`services/oauth.ts:328-334`).
- [x] 5.2 Extend `TokenGrant` / `CreateTokenInput` (`:59-63`) with a third arm carrying `projects: Project[]` and one `access: 'read' | 'write'` — **never `string[]`**. The existing comment at `:56-57` ("A bare project id would re-admit a slug, so the row itself is required") is the reason, and it carries to the set. Confirm with a throwaway (uncommitted) file that `projects: ['alpha']` is a compile error. **DONE, and the confirmation is committed rather than thrown away** — the file already had the idiom for exactly this (`tokens.test.ts` "does not admit a project scope string as caller-supplied input", a `CreateTokenInput[]` of `@ts-expect-error` lines). A third line was added: `{ name: 'slug-set', projects: ['alpha'], access: 'write' }`. Because an unused `@ts-expect-error` is itself an error, `tsc` passing IS the proof the line errors — and the loop was run both ways: with the directive removed, `tsc` reports `src/services/tokens.test.ts(53,38): error TS2322: Type 'string' is not assignable to type '{ id: string; slug: string; displayName: string | null; archivedAt: Date | null; createdAt: Date; isDefault: boolean; }'`. Restored, `tsc --noEmit` exits 0. This is stronger than a throwaway: it stays red if someone later widens the arm to `string[]`.
- [x] 5.3 Extend `composeGrant` (`:214-221`) to return the set literal plus the rows to insert. A one-element `projects` array MUST compose the **single-project** arm (`project:<id>` + `project_id`), per task 2.14 — the set arm starts at two. **DONE** — `composeGrant` returns `{ scope, projectId, memberProjectIds }`, and the one-element case delegates to the same `singleProject` helper the `project` arm uses, so the two cannot drift. Task 2.14 is green as a result. **One decision not in the task:** an EMPTY `projects` array throws `invalid_input` rather than composing a memberless set. The dashboard maps zero selections to the literal arm (`dashboard` delta's grammar table), so `[]` can only be a caller bug, and minting a credential that authorizes nothing silently is worse than refusing. **Left for §6.3:** a crafted POST naming the same slug twice would reach the PK as a duplicate insert; the form cannot produce it, but the handler should dedupe rather than let SQLite answer 500.
- [x] 5.4 Rewrite `isAuthorized` (`:268-290`) as `legacy(scope, action, target) || membership(token, action, target)`. **Additive only**: no membership branch may turn a `true` into a `false`. The two new literals fall through to the existing `return false` by string alone — verify that explicitly rather than assuming it. **DONE** — `isAuthorized` is now one line, `authorizedByScope(...) || authorizedByMembership(...)`, with the pre-existing body moved verbatim into `authorizedByScope` (so §7.1/§7.2 can drop either arm by name). The fall-through is asserted, not assumed: `tokens.test.ts` "the two set literals authorize nothing by string alone" pins `'projects'.startsWith('project:') === false` (position 7 is `s`, not `:`) and `'read:projects'.startsWith('read:project:') === false`, then sweeps both literals × both actions × three targets with an empty set — 12 cells, all false. **The signature changed**, which the task did not anticipate: the first parameter is now a `TokenReach` (`{ scope, memberProjectIds }`) rather than a bare `TokenScope`. An optional fourth parameter was rejected — a call site that forgot it would silently deny every set token, and there is no compiler help. `RequestContext extends TokenReach`, so all seven production call sites read `isAuthorized(ctx, …)` and cannot pass one half.
- [x] 5.5 Wire membership into the authorization path as a **fresh read per authenticated request** (design D7). Do **not** put it in `verifiedCache` (`:149-156`) — its permission to live forever rests on never substituting for the fresh check. Either a `LEFT JOIN` in `findById` or a second indexed read; both measured negligible (D11). **DONE as the second indexed read**, placed in `authorizeRow` — the method the cache hit and the cold path both funnel through, beside the `revoked_at` / `expires_at` re-reads and for the same stated reason. `verifiedCache` is untouched and still holds only `sha256(plaintext) → token id`. The read is unconditional rather than gated on the scope arm: one path, so a later arm cannot miss it, and D11 prices it at ~1.2–2.2 µs against a ≤332 µs warm auth step. **Proved non-vacuous by mutation** (hand loop, since `scripts/mutate.mjs` refuses a red baseline and this file is red by design): memoizing the read per token id on the service instance turns the new end-to-end test red — `AssertionError: expected { ok: true } to deeply equal { ok: false, code: 'forbidden' }` on the post-removal probe. `tokens.ts` restored byte-identical from a scratchpad copy.
- [x] 5.6 Correct `pinnedProjectId`'s docstring (`:303-308`): "The single project a token is pinned to" is no longer the whole truth. Keep the function and its signature — `null` for the set arm is the correct fail-closed answer. One line documenting the non-obvious why. **DONE** — function, signature and `?? pinned` behaviour all untouched; two lines added saying `null` is the answer rather than a gap, because a set is not a pin.
- [x] 5.7 Update the scope-grammar docstring (`:33-43`) and the `TokenGrant` comment (`:54-58`, currently "either global … or a **single** project"). Same one-line discipline. **DONE** — the grammar list gains the two arms, and the `TokenGrant` comment now says reach is unbound, one project, or an explicit set, keeping the reason the arm takes resolved ROWS (a bare id re-admits a slug) and extending it to the set.
- [x] 5.8 Leave `projectScopedGrant` (`:298-301`) untouched — still the single-project writer, still the OAuth path's. Note in the commit body that it was checked, not overlooked. **CHECKED AND UNTOUCHED**, noted in the commit body. `git diff` shows no change to it; its two callers are `singleProject` and `services/oauth.ts:245`.
- [x] 5.9 Leave the three literal admin gates literal: `server/dashboard-router.ts:156`, `server/http.ts:489`, `dashboard/maintenance.ts:143`. None of them starts consulting `isAuthorized`. Note in the commit body that all three were checked (design D8). **CHECKED, all three still literal `scope !== '*'` comparisons and none imports `isAuthorized`**, noted in the commit body. **A consequence §7.3 should know about:** because these gates never consult `isAuthorized`, making the membership arm unconditional cannot flip 2.7's login/`/admin/*` assertions — 2.7 catches "admin derived from breadth", which is a different mutation. §7.3's expectation that 2.7 goes red under an unconditional membership arm is likely wrong; it should be verified there rather than assumed, and 2.6 is the catcher that does bite.
- [x] 5.9a **Added in review — the set-arm grammar had three spellings, one of them a latent hazard.** `dashboard/tokens.ts`'s local `isProjectSet`, `authorizedByMembership`'s inline pair and `projectSetRemedy`'s inline pair all decided "is this a set arm" separately, and two of the three were not type predicates — so `scopeBadge` interpolated `${scope}` into `raw()` **unescaped** on the line after one that escapes, safe only because an untyped boolean gated it. Now one exported `isProjectSetScope(scope: TokenScope): scope is 'projects' | 'read:projects'` in `services/tokens.ts`, beside `pinnedProjectId` — whose own docstring states the value at stake, _"so the grammar has one writer and one reader"_. All three consumers call it (both already imported from that module), and the narrowing makes the unescaped interpolation provably a literal rather than provably-by-argument.
- [x] 5.10 Extend `projectPinRemedy` (`mcp/_shared.ts:108-121`) to name every member of a set, or to stay empty. Under-reporting here is fail-closed-looking rather than a security bug, so an empty remedy is acceptable; a remedy naming one arbitrary member is not. **DONE — names EVERY member**, in a separate `projectSetRemedy` branch rather than by generalising the pinned one, so the pinned message stays byte-identical (existing tests pin its wording). Slug-sorted for determinism, and empty when the denied project IS a member: there the refusal is the access verb and no `project.use` changes it — the same guard the pinned branch has for its own already-active case.
- [x] 5.10a **Corrected in review — the docstring described the pre-dispatch function.** `projectPinRemedy`'s docstring still said _"The one-call way in for a token pinned to one project… Empty otherwise"_ while its body had grown a `return projectSetRemedy(...)` in the otherwise case. 5.10's reason justifies **two message builders**, not putting the dispatch inside the pinned one. Now one `remedyFor(ctx, scope, projects)` does the path-scoped guard once and picks; `pinRemedy` and `setRemedy` keep their message bodies **byte-identical** (the pinned wording is pinned by existing tests) and lost their `ctx` parameter — each now takes only what it names (`pinned: string`, `memberProjectIds: readonly string[]`), so neither can consult the other's half of the reach.
- [x] 5.11 Confirm `mcp/project-tools.ts:200` (`project.list`'s filter) and `:128` (`project.use`'s read gate) need **no edit** — both are already `isAuthorized`-driven and become correct for free. Assert it with a test rather than by reading. **CONFIRMED BY TEST, not by reading** — `test/token-project-sets.test.ts` "reaches exactly its members at every gate, including the two project tools". `project.list` on a `{alpha, gamma}` set returns exactly those two and neither `beta` nor the default project; `project.use` succeeds on `gamma` and is `forbidden` on `beta`. Neither call site needed a behaviour edit (only the `ctx.scope` → `ctx` argument change the new signature forces, plus one comment line naming the set arm). **The subject is minted through the SERVICE, not the form**, because the form composes the set arm only once §6.3 lands — this test is therefore also §5's only end-to-end evidence until then, and it covers `/mcp/<slug>` read+write, `/api/<slug>/sessions` and the post-removal freshness leg on the same token.

- [x] 5.11a **Corrected in review — the "until" was already over, and two thirds of the test was a duplicate.** The test's comment said the service-minted path was the union's evidence _"until the dashboard handler lands"_; it landed in this same branch, so the comment documented a permanent exception as a temporary one. Its unique content is the `project.list` / `project.use` assertions 5.11 mandates — **both kept verbatim, nothing mandated removed**. The MCP/HTTP reach block duplicated 2.5/2.6 and the membership-freshness tail duplicated the whole of 2.9, which is _dedicated_ to the warm-cache scenario; both dropped, and the subject is now minted **through the form** like every sibling. The test is still self-controlling without them: `project.list` asserts two present slugs against two absent ones, and `project.use` a success against a refusal, in the same `it`. Renamed to what it now measures. Consequence worth naming: `TokensService` is no longer constructed anywhere in this file.
- [x] 5.12 **Deduplicated in review — one measurement, stated once.** The class docstring and `authorizedByMembership`'s docstring both carried "a set on a `*`/`read:*` base would be decorative / would reach every project regardless of the set", 335 lines apart in one file. Kept at `authorizedByMembership`, which is the code the measurement justifies (the confinement to the two set arms, with the measured counter-example `base read:* + {A, C} authorized B`); dropped from the class docstring, which keeps only the fall-through fact.

## 6. Dashboard

- [x] 6.1 Replace the single `<select name="project">` (`dashboard/tokens.ts:153-161`) with a multi-selection control. Use a checkbox list, not `<select multiple>`: measured, `<select multiple>` inherits `.main select`'s `appearance: none` plus arrow art and `min-height` (`styles/core/content.css:260-272`), cosmetically wrong for a listbox. **DONE as a checkbox list** — `<div class="chk-group">` holding one `<label class="chk"><input type="checkbox" name="project" value="<slug>">` per non-archived project, so N values arrive under one field name and `form.getAll('project')` reads the whole selection. The hint line states all three arms of the grammar table (`None selected` / `One` / `Two or more`), because the control no longer carries the old `<option>` that said what an empty selection means.
- [x] 6.1a **Removed in review — 6.1 introduced an unreachable branch the predecessor control did not have.** `selectable = projects.filter((p) => !p.archivedAt)` cannot be empty, so the `No projects yet.` fallback was dead on arrival. Verified at the source rather than by reading the filter: migration `0031_default_project.sql` inserts the default row with `is_default = 1` and no `archived_at` (so NULL) and `projects_is_default_uidx` admits exactly one such row; `services/projects.ts::archive` throws `conflict` for the default project; `setArchivedAt` is the only writer of `archived_at`, reached only from `archive`/`unarchive`; and `getDefault()` throws outright if no row carries `is_default`. The predecessor `<select>` had no such branch. Deleted.
- [x] 6.2 Extend the `input[type='checkbox']` rule from `.filters .group` (`styles/core/patterns.css:253-269`) to the form scope. **A new selector reusing `--lime` and `--fg-faint` only** — no new `:root` token, so `dashboard/spec.md:565-572` stays untouched and `:210`'s "introduces no new design tokens" stays true. Verify by diffing the `:root` block: it must be byte-identical. **DONE** — `.main form input[type='checkbox']` (+ `:checked`, `:focus-visible`) and the `.chk-group` / `.chk-list` / `.chk` layout, all in `styles/core/content.css` beside `.main form.stack`, which is where this very form's layout already lives. Tokens used: `--lime`, `--fg-faint`, `--fg`, `--fg-dim`, `--bg-elev`, `--f-mono`, `--s-*` — every one pre-existing. **The `:root` block is byte-identical, hashed rather than eyeballed:** `sha256` of the `:root { … }` block of `styles/core/tokens.css` is `8c1eb74fa9334be04b71aa6b7367908167c7062ee994bf82fcd9699e601ab29e` before and after, **642** bytes both times, and `git diff` on that file is empty. Measured in the browser: the checked box computes to `rgb(198, 242, 78)`, which IS `--lime`, not a look-alike. (**The byte count was wrong when first recorded — 606 — and is corrected here.** The hash is right; re-measured, the slice that produces it is `:root` through its closing brace **plus the trailing newline**, 642 bytes. An unmeasured number in an evidence document is the same defect this change's own §1 baseline had to correct.)
- [x] 6.2a **Corrected in review — 6.2 said it EXTENDED the checkbox rule; it duplicated it.** Measured with a headless engine over the six core stylesheets in `build-css.mjs`'s `CORE_ORDER`: the declaration bodies of `.filters .group input[type='checkbox']{,:checked,:focus-visible}` and `.main form input[type='checkbox']{…}` were **byte-identical — 12 declarations**, and the two subjects computed identically on all nine probed properties in both checked and unchecked states. The caption did the same: `.main .chk-group > .lab` restated **5 of 7** declarations of `.main label`. **17 total, now comma-extended instead of copied** — the three checkbox rules onto the `patterns.css` ones, the caption onto `.main label`. **Verified in the real engine, before and after, and the diff is stated rather than summarised:** every checkbox property is unchanged, every bounding box is unchanged (`.lab` `{x:48, y:110.69, w:480, h:14.69}`, the `.chk` row `28px`, the box `14×14`), no horizontal overflow — and exactly two computed values move, `.lab`'s `display: block → grid` and `gap: normal → 8px`, inherited from `.main label`. Both are inert for a span with one text child (the measured box proves it), and the stacking they exist for is already done by `.chk-group`'s own grid. Recorded rather than glossed because it IS a computed-style change. **`.main .chk-list .chk`'s four undo-declarations are deliberately left alone**: `.chk` is a real `<label>` inside `.main`, so it legitimately overrides the rule above.
- [x] 6.3 Rewrite the create handler's grant decision per the `dashboard` delta's table: zero selections → literal arm; one → single-project arm; two or more → set arm. **DONE**, and the three-way split is expressed as a two-way one on purpose: zero selections take the literal arm (`composeGrant` throws `invalid_input` on an empty `projects` array by design, so `[]` must never be passed), and any non-empty selection is handed to `create({ projects, access })` — where §5.3's `composeGrant` already routes one element to `singleProject`. One decision, one place; a second copy of the "one is not a set" rule in the dashboard could drift from it. **The crafted duplicate-slug POST is fixed AND regression-tested.** The selection is deduplicated with a `Set` before any project is resolved. Measured on the real boundary (`POST /dashboard/tokens` with two identical `project` values, `scratchpad/dup-slug-probe.mjs`): **with the dedupe → 302, `scope = 'project:<alpha>'`, `project_id` set, 0 `token_projects` rows** (one slug twice is one selection, so it is the single-project arm); **without it → 500 `Internal Server Error`, `SQLITE_CONSTRAINT_PRIMARYKEY` raised at `TokensRepository.insertProjects`**. A 16th test was added for it — "answers a crafted submission naming one slug twice without a server error" — with a two-distinct-slug control in the same `it` so the collapse cannot be the handler ignoring extra values. **Proved non-vacuous:** `node scripts/mutate.mjs --file apps/server/src/dashboard/tokens.ts --spec src/test/token-project-sets.test.ts -t 'crafted submission naming one slug twice' --mutation <the Set-wrapped read> --with <the unwrapped read>` → `CAUGHT by 1`, `expected 500 to be 302`; `tokens.ts` restored. **One behaviour left as-is, recorded rather than fixed:** a submission mixing a valid NEW slug with an invalid one can autocreate the valid project row before the invalid one is refused, because the strict regex lives in `ProjectsService.create` and pre-validating in the dashboard would duplicate it. The delta only requires that no `tokens` and no `token_projects` row is created (task 2.15 asserts both counts), project creation from this form is an explicit operator action, and the single-project handler behaved the same way before this change.
- [x] 6.3a **Corrected in review — 6.3's "left as-is" was wrong, and the test named for it could not see the leak.** 6.3 recorded that a submission mixing a valid NEW slug with an invalid one may autocreate the valid project before the invalid one is refused, and left it. Two things were wrong with that. (a) This change **widens** the class: the predecessor's single `<select>` could orphan at most one project, and only via a `tokens.create` conflict; the loop over N selections orphans every valid slug before an invalid one, so an invalid slug in position 2+ is a new trigger. Measured on the real boundary before the fix: `rejected mint whose EARLIER slug was new → status 400, deltas {tokens: 0, members: 0, projects: +1}`, with a valid multi-select control still answering 302. (b) Task **2.15's fixture made the leak undetectable in principle** — `[alpha.slug, 'INVALID Slug!']`, where `alpha` already exists, so no project could be created and the two counts it asserted were true while projects leaked. **Fixed:** `TokensService.createForSlugs(input, projects)` resolves-or-creates every named project and mints inside **one** `this.tx.transaction`, so a refusal from either half rolls the projects back; `create`'s own transaction nests as a savepoint (measured: an outer rollback undid an inner commit, `projects` count back from 2 to 1). The resolver is an argument, not a constructor dependency, so no existing construction site of `TokensService` grows one. **2.15 strengthened** to `[<a brand-new slug>, 'INVALID Slug!']` plus a `projects` count assertion, and observed red before the fix: `a refused mint autocreated a project: expected 5 to be 4`. **Mutation, one run:** replacing `this.tx.transaction(fn)` with a pass-through IIFE → `CAUGHT by 1`, exactly 2.15, on that same assertion; `tokens.ts` restored.
- [x] 6.3b **Removed in review — `composeGrant`'s empty-set throw was an uncovered guard, resolved by option (a) rather than by a test.** `'a project set must name at least one project'` had no reachable caller and no test. Typed away instead: `TokenGrant`'s set arm is now `readonly [Project, ...Project[]]` and `createForSlugs` takes `readonly [string, ...string[]]`, so the empty case is unrepresentable from the dashboard down. The cost is the one destructure the reviewer priced, at `dashboard/tokens.ts`'s create branch. This follows the arm's own recorded rationale (resolved ROWS for compiler help, `TokenReach` over an optional parameter for the same reason) rather than adding a test for a branch no caller can reach. **Verified by a hand `tsc` widen/restore loop, both directions** (`scripts/mutate.mjs` drives vitest, which ignores `@ts-expect-error`): the runtime throw is replaced by a fourth `@ts-expect-error` line in `tokens.test.ts`'s existing rejection list (`{ projects: [], access: 'write' }`), and relaxing the arm back to `Project[]` reports `src/services/tokens.test.ts(55,7): error TS2578: Unused '@ts-expect-error' directive` **plus** `tokens.ts(314,26): error TS18048: 'first' is possibly 'undefined'` — so the type now carries exactly what the deleted throw did. Restored byte-identical.
- [x] 6.4 Extend `unresolvable` / `stateOf` (`:37-47`) for the memberless-set state with the label from 0.7, and set precedence to `revoked` → `expired` → unresolvable → empty-set → `active`. `pinnedProjectId('projects')` returns `null`, so `unresolvable` already short-circuits correctly at `:38-39` — do not "fix" that. **DONE** — `no projects` / class `pending`, inserted between the unresolvable check and `active`, so the precedence reads in source order exactly as specified. `unresolvable` and `pinnedProjectId` are untouched: `pinnedProjectId('projects')` is `null`, so the `pinned !== null` guard short-circuits and a set row never reaches the slug lookup. Confirmed in the browser at both widths: `NO PROJECTS` renders as a dashed grey pill, visually distinct from lime `ACTIVE`, orange `INERT` and grey solid `REVOKED`. **One level of that precedence is vacuous, recorded rather than "fixed":** the `unresolvable → empty-set` step can never decide anything for a producible row. `unresolvable` is `pinned !== null && !slugById.has(pinned)`, and `pinnedProjectId` returns `null` for both set arms — so a set-scoped row is never unresolvable and the two levels never compete. The stated precedence is not wrong, it is partly untested by construction; a reader should not assume the ordering was exercised.
- [x] 6.5 Extend the project cell (`:56`) to render every member slug in slug-ascending order, archived members included (`deps.projects.list(true)` at `:34` already includes them, for the reason its comment at `:32-33` gives). **DONE** — the cell renders `members.join(', ')` when the token has membership rows, and otherwise keeps the existing pinned-slug/`—` behaviour untouched. The ordering is the repository's, not the template's: §4.1's `adminListProjectSlugs` orders by `(token_id, slug)` and resolves slugs through an `innerJoin` with no archived filter, so an archived member still resolves to its slug. `TokensDeps` gained `repos: Repositories` for that read; `dashboard/` is exempt from `invariants.test.ts`'s `admin*` allow-list by directory, which is exactly why §4 gave the read that prefix.
- [x] 6.6 Extend `scopeBadge` (`:286-290`) for the two new literals. **DONE** — both render as a plain `.pill`, the same treatment `read:*` already gets, because they are well-known literals rather than the opaque `project:<id>` string the `<code>` fallback exists for. The predicate is one shared `isProjectSet` helper, also used by `stateOf`, so the two places that must agree on what a set arm is cannot disagree.
- [x] 6.7 Extend the one-time-view panel to enumerate every member slug — not a count. **DONE** — `Scope <code>projects</code> — reaches <strong>alpha</strong>, <strong>gamma</strong>.`, read back off the persisted row and its membership rows, never off the query string, so the panel states what was minted. Verified in a real browser through the whole mint flow (tick three, submit, read the panel): `SCOPE PROJECTS — REACHES ALPHA, DOCS-SHARED, MOBILE-APP.` No nested `<div>` was introduced inside `.one-shot` — the tests extract the panel with a non-greedy `<div class="one-shot">[\s\S]*?</div>`, so one would truncate the panel they assert on.
- [x] 6.8 No `data-confirm` modal is added (creating a token is not destructive, `dashboard/spec.md:210`); the revoke action keeps its danger-tone modal. Timestamps already go through `formatTs`. Confirm both by inspection and note it. **CONFIRMED BY INSPECTION, all four points.** (a) The create form is `<form action="/dashboard/tokens" method="post" class="stack">` with no `data-confirm*` attribute — minting is not destructive. (b) The revoke form keeps all three attributes (`data-confirm`, `data-confirm-label`, `data-confirm-tone="danger"`) **on the `<form>`**, not on the `<button class="danger">` inside it; `command grep -n 'data-confirm' dashboard/tokens.ts` returns exactly those three lines and they sit inside the `<form …>` tag. (c) Both timestamp cells are `formatTs(t.createdAt)` / `formatTs(t.expiresAt)`; the file contains no `toISOString` and no `toLocaleString`. (d) No inline `<style>`: every new rule went into `styles/core/content.css`.
- [x] 6.9 Validate the rendered form and list headless (Playwright screenshot, no GUI available) at desktop and mobile widths, and confirm the checkbox list is legible in both. **DONE headless (chromium, `deviceScaleFactor: 2`), at 1440 / 900 / 390 px**, against a throwaway server on a temp data dir — **not** the dev container, whose `CMD` runs `seed-dev --reset` and would wipe the owner's corpus. Seeded with eight projects and one token of every shape (set/write, set/read, single-project, `read:*`, memberless set, legacy inert) so every rendering branch is on screen at once. Legibility is measured, not asserted from the picture: the box computes to **14×14 px** filled `rgb(198, 242, 78)` when checked, each label is a 28 px-tall row at 10.92 px mono, and at 390 px **neither the list nor the document overflows horizontally** (`scrollWidth <= clientWidth` for both) — the list wraps to three rows instead. The mint flow was driven end to end in the browser (tick three boxes → submit → one-time panel), which is what verified 6.7's panel and the new row's three-slug cell together.

**Three behaviours found in review and deliberately NOT fixed here.** Each was reproduced against a throwaway server on a temp data dir (`scratchpad/p7-probe.mjs`, one run, mint through the real `POST /dashboard/tokens` form) rather than argued from the code, and each is recorded so a later reader finds it instead of rediscovering it.

- [x] 6.10 **A crafted POST can put an ARCHIVED project in a set, and the dashboard then asserts reach that does not exist.** The checkbox list filters archived (`archivedOfferedByTheForm: false`, `liveOfferedByTheForm: true`), but the handler resolves through `findBySlug`, which does not — so a hand-written POST naming `archived-one` mints `scope = 'projects'` with the archived id among the members (302, `membersIncludeArchived: true`). **No reach is granted**, and the control is what proves that is the project's state rather than the token's: the archived member answers `403 project_archived` on both `/mcp/<slug>` and `POST /api/<slug>/sessions` for the set token **and identically for the `*` control**, while the same set token still answers 200 on its live member. The cost is therefore a membership row plus a dashboard cell (`archived-one, live-one`, state `active`) claiming a reach the server refuses. Pre-existing for the single-project arm too. Not fixed: it needs a decision about whether an archived project may be named at all, which belongs with `dashboard/spec.md`'s archive semantics, not here.
- [x] 6.11 **No bound on the number of submitted `project` values.** 200 slugs → `302`, `scope = 'projects'`, 200 membership rows, 200 projects created, in one request. It requires an authenticated admin session with a valid CSRF token, so it is a footgun rather than an attack; `insertProjects` is a single multi-row INSERT, so a large enough submission would eventually meet SQLite's host-parameter limit — **that ceiling was not measured and no figure is claimed here.** Not fixed: a bound is a product decision (what is a reasonable maximum?), and 200 succeeding cleanly is not a defect.
- [x] 6.12 **An `inert` row can display a member slug.** A legacy row whose scope names a project by SLUG (`project:live-one`, `project_id` NULL) with a membership row present renders `state = inert` **and** project `live-one` — the state check tests `unresolvable` first while the project cell tests `members.length > 0` first, so the two disagree about the same row. Reachable only by hand-written rows: no producer composes a scope that is unresolvable _and_ a membership set. Not fixed: `auth/spec.md`'s never-repair contract for `inert` says the row must not be touched, and suppressing the cell would hide the only evidence of how the row got there.

## 7. Mutation — the union has two arms and a test can be green with either one broken

Use `node scripts/mutate.mjs --file … --spec … --mutation … --with …` for each. This repo has had three tests in one session pass while proving nothing; a test green on both sides of a change is the default outcome, not the exception.

- [x] 7.1 **Drop the legacy arm** (make `legacy(...)` always `false`). The tests naming the pre-existing shapes — 2.1, 2.2, and the 1.2 matrix rows for `*`, `read:*`, `project:<id>`, `read:project:<id>` — MUST go red. **CAUGHT by 4** (`authorizedByScope(reach.scope, action, target) ||` → `false ||`): 2.1's admin control, **2.2's spine**, `authorizes nothing while its set is empty` and `cannot create the project it would need to reach`. The two the task names are both in it, which is what proves the union did not quietly reimplement the old behaviour instead of preserving it.
- [x] 7.2 **Drop the membership arm** (make `membership(...)` always `false`). Tasks 2.5, 2.8, 2.9 MUST go red. 2.6 and 2.10 will stay green — they assert refusals — which is exactly why 2.5 exists. **CAUGHT by 4** (`|| authorizedByMembership(...)` → `|| false`): the 2.5/2.6 member-reach test, 2.8's read-only set, 2.9's warm-cache freshness, and 5.11's project-tools filter. **The asymmetry the task predicts is confirmed: 2.6-as-refusal and 2.10 stayed GREEN**, because both assert refusals and a missing membership arm refuses more, not less. That is precisely why 2.5 exists — a suite of refusal assertions cannot notice a grant that never happens.
- [x] 7.3 **Make membership unconditional** (`membership(...)` always `true`). Tasks 2.6, 2.7 and 2.10 MUST go red. If 2.7 stays green, the escalation control is not doing its job and must be fixed before proceeding. **CAUGHT by 7** (`|| authorizedByMembership(...)` → `|| true`): 2.2, the 2.5/2.6 test, 2.8, 2.9, 2.10's empty set, 5.11 and 2.11. **CORRECTION, measured twice: the task's claim that 2.7 goes red here is FALSE.** 2.7 stayed green. §5 predicted this and I reproduced it independently: the three admin gates are literal string comparisons (`server/dashboard-router.ts:156` `resolved.scope !== '*'`, `server/http.ts:489`, `dashboard/maintenance.ts:143`) and **none consults `isAuthorized`**, so no change to the membership arm can reach them. **What 2.7 actually catches is admin derived from breadth**, and that is now demonstrated rather than asserted: mutating the login gate to `resolved.scope !== '*' && resolved.scope !== 'projects'` is **CAUGHT by 2** — 2.7 itself plus the empty-set test. A reader who trusted the task text would have found 2.7 green under 7.3 and been tempted to 'fix' the test, which is how a control gets weakened into uselessness.
- [x] 7.4 **Remove the per-request freshness** (cache the membership read). Task 2.9 MUST go red. If it stays green, the test is not exercising a warm cache. **CAUGHT by exactly 1** — 2.9, `loses a removed member on the next request with the credential cache already warm`. The mutation memoizes `listProjectIds` per token id for the process lifetime, which is what "remove the per-request freshness" means concretely. Catching exactly one test is the right answer here: no other test claims this property, and 2.9's warm-up read is what makes it bite (a cold-cache assertion would pass with the memo in place).
- [x] 7.5 **Make the set arm's base permissive** (`'projects'` treated as `'*'`). Tasks 2.6, 2.7, 2.10 MUST go red — this is the measured `read:*`-base leak, and it must be a red test rather than a design note. **CAUGHT by 5** (`if (scope === '*') return true;` → `… || scope === 'projects'`): the 2.5/2.6 test, 2.9, 2.10's empty set, 5.11 and 2.11. This is the measured `read:*`-base leak the design rejected, now a red test rather than a design note. **2.7 is absent here too, for the same root cause as 7.3** — the admin gates never consult `isAuthorized` — so the task's expectation is wrong in both places, consistently, not as an isolated slip.
- [x] 7.6 Record each mutation, the tests that went red, and any that unexpectedly stayed green. **DONE — six runs, one mutation each, every file restored byte-identically** (`git status` clean after the set). Verdicts above. Two things worth carrying forward: (a) the task text's 2.7 expectation is wrong in 7.3 and 7.5 and is corrected in place, with the mutation 2.7 _does_ catch demonstrated; (b) nothing stayed unexpectedly green once that correction is applied — every prediction the task makes about 2.1, 2.2, 2.5, 2.6, 2.8, 2.9 and 2.10 reproduced. `scripts/mutate.mjs` refuses to run over a red baseline, and `apps/plugin/test/stop-sync.test.ts` is a known load flake, so every run was scoped to `src/test/token-project-sets.test.ts` rather than the whole suite.

## 8. Re-run the baseline and diff it cell by cell

- [x] 8.1 Re-run the exact script from 1.5 against the changed server. Diff the matrix **cell by cell**, comparing HTTP status **and** structured error code. **DONE, re-run with my own instrument rather than citing the review's.** `authorization-baseline.mjs` executed against the changed tree; the committed before-run was copied out first and `git checkout`-restored after, so the tracked JSON is still the before-run. Result: **45 cells compared, 0 moved.** ULIDs were normalised before comparing (they differ per run by construction); every HTTP status, every `ok`, every structured `code` identical. This reproduces the review agent's independent result exactly, which is worth more than either run alone.
- [x] 8.2 Every pre-existing-shape cell MUST be byte-identical. Any divergence is a stop-and-explain, not a baseline to update. **DONE — every pre-existing-shape cell is byte-identical on both status and code.** The only difference anywhere in the payload is the `created_at` column of the persisted-scope table (`10:36:15 UTC` → `13:51:56 UTC`), which is two run times and not a behaviour. Scope strings and project cells match exactly: `*`/`—`, `read:*`/`—`, `project:<ID>`/`alpha`, `read:project:<ID>`/`alpha`.
- [x] 8.3 Re-assert the non-vacuity count from 1.4: the number of successful cells MUST be unchanged and non-zero. An "identical" diff over two empty or all-refused sets proves nothing. **DONE: 16 successes of 45 before, 16 of 45 after** — unchanged and non-zero, so the identical diff above is not an identical diff over an all-refused matrix. This is the assertion §1 had to learn the hard way: its first run sent `type: 'note'`, not in `MEMORY_TYPES`, and the strict tool schemas refused all six write cells with `-32602`, so the matrix read as "writes are denied" while the count would have hidden it. The script now aborts unless the `*` token succeeds on all three writes and both session posts.
- [x] 8.4 Append the new rows (set token over `{alpha, gamma}`, set token over every project, memberless set token) to the committed matrix, clearly separated from the pre-existing rows. **DONE, and the source of each row is named rather than implied.** The three new arms are NOT measured by `authorization-baseline.mjs` — it mints only the four pre-existing shapes, deliberately, so the regression instrument stays exactly what it was when the before-run was taken. The new surface is measured instead by `src/test/token-project-sets.test.ts`, at the same boundaries (dashboard mint form, MCP SDK transport, `POST /api/<slug>/sessions`), and appended to `measurements/authorization-baseline.md` under its own heading so a reader can tell the regression surface from the new one:

  | new arm                    | evidence, and where                                                                                                                                                                                                                     |
  | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | set over `{alpha, gamma}`  | reaches both members read+write on `/mcp/<slug>` and `POST /api/<slug>/sessions`; refused on `beta`, on the default project and on the path-less `/mcp` — one test, so the grant and the refusal cannot be skipped separately (2.5/2.6) |
  | set over **every** project | denied `POST /dashboard/login` (401) and `/admin/*` (403 `forbidden`) while the `*` control succeeds on both (302, 200) — the escalation control, 2.7                                                                                   |
  | memberless set             | authorizes nothing on any surface, and renders as `no projects` (class `pending`), distinct from `active`, `revoked`/`expired` and the unresolvable `inert` (2.10, 2.12)                                                                |

  Two properties of the new surface that only a mutation could establish, both in §7: dropping the membership arm reds the member-reach test while leaving the refusal tests green, and memoizing the membership read reds exactly the warm-cache freshness test.

## 9. Migration proof on populated data — real Docker, pre-existing seeded data

Standing requirement for anything touching migrations, MCP, HTTP or production behaviour. This change touches all four. Use the `rembric-smoke-tests` playbook; note that `dev:docker:up` reseeds with `--reset` on every boot, so the "pre-existing" corpus must be created **before** the upgrade boot and preserved across it.

- [x] 9.1 Bring up the **pre-change** image against a seeded database. Mint one token of each pre-existing shape, including a deliberately malformed legacy row (`scope = 'project:<slug>'`, `project_id IS NULL`) inserted through the repository. Record `SELECT count(*) FROM tokens` and a digest of every `tokens` row. **DONE, with a declared deviation on the instrument.** The volume was populated by the **base** code (a `git worktree` at `c26ecfb`, the branch's merge base, whose migrations end at `0031` and which has no `token_projects`), seeded through `seed-dev.ts` and then given a **deliberately malformed legacy row** inserted straight into `tokens`: `scope = 'project:demo'` with `project_id IS NULL` — a shape the typed producer cannot compose, which SQLite accepts because `tokens_project_scope_check` is deliberately one-directional. Before-state: `memory=35 projects=1 sessions=5 prompts=0 tokens=4`, `tokensDigest = 206a25edd9ca0adb3378d01cdf3eb039685d5be3d6481a281191eb55b58e1dc9`, `foreign_key_check` 0, `integrity_check ok`, `token_projects` **absent**.

  **DEVIATION, and the reason:** this used the already-built `rembric-dev:local` image with `apps/server/src` bind-mounted and a worktree per code state, **not two production image builds**. Building two ≈5 GB distroless images is what pinned this host's CPU at 99% earlier in the day and forced a reboot. What §9 asserts is code behaviour — migration on populated data, `tokens` untouched, derived state intact, a set token at the wire, rollback fail-closed — and all of it is exercised identically with the source mounted. The one thing this does **not** cover is that the built image boots, and CI's `docker-build-check` (amd64 + arm64) covers exactly that on the PR.

- [x] 9.2 Upgrade in place to the changed image. Assert: every `tokens` row byte-identical, **with the non-empty row count asserted** — a digest match over two empty sets is vacuous. **DONE, and the digest match is not vacuous.** Upgrading in place applied **exactly one** migration (`[migrate] applying 0032_token_projects.sql`) and the `tokens` digest is **byte-identical**: `206a25ed…` before and after, with `tokenCount: 4` asserted on both sides. A digest match over two empty sets would prove nothing, which is why the count is part of the assertion. Boot counts unchanged: `memory=35 projects=1 sessions=5 tokens=4 prompts=0`. The created table's DDL is exactly what §3 specified — both FKs real, composite `PRIMARY KEY (token_id, project_id)`, `WITHOUT ROWID`, **no secondary index**.
- [x] 9.3 `PRAGMA foreign_key_check` returns no rows; `PRAGMA integrity_check` returns `ok`. **DONE** — `PRAGMA foreign_key_check` returns 0 rows and `PRAGMA integrity_check` returns `ok`, both after the upgrade on the populated volume.
- [x] 9.4 The legacy inert row's first-boot behaviour is unchanged: still `forbidden` against its named project, still `inert` in the dashboard, every column unchanged — `auth/spec.md:274-283` verbatim. **DONE — the legacy inert row is unchanged in every column.** After the upgrade: `scope = 'project:demo'`, `project_id = null`, `revoked_at = null`, exactly as inserted. The migration does not touch `tokens` at all, so this is conserved by construction rather than by care; asserting it is what turns that argument into evidence.
- [x] 9.5 Confirm no derived data was invalidated: `memory_fts`, `memory_vec` and the three entity tables are functions of `memory`, and no memory row is touched. Assert row counts unchanged rather than asserting the absence of a rebuild. **DONE, with one honest limit stated rather than papered over.** Every derived count is unchanged across the upgrade: `memory` 35, `memory_entity_scan` 35, `memory_entities` 1, `memory_entity_links` 1, `confirmations` 0, `sessions` 5, `prompts` 0. **`memory_vec` was NOT counted directly**: it is a `vec0` virtual table and a plain `better-sqlite3` connection without the sqlite-vec extension loaded reports it absent, so a count from that instrument would have been a measurement of my probe rather than of the table. It is unaffected by construction — the migration is one `CREATE TABLE` and touches no `memory` row — and the server booted and served on the upgraded volume, which it could not do with a broken vec index. Counting it properly needs the extension-loading path the server itself uses.
- [x] 9.6 Mint a set token through the real dashboard form in Docker and exercise it over the MCP SDK transport on two `/mcp/<slug>` endpoints and one it must not reach, plus `POST /api/<slug>/sessions`. This is the boundary the real caller uses; a direct handler call bypasses the zod schema and proves nothing about the tool. **DONE at the wire, through the real dashboard form, with the control that makes the refusal mean something.** Three projects created through `POST /dashboard/projects/create`; a set token minted through `POST /dashboard/tokens` with two `project` values, which answered 302 with the plaintext in the `Location` query string. Persisted shape read back from the DB: `scope = 'projects'`, `project_id = null`, members exactly `['beta9','gamma9']` — `delta9` exists and is deliberately not a member.

  | instrument                  | set token `{beta9, gamma9}` | control                                 |
  | --------------------------- | --------------------------- | --------------------------------------- |
  | `POST /api/beta9/sessions`  | **200**                     | —                                       |
  | `POST /api/gamma9/sessions` | **200**                     | —                                       |
  | `POST /api/delta9/sessions` | **403 `forbidden`**         | admin `*` on the same endpoint: **200** |
  | `POST /dashboard/login`     | **401**                     | admin `*`: **302**                      |

  The admin control on `delta9` is what distinguishes "this token's reach ends here" from "this endpoint is broken", and the login row is the escalation control: reach over several projects is not admin.

- [x] 9.7 **Rollback rehearsal.** Downgrade to the pre-change image with the set token in place and confirm it authorizes **nothing** — `pinnedProjectId('projects')` is `null` and `isAuthorized('projects', …)` is false everywhere, so the older binary strips it of all reach. Confirm it is fail-**closed**, and that pre-existing tokens still work. **DONE — the rollback is fail-closed, verified rather than asserted.** The base binary was booted on the **upgraded** volume with the set token in place. It boots clean (`healthz 200`), emits **no `applying` line** — it neither re-runs nor undoes `0032`, and the `_migrations` row survives — and the data is intact (`memory=35`, `projects=4`, `tokens=7`).

  The set token then authorizes **nothing**: `403 forbidden` on `beta9` and `gamma9`, its own members, and on `delta9`; `401` on `POST /dashboard/login`. Its scope string `projects` is unrecognised by the old `isAuthorized`, which ends in `return false`.

  Controls proving that is the token's reach and not a dead server: the admin `*` token still gets **200** on `beta9` and **302** on login, and the seeded read-only `demo-reader` still gets **403** on a session POST — i.e. pre-existing read/write semantics are unchanged too. One pre-existing behaviour worth naming because it is visible here: `/healthz` answers **200** for the set token, since it accepts any authenticated token and is not scope-gated. That predates this change.

## 10. Verification and release

- [x] 10.1 `pnpm run typecheck` **PASS** — `tsc --noEmit` clean at the final state.
- [x] 10.2 `pnpm run lint` **PASS** — `eslint .` clean at the final state.
- [x] 10.3 `pnpm test` **PASS, and fully green: 134 files passed / 1 skipped, 2430 tests passed / 10 skipped.** Worth recording because it corrects a prediction I made: `apps/plugin/test/stop-sync.test.ts` failed in an earlier full run and I expected it to fail again. It passed here. That **confirms** the load-flake diagnosis rather than contradicting it — the earlier run overlapped a Docker teardown, this one did not, and the test passes 14/14 in isolation either way. The skipped file is the pre-existing `review-reads.bench.test.ts`.
- [x] 10.4 **`pnpm run eval` is deliberately NOT run.** No retrieval code path, ranking weight, corpus or query is touched, and no memory row is read or written. Recorded so the omission is a decision rather than an oversight. (Design D13 also records why the harness could not settle a widening question even if it were run: `archive/2026-08-03-weight-relevance-levels-by-idf/design.md:256` shows `P@8` and `R@8` both pinned at their ceilings.) **CONFIRMED NOT RUN, as a decision.** No retrieval code path, ranking weight, corpus or query is touched, and no `memory` row is read or written by anything this change adds — the only new table is `token_projects` and the only rewritten function is `isAuthorized`. Recorded so the omission is traceable rather than looking like an oversight, which is the whole point of this task existing.
- [x] 10.5 **No performance gate is added.** Design D11 records the two instruments separately — isolated statements (marginal ~1.2–2.2 µs) and end-to-end HTTP (the whole warm auth step is ≤332 µs of a 1.687 ms request). Anyone later claiming a cost here owes **both** numbers, named. Do not add a threshold test over a µs-scale statement timing. **CONFIRMED: no performance gate added.** Design D11 keeps the two instruments apart and so does this record: isolated statements are marginal (~1.2–2.2 µs) and the whole warm authorization step is ≤332 µs of a 1.687 ms end-to-end request. Anyone later claiming a cost here owes **both** numbers, named. A threshold test over a µs-scale statement timing would be a gate on noise — this repo has already had a 39× statement speedup that was 2.5× end-to-end because the instrument changed silently between rows.
- [x] 10.6 **Release note MUST state the rollback behaviour** (design D-risk, task 9.7): a downgraded server silently strips set-scoped tokens of all reach, because it never reads `token_projects` and the `projects` literal authorizes nothing on its own. Fail-closed, but silent — operators rolling back must re-mint deliberately rather than discover it from failing agents. **DONE — written for the operator, in `docs/updates.md`, under "Tokens that reach several projects", as a `> [!WARNING]`.** It states the measured behaviour rather than the mechanism: a rolled-back server refuses a set-scoped token **everywhere**, `403` on every project _including the ones the token names_ and `401` at the dashboard login, because it has no idea what the `projects` scope is. Nothing is deleted and the memberships return on re-upgrade. The point the warning leads with is that it is **silent** — no message explains it and an agent using that token just fails — so an operator who rolls back should re-mint single-project tokens deliberately instead of finding out from failing agents. Measured in §9.7 with its controls: the admin token still worked on the same instruments in the same arm.
- [x] 10.7 **Release note MUST state the deferred capability** (design D5, OQ3): read-shared/write-own is not expressible with one verb, and not expressible as two tokens either because a client install has one credential slot (`rembric-bridge.mjs:43-44`). The upgrade path is `ALTER TABLE token_projects ADD COLUMN access TEXT NOT NULL DEFAULT 'write'`, measured rebuild-free. **DONE, in the same section.** Framed as what an operator can and cannot ask for rather than as a schema fact: `read` or `write` applies to the whole selection, so "read these three, write this one" needs two tokens today — and because a client install has a **single credential slot** (`rembric-bridge.mjs`), that really means choosing the wider verb or splitting the work across two installs. The note closes by saying it is a deferral and not a dead end, since the upgrade path is `ALTER TABLE token_projects ADD COLUMN access TEXT NOT NULL DEFAULT 'write'`, measured rebuild-free.
- [x] 10.8 Record the explicitly-rejected items so they are not silently lost: option 1b (D3), the delimited-list scope string (D4), deriving admin from breadth (D8), multi-project OAuth (D10), and `all_projects` — related but **not** unblocked by this change (D13), including the counter-argument that set tokens make cross-project reads common and therefore make `all_projects` harder, not easier. **RECORDED, each with the reason it was rejected rather than just the fact.** (a) **Option 1b** (keep `scope = project:<primary-id>`, extras in the join table) — it would leave the `project` column under-reporting a token's real reach, the same truthfulness defect the predecessor spent five waves removing. (b) **A delimited-list scope string** (D4) — it re-admits parsing to a field whose whole grammar is currently comparable by equality. (c) **Deriving admin from breadth** (D8) — rejected, and now the _measured_ catcher of it: §7 demonstrated that admitting `projects` in the login gate is CAUGHT by 2 tests, so the rejection is enforced rather than trusted. (d) **Multi-project OAuth** (D10) — a path-less `/mcp` grant stays unbound per `mcp-oauth`, so no consent screen can express a set; inventing one is its own change. (e) **`all_projects`** — related but **not** unblocked by this change (D13), and the counter-argument stands: set tokens make cross-project reads common and therefore make `all_projects` harder to reason about, not easier.
- [x] 10.9 Open the follow-up for OQ6 (`dashboard/spec.md:710`'s vacuous project-scope dashboard session), pre-existing and deliberately not folded in. **FOLLOW-UP OPENED (recorded here; pre-existing, deliberately not folded in).** `dashboard/spec.md`'s project-scope dashboard session is vacuous today because login requires `*` (`server/dashboard-router.ts:156`), so no project-scoped token — and now no set-scoped token either — can ever hold one. This change makes the vacuity _more_ visible without causing it: §2's escalation control (2.7) now pins that a set token is refused login, which is the correct behaviour and also the reason the spec sentence has no reachable instance.
- [x] 10.11 **Open the follow-up for the test-construction duplication this change paid for twice — DEFERRED here, deliberately, and named so it is not lost.** Counted exactly, not estimated: **34** `new TokensService(` call sites in `*.test.ts` absorbed §4.3's second constructor parameter one by one, and **18** `memberProjectIds: []` literals across **14** test files absorbed §5.4's `TokenReach`. A `createTokensService(handle, now?, cacheMax?)` beside the existing `mintTestToken` (`test/tokens.ts`, which already constructs the service internally and took the `tx` parameter in ONE place) would have absorbed the first 34; the second 18 want a shared request-context factory. **Deferred, with reasons rather than as an omission:** (a) the 52 edits are already paid, so the lever buys this change nothing and its whole value is contingent on the next field; (b) the second half is **not** mechanical — the 18 sites are two different shapes, some hand-building a whole `Token` row literal inline (`mcp/memory-tools.test.ts` ×5, `observability-tools.test.ts`) and some already taking `(token, overrides: Partial<RequestContext>)`, so consolidating means designing what a shared factory DEFAULTS, and a factory that defaults `requestedSlug: null` would silently weaken the very files that exist to pin path scoping (`mcp/unresolvable-slug.test.ts`, `mcp/session-scope-resolution.test.ts`); (c) a ~40-file zero-behaviour commit landing between the review fixes and §8's cell-by-cell baseline diff costs exactly the reviewability this change has spent its evidence on. Whoever picks this up: do the `createTokensService` half first and on its own, and make the context factory take its scope-shaping fields **explicitly** rather than by default. **DEFERRED DELIBERATELY, and named so it is not lost — this is the debt this change paid twice.** 52 mechanical test edits were avoidable: **34** `new TokensService(` sites that a sibling of `apps/server/src/test/tokens.ts` (which already builds one internally and absorbed the `tx` parameter in a single place) would have absorbed, and **18** `memberProjectIds: []` literals across 13 files, each hand-rolling its own `makeContext`/`ctxFor`/`scopedCtx` — `mcp/memory-tools.test.ts` alone has five. The cost is not yesterday's edits; it is that the **next** field added to `RequestContext` or to the `TokensService` constructor pays them again. Not done here because it edits many test files unrelated to this change and would bury a reviewable diff in churn; `apps/server/src/test/` already hosts exactly this class of helper, so the landing place is known.
- [x] 10.10 No `apps/plugin/` change is expected. Confirm with `git status apps/plugin/` that the tree is clean — nothing in the plugin parses a token scope, the bridge only forwards the credential. If a plugin file did change, remember it ships to FOUR clients as ONE shared resource. **CONFIRMED CLEAN.** `git status --porcelain apps/plugin/` is empty, `git ls-files apps/plugin/ | wc -l` is **68** — unchanged — and a grep for `TokenScope` / `project:` / `read:project` across the whole plugin tree returns **0** matches, which is the substantive point: no client parses a token scope, the bridge only forwards the credential. So there is nothing for the plugin's four clients to learn about set tokens and no unified `plugin` version bump owed.
