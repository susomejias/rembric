# add-rembric

Introduce **Rembric** v0 (npm package and CLI `rembric`, singular): a
cloud-only, self-hosted memory system for AI agents, exposed via MCP HTTP,
with automatic consolidation-based consolidation and a minimal web dashboard.
