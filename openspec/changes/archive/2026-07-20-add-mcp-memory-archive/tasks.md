## 1. Service + journaling

- [x] 1.1 Confirm `consolidation_ops.op_type` has no CHECK constraint that would reject `agent_memory_archive` (grep migrations for `op_type`; if a CHECK exists, add a table-rebuild migration per the SQLite rebuild dance in CLAUDE.md). Result: DDL is plain `op_type text NOT NULL` (no CHECK) — no migration needed; only the Drizzle enum in `apps/server/src/db/schema/consolidation.ts` gained `'agent_memory_archive'`.
- [x] 1.2 In `apps/server/src/services/memory.ts`, make `MemoryService.archive(id, scope)` journal the flip: within one `db.transaction`, call `markArchived` then insert a `consolidation_ops` row with `op_type = 'agent_memory_archive'`, `affected_ids = [id]`, `created_id = null`, and a static reasoning string (mirror the `purgeDisconnectedArchived` journaling shape, run scope `'maintenance'`). Keep the existing `memory_not_found`/`conflict` guards unchanged.
- [x] 1.3 Dashboard parity: `apps/server/src/dashboard/memories.ts:563` already calls `deps.memory.archive(id, scope)`, so it now journals via the same method (parity by construction). The decay sweep uses `archiveOne`/`archiveActive` directly and keeps journaling `decay`, so no double-journaling.
- [x] 1.4 Unit-test `MemoryService.archive` so that `vitest run src/services/memory.test.ts` covers: (a) active→archived flip; (b) `consolidation_ops` row written with `op_type='agent_memory_archive'` + reasoning + not reverted; (c) `memory_not_found` on cross-scope id; (d) `conflict` on superseded/archived; (e) `content`/`title`/`replaces` unchanged and no `memory_relations` row inserted.
- [x] 1.5 Carve `agent_memory_archive` out of the purge pin: in `apps/server/src/db/repositories/memory-repository.ts` `PURGE_PREDICATE`, add `WHERE co.op_type != 'agent_memory_archive'` to the `affected_ids` subquery so an agent-archived memory stays purgeable from maintenance. Existing purge tests in `src/services/memory.test.ts` pass again.
- [x] 1.6 Make `agent_memory_archive` reversible: in `apps/server/src/consolidation/operations.ts` `undoOp`, treat it like `decay` (both the purged-referent guard and the reactivate branch) so an operator can revert an agent archive from the consolidation view.
- [x] 1.7 Reversibility test in `src/consolidation/operations.test.ts`: archive via the service, `undoOp` the `agent_memory_archive` op, assert the memory is `active` again and the op is `reverted_at`.

## 2. MCP tool wiring

- [x] 2.1 In `apps/server/src/mcp/memory-tools.ts`, register a `memory.archive` tool (schema `memoryArchiveSchema` `{ id }`, output `memoryArchiveOutput` `{ ok, id, status:'archived' }`, `handleArchive`) resolving scope via `resolveEffectiveScope` exactly like `memory.confirm`, delegating to `deps.memory.archive(id, scope)`. Map `memory_not_found`→`not_found` and let `conflict` flow through `errToMcp` (code `conflict`). Registered in `apps/server/src/mcp/server.ts` with `WRITE_ANNOTATIONS`.
- [x] 2.2 Wrote `ARCHIVE_DESCRIPTION` (server.ts) implementing the anti-autonomous-retirement contract: ONLY on explicit user request to retire/remove/forget; PREFER `topic_key`/`memory.judge` supersede when a replacement exists (archive = no-successor path); MUST NOT archive as autonomous housekeeping during recall/save; reversible from the dashboard; includes the cross-project move recipe. In the description text, not only `describe()`.
- [x] 2.3 MCP tests in `src/mcp/memory-tools.test.ts`: (a) archive succeeds on a path-scoped project active memory and flips status; (b) other-project id → not_found (status unchanged); (c) unscoped /mcp archive of a project id → not_found (no cross-scope archive); (d) re-archiving → conflict. Note: `memory.archive` has no `scope` argument (mirrors `confirm`/`get`), so it uses the effective scope — the `scope_locked`/`project_required` errors only apply to tools taking an explicit `scope` (save).
- [x] 2.4 Description-contract test in `src/test/mcp-integration.test.ts` asserting the `memory.archive` `tools/list` description contains the guard elements (explicit-request, retire/remove/forget, prefer-supersede + topic_key, no-autonomous-cleanup, reversible/undo, dashboard); also added `memory.archive` to the exhaustive `WRITE_TOOLS` annotation-partition set.

## 3. Docs + plugin surface

- [x] 3.1 Docs carry no exhaustive tool catalog (tool copy is served live from the MCP server), and the plugin tree hardcodes no tool list — only commands + session-lifecycle wiring — so nothing goes stale. Added one operator-facing line to the README append-only invariant noting the agent `memory.archive` path (explicit user request, reversible/journaled, purge stays operator-only). No `apps/plugin/` change needed (verified via `grep` — no catalog to sync).
- [x] 3.2 No change: the `initialize` `instructions` block is a proactive SAVE/RECALL/SUMMARIZE crib-sheet, not a tool enumeration. `memory.archive` is deliberately non-proactive (explicit-request only), so adding it there would contradict the anti-autonomous-retirement contract and eat into `INSTRUCTIONS_MAX_LENGTH`.

## 4. Verify

- [x] 4.1 `pnpm run typecheck` and `pnpm run lint` pass.
- [x] 4.2 `pnpm test` passes (94 server files + plugin vitest + 71 Hermes python tests; 1255 vitest passed / 1 skipped), including the append-only invariant tests in `apps/server/src/test/invariants.test.ts` (archive stays a status flip — no DELETE/content-UPDATE gate tripped; the journaling calls repo methods, no raw SQL in the service).
- [x] 4.3 Docker smoke done against `pnpm run dev:docker:up` (slug `demo`, port 8788). Verified over MCP: `tools/list` has `memory.archive`; happy-path archive → `{ok,id,status:archived}`; default `memory.search` excludes it; re-archive → `conflict`; unknown/cross-scope id → `not_found`. DB (via container `better-sqlite3`): `agent_memory_archive` journal row with `affected_ids=[id]` + correct reasoning, `reverted_at` NULL; purge predicate confirms the row is purgeable once the carve-out is applied (held back only by legitimate pending candidate relations from the save path). Reversibility (undoOp) covered by unit test (operator/dashboard action, not MCP).
