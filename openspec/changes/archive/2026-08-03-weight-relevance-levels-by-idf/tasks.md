## 0. Record the before, once, and never re-derive it

- [x] 0.1 Copy `openspec/changes/archive/2026-07-28-rescore-relevance-abstention/measurements/sweep.txt` to this change's `measurements/sweep-before.txt` unmodified, and record its `sha256` in `measurements/README.md`. Every later comparison cites this file, not a re-run.
- [x] 0.2 Run `pnpm run eval` on unmodified `main` and commit the JSON at `measurements/eval-before.json`. Record hybrid `P@5 P@8 R@5 R@8 MRR@5 MRR@8 abstentionFalsePositiveRate overAbstentionRate avgTokensReturned p50LatencyMs p95LatencyMs` in `measurements/README.md` as a table with the retriever and `k` named on every row.
- [x] 0.3 Dump per-query result id lists for all 24 committed queries at `limit` 5 / 8 / 200 through `MemoryService.search` on unmodified `main`, commit as `measurements/ids-before.json`, and record its digest. **Assert the dump is non-empty and record the total id count** — two empty sets hash identically, so a digest comparison with no count assertion proves nothing.
- [x] 0.4 Confirm `git diff --stat main -- apps/server/src/test/retrieval/corpus.ts apps/server/src/test/retrieval/queries.ts` is empty, and re-confirm it in task 7.1. The corpus is frozen for this change (`design.md` D9).

## 1. Align the tokenisation with the index (`design.md` D3)

- [x] 1.1 Re-implement `tokenizeWords` (`apps/server/src/services/hybrid-search.ts:534`) to agree with FTS5 `unicode61`: split on anything that is not a Unicode letter or digit, fold diacritics, lowercase. `tokenSet` keeps its shape.
- [x] 1.2 Add the agreement test: run the FULL committed corpus text and all 24 query strings through `tokenizeWords`, and separately index the same text into a scratch FTS5 table and read its `fts5vocab` term list. Assert set equality. The corpus is bilingual, so this covers accented text. Not a hand-picked sample — the assertion is over the whole fixture.
- [x] 1.3 Mutation-check 1.2 with `node scripts/mutate.mjs`, weakening each normalisation separately: (a) drop diacritic folding, (b) drop punctuation splitting, (c) drop case folding. Each must fail the test in 1.2 on its own. A mutation that leaves the suite green means the test asserts nothing.
- [x] 1.4 Re-run the 0.3 dump on the tokeniser change alone and diff against `ids-before.json`. Record the result either way in `measurements/README.md` — identical, or the exact queries whose ids moved and why. Do NOT regenerate baselines yet.
- [x] 1.5 Measure the tokeniser's effect on `findSaveTimeCandidates` (`apps/server/src/services/save-time-candidates.ts:5`): capture reported `similarity` for a fixed set of save/candidate pairs before and after, and confirm the range stays `[0,1]`, monotonicity in match quality is preserved, and the value is unchanged by which scope it is computed in (the scenario added to "Save-time lexical candidate scoring MUST increase with match quality"). Also record the effect on `sanitizeFtsQuery`'s `maxTerms` accounting — splitting `rate-limit` into two terms changes what the cap counts.

## 2. Source the term statistics (`design.md` D2)

- [x] 2.1 Add the DDL-only migration creating `memory_fts_vocab` as `fts5vocab('memory_fts','row')`. No table rebuild, so no FK dance; assert in a migration test that `PRAGMA foreign_key_check` returns no rows and `PRAGMA integrity_check` is `ok` after it applies.
- [x] 2.2 Add a migration test that applies the migration to a database seeded BEFORE it (hundreds of memories) and asserts a non-zero document frequency is readable immediately, with no backfill — the "populated the moment it exists" scenario.
- [x] 2.3 Add tests for the two lifecycle scenarios in the `persistence` delta: `INSERT INTO memory_fts(memory_fts) VALUES('rebuild')` is reflected without DDL, and a drop-and-recreate of `memory_fts` leaves the vocabulary read working afterwards.
- [x] 2.4 Add the term-statistics read to `apps/server/src/db/` (SQL confined to `db/`, per the data-access invariant): `documentCount()` and `termDocumentFrequencies(terms)`. No scope parameter — the statistic is index-global by decision, so it does NOT carry the `admin*` or `unsafe*` prefixes' meaning; name it so a reviewer cannot mistake it for a scoped read, and add a comment naming the spec clause that permits it (one line, a why).
- [x] 2.5 Assert in `apps/server/src/test/invariants.test.ts` (or a co-located test if that file's grep set is the wrong home) that no response payload carries a document frequency, document total, or per-term weight — the "not a cross-scope read channel" scenario.

## 3. Weight the level (`design.md` D1)

- [x] 3.1 Implement the weighted lexical component: `Σ w(t) over covered query terms / Σ w(t) over all distinct query terms`, `w(t) = ln(1 + (N − df(t) + 0.5) / (df(t) + 0.5))`, clamping `df` to `[0, N]`. `relevanceComponents` keeps returning `{ level, coverage, cosine }` so the sweep's per-component reporting still works.
- [x] 3.2 Leave `tokenContainment` unweighted and still shared with `save-time-candidates.ts` (`design.md` D4). Add the test that fails if the save-time path starts reading the weighted function.
- [x] 3.3 Unit-test the three edge behaviours from the `memory` delta, each as its own assertion: `N = 0` (all weights equal, level defined, no division by zero); every term at `df = N` (positive weights, weighted fraction equals the unweighted one); a term at `df = 0` (maximum weight). Include the five-memory case with real numbers.
- [x] 3.4 Test the monotonicity clause: raising a term's document frequency SHALL never raise its weight, and a row carrying the query's rarer terms SHALL never lose ground to a row carrying only its commoner ones. This is the guard behind the rewritten "Corpus growth does not turn a found answer into an abstention" scenario.
- [x] 3.5 Mutation-check 3.3 and 3.4 with `scripts/mutate.mjs`, weakening one condition at a time: (a) replace the smoothed weight with `ln(N/df)`, (b) drop the `+0.5` smoothing on the numerator only, (c) return the unweighted fraction. Each must red a test that names the condition. Record which test caught which mutation.
- [x] 3.6 Fix the two stale docstrings this change touches: `hybrid-search.ts:40` still says "the gate window", a concept deleted by `archive/2026-07-28-rescore-relevance-abstention` task 9.1; `test/retrieval/run-eval.ts:322-328` still says `ratio = 0` isolates a pool-narrowing mechanism that the same change's task 4.2 records as removed.

## 4. Extend the sweep (`retrieval-evaluation` delta)

- [x] 4.1 Report, per query, the document total and the per-term document frequencies behind that query's leading level, so the level is recomputable by hand from the committed corpus.
- [x] 4.2 Keep the grid steps at 0.05 (floor) and 0.1 (ratio) — `test/retrieval/run-eval.ts:321`/`:329`. Do NOT refine the grid; the plateau bar is now an absolute width in level units and refining is what the criterion was rewritten to forbid.
- [x] 4.3 Confirm the determinism scenario still holds: two consecutive sweeps byte-identical apart from latency.

## 5. Measure the cost (`design.md` D5)

- [x] 5.1 `EXPLAIN QUERY PLAN` the term-statistics read and record the plan verbatim. The propose-phase probe measured `SCAN memory_fts_vocab VIRTUAL TABLE INDEX 259:` for both `term = ?` and `term IN (…)`; confirm against the real schema.
- [x] 5.2 Wall-clock the END-TO-END search path at 1 000 / 20 000 / 50 000 memory rows, enabled versus the pre-change tree, at `limit = 8` and `limit = 200`. Report absolute added milliseconds, name the instrument on every row, and do NOT mix statement timings into the same table (the propose-phase statement probe measured 0.097–0.27 ms per term and 5.2 ms for a 19-term query at 50k rows — a different series).
- [x] 5.3 Hold the budget: added end-to-end p50 ≤ 1.0 ms at 50 000 rows, against the committed hybrid p50 of 11.659 ms (`archive/2026-07-28-rescore-relevance-abstention/measurements/eval-after.json`). If the plain read misses it, add the bounded `term → df` memo invalidated on document-count change (`design.md` D5) and re-measure. Record the decision and both numbers.
- [x] 5.4 If neither clears the budget, **stop and record it**: the level function ships unweighted, cost is the reason, and it is a distinct outcome from a failed calibration. Do not ship an unmeasured read on the hot path.
- [x] 5.5 Record the memo's staleness window if the memo ships: a tag edit changes a tag term's document frequency without changing the document count, so that term's weight can lag one insert.

## 6. Sweep, then decide (nothing here is decided in advance)

- [x] 6.1 Run `pnpm --filter @rembric/server run eval --sweep-abstention` against the frozen corpus and query set, and commit the full output as `measurements/sweep-after.txt`. Two consecutive runs byte-identical.
- [x] 6.2 Record the separability verdict the harness prints, and compare it line-for-line with `measurements/sweep-before.txt`: lowest gold-bearing level, highest abstention level, whether the classes still overlap, and by how much the two moved. This is the change's headline measurement.
- [x] 6.3 Apply the five criteria at `openspec/specs/memory/spec.md:1251-1257` **as modified by this change's delta** — including the plateau expressed as ≥ 0.10 in level units, not as a count of grid steps. Record which grid points fail and why, in the shape `archive/2026-07-28-rescore-relevance-abstention/tasks.md:43-51` used.
- [x] 6.4 Decide `ABSTENTION_FLOOR` from 6.3 and record the decision either way. If no value clears all five, it stays `null`, the grid is committed, and that is a successful outcome — cite the `DIVERSITY_CAP` precedent at `hybrid-search.ts:59-67`. If the surviving overlap is driven by the dense cosine rather than the lexical component (`design.md` D6 predicts a floor of 0.307 from `q-abstain-atlas-invoice-pdf-slo`), record that as measured evidence for per-branch thresholds and hand it to the next change; do not implement them here.
- [x] 6.5 Re-derive `RELATIVE_LEVEL_RATIO` from the same grid (`design.md` D7). It is NOT carried forward at 0.40 on the strength of the superseded grid — the delta forbids that explicitly. Record the chosen value, its admissible band in level units, and the criteria it clears.
- [x] 6.6 If a gate ends up enabled, assert `memory.context`'s relevance channel behaves sanely under it — it runs the same scoped hybrid search through `mcp/memory-tools.ts` and inherits the module constants with no separate wiring, so an enabled floor can silently empty a channel the agent reads at session start. This was carried forward unresolved from `archive/2026-07-28-rescore-relevance-abstention` task 4.5.

## 7. Regenerate the baselines, with both sides recorded

- [x] 7.1 Re-confirm the corpus and query fixtures are byte-identical to `main` (task 0.4).
- [x] 7.2 Re-run `pnpm run eval` and commit `measurements/eval-after.json`. Diff every metric against `measurements/eval-before.json` in `measurements/README.md`, per retriever and per `k`. A number with no recorded before is not evidence.
- [x] 7.3 State plainly which metrics could move and which could not: `P@8` is pinned at its committed ceiling of 0.15625 and `R@8` at 1.000, so neither can register an improvement; `abstentionFalsePositiveRate` (measured 1.000) is the only metric with headroom, and only if 6.4 enables a floor. Do not describe this change as a ranking win in the PR body unless a gated metric measurably moved.
- [ ] 7.4 **NOT DONE, deliberately — a decision, not an omission.** `pnpm run eval` is green against the committed floors and caps, so no regeneration is required. The up-only ratchet would raise the MRR floor 0.73333 -> 0.74688 and leave the other five bounds. Not spent: the only real gain is MRR +0.0135, and minting a tighter bound on a 40-row fixture whose pool-size reservation is explicitly unresolved (`design.md` D9) buys nothing the eval does not already enforce. See `measurements/README.md` §7.4. Original text: Regenerate `test/retrieval/baselines/hybrid.json` only if 7.2 justifies it, and verify the up-only ratchet held: `@5` precision 0.1875, recall 0.91875, mrr 0.7333; `@8` precision 0.10625, recall 0.95, mrr 0.7333. A regenerated floor BELOW a committed one is a failure, not a new baseline.
- [x] 7.5 Prove the caps are not vacuously green: temporarily set `ABSTENTION_FLOOR` to a value the grid shows is inadmissible and confirm `pnpm run eval` fails naming `overAbstentionRate` and both values. Revert.

## 8. Verify

- [x] 8.1 `pnpm run typecheck` clean, `pnpm run lint` clean, `pnpm test` **2270 passed / 0 failed / 10 skipped (2280)**. Test count 2249 -> 2280 (+31). Original text: `pnpm run typecheck` · `pnpm run lint` · `pnpm test`, all clean. Record the test count against the pre-change count; a suite that did not grow means the new guards have no tests.
- [x] 8.2 `pnpm run eval` green against the committed floors and caps.
- [x] 8.3 Re-run the 0.3 id dump on the final tree, commit as `measurements/ids-after.json`, and record the diff against `ids-before.json` with the non-empty count asserted on both sides. Result composition IS expected to change here; the artifact is the record of by how much, per query.
- [x] 8.4 **RUN, in the amendment session — superseded by §10.7 and recorded in `measurements/docker-smoke.md`.** The migration and the upgrade path are verified against pre-existing seeded data (rolled back to the pre-`0030` schema first so the upgrade was real). Original text of the prior session's deferral: NOT RUN — the dev stack is a shared resource across concurrent worktrees and that session was instructed not to bring it up. Original task text: **Real Docker smoke against pre-existing seeded data.** Bring the stack up against the existing `data-dev` corpus WITHOUT reseeding (the dev CMD passes `seed-dev --reset`; override `command` and set `REMBRIC_ALLOW_DESTRUCTIVE_SEED=0`), after a tar backup of `data-dev`. Verify mounts point at this worktree and the boot log contains zero seed lines. Then: (a) the migration applies to the pre-existing database and the vocabulary read returns non-zero frequencies for real terms; (b) `memory.search` returns sane, non-empty results and its ids are captured for comparison; (c) `abstained` / `abstainReason` are present on every MCP response; (d) `memory.context` returns both channels populated; (e) a question-shaped query with no answer in the corpus scores measurably lower than it did pre-change — the production defect that motivated the change. Compare logical content (row counts per table, `memory` and `confirmations` digests) against the tar backup afterwards; do not byte-compare `data.db`, WAL checkpoints move it.
- [x] 8.5 Confirm the disabled-path guarantee still holds where it is still claimed: with BOTH gates overridden to `null`, the branch issues no pool-text read and no term-statistics read. This is now an override-only configuration — no production caller reaches it while `RELATIVE_LEVEL_RATIO` is non-null — so assert it at the seam, not by inference.

## 9. Deferred and explicitly rejected — recorded so it is not lost

- [x] 9.1 **Corpus growth toward production-sized pools is NOT in this change** (`design.md` D9). The reservation at `archive/2026-07-28-rescore-relevance-abstention/tasks.md:104` — "the fixture's pools are 10–26 rows against a production scope's much larger ones, so the filter's behaviour at scale is extrapolated rather than measured" — remains open and unaddressed. It is the next change, and it must land after this one or the before/after comparison this change depends on stops existing.
- [x] 9.2 **Cross-project fixtures, `all_projects`, scope widening and making `global` a project are out of scope.** Unrelated to the level function.
- [x] 9.3 **A curated stopword list stays rejected** (`design.md` D1), not deferred: per-language for a bilingual corpus, and a second invisible retrieval knob.
- [x] 9.4 **Per-branch floors stay deferred** (`archive/2026-07-28-rescore-relevance-abstention/tasks.md:99`). Task 6.4 may produce the evidence that justifies them; opening that change is not this change's job.
- [x] 9.5 **Minimum-should-match on the lexical branch stays rejected here** (`tasks.md:100`): it makes the candidate set itself the relevance gate and removes recall unmeasured.
- [x] 9.6 **`DIVERSITY_CAP` stays `null`** — still needs a session-labelled fixture; every corpus row carries `session_id = NULL`.
- [x] 9.7 **`checkAbstentionFlags` asserting `abstained ⇔ empty` is not fixed here** (`tasks.md:117`). Still sound while the harness runs `offset = 0` only; it becomes wrong when the harness gains an `offset > 0` query, which belongs to 9.1's change.
- [x] 9.8 **`abstentionFalsePositiveRate`'s cap of 1.000 remains structurally unfailable** until a floor is enabled. If 6.4 enables one, ratchet the cap to the newly measured value and say so; if not, record that the cap still cannot bind.

## 10. AMENDMENT — the `df` lookup key comes from SQLite (`design.md` D3b/D3c/D3d)

The delta shipped a requirement that is false as implemented: `indexTerms` does not produce the terms `memory_fts` stores outside single-diacritic Latin, so a query term the index does not hold gets `?? 0` — the maximum weight — and crushes coverage on every row. Everything below is unstarted.

### 10.1 Record the before for the amendment, separately from §0

- [x] 10.1.1 **DONE** — `measurements/tokenizer-divergence.mjs` + `measurements/tokenizer-divergence.txt`. The three controls (Spanish, German, Cyrillic without `й`/`ё`) measure 0%; the divergent arms reproduce the design's table (Cyrillic-`й`/`ё` 100%, Greek 80%, Vietnamese 50%, Devanagari 83%, Arabic 17%, Japanese 100%, stacked-diacritic Latin 100%). The probe's tokenizer is derived from the migrated declaration, not restated. Original text: Commit the divergence probe as a runnable artifact at `measurements/tokenizer-divergence.mjs` and its output at `measurements/tokenizer-divergence.txt`: for each of Spanish, German, plain Cyrillic, Cyrillic with `й`/`ё`, Greek, Vietnamese, Devanagari, Arabic, Japanese and stacked-diacritic Latin, the app's terms, the index's terms and the absent count. **Spanish and German at 0% are the controls and MUST be present** — with only failing arms you cannot tell a real divergence from a broken probe.
- [x] 10.1.2 **DONE** — runnable artifact `measurements/mcp-script-arms.mjs` (raw Streamable-HTTP JSON-RPC against an in-process server, so the zod schema and the transport are in the path), output `measurements/mcp-script-arms-before.txt`. Reproduces the owner's headline: the three agreeing arms (Spanish, German, Cyrillic without `й`/`ё`) return **1 of 20**, the seven divergent arms return **20 of 20**, and the app's term for each divergent arm's commonest word is absent from the index so it takes w(absent) = 3.7377 where the held term takes 0.0241. Original text: Reproduce the end-to-end symptom through the MCP boundary and commit it: per script arm, rows returned out of 20 under the shipped `RELATIVE_LEVEL_RATIO = 0.4`, plus the weight the corpus's commonest word receives. The Latin arm returning 1 of 20 while the non-Latin arms return 20 of 20 is the headline. Probe through the real MCP tool, not the handler — a handler call bypasses the zod schema and proves nothing about the tool.
- [x] 10.1.3 **DONE** — `measurements/eval-before-amendment.json` (sha256 `73dc83bc926c6fa811893436517d3c3703be7677878746eedc302e9b1e0e0977`), hybrid `P@8=0.156 R@8=1.000 MRR@8=0.797 tokens=318`, identical to the change's own after-figures. Original text: Run `pnpm run eval` on the current (pre-amendment) applied tree and commit as `measurements/eval-before-amendment.json`. This is the amendment's before; `eval-before.json` is the change's before and is NOT re-derived.

### 10.2 Derive the tokenising table from the shipped declaration (`design.md` D3c)

- [x] 10.2.1 Read `memory_fts`'s declaration from `sqlite_master` at startup, **after** the migration runner has finished, and derive the tokenising table's DDL from it: split the argument list on unquoted commas, drop `content=` / `content_rowid=`, carry the rest.
- [x] 10.2.2 Declare the table contentless (`content=''`) in the connection's temp schema, plus its `fts5vocab('…','row')` companion. Assert no `…_content` shadow table exists — the query text must not be stored anywhere.
- [x] 10.2.3 **Fail startup, naming the argument, on any carried argument the derivation does not recognise** (recognised: `tokenize`, `prefix`, `detail`, `columnsize`). A silently dropped `tokenize=` is the exact defect this mechanism exists to prevent, so it must be loud.
- [x] 10.2.4 Record the creation cost (propose-phase probe: 0.137 ms for both tables, once per process) and confirm it is at startup, not on the first query.

### 10.3 Replace the `df` read with one `LEFT JOIN` (`design.md` D3b)

- [x] 10.3.1 Replace `TermStatisticsRepository.termDocumentFrequencies`' `term IN ${idJsonSet(...)}` shape with: empty the tokenising table, insert the query text, then `SELECT q.term, v.doc FROM <temp vocab> q LEFT JOIN memory_fts_vocab v ON v.term = q.term`. This **replaces** the read; it does not add one. SQL stays confined to `db/`.
- [x] 10.3.2 Return an **explicit** absent marker for `v.doc IS NULL`, not an omitted map key, and make `termWeightsFor` consume the marker rather than `?? 0` on a `Map.get` miss. The weight value is unchanged — maximum — but it is now applied on reported evidence.
- [x] 10.3.3 Empty the table before every tokenisation (`INSERT INTO t(t) VALUES('delete-all')`). Assert two consecutive queries with disjoint vocabularies do not leak terms.
- [x] 10.3.4 `EXPLAIN QUERY PLAN` the new read against the real migrated schema and record it verbatim in `measurements/cost.md` §5.1. The propose-phase probe measured `SCAN q VIRTUAL TABLE INDEX 1:` then `SCAN v VIRTUAL TABLE INDEX 259: LEFT-JOIN`; confirm index 259 (the term-constrained seek) still appears.

### 10.4 Guards, each mutation-checked (`design.md` D3d)

- [x] 10.4.1 **Derivation guard**: assert the tokenising table's tokenizer came from `sqlite_master` and that no source file restates the DDL as a literal. Mutation: (a) restate the DDL as a hard-coded `fts5(body)`, (b) remove the unrecognised-argument check. Each must red a test **naming that condition**, weakened separately.
- [x] 10.4.2 **Absence guard**: assert an index-absent term is reported absent and distinguishable from a held term. Mutation: turn the `LEFT JOIN` into an inner join — must red.
- [x] 10.4.3 **Asymmetry guard**: over Cyrillic (`й`/`ё`), Greek (final sigma), Vietnamese and Japanese text, assert (a) every query term resolves to the document frequency the index records, and (b) a row-side disagreement lowers coverage only — no maximum weight, no raised level, cosine untouched. Mutation: point the `df` lookup back at `indexTerms` — must red (a); remove the `max(coverage, cosine)` fallback — must red (b).
- [x] 10.4.4 **Durability guard**: assert the durable database file and its `-wal` do not grow across many searches (propose-phase probe: 0 bytes over 2 000 cycles), and that the tokenising table is absent from the migration ledger, the schema-drift inventory and the shrinkage guard's table counts.
- [x] 10.4.5 **Retire `tokenizer-agreement.test.ts` as an agreement guard, deliberately and in the open.** Its first assertions are now partly tautological (the query side holds by construction) and partly false-but-green (the row side, because `CORPUS` + `QUERIES` are en/es). Keep the `indexTerms`-versus-index assertion, retitled to what it actually measures — the row side agrees on the corpus the eval's evidence is drawn from — and delete the claim that it establishes agreement in general. Also delete its restated `CREATE VIRTUAL TABLE t USING fts5(body)` (`:29`) in favour of the derived declaration, which is the flagged weakness. Record in `measurements/README.md` that this is a retirement with a named replacement, not a deletion.
- [x] 10.4.6 Run every mutation above with `node scripts/mutate.mjs --file … --spec … --mutation … --with …` and record which test caught which, in `measurements/README.md`. A mutation that leaves the suite green means the guard asserts nothing — that is the default outcome, not the exception.

### 10.5 Cost, on the named instruments

- [x] 10.5.1 Statement-level, 50 000 rows, 400 iterations, one warm process: A) today's JS tokenisation + `term IN (…)` versus B) insert + one `LEFT JOIN`. Report the marginal. Propose-phase figures to beat or explain: **0.1034 ms (owner) and 0.4917 ms (re-measured on a differently-shaped corpus)**, against a **1.0 ms** budget. Record which corpus each figure came from.
- [x] 10.5.2 End-to-end, the budgeted instrument: extend `measurements/cost.md` §5.2's table with the amendment arm at 1 000 / 20 000 / 50 000 rows and `limit` 8 / 200. **Do NOT tabulate 10.5.1 with 10.5.2** — `CLAUDE.md` has a rule about exactly that, and this change's own §5.2 already keeps them apart. Hold the same budget: added end-to-end p50 ≤ 1.0 ms at 50 000 rows.
- [x] 10.5.3 Record the term-count change as evidence, not colour: B returns more terms than A because absent ones come back `NULL` (propose-phase: 18.8 versus 16.8 re-measured, 19 versus 13 owner). Report the measured pair.
- [x] 10.5.4 **DONE, with a finding that contradicts the design — see `measurements/cost.md` §5.2e.** Four of the five variants confirm D3b's direction (2.6-2.9x the JS arm at pool 400), but `cached by memory id, warm instance read` measures 0.273 / 0.520 / 0.941 ms against D3b's 4.771 / 6.102 / 8.521 - UNDER the 1.0 ms budget at every pool size. The same probe measures why it does not survive a real process (0.966 ms at a 400-row cache, 2.395 at 5 000, 13.099 at 39 998), which is the mechanism D3b named. Handed to 10.8.1; the shipped decision was NOT re-opened. Original text: Confirm the row side is NOT changed, and commit the price that justifies it: pool-insert + per-term `MATCH` 7.807 / 18.359 / 26.890 ms, pool-insert + filtered instance read 7.968 / 19.835 / 28.949 ms, cached-by-id warm 4.771 / 6.102 / 8.521 ms, per-term `MATCH` against `memory_fts` ∩ pool 6.587 / 20.703 / 42.696 ms, versus today's JS `indexTerms` 0.865 / 2.362 / 4.802 ms, at pool 64 / 200 / 400. Re-take these against the real schema; if any lands under 1.0 ms the design decision changes and D3b must be revisited.

### 10.6 Re-verify the change's own conclusions on the amended tree

- [x] 10.6.1 typecheck clean, lint clean, `pnpm test` **2245 passed / 0 failed / 10 skipped (2255)**, up from 2232 on the same invocation pre-amendment — +23, exactly the new guards. §8.1's recorded 2280 does not reproduce under any invocation on this tree; see `measurements/README.md` §10.6.1. Original text: `pnpm run typecheck` · `pnpm run lint` · `pnpm test`, all clean. Record the test count against the amendment's before (2280) — a suite that did not grow means the new guards have no tests.
- [x] 10.6.2 **DONE — the prediction held: every quality metric bit-identical across all three retrievers at both `k`, only latency moved.** Diff in `measurements/README.md` §10.6.2. Original text: **Re-run `pnpm run eval` and record before AND after.** `measurements/eval-before-amendment.json` (10.1.3) versus `measurements/eval-after-amendment.json`, diffed per retriever and per `k` in `measurements/README.md`. A number with no recorded before is not evidence. Expected: unmoved on the en/es corpus, because Spanish measures 0% divergence — **an unmoved eval is the predicted result and is a pass, not a disappointment**; a moved one means something other than the tokenizer changed and must be explained before landing.
- [x] 10.6.3 **DONE — §6 survives untouched**: overlap still `[0.296, 0.307]`, `ABSTENTION_FLOOR` stays `null`, ratio stays 0.40, two consecutive runs byte-identical, and the only diff against `sweep-after.txt` is the df report's `0` → `—` for an absent term. No docstring correction needed. Original text: Re-run `--sweep-abstention` and confirm §6's conclusions survive: `ABSTENTION_FLOOR` stays `null` with the overlap still on `[0.296, 0.307]`, and `RELATIVE_LEVEL_RATIO` stays 0.40 inside `[0.30, 0.60]`. If either moves, §6.4/§6.5 are re-derived and the docstring at `hybrid-search.ts:47-56` is corrected with them.
- [x] 10.6.4 **DONE — 72 entries / 358 ids on both sides, 0 moved, same digest.** Original text: Re-run the id dump (§0.3's shape) on the amended tree, commit as `measurements/ids-after-amendment.json`, and diff against `ids-after.json` with the non-empty count asserted on both sides.
- [x] 10.6.5 **DONE — `delta-freshness: ok (1 active change, 2 body differences to review)`**, the same two REVIEW notices as before the amendment; no published scenario title dropped. Original text: Re-run `pnpm run check:delta-freshness` and confirm it stays green. It reports body differences for review and **fails on a dropped published scenario title** — the amendment rewrites an ADDED requirement (never published, so its title is free to change) and adds one sentence to a MODIFIED body; no published title moves. This check tripped once on this change already.
- [x] 10.6.6 `pnpm run check:spec-provenance` clean (`spec-provenance: ok (main...HEAD)`).

### 10.7 Real Docker smoke against pre-existing seeded data — now MANDATORY, and it covers §8.4 too

- [x] 10.7.1 **DONE — `measurements/docker-smoke.md`.** Mounts verified against this worktree, boot log has ZERO seed lines, and the pre-existing `data-dev` was first rolled back to the pre-`0030` schema (it already carried `0030` from an earlier run) so the upgrade was real. Original text: §8.4 was left NOT RUN. The amendment changes startup (a derived DDL executed after migrations) and the hot read, so it can no longer be deferred. Bring the stack up against the existing `data-dev` corpus WITHOUT reseeding (the dev CMD passes `seed-dev --reset`; override `command` and set `REMBRIC_ALLOW_DESTRUCTIVE_SEED=0`), after a tar backup of `data-dev`. Verify mounts point at this worktree and the boot log contains zero seed lines.
- [x] 10.7.2 **DONE, with one deviation recorded: `abstainReason` is present only when abstaining, and with `ABSTENTION_FLOOR = null` no response can abstain.** All other checks pass; the boot log names the inherited tokenizer (`fts5 defaults`). Original text: (a) migration 0030 applies to the pre-existing database and the vocabulary read returns non-zero frequencies for real terms; (b) the tokenising table is derived at startup and the boot log names the tokenizer it inherited; (c) `memory.search` returns sane, non-empty results and its ids are captured; (d) `abstained` / `abstainReason` present on every MCP response; (e) `memory.context` returns both channels populated.
- [x] 10.7.3 **DONE** — `майский` resolves to df=20 / w=1.0049 against the app key's absent-term maximum of 4.7185, measured with the shipped code in the shipped container against the real database; through the MCP tool the Cyrillic arm returns 10 of 20 where the pre-amendment construction returns 20 of 20. The Docker arm is not a controlled pair with the artifact (real embedder versus hash embedder) and is reported as the real-data confirmation, not as the controlled before/after. Original text: The production defect, measured on real data: a question-shaped query with no answer in the corpus scores measurably lower than pre-change, AND a non-Latin query term the corpus does hold resolves to its real document frequency rather than the maximum weight. Both through the real MCP tool.
- [x] 10.7.4 **DONE — 0 bytes of growth across 2 000 in-container tokenisations**, and the 35 pre-existing rows are byte-identical to the tar backup (0 missing, 0 changed, digests equal), as are `confirmations`, `memory_replaces`, `sessions`, `tokens`, `projects`, `prompts`. The pre-amendment comparison arm was NOT run in Docker (one tree per container); the claim is an absolute zero, which needs no baseline. Original text: Confirm the durable database did not grow from query tokenisation: compare `data.db` + `-wal` size deltas against a run with the same number of searches on the pre-amendment tree. Then compare logical content (row counts per table, `memory` and `confirmations` digests) against the tar backup; do not byte-compare `data.db`, WAL checkpoints move it.

### 10.8 Deferred by the amendment, recorded so it is not lost

- [x] 10.8.1 **Making the row-membership half index-authoritative is a later change** — and the re-take (10.5.4) sharpens its brief rather than confirming the 8–29× figure: `cached by memory id, warm instance read` measures **0.273 / 0.520 / 0.941 ms** at pool 64/200/400, UNDER the budget, against `design.md` D3b's 4.771 / 6.102 / 8.521. It degrades to 2.395 ms at a 5 000-row cache and 13.099 ms at 39 998, so the open question is a bounded cache versus the cold-path insert. Recorded in `measurements/cost.md` §5.2e; the shipped decision was not re-opened. Original text: deferred on the measured 8–29× cost overrun (10.5.4), not on preference. Its brief: find a per-row presence read that costs under 1.0 ms at pool 400, or accept the asymmetry permanently and say so in the spec.
- [x] 10.8.2 **Widening the eval corpus to include a non-Latin script is NOT done here** — D9 freezes `test/retrieval/{corpus,queries}.ts` and the amendment does not relax it. It belongs to §9.1's change, and until it lands no committed harness fixture can see the row-side residual; the asymmetry guard (10.4.3) is what stands in for it.
- [x] 10.8.3 **`remove_diacritics=2` on `memory_fts` stays rejected** — measured to fix Vietnamese and stacked-diacritic Latin only; Devanagari, Japanese and Greek diverge under both `rd1` and `rd2`. It would also be a rebuild of a populated FTS index for a partial fix.
- [x] 10.8.4 **Weighting an absent term the minimum stays rejected**, not deferred: it disarms abstention, and `hybrid-search.test.ts::'reaches the same abstention verdict at every offset'` catches it.
- [x] 10.8.5 **Precondition for the follow-up floor change, written down here rather than discovered later.** On a divergent-script query the lexical arm goes quiet (D3b's bound), so the shipped level is cosine-only. A floor calibrated on the committed en/es corpus is therefore calibrated against a quantity such a query does not produce, and the failure mode there is **total abstention on that query, not degraded ranking**. Any change that enables `ABSTENTION_FLOOR` must either establish its value against non-Latin content or state that its calibration does not hold there; a floor admissible at en/es is not evidence about a corpus whose lexical arm is silent.
- [x] 10.8.6 **A residual reasoned from code and NOT measured — recorded as an inference, not a finding.** `memory_vec` is backfilled by a background worker (`apps/server/src/services/embedding-worker.ts:94`), so a just-saved row has cosine 0 until the worker reaches it. On a divergent-script corpus that row's coverage is also ≈0, so its level is 0 and the relative filter drops it while the leader sits above 0; on a Latin corpus coverage rescues it. That makes it a transient, script-specific invisibility window. No probe was run: this follows from reading the two paths and must be measured before it is repeated as a fact.
- [x] 10.8.7 **The durable form of the unscoped-read invariant is TYPE-level, and is NOT built here.** The prefix plus a closed inventory catches an unscoped read by NAME and by a text scan; what would catch it by construction is asserting that every unscoped repository read's return type belongs to a closed set of aggregate shapes — no memory id, no content, no `project_id` — which needs no prefix at all and cannot be defeated by a rename. The repo already reached the equivalent conclusion for the scoped half (`archive/2026-07-25-fix-audited-defects/design.md:37`: "Renaming it is necessary but not sufficient: the durable fix is that the scoped variant _requires_ a `Scope` argument"). Here there is no scoped variant to require, so only the return-type half is available. Its brief: express the closed set in the type system (or via the TypeScript compiler API in the invariants suite), and retire the text-scanning inventory of §11.4 once it does.

## 1.6 Discovered during apply — RESOLVED by splitting the tokeniser (`design.md` D3a)

- [x] 1.6 **`tokenizeWords` was one function doing two jobs.** Sanitising an FTS5 MATCH expression needs whole whitespace-delimited words (quoting one makes FTS5 parse its parts as an adjacency PHRASE); comparing token sets needs the index's vocabulary. Aligning both broke two things: `openspec/specs/memory-entities/spec.md:84-92`'s published per-kind noise table moved for 7 of 13 kinds, and pending judgments in the `mcp-integration` shared server went 17 -> 89. **Resolved (option B, owner-decided during apply):** `tokenizeWords` keeps the whitespace rule and serves `sanitizeFtsQuery` only; new `indexTerms` implements the `unicode61` rule; `tokenSet` is built on `indexTerms` and is the ONE vocabulary both token-set comparisons use. Save-time containment's arm was decided by measurement, not inheritance — pending relations 17 under BOTH arms, so the 17 -> 89 blowup was entirely the MATCH and none of it containment. All three acceptance conditions verified: noise table byte-identical and green, `mcp-integration` 49 passed with pendings at 17, and `proposal.md`'s "`memory-entities` … deliberately NOT modified" true again. Both directions of the split are mutation-pinned. Evidence in `measurements/README.md` §1.3, §1.6, §1.7.

### 10.9 One delta-spec correction the apply phase made, beyond the amendment's brief

- [x] 10.9.1 `specs/memory/spec.md`'s "eight to twenty-nine times the change's own cost budget" is a published measurement claim that 10.5.4's re-take partly contradicts, so one clause was added: the variant that measures under budget does so only while a per-row cache holds about one pool's worth of rows. The DECISION is unchanged and was not re-opened; only the claim was made to match what was measured. Flagged for review because it is a spec-text edit this session was not asked for.

## 11. AMENDMENT 2 — the term-statistics reads are `data-access`-compliant

The two reads shipped unscoped AND unprefixed, consumed from `services/hybrid-search.ts`, which `data-access` forbids in two requirements the change had a delta for neither of. Closed by correcting `admin*`'s DEFINITION to what the gate has always enforced — confinement of a name to allow-listed call sites — rather than by a fourth prefix.

- [x] 11.1 Add `specs/data-access/spec.md` with two MODIFIED requirements: "Scoped, unsafe, and admin method families" (redefine `admin*` as an unscoped read invocable only from allow-listed call sites; the fourth allow-listed call site argued on empty row-space; a read-only carve-out for a write confined to the `temp` schema; the closed inventory) and "Scoped repository reads MUST require a `Scope` parameter" (an MCP-reachable read that legitimately takes none, admitted on a recorded measurement). No existing scenario renamed or dropped — `check:delta-freshness` green, `openspec validate --all --strict` green.
- [x] 11.2 Rename to `adminDocumentCount()` / `adminQueryTermFrequencies()` across the repository, `services/hybrid-search.ts:177`/`:181`, six test files and the two runnable measurement artifacts (`row-side-price.mjs`, `cost-amendment.mjs`). Includes the two proxy-recorded method-name lists in `hybrid-search.test.ts:1091`/`:1112`, which are string literals the compiler cannot catch.
- [x] 11.3 Convert the `admin*` confinement gate from whole-file licences to `(file, method)` pairs and add `services/hybrid-search.ts → {adminDocumentCount, adminQueryTermFrequencies}`. A file-level exemption there would license every future `admin*` call in the row-assembly heart of retrieval. The three existing file exemptions (`server/dashboard-router.ts`, `server/bootstrap.ts`, `services/agent-sessions.ts`) are converted too, and a second assertion anchors every listed pair to a call that still exists, so a stale licence fails.
- [x] 11.4 Add the closed inventory of unscoped, un-keyed, unprefixed repository reads, asserted by set equality, plus a closure assertion that every repository file is classified as scoped-content or control-plane. **Measured 13 entries, not the 7 carried into the session** — the extra six are `agent-sessions-repository.ts::{list,countPurgeableEmpty,findPurgeableEmptyIds}`, `entities-repository.ts::findMissingScans`, `relations-repository.ts::countRowsByStatus` and `vectors-repository.ts::findMissingEmbeddings`. Detector resolves same-file `Opts` interfaces, so `partitionKey` (`memory_vec`'s scope column) and the opts objects carrying `scope`/`projectId` are correctly excluded; no absence-side regex over query text, per the measured false negative.
- [x] 11.5 `memory-repository.ts::countByProject` listed with an inline marker as a known pre-existing violation (unscoped, unprefixed, called from `mcp/project-tools.ts:202`, reaching `project.list`'s `memoryCount`). Present at `HEAD`, so not this change's regression. `project.list` untouched.
- [x] 11.6 Rewrite `term-statistics-repository.ts`'s class docstring: its premise was that the prefixes "mark reads that can return an out-of-scope ROW", which is `unsafe*`'s property and not `admin*`'s — `vectors.adminBacklogCount()`, `consolidation.adminLatestRun()` and `entities.adminBacklogCount()` all return counts. The rationale lives in the spec, not in the comment.
- [x] 11.7 Record the scoped-`df` price in `measurements/`: runnable probe `scoped-df-price.mjs`, output `scoped-df-price.txt`, table at `cost.md` §5.2f. Measured **29.6–115.6 ms per search** (isolated statement time, 50 000 rows, six scopes, p50 of 40) against **1.2–3.8 ms** for the shipped index-global read, control 228/228 `(scope, term)` pairs with scoped `df` ≤ global `df`. §5.2's end-to-end 15.2–15.9 ms is quoted as a separate instrument and never in the same table. The figure carried into the session (37–121 ms) was not in `measurements/`; this re-take has a script behind it and reaches the same conclusion.
- [x] 11.8 Mutation-check both gates with `scripts/mutate.mjs`, each condition weakened separately — 9 mutations, 9 caught. Recorded in `measurements/README.md` §11.8.
- [x] 11.9 Verify: typecheck clean, lint clean, `pnpm vitest run` in `apps/server` **2314 passed / 10 skipped (2324)** against the pre-amendment 2311/2321 (+3, the three new gate assertions), `pnpm run eval` reproduces `measurements/eval-after-amendment.json`, `check:delta-freshness` and `check:spec-provenance` green.
- [ ] 11.10 **NOT DONE — no Docker smoke.** The session was instructed not to bring the dev stack up, and the amendment is a rename plus test-only gates: no migration, no HTTP surface, no MCP schema and no runtime behaviour change. §10.7's smoke against pre-existing seeded data still covers the shipped behaviour; a rename that `pnpm test` and `pnpm run eval` both show to be observationally inert adds nothing a container would reveal. Recorded as a decision with its reason, not as an omission.
