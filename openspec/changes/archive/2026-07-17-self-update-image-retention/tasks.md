# Tasks: self-update-image-retention

## 1. Engine API wrappers

- [x] 1.1 Add `pruneContainers(filters)` to `apps/server/src/services/self-update/engine-api.ts` — `POST /containers/prune?filters=<url-encoded JSON>`, returning the daemon's `{ ContainersDeleted, SpaceReclaimed }` body; same `node:http`-over-socket pattern and `assertOk` handling as existing methods, no new dependencies. NOTE: the daemon returns `ContainersDeleted: null` (not `[]`) when nothing was reclaimed — type it nullable.
- [x] 1.2 Add `pruneImages(filters)` to the same file — `POST /images/prune?filters=<url-encoded JSON>`, returning the daemon's `{ ImagesDeleted, SpaceReclaimed }` body (`ImagesDeleted` is likewise `null` when nothing was reclaimed).
- [x] 1.3 Label-scope guard in BOTH wrappers: throw synchronously (before any socket I/O) if `filters` lacks a non-empty `label` entry, so an unscoped prune — which would remove every stopped container or dangling image on a shared host — is impossible by construction.
- [x] 1.4 Unit tests in `engine-api.test.ts` for both wrappers against the existing fake-socket harness: exact URL-encoded filter string sent (`label`, `dangling`), OK parsing, non-2xx → `EngineApiError`, and the guard: missing/empty `label` filter throws with zero socket writes. Verify with `pnpm vitest run apps/server/src/services/self-update/engine-api.test.ts`.

## 2. Orchestrator cleanup step

- [x] 2.1 Implement a `cleanupStaleUpdateArtifacts(engine, log)` step in `apps/server/src/services/self-update/orchestrator.ts`: (a) `pruneContainers` with `label=rembric.upgrader=1` (native prune only removes stopped containers — running upgraders are structurally safe); (b) `pruneImages` with `dangling=true` + `label=rembric.stage=runtime`; log a start line before pruning (a large first-time backlog takes seconds and must not read as a hung update) and a one-line summary after (containers removed, images/space reclaimed) tolerating the daemon's `null` deleted-lists.
- [x] 2.2 Wire it into `pullAndLaunch()` after the backup gate and before `pullImage` (pre-pull so a disk-full host reclaims space before the step that needs it), wrapped in a single try/catch so any cleanup failure logs and never aborts the update. Extend `OrchestratorDeps.engineFactory`'s `Pick<DockerEngineApi, …>` with `'pruneContainers' | 'pruneImages'`. Do NOT add a new `UpdateStatus.phase` value — cleanup lives inside the existing pull→launch window and is invisible to the dashboard except in logs.
- [x] 2.3 Anti-drift for the two filter labels: (a) define the upgrader label once (shared constant used by BOTH the upgrader `createContainer` Labels and the `pruneContainers` filter — today `'rembric.upgrader': '1'` is a loose literal at the create site); (b) the runtime-stage label lives in the Dockerfile where TS can't import it — the existing Dockerfile-label invariant in `apps/server/src/test/invariants.test.ts` now asserts `LABEL ${RUNTIME_IMAGE_LABEL_FILTER}` (imported from the orchestrator) inside the runtime stage block, so renaming either side fails the suite; a mismatched filter would silently resurrect the leak. (Landed in the existing invariant rather than a new duplicate test.)
- [x] 2.4 Orchestrator tests: cleanup runs post-backup/pre-pull (call ordering on a mock engine); a throwing cleanup still launches the upgrader; filters passed verbatim to both prune calls. Verify with `pnpm vitest run apps/server/src/services/self-update/orchestrator.test.ts`.

## 3. Docs and verification

- [x] 3.1 Document the retention behavior in `docs/updates.md`: automatic prune on each update, keep-1 guarantee via container pinning, tagged/pinned images never touched, and `docker image prune -a` as the manual fallback for hosts that accumulated images before this release.
- [x] 3.2 Run `pnpm run typecheck && pnpm run lint && pnpm test` clean at the workspace root.
- [x] 3.3 (Operator-only, optional) Manual smoke on a Docker host with the socket mounted: seed a dangling runtime-labeled image + an exited `rembric.upgrader=1` container, trigger a dashboard update, confirm both are reclaimed, the previous image survives, and the update completes healthy. — Done 2026-07-17 against a real Docker 29.6.1 daemon by driving `DockerEngineApi` + the exported filter constants directly (the cleanup sequence verbatim): zombie swept, its pinned image reclaimed, pinned/runtime image survived (keep-1), unlabeled + dev-labeled + tagged images and non-upgrader containers untouched, unscoped-prune guard refused pre-socket. The dashboard-driven swap portion is pre-existing unchanged code, not re-driven.
