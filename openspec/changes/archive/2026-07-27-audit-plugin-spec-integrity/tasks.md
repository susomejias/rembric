# Tasks

Ordering is deliberate: section 1 removes the contradiction that could destroy working code, section 2 builds the tests that make every number in the deltas self-defending, and only then do sections 3–5 touch the remaining prose. Do not reorder 1 before its verification.

## 1. Kill the spec-vs-spec contradiction first

- [x] 1.1 Confirm at HEAD, before editing anything, that all three `plugin-session-protocol:259` clauses are still false: `hooks.json` wires `Stop` with `"async": true`; `apps/plugin/scripts/pre-compact.sh` exists and is mode 755; `hooks.codex.json` wires `PreCompact`. Record the line numbers found — if the tree has moved since this change was written, stop and re-audit rather than applying a stale delta. - Verified at HEAD `d91e65b`. All three clauses still false: `hooks.json:71` declares `Stop`, `:76` invokes `stop-sync.sh claude-code`, `:77` carries `"async": true`. `apps/plugin/scripts/pre-compact.sh` exists, `-rwxr-xr-x` (755), 2860 bytes. `hooks.codex.json:51` declares `PreCompact`, `:56` invokes `pre-compact.sh codex-cli`. Delta is not stale.
- [x] 1.2 Verify that nothing else in `openspec/specs/` asserts the absence of `Stop`, `PreCompact` or `PostCompact`. `grep -rn 'SHALL NOT.*\(Stop\|PreCompact\|PostCompact\)' openspec/specs/` must come back empty once the `plugin-session-protocol` delta merges. A second surviving prohibition reproduces the whole defect. - Three hits, all at `plugin-session-protocol:265`, `:266`, `:272` — every one inside the `Per-client lifecycle mapping` requirement the delta MODIFIES, so all three are replaced by the merge. The fourth hit (`mcp-api:389`) is `SHALL NOT reject a call`, not an absence assertion, and correctly names the shipped `PreCompact`/`SessionStart:compact`/`Stop` nudges. No second surviving prohibition.
- [x] 1.3 Re-read the merged `plugin-session-protocol` lifecycle mapping table against `grep -n rembric_post apps/plugin/scripts/*.sh` and confirm every POST call site has a row and every row has a call site. Eleven bash call sites across seven scripts at time of writing. - Deviation from the task's count: **twelve** call sites across **six** scripts, not eleven across seven. `pre-compact.sh` 66/69/75, `session-end.sh` 55/58/63, `post-compaction.sh` 40/46, `stop-sync.sh` 73/76, `post-compact.sh` 29, `session-start.sh` 40. (`_api.sh` 7/63 are the doc comment and the definition, not call sites — counting them is what yields "seven scripts".) The substantive check passes: every one of the twelve maps to a delta row, and every delta row has a call site. `final` verified per branch — `stop-sync.sh:36-39` sets `FINAL_JSON=',"final":false'` only for `codex-cli`, empty for Claude; `pre-compact.sh:67/70` and `post-compaction.sh:41` send `final:false` on both clients; `session-end.sh:56/59` sends `final:false`; the three degraded branches POST `{}`.

## 2. The evidence gate — tests, before any prose is touched

- [x] 2.1 Add one per-fixture byte assertion to `apps/plugin/test/nudge-fixtures.test.ts` for each row of the `claude-code-plugin` token-budget requirement's per-line table: `save` ≤132, `summary` ≤260, `sessionIdTemplate` rendered with a 36-character UUID ≤224, `firstPromptRelevance` ≤140, the recall nudge ≤100, the `SessionStart` nudge ≤100. Measure with `Buffer.byteLength(s, 'utf8')`, not `.length` — the multi-byte `≤ · —` characters are why two different published figures exist for the same string. Each must be its own `it(...)` so a failure names one line. - Measured at HEAD, `Buffer.byteLength(s, 'utf8')`, every figure reproducing D3's table exactly: `SessionStart` nudge 89, recall nudge 90, `firstPromptRelevance` 125, `save` 119, `sessionIdTemplate` rendered with a 36-char UUID 204, `summary` 237, `postCompact` 552. The `SessionStart` and recall lines are not fixture keys, so their assertions drive the real scripts (`session-start.sh` in an empty temp cwd; `prompt-search.sh` on turn 2 with a keyword, so the first-prompt line does not co-fire) rather than inventing shared-fixture keys that would imply a four-client lock-step obligation those two lines do not have.
- [x] 2.2 Realign the single existing budget assertion at `nudge-fixtures.test.ts:313` from `fixtures.postCompact.length <= 1000` to `Buffer.byteLength(fixtures.postCompact) <= 600`. Prove the realignment matters: assert that the old bound would have admitted the block at any size up to 250 tokens against a published 120-token cap. - Realigned. The proof is a second assertion stated as a falsifiable fact about the shipped text rather than as arithmetic: `postCompact.length ≤ 1000` (it passed the retired bound) AND `bytes/4 > 120` (it violated the published cap) — both true simultaneously, which is the defect. 552 bytes = 138.0 tokens against the retired ≤120.
- [x] 2.3 Prove every assertion in 2.1 and 2.2 can fail. For each, temporarily append filler to the fixture value in memory and confirm the specific assertion reports the specific fixture. An assertion that has never been observed red is not a gate. - All nine observed red, one mutation at a time, each run filtered to the single assertion and each failure naming exactly one line: `save`/`summary`/`sessionIdTemplate`/`firstPromptRelevance` +40B and `postCompact` +80B via `nudge-fixtures.json`; the `SessionStart` and recall lines +40B via their script literals; the per-firing-turn ceiling via recall +200B; the amortised mean via the save nudge +600B. Tree restored with `git checkout` after each probe.
- [x] 2.4 Add the per-firing-turn ceiling assertion: drive `prompt-search.sh` and `prompt-nudge.sh` through a real turn-1 invocation with a recall keyword in the stdin `prompt`, sum the emitted bytes of both scripts' stdout, and assert ≤720 bytes (≤180 tokens). Record the measured value in the task note. Expected ~660 bytes. - Measured **660 bytes = 165.0 tokens** against the ≤720/≤180 cap: `prompt-search.sh` 217 (firstPromptRelevance 125+\n, recall 90+\n) + `prompt-nudge.sh` 443 (sessionId 204+\n, summary 237+\n). Matches the expected ~660 exactly.
- [x] 2.5 Add the amortised assertion: drive ten consecutive `UserPromptSubmit` turns through real per-session counter files in an isolated `TMPDIR`, sum all emitted bytes, and assert the mean ≤180 bytes/turn. Also assert that turns 2, 3, 4, 6, 7, 8 and 9 emit zero bytes when no recall keyword is present — that zero is what makes the mean honest. Expected mean ~146 bytes/turn (36.4 tokens). - Measured per-turn bytes `569,0,0,0,325,0,0,0,0,563` = 1457 total, **mean 145.7 bytes/turn = 36.4 tokens/turn** against ≤180/≤45. The seven zero turns assert individually. Matches the expected ~146 exactly.
- [x] 2.6 New test file `apps/plugin/test/hook-manifests.test.ts`: assert the **exact** event-type set and handler count of both manifests — `hooks.json` = `{SessionStart, UserPromptSubmit, SessionEnd, PreCompact, PostCompact, Stop}` / 8 handlers; `hooks.codex.json` = `{SessionStart, UserPromptSubmit, Stop, PreCompact, PostCompact}` / 7 handlers. Use exact set equality, never `toContain`: a containment assertion cannot catch a spec claiming an event type is _absent_, which is the exact defect being fixed. - Event-type sets asserted with sorted-array equality, not `toContain`. Both numbers asserted separately per manifest (6 types / 8 handlers; 5 types / 7 handlers), which is also the default recorded for the unresolved Codex trust-banner question — whichever number the banner counts, the pair is pinned.
- [x] 2.7 In the same file, assert that neither `UserPromptSubmit` entry in either manifest carries a `matcher` key, that `SessionStart` declares exactly the literal matchers `startup|resume|clear` and `compact` in both, that the `Stop` handler in `hooks.json` carries `"async": true`, and that neither manifest declares `PostToolUse`. Confirm each fails when the corresponding manifest key is added or removed. - Matcher-lessness is asserted as `Object.keys(group) === ['hooks']`, which catches an added `matcher` and any other stray key. Eleven mutations observed red, one at a time: `+PostToolUse` (against both the PostToolUse and the event-set assertions), `-Stop`, `UserPromptSubmit` `+matcher` (both manifests), `SessionStart` matcher rewritten, `-async` on `Stop`, a 9th handler on `hooks.json`, `-PreCompact` and `+SessionEnd` and an 8th handler on `hooks.codex.json`. Tree restored after each.
- [x] 2.8 New test file `apps/plugin/test/command-arguments.test.ts`: for every `apps/plugin/commands/*.md`, parse each `memory.<tool>({…})` call site in the body and assert every argument key is a key of that tool's exported zod schema. Import `memorySearchSchema` / `contextSchema` / `memorySaveSchema` from `apps/server/src/mcp/memory-tools.ts` and `sessionSummarySchema` from `apps/server/src/mcp/session-tools.ts` by relative path — `apps/server/vitest.config.ts` already includes `../plugin/test/*.test.ts`, so no config change is needed (design D7). - Balanced-brace scan over each `memory.<tool>({…})` site, then a top-level-comma split that is quote- and nesting-aware, so `<concise ≤100-char headline of the text>` parses as one value. An **unmapped tool is a failure, not a skip** — otherwise a new call site would pass unchecked. Verified green on all four shipped bodies, including `context.md`'s `memory.save({topic_key})` bare-key form (`topic_key` is in `memorySaveSchema`) and `recall.md`'s `limit` (which _is_ in `memorySearchSchema` — only `contextSchema` lacks it).
- [x] 2.9 Prove 2.8 catches the historical defect class: with the test in place, confirm it goes red for each of `memory.search({q})`, `memory.context({limit})` and `memory.session_summary({auto})` — the three calls the spec published — and green for the four bodies that actually ship. - All three observed red, one at a time, each naming the file and the unknown key: `recall.md: memory.search argument "q"`, `context.md: memory.context argument "limit"`, `summary.md: memory.session_summary argument "auto"`. The other nine cases stayed green in each run. Tree restored after each.
- [x] 2.10 Add the frontmatter budget assertions from the command-catalog requirement: each `description` ≤80 bytes, and the sum of `/rembric:<name> <description>` across all four ≤320 bytes. Record the measured values; expected 44 / 52 / 41 / 70 bytes individually and ~275 bytes for the sum. Pin the listing string convention in a one-line comment so a future reader knows what the aggregate measures. - Measured, matching the expected figures exactly: `context` 44, `recall` 52, `remember` 41, `summary` 70 bytes. Listings `/rembric:<name> <description>` = 61 / 68 / 59 / 87, **total 275 bytes = 68.8 tokens** against the ≤320/≤80 cap. The listing convention is pinned in a one-line comment at the aggregate assertion.

## 3. Codex manifest and docs

- [x] 3.1 Align `apps/plugin/.codex-plugin/plugin.json`'s `author.url` to `https://github.com/susomejias/rembric` so the tightened `codex-distribution` "shared identity fields agree" scenario is true. One field, no behavioural effect. Note in the task that this bumps the unified `plugin` release component and does not rebuild the server image. - One field changed: `https://github.com/susomejias` → `https://github.com/susomejias/rembric`, now byte-identical with `.claude-plugin/plugin.json`. Bumps the unified `plugin` component (tag `plugin-v*`); `publish-docker` gates on `server_release_created`, so no server image rebuild.
- [x] 3.2 Add the shared-identity assertion to `apps/plugin/test/hook-manifests.test.ts` (or a sibling manifest test): `name`, `version`, `license`, `repository`, `author.name`, `author.url`, `keywords` byte-identical across `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`. Confirm it goes red against the pre-3.1 value. - Added to `hook-manifests.test.ts`. Observed red **before** 3.1 landed, against the real pre-fix value, with the diff naming the field: `- "url": ".../susomejias/rembric"` / `+ "url": ".../susomejias"`. Green after. Also asserts neither manifest declares `commands` and that `.codex-plugin`'s only capability declarations are `mcpServers` and `hooks`, which is the manifest half of the new Claude-Code-only requirement.
- [x] 3.3 Add one statement to the Codex section of `docs/agents.md` that `/rembric:*` slash commands are Claude-Code-only and Codex users should ask the agent in plain language. Place it near the MCP/hooks capability description, not buried in the troubleshooting table. - Added as a third bullet under "What the plugin registers for Codex" (`docs/agents.md:120`), directly beneath the MCP-server and five-hook bullets and well above the troubleshooting table. Names the four commands, the reason (auto-discovery from `commands/*.md`; `.codex-plugin/plugin.json` declares only `mcpServers` and `hooks`), the plain-language alternative, and that `initialize.instructions` is client-agnostic. The `/hooks` trust banner wording at `:155` is left at five event types per 5.8.

## 4. Prose sections the delta format cannot reach (design D5)

- [x] 4.1 Correct `openspec/specs/claude-code-plugin/spec.md:404`. It states the bridge checks `${CLAUDE_PROJECT_DIR}/.rembric` "otherwise `${process.cwd()}/.rembric`", contradicting `:33` in the same file, which correctly gives the three-step chain `CLAUDE_PROJECT_DIR` → `PWD` → `process.cwd()` matching `rembric-bridge.mjs:33–41`. Replace `:404` with a pointer to `:33` rather than a second copy of the chain — two copies is how they diverged. The `PWD` step is load-bearing for Codex (`codex-distribution`'s `env_vars` requirement depends on it), so verify the corrected text names it. - Replaced the stale two-step chain with a pointer to the single normative copy under "MCP bridge contract" (`:33`), naming the `PWD` step and why it is load-bearing for Codex, plus an explicit "SHALL NOT be restated here". The whole chain is deliberately **not** re-enumerated — restating it is what produced the divergence.
- [x] 4.2 Correct `openspec/specs/claude-code-plugin/spec.md:414`. Bootstrap is described as "guided by the `rembric-memory` skill"; the skill was removed and the same file says so at `:44–45`. Replace with the server-side `initialize.instructions` handshake, which is what actually carries the guidance. - Now reads "guided by the protocol text the server delivers through the MCP `initialize.instructions` handshake (`apps/server/src/mcp/instructions.ts`)", matching the `## Skill catalog` section's own account at `:44`.
- [x] 4.3 Delete the now-superseded prose sections that the deltas promote into requirements — `## Command catalog` (`:47–55`) and `## Token budget` (`:420–436`) — as part of the archive merge, so the file does not carry a prose copy and a requirement copy of the same numbers. Two copies of a budget is the failure mode 4.1 is fixing in miniature. - Both sections deleted in place. Deviation from the task's "as part of the archive merge": done now, at apply time, because the delta format reaches only `### Requirement:` blocks — the archiver would leave both prose copies behind permanently. Deleting now is also strictly safe: every number in them was false, and the replacement requirements land in the same archive merge. Verified by grep: `memory.search({q`, `{limit: 10}` and `auto: true` are now absent from all three specs.
- [x] 4.4 `grep -n 'skill' openspec/specs/claude-code-plugin/spec.md` and confirm every surviving mention is either the accurate `## Skill catalog` section (which correctly states no skills ship) or a reference to `initialize.instructions`. No mention of `.claude-plugin/skills/`, no skill token budget. - Nine hits. `:42/:44/:45` are the accurate `## Skill catalog` section. `:217` and `:225–229` (the `skill content` phrase and the `.claude-plugin/skills/` scenario) sit inside the coexistence requirement the delta MODIFIES — its replacement says "command catalogue, and any server-delivered protocol guidance" and its scenario reads `initialize.instructions` + `commands/`, so both go at the archive merge. The skill token budget is gone with `## Token budget` (4.3), and the replacement requirement states explicitly that no skill line belongs in either budget.

## 5. Verification

- [x] 5.1 `pnpm run typecheck` · `pnpm run lint` · `pnpm test`. Record the before/after test counts; the three new test files plus the per-fixture assertions should add roughly 20 cases. `pnpm run eval` is NOT required — this change touches no retrieval path. - `pnpm run typecheck` clean. `pnpm run lint` clean (run explicitly — the pre-push hook runs tests, not lint). `prettier --write` applied to the two new test files. `pnpm test`: **107 test files passed, 1 skipped; 1650 tests passed, 10 skipped** plus the Python Hermes suite **72 tests OK**. Before/after: `nudge-fixtures.test.ts` 25 → 34 (verified by `git stash`), `hook-manifests.test.ts` +18, `command-arguments.test.ts` +10 → **+37 cases, 105 → 107 files**. `pnpm run eval` not run, correctly — no retrieval path touched.
- [x] 5.2 Re-read each merged spec **end to end**, not just at the edited lines. The contradictions this change fixes were all _between_ requirements, not within one. Specifically confirm: `claude-code-plugin`'s `:33` and the corrected `:404` agree on the three-step chain; the post-compact cap reads `≤150` at both `claude-code-plugin` and `plugin-session-protocol` and nowhere reads `≤120`; no requirement still asserts a flat per-turn `UserPromptSubmit` figure; and the `sessionId` nudge requirement in `plugin-session-protocol` does not contradict the token-budget requirement's statement that it must not be removed. - **Caveat on the word "merged": the archive merge has not run yet**, so this was verified as published-spec + delta, not as one file. All four named checks pass. (a) `:33` and the corrected slug-lookup line agree — the latter now points at the former instead of restating it. (b) `≤150` is normative at four delta sites (`claude-code-plugin` `:32` and its budget table `:224`; `plugin-session-protocol` `:78` and `:95`); the only `≤120` occurrences left in the deltas are two explicit historical-narrative references ("replaces a previously-published `≤120`", "against a published `≤120` cap"), never a cap. Both surviving normative `≤120` sites in the published specs (`plugin-session-protocol:284`, `claude-code-plugin:75`) are inside requirements the deltas MODIFY/REMOVE. (c) The flat per-turn figure is gone: the prose that carried it was deleted in 4.3, and the replacement requirement says outright that `UserPromptSubmit` "SHALL be governed by the pair … and not by a flat per-turn figure". The surviving `≤30` is only the `SessionStart` cap, which measured 22.5 and was always true. (d) No contradiction on the sessionId nudge: `plugin-session-protocol:216–240` requires the line be emitted whenever save or summary fires, and the token-budget requirement says it SHALL NOT be removed to save budget — they reinforce. Additionally verified that every delta MODIFIED/REMOVED requirement name matches a published heading **verbatim**, so the archive merge will replace rather than append: 5 in `claude-code-plugin`, 3 in `codex-distribution`, 2 in `plugin-session-protocol`.
- [x] 5.3 `grep -rn '≤120\|exactly four hooks\|memory.search({q\|{limit: 10}\|auto: true\|rembric-memory' openspec/specs/` must come back empty for these three specs. Each string is one of the corrected falsehoods; a survivor means an incomplete merge. - `memory.search({q`, `{limit: 10}` and `auto: true` are **already empty** (deleted with the prose in 4.3). `≤120` (2 sites) and `exactly four hooks` (1 site) survive only inside requirements the deltas MODIFY/REMOVE, so they go at the archive merge, not now. **Deviation:** `rembric-memory` will NOT come back empty, and should not. Its one survivor is `claude-code-plugin:45` — "An earlier iteration shipped a `rembric-memory` skill …; it was removed once `initialize.instructions` was verified to carry equivalent guidance" — which is true, is the accurate `## Skill catalog` section that task 4.4 explicitly blesses, and is what justifies 4.2's rewrite. 5.3's grep string over-matches an accurate historical record. Kept; not deleted to satisfy a proxy.
- [x] 5.4 `npx openspec validate --specs --strict` — 24/24. State explicitly in the task note that this passing proves nothing about semantic accuracy: it passed on all 24 specs containing every divergence this change fixes. It is a structural check only. - `npx openspec validate --specs --strict` → **Totals: 24 passed, 0 failed**. `npx openspec validate audit-plugin-spec-integrity --strict` → valid. **This proves nothing about semantic accuracy.** The same command passed 24/24 on the specs while they forbade a shipped hook, published three zod-rejecting calls and asserted nine violated budgets. It checks structure only.
- [x] 5.5 `git ls-files apps/plugin/` sanity check — ONE copy of every shared resource. Legitimate divergences are only the four per-client manifest dirs and `hooks{,.codex}.json`. The new test files must not introduce a per-client fixture copy. - Duplicate basenames are exactly `CHANGELOG.md`, `README.md`, `install.sh`, `mcp.json`, `package.json`, `plugin.json`, `uninstall.sh` — every instance inside `.claude-plugin/`, `.codex-plugin/`, `.hermes-plugin/`, `.opencode-plugin/` or the plugin root. `scripts/` (9 files), `commands/` (4) and `test/` are one copy each; the two new test files are singletons and add no per-client fixture. `hooks.json`/`hooks.codex.json` are distinct basenames, as designed.
- [x] 5.6 **Real Docker smoke against pre-existing seeded data** (standing requirement, per `rembric-smoke-tests`). Bring up `pnpm run dev:docker:up` against a data dir that already holds seeded memories and sessions — do NOT wipe. Drive a real session through the plugin and confirm at `/dashboard/sessions` that: a `SessionStart` creates the row; a `Stop` updates `summary` without setting `summary_final`; and pre-existing rows are untouched. This change edits no server code and no migration, so the smoke is a regression check on the manifest edit, not a migration check — say so in the note rather than implying more. - Run against the pre-existing 77 MB `data-dev/data.db` (50 memories, 5 sessions, 70 relations, 2 confirmations, 3 tokens, project `demo`), backed up first. **Deviation forced by the tree:** `dev:docker:up` cannot satisfy "do NOT wipe" as written — the dev stage `CMD` runs `seed-dev --reset` and `docker-compose.dev.yml` sets `REMBRIC_ALLOW_DESTRUCTIVE_SEED=1`, so a plain bring-up wipes. Brought up with a third `-f` override whose only change is dropping `--reset` from the command. Mounts verified to this worktree; healthy; `[bootstrap] listening on http://0.0.0.0:8787`.
      All three assertions hold. `SessionStart` (`session-start.sh claude-code`) created the row and emitted its nudge — `sessions` 5 → 6, `agent='claude-code'`, `status='active'`. `Stop` (`stop-sync.sh claude-code`) emitted **empty stdout** and wrote `summary`/`title` with `summary_final=0` and `title_final=0`. `/dashboard/sessions` (cookie auth, HTTP 200) renders the new row with the Stop-written title. Pre-existing rows untouched: memory 50, relations 70, confirmations 2, tokens 3 all unchanged, and the three `summary_final=1` curated sessions were not overwritten.
      Two honest corrections to the task's framing. **(1) A migration DID run** — the pre-existing DB was at `0025_confirmation_review_index.sql` and the bring-up applied `0026_confirmation_verdict_check.sql`, a table-rebuild `CHECK` migration from an earlier commit, not from this change. It committed cleanly and the 2 confirmations survived, so this pass is incidentally the upgrade-safety evidence for `0026` as well. **(2)** Two pre-existing sessions flipped `active` → `abandoned` during the run. That is the documented `abandonStale` sweep firing on rows two days older than the 24 h default, not an effect of this change.
- [x] 5.7 **Four-client e2e per the `rembric-plugin-development` skill** (`references/e2e-walkthrough.md`), mandatory for the `.codex-plugin/plugin.json` edit and the manifest tests because both touch what all four clients load. If — contrary to design D6 — any nudge _text_ changed, this pass is doubly required and must confirm byte-identical emission across Claude Code, Codex CLI, Hermes and opencode. - **NOT RUN as a four-host interactive walkthrough. Do not read this as covered.** D6's prediction held exactly: **no nudge text changed** — `nudge-fixtures.json` and all nine `scripts/*.sh` are byte-identical to HEAD (`git status` shows only `.codex-plugin/plugin.json`, `nudge-fixtures.test.ts` and two new test files under `apps/plugin/`), so the byte-identical-emission dimension has nothing to exercise. What WAS run instead: the existing four-client lock-step suite (bash ↔ TS ↔ Python fixture identity, 34 cases in `nudge-fixtures.test.ts`) green; the full 1650-case suite plus the 72-case Python Hermes suite green; and a real Claude-Code-shaped session driven end to end through `session-start.sh` and `stop-sync.sh` against the live Docker stack (5.6). The `.codex-plugin/plugin.json` edit is one `author.url` string with no runtime consumer — Codex caches plugins by `version`, which is unchanged. What remains genuinely unverified: an interactive `codex`/`hermes`/`opencode` host actually loading the edited manifest.
- [ ] 5.8 Operator-only, pause here: confirm the Codex `/hooks` trust banner's count against a live `codex-cli`, to settle design Q1 (five event types vs seven handler entries). Not available in the environment this change was written in. If it shows seven, correct `codex-distribution:209`/`:220` and `docs/agents.md`'s "each of the 5" wording in a follow-up; if five, the shipped doc is right and Q1 closes as recorded. - **LEFT OPEN — still unresolved, as the task instructs.** `codex-cli 0.144.4` IS on PATH here, but the rembric plugin is not installed under it (`~/.codex/plugins/cache` is empty), and installing it plus launching interactively to read a startup banner would modify the operator's Codex config — out of bounds for this pass. Read-only evidence gathered instead, which **leans toward handlers but does not settle the banner**: `~/.codex/config.toml`'s `[hooks.state]` keys are `"<file>:<event>:<group_index>:<handler_index>"` (e.g. `":session_start:0:0"`, `":user_prompt_submit:0:0"`). Trust is therefore recorded **per handler entry**, matching `codex-distribution:222`'s "only once per hook handler". That says nothing conclusive about what the banner _displays_, and the available sample cannot discriminate — every event in that user-level `hooks.json` has exactly one handler, so per-event and per-handler counts coincide there. **Default taken as recorded:** `docs/agents.md:155` and `codex-distribution:209`/`:220` stay at five event types, and `hook-manifests.test.ts` asserts BOTH numbers (5 types AND 7 handlers) for `hooks.codex.json`, so whichever the banner counts, the pair is pinned and the follow-up is a one-line doc edit.

## 6. Deferred and explicitly rejected — do not silently lose these

- [x] 6.1 Record in the change's completion notes that the **first-prompt relevance prefetch was rejected, not forgotten** (design D1), with the reason: an HTTP call on the first prompt of every session, on the latency-critical path, in bash, across four clients, replacing an instruction that works. This is the second time the verdict has been recorded; the first (`archive/2026-07-25-reconcile-specs-with-shipped-behaviour/design.md:59–63`) carried no delta and therefore did not stick. This time the delta carries it. - **REJECTED, not forgotten.** No prefetch was built. Verified at HEAD that `prompt-search.sh` contains no `rembric_post` and no `curl` — it sources `_api.sh` only for `rembric_prompt_from_stdin_json` and `rembric_turn_count`, and emits one fixed line byte-identical whether or not the scope holds a relevant memory. The reason stands: an HTTP call on the first prompt of every session, on the latency-critical path, in bash, replicated across four clients, to replace an instruction that works. **This time it sticks** because the delta REMOVES the prefetch requirement with a `**Reason**`/`**Migration**` pair and ADDS "The first prompt of a session MUST receive a relevance instruction", which states the resulting dependency on model cooperation in the requirement text — so a future reader finds the limitation in the contract, not only in an archived design note.
- [x] 6.2 Record that **cutting the `sessionId` nudge is blocked on a server-side fix**, not declined on taste (design D2): `resolveActiveSessionId`'s `SessionRouter` fallback is populated only by `memory.session_start`, which the plugin never calls, and `findActiveForTransport` refuses by design to guess under concurrent ambiguity inside its 30-minute staleness window. Two terminals on one repo leave the nudge as the only attachment mechanism. That server fix is out of scope here and needs its own change. - **BLOCKED on a server-side fix, not declined.** Every clause re-verified against source this session, not taken on trust. `resolveActiveSessionId` (`apps/server/src/mcp/memory-tools.ts:528–559`) has exactly three paths: the explicit argument (`:533`), the `SessionRouter` entry (`:541–547`), and `findActiveForTransport` (`:548–557`). `SessionRouter.setActiveSession` (`apps/server/src/server/session-router.ts:95`) has exactly **one** non-test caller — `apps/server/src/mcp/session-tools.ts:195`, inside the `memory.session_start` handler — and `grep -rn session_start apps/plugin/` over `*.sh`/`*.ts`/`*.py`/`*.md`/`*.json` returns **nothing**, so no client ever populates that fallback. `findActiveForTransport` (`apps/server/src/db/repositories/agent-sessions-repository.ts:162–169`) does `.limit(2)` then `return rows.length === 1 ? rows[0] : undefined` — it refuses by construction the moment two live rows exist, inside `TRANSPORT_STALENESS_MS = 30 * 60_000` (`agent-sessions.ts:23`). With two hosts open on one repo the nudge is the only attachment mechanism; without it the memory lands with `session_id = NULL`. The server fix is out of scope here and needs its own change.
- [x] 6.3 Record design Q2 as open: whether `firstPromptRelevance` and the recall nudge should both fire when the first prompt of a session contains a recall keyword. They are near-duplicative and together account for the 165.0-token worst case rather than 142.3 — the largest cheap saving available — but suppressing one is a behaviour change on a four-client lock-step surface and needs a measurement of which instruction the model actually follows on turn 1. - **OPEN. Nothing changed, and the measurement now exists to price it.** Re-measured this session: turn 1 with a recall keyword emits 660 bytes / 165.0 tokens, of which `prompt-search.sh` contributes 217 (`firstPromptRelevance` 125 + recall 90). Suppressing one would save ~91–126 bytes on that turn alone, the largest cheap saving available. Deliberately not taken: the two lines name different tools (`memory.context` with `focus` vs `memory.search` with the user keywords), a user who explicitly asks to recall probably wants the keyword search, and suppression is a behaviour change on a four-client lock-step surface. `claude-code-plugin`'s scenario "Both prompt-search.sh lines can coincide on turn 1" now documents the both-fire case as the budget worst case, and the ≤180-token ceiling is set against it — so the question is recorded in the contract, not just the design.
- [x] 6.4 Record the limit no gate closes: **none of these tests detects a NEW false statement.** They pin exactly three surfaces — hook manifests, command arguments, nudge budgets. Every other sentence in these three specs is verified only by the read performed for this change. `openspec validate --specs --strict` passes today, 24/24, on specs containing every divergence fixed here. Do not let the green suite be read as a clean bill of health. - **Recorded as the honest limit.** The 37 new cases pin exactly three surfaces: hook manifests (event sets, handler counts, matchers, `async`), command arguments (every key against its zod schema, plus frontmatter budgets), and nudge budgets (seven per-line caps, the per-firing-turn ceiling, the amortised mean). **Nothing else.** Every other sentence in these three specs is verified only by the read performed for this change, and structural validation cannot see semantic falsehood — `openspec validate --specs --strict` returned 24/24 today on specs that forbade a shipped hook, published three zod-rejecting calls and asserted nine violated budgets. A green suite after this change is **not** a clean bill of health for these specs.
- [x] 6.5 Record that `opencode-plugin` and `hermes-agent-plugin` were checked on exactly one point — neither claims the recall-hook matcher, correcting the referring audit's "four specs" to two — and were otherwise **not** audited. They remain unexamined end to end, as `claude-code-plugin` and `codex-distribution` were before this change. - Re-confirmed: `grep -rn -i matcher openspec/specs/{opencode-plugin,hermes-agent-plugin}/spec.md` returns **zero** hits in either file — the word does not appear at all, so neither can claim the recall-hook matcher. The referring audit's "four specs" is corrected to **two** (`claude-code-plugin`, `codex-distribution`). Beyond that single point **neither spec was audited**; both remain unexamined end to end, exactly as `claude-code-plugin` and `codex-distribution` were before this change.
