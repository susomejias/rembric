## 1. The predicate

- [x] 1.1 Create `scripts/check-spec-provenance.mjs` exporting `parseNameStatus(stdout)` → `Array<{ status, path, newPath? }>` and `checkProvenance(entries, { trailers })` → `{ ok, violations: [{ capability, expectedPath }], exempt }`. No dependency beyond `node:*` (D5).
- [x] 1.2 Implement the diff-level rule: any `A`/`M`/`D`/`R` entry matching `openspec/specs/*/spec.md` requires at least one `A` or `R` entry under `openspec/changes/archive/` (D2, D4). A modified archive path SHALL NOT satisfy it. — `hasArchiveArrival`; a rename _out of_ the archive does not satisfy it either.
- [x] 1.3 Implement the per-capability rule: for each affected `<cap>`, require an `A`/`R` entry at `openspec/changes/archive/*/specs/<cap>/spec.md`; report each unpaired capability with the path that would have satisfied it (D3).
- [x] 1.4 Implement the `Spec-Provenance-Exempt: <reason>` trailer exemption — non-empty reason only — returning `exempt` with the reason and the waived paths rather than silently passing (D7).
- [x] 1.5 Add the CLI wrapper: `--base`/`--head` shell out to `git diff --name-status --find-renames <base>...<head>`; unresolvable range (all-zeros `before`, force-push) logs "range unresolvable, skipping" and exits 0 (D1).
      **Deviation (defect found and fixed):** the first cut gated on `git merge-base --is-ancestor base head`, per D1's "force-push" wording. That is wrong for the PR case — `pull_request.base.sha` is the base branch _tip_, which is normally NOT an ancestor of the PR head — so the gate would have logged "range unresolvable, skipping" and exited 0 on essentially every real PR. A gate that never gates. `resolveRange` now requires only that a merge base exists (which is what `base...head` actually diffs from); a force-pushed `before` that no longer resolves is still caught by the `rev-parse --verify` check. Regression test: `resolveRange > resolves a base that is not an ancestor of head`.
- [x] 1.6 Add a root `check:spec-provenance` script to `package.json` defaulting to `main...HEAD` so a contributor can run the gate locally before pushing (D6).

## 2. Tests — synthetic (must pass with no `.git`)

- [x] 2.1 Create `scripts/check-spec-provenance.test.ts` wired so `pnpm test` runs it (mirror how `install.test.ts` is executed), with fixtures covering every scenario in the delta spec: unpaired edit fails; paired archive passes; modified-only archive fails; archive of `<cap>` plus edit of another capability fails naming only the second; added and deleted published spec both require provenance; delete+add instead of rename still passes; empty-reason trailer does not exempt; a diff touching no published spec passes.
      Wired via `apps/server/vitest.config.ts::include += '../../scripts/*.test.ts'` (the `install.test.ts` mechanism), and NOT added to the `--exclude` list, so `pnpm test` and the CI `test:coverage` gate both run it. **Added beyond the list:** a rename _out of_ the archive does not satisfy the rule; a cross-capability spec rename flags both capabilities; a trailer on a diff that needed no exemption reports `exempt: null`; five CLI end-to-end tests driving `node scripts/check-spec-provenance.mjs` against scratch git repos (exit 1 with the capability named, exit 0 for sync+`git mv` in one commit, trailer honoured from any commit in the range, trailer NOT honoured from a commit outside the range); and the `resolveRange` tests from 1.5.
- [x] 2.2 Assert the suite passes with `.git` absent or shallow — no test in this file may require git history (D5, delta scenario "The predicate is testable without git history"). — `the predicate needs no git history` copies the module into a `.git`-less tmpdir and runs it in a child process; the replay block is `describe.skipIf`-guarded on `git cat-file -e` for every pinned SHA.

## 3. Evidence gate — replay against real history

- [x] 3.1 Add a history-replay test that self-skips when the SHAs are unreachable (`git cat-file -e`), and otherwise runs the predicate over each of the 40 commits touching `openspec/specs/` in `77cbc2f..1814f7b`.
      **Correction:** `77cbc2f..1814f7b` is exclusive of `77cbc2f` and yields **39** commits. The 40-commit set the proposal measured is `77cbc2f~1..1814f7b`, which the test uses. `77cbc2f` itself is compliant (4 published specs, 4 paired deltas), so the violation counts are unaffected.
- [x] 3.2 It MUST flag exactly these eight: `1814f7b`, `48b7c58`, `b14368d`, `ef4ac49`, `33c5ece`, `11e0b78`, `45fe9f0`, `f320036` — and MUST pass the other 32. Assert the count, not just membership, so a widened rule cannot slip in. — Confirmed exactly, for the **diff-level** rule: 8 flagged, 32 pass.
- [x] 3.3 Assert the per-capability rule flags the same eight and nothing more (D3's measurement), including that `02b3a7c` pairs all 8 of its capabilities.
      **Deviation — the artifacts' measurement was wrong and is now corrected.** The per-capability rule flags **nine**, not eight. The ninth is `3baff49`, which archived `add-entity-index` (deltas for `dashboard`, `mcp-api`, `memory-entities`, `persistence`) while _also_ rewriting a requirement in `openspec/specs/memory/spec.md` that no delta covered — a **true positive**, and exactly the laundering mode D3 exists to stop. So D3 is not "forward-looking only": it catches a real historical case the diff-level rule waves through. False positives remain zero (31 of 40 pass). `02b3a7c` does pair all 8. `design.md` D3 and `proposal.md` updated with the corrected numbers.
- [x] 3.4 Assert the capability attribution on the multi-capability case: `b14368d` reports both `mcp-api` and `plugin-session-protocol`.
- [x] 3.5 **Added:** three pinned cases from after the measurement window, since two violations landed while this gate was being built. `1b41583` (`fix(dev): keep the dev stack off the published production image tag`) edited `openspec/specs/development-environment/spec.md` with no change folder at all — it fails both rules on `development-environment`. `82d672e` archived `guard-tool-description-truncation` (delta for `mcp-api`) while hand-editing `openspec/specs/plugin-session-protocol/spec.md` — it **passes** the diff-level rule and **fails** the per-capability rule, a second live instance of the 3baff49 mode. `878171b` is a clean three-capability archive and passes both. All three are asserted.

## 4. CI wiring

- [x] 4.1 Add a `spec-provenance` job to `.github/workflows/ci.yml` on `pull_request` and `push: branches: [main]`, with `actions/checkout@v7` at `fetch-depth: 0`; leave the existing `test`, `retrieval-eval` and `docker-build-check` checkouts at their current depth (D9). — The workflow's existing `on:` block already carries both triggers; the other three checkouts are verified still at the default depth (no `with:`).
- [x] 4.2 The job runs `node scripts/check-spec-provenance.mjs` with base/head from the event payload — PR: `base.sha`…`head.sha`; push: `github.event.before`..`github.sha`. No pnpm install, no build (the script has no dependencies), so the job stays seconds long.
- [x] 4.3 Name the job and its failure message in terms of **provenance** — never "valid" or "correct" (D10, delta requirement's closing paragraph). — Job `spec-provenance`, step "Check published-spec provenance", failure text "a published spec changed without an archived change folder in the same diff". Asserted mechanically: the parsed job block contains neither "valid" nor "correct".
- [x] 4.4 Verify `eslint .` picks up the new root `.mjs` and `.test.ts` without config changes; if it does not, extend `eslint.config.js` rather than exempting the files.
      It does **not**: the type-checked default project has no tsconfig covering root `scripts/`. `eslint.config.js` extended two ways — `scripts/*.mjs` joins the existing non-type-checked `.mjs` block, and `scripts/check-spec-provenance.test.ts` joins `install.test.ts` in `allowDefaultProject` + `disableTypeChecked`. Coverage proven by injecting an unused variable into each new file and confirming eslint reports it (3 errors), then reverting.

## 5. Make the convention a contract

- [x] 5.1 Edit `.agents/skills/openspec-archive-change/SKILL.md` so step 5 states explicitly that the delta sync (step 4) and the move into `openspec/changes/archive/` land in a SINGLE commit, and why (the CI gate reads that co-occurrence as the provenance signal) — D8. — Added to step 5 and to the Guardrails list.
- [x] 5.2 Confirm the skill's `.claude/skills/` symlink still resolves after the edit (`ls -l .claude/skills/openspec-archive-change`); source stays under `.agents/skills/`.
      **Deviation — the premise is false.** `.claude/skills/openspec-archive-change/` is **not** a symlink; it is a directory holding a tracked duplicate `SKILL.md` (same for `openspec-apply-change`, `openspec-explore`, `openspec-propose` — the openspec CLI writes them there, contrary to `CLAUDE.md § Skills`). That copy is what the Skill tool actually loads, so editing only `.agents/` would have left the contract text invisible to the agent doing the archiving. Both files were patched identically. Converting the four to symlinks is out of scope here (the CLI would clobber them) and is left unaddressed rather than silently done.

## 6. Verification

- [x] 6.1 `pnpm run typecheck` — clean.
- [x] 6.2 `pnpm run lint` — clean, and proven to actually cover both new files (see 4.4). `prettier --check` clean on every path this change touches; `.claude/skills/openspec-archive-change/SKILL.md` was already prettier-unformatted before this change (as are the other three openspec CLI copies) and was left that way.
- [x] 6.3 `pnpm test` — the synthetic suite green, the replay suite green on a full clone and skipped on a shallow one. — 107 files / 1710 tests passed, 1 file / 10 tests skipped (pre-existing), plus 72 Hermes Python tests OK. The new file contributes 34 tests. `test:coverage` (the CI gate) also green at 90.97% lines. `pnpm run e2e:installer` still resolves to `install.test.ts` alone (1 file, 57 tests) after the `vitest.config.ts` change.
- [x] 6.4 `openspec validate enforce-spec-archive-provenance --type change --strict` green, and `openspec validate --all` shows no NEW spec failure. — 26 passed, 1 failed; the single failure is `change/tune-hot-query-paths`, a pre-existing in-flight change with no deltas, untouched here. All 24 published specs pass.
- [x] 6.5 `pnpm run eval` is NOT required: this change touches no retrieval code path. — Confirmed: nothing under `apps/server/src/{db,services,mcp,consolidation,embeddings,llm}/` is touched. Not run.
- [x] 6.6 Docker smoke is NOT applicable and the reason is asserted, not assumed: confirm `git diff --name-only main...HEAD` for this change touches nothing under `apps/`, no migration, no MCP tool, no HTTP route, and no plugin file — the change ships zero runtime code, so there is no production behaviour to smoke against seeded data.
      **Deviation, narrow:** the change set does touch exactly one file under `apps/` — `apps/server/vitest.config.ts`, adding `'../../scripts/*.test.ts'` to the vitest `include` array. That is the mechanism task 2.1 mandates (it is how `install.test.ts` is wired) and it is test-harness configuration, not shipped code: it is excluded from `tsconfig.build.json`'s output and never loaded by the server. Everything else the task asserts holds — no migration, no MCP tool, no HTTP route, no `apps/plugin/` file, no SQL, no schema change. **No Docker smoke was run, and none is claimed.** The change ships zero runtime code, so there is no production path to exercise against seeded data.

## 7. End-to-end proof in CI (operator-visible, do not skip)

These three require pushing to GitHub and opening PRs. **Not performed** — this session was instructed not to commit or push, so the job has never executed on a real event. Until 7.1–7.3 are done the gate is verified only locally (predicate, CLI, and workflow YAML), and the `github.event.*` payload wiring in particular is unproven against a live event.

- [ ] 7.1 **Operator action (GitHub):** open a throwaway PR that modifies one line of `openspec/specs/mcp-api/spec.md` and nothing else; confirm `spec-provenance` fails and names `mcp-api`. Close the PR without merging.
- [ ] 7.2 **Operator action (GitHub):** on that same throwaway PR (or a second one), add a commit carrying `Spec-Provenance-Exempt: throwaway verification` and confirm the job passes and prints the reason and the waived path. Close without merging.
- [ ] 7.3 **Operator action (GitHub):** confirm the next real archive PR after this change lands passes `spec-provenance` with no exemption — the plugin-spec audit change queued behind this one is the first live archive it will validate. If it fails, the failure is a defect in this gate, not in that change.
      Note: that change (`audit-plugin-spec-integrity`) has since landed as `878171b` and is asserted in the replay test as passing both rules, so the live run should be a formality.
- [ ] 7.4 Record the outcome of 7.1–7.3 in this file before archiving; an unverified gate is worse than none.

## 8. Deferred — recorded so it is not silently lost

- [x] 8.1 Do NOT add spec **content** validation here (spec-quoted tool arguments vs zod schemas, sync fidelity between a published spec and its archived delta, `Impact:` list vs delta set). Deferred as a family — D10 and design Open Question 1. — Honoured; nothing added.
- [x] 8.2 Do NOT add `openspec validate --all --strict` to CI in this change: it is red today because in-flight change folders have no deltas yet. A specs-only scope would be needed first. — Honoured; confirmed still red on `tune-hot-query-paths` (see 6.4).
- [x] 8.3 Do NOT write a retroactive change folder for the eight historical violations. They are cited in `proposal.md`; the gate is diff-scoped and never re-evaluates old commits. — Honoured. The two post-window violations (`1b41583`, `82d672e`) are likewise cited, not retro-fixed; `openspec/specs/development-environment/spec.md` and `openspec/specs/plugin-session-protocol/spec.md` keep the undocumented text they acquired.
- [x] 8.4 Requiring a fully-ticked `tasks.md` at archive time (design Open Question 2) belongs in `invariants.test.ts`, not this gate — out of scope here. — Honoured; nothing added. Note that section 7 above leaves this change itself with unticked tasks at archive time.
