## 0. Settle the two open questions before writing code

- [x] 0.1 **Operator decision.** `design.md` OQ1 — keep or drop the create handler's auto-create of an unknown project slug (`dashboard/tokens.ts:169-170`). Default if unanswered: **keep**, as specified in the `dashboard` delta's scenario "An unknown project slug submitted to the create form". If dropped, edit that scenario before implementing. → **Default taken: kept.** The delta scenario is implemented as written.
- [x] 0.2 **Operator decision.** `design.md` OQ2 — ship the `CHECK` table rebuild (D4) in this change or sequence it separately. Default if unanswered: **ship it here**. If deferred, delete task groups 4 and 8.3-8.5 and the whole `persistence` delta, and record the deferral in `proposal.md` so it is not silently lost. → **Default taken: shipped here** as migration `0029_tokens_project_binding.sql`.
- [x] 0.3 Pick the label for the unresolvable-token state pill. Default `inert`. Whatever is chosen must be distinct from both `active` and `revoked` in the rendered HTML, per the `dashboard` delta. → **Default taken: `inert`**, rendered with the existing `pill legacy` class (no new design token).

## 1. Failing tests first — the create → authorize boundary

Every test in this group MUST be observed **failing** before any production line changes. A unit test on the create handler would assert the same wrong string the handler produces (design D7), so each arm posts the real form to `/dashboard/tokens`, takes the plaintext from the redirect's `created` query parameter, and uses it as a bearer against `POST /api/<slug>/memory/recall`. `apps/server/src/test/dashboard-e2e.test.ts` already boots a real server and drives it with `fetch`; extend it there.

- [x] 1.1 Add a helper to `dashboard-e2e.test.ts` that mints through the form and returns the plaintext plus the row as rendered in the list. The existing token test (`:200-236`) is the shape to follow; it passes `project: ''` (`:212`), which is why neither branch of the handler's conditional is exercised today.
- [x] 1.2 **Failing (#307):** form with `project = alpha`, `access = write` → the minted token is authorized on `POST /api/alpha/memory/recall`. Observe it fail with 403 `{"ok":false,"code":"forbidden","message":"token scope does not cover this project"}` and record that verbatim in the commit body. (`auth` delta, "A token minted for a project authorizes that project".)
- [x] 1.3 **Control that MUST pass before and after:** the admin `*` token returns 200 on `POST /api/alpha/memory/recall`. Without it a 403 cannot be told from a misconfigured endpoint.
- [x] 1.4 **Control that MUST pass before and after — the one a lazy fix would break:** the token minted for `alpha` is rejected on `POST /api/never-selected/memory/recall`. Without this, a change making `isAuthorized` return `true` unconditionally passes group 1 entirely. This control is red-if-the-fix-overreaches, so it must be asserted in the same test as 1.2, not in a separate one that could be skipped.
- [x] 1.5 **Failing (#309):** form with `project = alpha`, `access = read` → persisted scope is `read:project:<id of alpha>`, authorized for a read on `alpha`, rejected on `never-selected`, and rejected for a write on `alpha`. Observe it fail — today `read:project:<id>` is unmintable at all (`dashboard/tokens.ts:133-135` offers exactly three options). (`dashboard` delta, "A project selection with read access mints `read:project:<id>`" and "A project selection is never discarded by the access selector"; `auth` delta, "A token minted for a project with read access authorizes reads only".)
- [x] 1.6 **Failing:** the tokens list contains a `project` header and a cell rendering `alpha`, and no cell matching `/project:01[A-Z0-9]+/`. Observe it fail — measured on `main`, the rendered headers are exactly `name`, `scope`, `created`, `expires`, `state`, `actions`. Mirror the assertion style at `:542` and `:552`. (`dashboard` delta, "The list shows the project slug, never the id".)
- [x] 1.7 **Failing:** a token bound to a project that is then archived still renders that project's slug, not `—`. (`dashboard` delta, "A token bound to an archived project still shows its slug".)
- [x] 1.8 **Failing:** a legacy row inserted directly through the repository with `scope = 'project:alpha'` and `project_id = null` renders a state distinct from both `active` and `revoked`, a `—` project cell, and its raw scope string; and the same row when revoked renders `revoked`. (`dashboard` delta, the two unresolvable-state scenarios.)
- [x] 1.9 **Failing:** the one-time-view panel after creating a token for `alpha` with read access contains the minted scope and the slug `alpha`. (`dashboard` delta, "The one-time view states the minted scope".)
- [x] 1.10 **Failing:** a create request carrying a `scope` form field is refused with a flash error naming `access` and creates no token row. Assert the row count is unchanged — an assertion over an empty result set proves nothing. (`dashboard` delta, "The retired `scope` field is refused".)
- [x] 1.11 **Controls that MUST pass before and after:** empty `project` + `access = write` → `*`, `project_id IS NULL`; empty `project` + `access = read` → `read:*`, `project_id IS NULL`. These are the arms that work today and must not move.
- [x] 1.12 Record in the commit body which of 1.2-1.10 failed and with what, and confirm 1.3, 1.4 and 1.11 were **green before** the change. A control that is red at this point means the harness is wrong, not the code.

## 2. The typed grant — make the malformed scope stop compiling

- [x] 2.1 In `apps/server/src/services/tokens.ts`, replace `CreateTokenInput` (`:49-54`) with the discriminated grant from design D3: `{ scope: '*' | 'read:*'; project?: never; access?: never } | { project: Project; access: 'read' | 'write'; scope?: never }`, intersected with `{ name; expiresAt? }`. `projectId` is no longer caller-supplied.
- [x] 2.2 `create` (`:83-105`) composes `{ scope, projectId }` from the grant in one place and passes both to `repos.tokens.insert`. The project arm yields `` `project:${project.id}` `` / `` `read:project:${project.id}` `` and `projectId: project.id`; the scope arm yields the literal and `projectId: null`.
- [x] 2.3 Verify the type actually rejects what it must, by compiling a throwaway file (not committed) containing all seven cases from design D3's table and confirming exactly four errors, at the four expected lines. Measured baseline on the isolated shape: `TS2322` for `` scope: `project:${slug}` ``, `TS2322` for `` scope: `project:${proj.id}` ``, `TS2322` for the both-arms `#309` shape, `TS2345` for a missing `access`.
- [x] 2.4 Update the two non-dashboard project-arm call sites — `apps/server/src/scripts/seed-dev.ts:131` and `:136` — to the grant arm; both already pass `projectId: proj.id`, so the composed row is identical. `seed-volumetric.ts:527` and `seed-dev.ts:124` pass `scope: '*'` and MUST compile unchanged.
- [x] 2.5 Confirm the churn claim: `pnpm run typecheck` shows errors only at the project-arm call sites. 31 `create({…})` sites across 21 test files pass `scope: '*'` or `scope: 'read:*'`; if any of them errors, that is a finding about the type, not a call site to patch. → **The churn claim is wrong; the type is right.** Measured on this tree, `tsc --noEmit` reported **21** errors, not 3. Two classes, both legitimate: (a) 14 sites passed `projectId: null`, which the retired field turns into a `TS2561` excess property — the argument no longer exists, so the key was deleted; (b) 6 sites genuinely pass a project-form scope string (`mcp/authorization.test.ts:56`, `server/api-router.test.ts:72,800`, `server/http.test.ts:84`, `test/tokens.ts:14`, and 4 `mintTestToken` calls in `mcp/unresolvable-slug.test.ts`) — exactly what D3 exists to reject, so each moved to the grant arm. `test/tokens.ts::mintTestToken` now takes a `TokenGrant` instead of a `TokenScope`. Nothing about the type was weakened to accommodate a call site.
- [x] 2.6 Leave `bootstrapAdmin` (`:181-200`) alone — it inserts through the repository with `scope: '*', projectId: null`, which the D4 `CHECK` permits. Note in the commit body that it was checked, not overlooked.
- [x] 2.7 Correct `pinnedProjectId`'s docstring (`:276-278`). It currently asserts "`tokens.project_id`, which the only production creation path leaves NULL" — false after 2.2. Keep the function parsing the scope string (design D8); do not change its signature. One line, documenting the non-obvious _why_ it still parses; no banner, no restatement.
- [x] 2.8 Update the scope-grammar docstring at `:33-38` to say where the project arm is composed. Same one-line discipline.

## 3. The dashboard form and list

- [x] 3.1 Replace the `<select name="scope">` block (`dashboard/tokens.ts:130-137`) with `<select name="access">` offering `write` (default, selected) and `read`. Labels must state the resulting reach in plain words; no live client-side preview (design D1) and no new design tokens.
- [x] 3.2 Rewrite the handler's scope decision (`:180-188`) as the composition table in the `dashboard` delta. The project arm passes the resolved `Project` row `p` (already in hand at `:169-170`) straight into `create`; `projectId: null` at `:208` disappears with the old input shape.
- [x] 3.3 Reject a submitted `scope` field with a flash error naming `access`. Do not ignore it — silently dropping a submitted field is the #309 defect.
- [x] 3.4 Extend the one-shot panel (`:68-80`) to state the minted scope and the bound project slug (or that there is none).
- [x] 3.5 Add the `project` column to the header (`:99-104`) and the rows (`:36-64`). Resolve id → slug from the `deps.projects.list(true)` the router already calls at `:66`, built once into a map; include archived so 1.7 passes. No new repository method and no `admin*` call site — `data-access` must stay untouched.
- [x] 3.6 Add the unresolvable arm to `stateOf` (`:30-34`) with precedence `revoked` → `expired` → unresolvable → `active`. Detection: the scope names a project (`pinnedProjectId(scope) !== null`) and that value is not the id of an existing project. Reuse the existing pill classes.
- [x] 3.7 Confirm the revoke form keeps its `data-confirm` danger-tone modal (`:53-55`) and that the create form gains none — creating a token is not destructive.
- [x] 3.8 Confirm every test in group 1 is now green and every control in 1.3, 1.4 and 1.11 is still green.

## 4. The migration and the schema constraint

Skip this group entirely if OQ2 (task 0.2) is answered "defer".

- [x] 4.1 Add the `CHECK` to `apps/server/src/db/schema/tokens.ts` and update the scope docstring (`:7-12`) to name `project_id` as the enforced binding.
- [x] 4.2 Write `apps/server/src/db/migrations/0029_tokens_project_binding.sql` as the table-rebuild dance, modelled on `0026_confirmation_verdict_check.sql`. The `INSERT … SELECT` is a **verbatim copy** — no `CASE`, no normalisation (design D4: every pre-existing row has `project_id IS NULL` and passes unchanged). Add **no** `foreign_keys` pragma; the runner owns them.
- [x] 4.3 The header comment states the three facts a future reader needs: that `tokens` is an FK **parent** of `sessions` and `dashboard_sessions` so the runner's `foreign_keys = OFF` envelope is what makes the drop legal; that the copy is verbatim because the legacy malformed rows must stay inert; and that `tokens_revoked_at_idx` is deliberately **not** recreated because `0028` dropped it as unusable.
- [x] 4.4 Recreate `tokens_name_unique` and redeclare `id text PRIMARY KEY NOT NULL` so `sqlite_autoindex_tokens_1` returns.
- [x] 4.5 Confirm `apps/server/src/test/schema-drift.test.ts` passes with no edit to the expected column list (`:302-311`; columns are compared as sorted sets at `:400-405`) and no edit to the index snapshot (`:226-227`). If either needs editing, that is a finding: the rebuild changed something it should not have.
- [x] 4.6 Test the migration against a database populated **before** it: seed an admin `*` row, a `read:*` row, a legacy `project:<slug>` row with `project_id IS NULL`, plus `sessions` and `dashboard_sessions` rows referencing them. Assert every column of every row is unchanged and the pre-commit `foreign_key_check` reports nothing. (`persistence` delta, "The rebuild preserves history verbatim" and "Dropping the FK parent does not dangle a child row".)
- [x] 4.7 Test the `CHECK` directly: `project_id = X` with `scope = 'project:' || Y` is rejected; `project_id = X` with `'project:' || X` and with `'read:project:' || X` are accepted; `project_id IS NULL` with any scope is accepted. (`persistence` delta, first two scenarios; `auth` delta, "A scope string disagreeing with `project_id` is rejected by the database".)
- [x] 4.8 Test the FK directly, with a control: `project_id = <a real project id>` is accepted, `project_id = '<a slug>'` is rejected with `FOREIGN KEY constraint failed`. Measured on `main` before the change, both outcomes already hold — this test pins the behaviour the change now depends on rather than proving new code.
- [x] 4.9 Run the migration twice against the same database and confirm `_migrations` records it exactly once and the `CHECK` appears once in `sqlite_master`. (`persistence` delta, "A second boot re-applies nothing".)
- [x] 4.10 Deliberately probe the abort path once, locally: hand-insert a row violating the `CHECK` into a pre-migration database and confirm the migration aborts cleanly with the transaction rolled back and no partial `tokens_new` left behind. Record the observed error. This is the design's stated risk; its behaviour must be known, not assumed. → Observed: `SqliteError: CHECK constraint failed: tokens_project_scope_check` (`code: SQLITE_CONSTRAINT_CHECK`), thrown out of `createDb` at boot. Pinned as the last case in `migrations-0029.test.ts`, which also asserts the rollback was total: `_migrations` carries no `0029` row, no `tokens_new` survives, the `CHECK` is absent from the `tokens` DDL, and all four rows are intact.

## 5. Mutation checks — prove each new guard is actually covered

Each condition is weakened **separately**; a mutation that reddens nothing is the finding. `scripts/mutate.mjs` does the backup/mutate/run/restore loop.

- [x] 5.1 Composition, project arm: mutate the handler so the project row is dropped when `access = 'read'` (i.e. reproduce #309 for the read arm). Expect 1.5's test red.
- [x] 5.2 Composition, access arm: mutate the handler so `access` is ignored and `write` is always used. Expect the read-arm scope assertion in 1.5 red.
- [x] 5.3 Slug-vs-id: mutate `create`'s composition from `project.id` to `project.slug`. Expect 1.2 red. This is the exact original defect; if it stays green the boundary test is not crossing create → authorize.
- [x] 5.4 Over-broad fix guard: mutate `isAuthorized` (`services/tokens.ts:265-268`) to `return true`. Expect 1.4 (denied on a different project) red. If it stays green, the second control is not doing its job and group 1 proves nothing.
- [x] 5.5 `project_id` population: mutate `create` to pass `projectId: null` for the project arm. Expect 1.6's project-column test red. If green, the column is being resolved from the scope string instead of from `project_id` — a finding.
- [x] 5.6 Rejected field: mutate the handler to ignore a submitted `scope` field instead of rejecting it. Expect 1.10 red.
- [x] 5.7 Unresolvable state, each condition separately: (a) mutate the detection to drop the "scope names a project" half; (b) mutate it to drop the "does not resolve to an existing project" half. Expect 1.8 red on both, independently.
- [x] 5.8 State precedence: mutate `stateOf` so the unresolvable arm is tested before `revoked`. Expect the revocation-outranks test in 1.8 red.
- [x] 5.9 Archived resolution: mutate `list(true)` to `list()`. Expect 1.7 red.
- [x] 5.10 Record every mutation and its outcome in the commit body. Any mutation that reddens nothing is reported as a finding and the test is strengthened before proceeding — not waived. → All nine listed mutations were CAUGHT (5.1 by 2 tests, 5.2 by 3, 5.3 by 6, 5.4 by 2 — including the different-project control, 5.5 by 5, 5.6 by 1, 5.7a and 5.7b by the unresolvable test independently, 5.8 by 1, 5.9 by 1). Three extra mutations were run against the new `CHECK`, which group 5 does not list but the standing rule covers: neutering it to `CHECK (1 = 1)` was CAUGHT by 2 migration tests; deleting the `read:project:` disjunct was CAUGHT by 1. **Deleting the `project_id IS NULL OR` disjunct reddened nothing** — an _equivalent_ mutant, not an uncovered guard: SQLite treats a `CHECK` expression evaluating to `NULL` as satisfied, so with `project_id` NULL the remaining comparisons are already NULL and the row is accepted either way. Measured directly with a control on the same table: `('project:alpha', NULL)` ACCEPTED, `('project:X', 'Y')` REJECTED with `CHECK constraint failed`. The disjunct is kept because it states the intent the `persistence` delta spells out; the two tests that would catch a real change here (`01UNBOUND` in the CHECK test, and the legacy row in "preserves history verbatim") were green on both sides, which is what proves the equivalence rather than a gap.

## 6. Verification

- [x] 6.1 `pnpm run typecheck` clean.
- [x] 6.2 `pnpm run lint` clean.
- [x] 6.3 `pnpm test` fully green, with no test skipped or weakened to accommodate the change. An existing test going red is a finding to report, not a test to adjust. → 2188 passed, 10 pre-existing skips, 0 failures. One existing test did go red and is reported rather than adjusted-away: `db/migrations.test.ts::"rebuilds sessions cleanly when prompts/memory/confirmations reference it"` asserts `result.applied` as an exact ordered inventory of every migration from `0011` onward, so `0029` had to be appended. That is an inventory update, not a weakening — no assertion was relaxed, and the same test now also proves `0029` applies cleanly against a database where `tokens` already has FK children.
- [x] 6.4 Confirm `apps/server/src/test/invariants.test.ts` is unaffected: no SQL moved out of `db/`, no new `admin*` or `unsafe*` call site, no new purge path.
- [x] 6.5 `pnpm run eval` is **not** required and MUST NOT be run as evidence here — no retrieval path, ranking input, or memory read is touched. Stated explicitly so its absence reads as a decision.
- [x] 6.6 Confirm no plugin file needs a matching edit: `grep -rn "project:" apps/plugin/ | grep -v node_modules` shows nothing that mints or parses a token scope. The plugin tree ships to four clients; a needed change there would be one shared resource, not four. → One hit, `apps/plugin/.opencode-plugin/plugin.ts:496` — prose inside a prompt string (`Use project: '<slug>'.`), not a scope. No plugin file changes.
- [x] 6.7 `pnpm run check:spec-provenance` clean.

## 7. Docker smoke against pre-existing seeded data (standing requirement)

Migrations, HTTP and dashboard behaviour all change, so a real container against data that existed **before** the upgrade is mandatory — not the dev stack's fresh reseed. `dev:docker:up` runs `seed-dev --reset` on every boot and would destroy the very population being tested.

> **Not run in this session, by instruction.** The change was implemented in an isolated worktree alongside a second concurrent applier, and the Docker dev stack is a single shared resource across worktrees. Every step below is therefore **NOT RUN**, not passed. The in-process equivalents that _were_ run (`db/migrations-0029.test.ts`: verbatim row preservation, populated `sessions` + `dashboard_sessions` surviving the parent drop with an empty `foreign_key_check`, the exact index set, the `CHECK` and FK behaviour with controls, idempotency on a second boot, and the abort path) cover the same properties against a real migrated SQLite file, but not against a real container, a real image upgrade, or a real pre-existing operator login.

- [ ] 7.1 **Operator-run.** Boot the _previous_ released image against a scratch data directory, log into the dashboard, and mint one token through the **old** form with a project selected and no override — producing a genuine legacy `project:<slug>` row. Also mint an admin `*` token. Record both plaintexts locally; do not paste them into any tracked file.
- [ ] 7.2 **Operator-run.** Stop that container and boot the **new** image against the same data directory. Confirm migration `0029` applies once, the boot banner reports the same `tokens=N` count as before, and the data-loss guard does not trip.
- [ ] 7.3 **Operator-run.** Confirm the legacy row is byte-identical after the upgrade (scope still the slug form, `project_id` still NULL) and is still denied on its own project — with the admin `*` token succeeding on the same endpoint as the control. This is the `auth` delta's "A pre-existing malformed token is still denied after upgrade" and "The upgrade does not rewrite the row", verified on real data rather than in a fixture.
- [ ] 7.4 **Operator-run.** Confirm the legacy row renders in the tokens list with the unresolvable state and a `—` project cell.
- [ ] 7.5 **Operator-run.** Mint a fresh token through the **new** form for the same project with `access = write`, and confirm it is authorized on that project and denied on another. Repeat with `access = read` and confirm the read/write asymmetry.
- [ ] 7.6 **Operator-run.** Confirm the pre-existing `dashboard_sessions` row from step 7.1's login still authenticates after the rebuild — this is the FK-parent drop, proven end to end rather than by `foreign_key_check` alone.
- [ ] 7.7 **Operator-run.** Rollback check: stop the new image, boot the previous one against the same directory, and confirm it starts, authenticates the admin token, and can still mint a token (design's Migration Plan step 4 — the `CHECK` must not be a downgrade barrier). Record the outcome either way.
- [ ] 7.8 Record every result in the commit body with the image tags used. A step not run is recorded as not run, never as passed.

## 8. Close-out

- [ ] 8.1 `/simplify` over the diff, then `/code-review`; resolve findings before archiving. → **Not done.** A manual read-through of the full production diff was performed (and produced one fix: the one-time-view panel reads the minted scope off the persisted row rather than echoing a query parameter). The two review commands themselves are owner-interactive and were left for the owner.
- [x] 8.2 Confirm the release notes name the one breaking surface: a scripted `POST /dashboard/tokens` sending the retired `scope` field now errors instead of minting a token with different reach. Name the substitution (`scope=*` → `access=write`, `scope=read:*` → `access=read`, project reach now comes only from `project`). → Text prepared for the commit's `BREAKING CHANGE:` footer, which release-please renders into the `server` CHANGELOG — the home this repo settled on in `archive/2026-08-02-tell-the-truth-about-unresolvable-scopes/tasks.md:7.6`, and the reason no second copy is written to a tracked file. Note that commit stays `fix(dashboard):` without the `!` marker, which this repo's commitlint rejects. Footer text: _`POST /dashboard/tokens` no longer accepts a `scope` field. It is refused with an error naming `access` rather than minting a token whose reach differs from the request. Substitute `scope=*` with `access=write` and `scope=read:*` with `access=read`; project reach now comes only from the `project` field, which composes with `access` instead of being voided by it. The interactive form is unaffected._
- [x] 8.3 Confirm the release notes also tell operators that pre-existing project-scoped tokens minted from the dashboard were already inert, remain inert by design, and are now visibly marked — and that the remedy is to mint a replacement and revoke the old row. → Second paragraph of the same footer: _Project-scoped tokens minted from the dashboard before this release named their project by slug where the server compares an id, so they authorized nothing on any endpoint. They are left byte-identical — rewriting them would activate a credential nobody has seen work — and now render in `/dashboard/tokens` with the state `inert` and an empty project cell. The remedy is to mint a replacement through the fixed form and revoke the old row._
- [x] 8.4 Deferred by decision, recorded here so it is not lost: whether a token pinned to a project that is later **archived** should keep authorizing. Pre-existing behaviour (`isAuthorized` does not consult `archived_at`), unchanged by this change, but newly visible in the list. Needs its own evidence and its own change.
- [x] 8.5 Rejected by decision, recorded here so it is not re-proposed: resolving a non-id project segment by slug in `isAuthorized` (gives the string two valid readings — the cause of the defect); migrating or revoking existing malformed rows (design D5); branding `ProjectId` (design D3, measured to work but ~10× the churn and it leaves `project_id` NULL); making `tokens.project_id` the sole truth with the scope string derived (design D3, contradicts `openspec/specs/auth/spec.md:41` and leaves `tokens.scope` with two vocabularies).
- [ ] 8.6 Archive via the `sdd-archiver` agent; verify all three delta specs merged into `openspec/specs/{auth,dashboard,persistence}/spec.md` and that the `dashboard` MODIFIED requirement did not drop the two original scenarios. → Not done: the change is not committed, and group 7 is unrun.
- [ ] 8.7 Close #307 and #309, each with the measured before/after from group 1 and group 7. → Not done: group 7's measurements do not exist yet.
