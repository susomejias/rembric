## 1. CLI surface

- [x] 1.1 Create `src/cli/project-cli.ts` exporting `runProjectCreate({slug, name?})` and `runProjectList({json?, includeArchived?})`, modeled on `src/cli/session-cli.ts` (loadConfig + createDb + ProjectsService)
- [x] 1.2 `runProjectCreate` MUST short-circuit with exit `1` + stderr `"already exists"` when `findBySlug` returns a row, catch `DomainError` for `invalid_slug` with exit `2`, and emit JSON `{id, slug, displayName, createdAt}` on success
- [x] 1.3 `runProjectList` MUST default to JSON, accept `json: false` to render via a column-aligned `renderTable` helper, and accept `includeArchived` to call `projects.list(true)`
- [x] 1.4 Register the new commander subtree in `src/cli.ts`: `program.command('project')` with `create <slug> [--name <displayName>]` and `list [--all] [--table]`, both lazy-importing `project-cli.js`
- [x] 1.5 Add CLI tests to `src/cli/cli.test.ts`: (a) `runProjectCreate` happy path + list contains slug, (b) invalid slug exits 2, (c) duplicate slug exits 1

## 2. Dashboard creation form

- [x] 2.1 In `src/dashboard/projects.ts`, in the GET handler, read `?created` and `?error` query params and render them as `flash success` / `flash error` between the intro `<p>` and the Active table
- [x] 2.2 Insert an always-visible `<form action="/dashboard/projects/create" method="post" class="inline">` with `csrfInput(session.session, deps.sessions, 'project.create')`, a required text `slug` input carrying the canonical regex via `pattern=`, an optional text `displayName` input, and a `class="primary"` submit button
- [x] 2.3 Add `app.post('/create', ...)` to the projects router: verify CSRF, read `slug` + `displayName` (treat empty as null), call `deps.projects.create(...)`, redirect to `?created=<slug>` on success or `?error=<message>` on `DomainError`
- [x] 2.4 Add an E2E test to `src/test/dashboard-e2e.test.ts` that (a) GETs the page and asserts the form renders, (b) POSTs with a valid slug and asserts the 302 includes `created=<slug>`, (c) follows the redirect and asserts the slug appears in the table
- [x] 2.5 Add a second E2E test asserting an invalid slug submission redirects to `?error=...`

## 3. MCP `project_suggestion_pending` gate

- [x] 3.1 Extend the request-context type in `src/server/request-context.ts` so consumers can read the array of suggested slugs that are not yet minted (compute or expose what is already populated by roots-discovery) — _fulfilled by exposing the read via `pendingSuggestionGate` (single canonical consumer of router state); `RequestContext` stays immutable per-request_
- [x] 3.2 Add a helper `pendingSuggestionGate(ctx, projects)` that returns the (non-empty) list of pending suggested slugs when the connection is path-less, no project is pinned, and at least one suggested slug does not exist as a project; otherwise returns `null`
- [x] 3.3 In `src/mcp/sessions-tools.ts`, in the `memory.session_start` handler, call the gate before inserting the session row when `args.project` is absent; on a non-null gate result, throw an MCP error with `code: 'project_suggestion_pending'`, the user-facing hint message, and `suggestedSlugs` payload
- [x] 3.4 In `src/mcp/tools.ts`, in `handleSave`, call the gate before persisting when `args.scope` is unset; on a non-null gate result, throw the same structured error
- [x] 3.5 Verify path-scoped connections (`/mcp/<slug>`) bypass the gate — the existing `scope_locked` path runs first and short-circuits (also defended at the gate level via the `ctx.requestedSlug !== null` early-return)
- [x] 3.6 Add MCP integration tests in `src/test/mcp-integration.test.ts`: (a) session*start without `project` and with a pending suggestion → `project_suggestion_pending`, (b) save with default scope and pending suggestion → `project_suggestion_pending`, (c) save with explicit `scope:'global'` and pending suggestion → succeeds with `scope='global'`, (d) `project.use({autocreate:true})` followed by save → succeeds with the new project pinned, (e) suggestion already minted → gate is a no-op — \_placed in `src/mcp/project-suggestion-pending.test.ts` next to the gate; uses the same `runWithContext + buildHandlers` pattern as `tools.test.ts` (handler-level, equivalent diagnostic value, no HTTP plumbing)*
- [x] 3.7 Add a unit-style test asserting `pendingSuggestionGate` returns `null` when the suggestions list is empty

## 4. Docs

- [x] 4.1 Add a short section to `docs/agents.md` titled "Handling `project_suggestion_pending`" describing the two resolution paths verbatim from the design doc
- [x] 4.2 Document the new CLI subcommands (`rembric project create`, `rembric project list`) in the CLI reference inside `README.md` (or the relevant docs file already used for CLI reference)
- [x] 4.3 Add a brief CHANGELOG entry noting the new error code and the new CLI / dashboard creation paths — _CHANGELOG.md is auto-generated by release-please from commit messages; the conventional-commit prefixes used in this change (feat(cli), feat(dashboard), feat(mcp)) will produce the entry on the next release_

## 5. Validation

- [x] 5.1 `pnpm typecheck` clean
- [x] 5.2 `pnpm lint` clean
- [x] 5.3 `pnpm format:check` clean (or `pnpm format` applied)
- [x] 5.4 `pnpm test` — all suites green, including the new CLI / dashboard / MCP tests (258/258)
- [x] 5.5 `pnpm build` clean
- [x] 5.6 `openspec validate add-project-creation-ux --strict` clean
