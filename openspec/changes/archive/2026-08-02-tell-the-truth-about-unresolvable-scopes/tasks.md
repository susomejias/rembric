# Tasks — tell the truth about unresolvable scopes

Read `design.md` first: D1–D7 are cited by number below.

## 1. Error payload plumbing (D2)

- [x] 1.1 Add an optional structured payload to `DomainError` (`apps/server/src/services/errors.ts`) — e.g. `details?: Record<string, unknown>` as a third constructor argument. Additive: no existing `new DomainError(code, message)` call site changes.
- [x] 1.2 Forward it in `errToMcp` (`apps/server/src/mcp/errors.ts:26-29`) so a thrown `DomainError` reaches the client with the same shape a hand-built `mcpError(code, message, extra)` produces. Verify with a unit test in `apps/server/src/mcp/errors.test.ts` that `errToMcp(new DomainError('project_not_found', 'x', { suggestedSlugs: ['a'] }))` yields a body containing `suggestedSlugs`.
- [x] 1.3 Confirm no existing test asserts that `errToMcp` output has exactly three keys (`ok`, `code`, `message`) — grep `errors.test.ts` and `authorization.test.ts` before landing 1.2.
      Verified: neither file contains `Object.keys` or `toEqual({` against an error body; the existing `errToMcp` assertion uses `toMatchObject`, which is not exact-key. A new test now pins the three-key shape for a payload-free `DomainError`, so the addition stays opt-in.

## 2. The resolver refuses (D1)

- [x] 2.1 In `apps/server/src/mcp/_shared.ts`, replace `if (ctx.requestedSlug !== null) return { scope: SCOPE_GLOBAL, project: null };` (`:52`) with a throw of `DomainError('project_not_found', …)`. Reuse the message already shipped at `apps/server/src/mcp/memory-tools.ts:778` verbatim — do not author a second wording.
- [x] 2.2 Attach `suggestedSlugs` from `deps.projects.findSimilarSlugs(ctx.requestedSlug)` when `deps.projects` is present; attach `suggestedSlugs: []` when it is absent. The refusal fires either way — never a global scope (D2, closing paragraph).
- [x] 2.3 Extract the message + payload construction into one exported helper in `_shared.ts` so `handleSave` can call it too, and update `memory-tools.ts:775-780` to use it. This is what gives the existing write-path `project_not_found` its missing `suggestedSlugs[]` (required by `openspec/specs/projects/spec.md:122`).
- [x] 2.4 Update the `resolveEffectiveScope` docstring: source #3 is no longer "global scope when neither source resolves a project" unconditionally. Keep it to the facts — no banner, no restatement of the diff (house comment policy).

## 3. Call sites stop leaking the throw (D3)

- [x] 3.1 Move the `resolveEffectiveScope` call inside the existing `try` in each of the six unguarded handlers: `memory-tools.ts:928` (`handleSearch`), `:1066` (`handleGet`), `:1187` (`handleConfirm`), `:1232` (`handleArchive`), `observability-tools.ts:190` (`handleCapturePassive`), `prompt-tools.ts:103` (`handleSavePrompt`). Each already ends in `return errToMcp(err)`; add no new `try` blocks.
      **DEVIATION — a SEVENTH handler needed fixing: `memory.session_start`.** `design.md` §Context Fact 1 lists 11 resolver call sites in 5 modules and asserts "19 of the 23 registered tools resolve scope (all but `memory.about`, `project.use`, `project.list`, `project.current`)". That is wrong: `handleSessionStart` (`session-tools.ts`) resolves the session's project with its own ladder (`ctx.project?.id ?? router entry ?? null`) and never calls `resolveEffectiveScope`, so the resolver fix does not reach it. Measured before touching it, with a control: on `/mcp/no-such-project` with a `*` token it returned `{"scope":"global","projectId":null}` and inserted an `agent_sessions` row, while the same call on a resolvable slug correctly returned `{"scope":"project","projectId":"…"}`. That is a third write leak, beyond the two `proposal.md` names. Fixed with an explicit guard through the same shared helper, so the wording stays single-sourced. Tasks 9.4 and 5.6 both already required `memory.session_start` to refuse, so the tasks were right and the design's table was incomplete.
- [x] 3.2 In `handleGet` and `handleConfirm` this puts scope resolution ahead of the `provide exactly one of 'id' or 'ids'` check. Confirm no existing test asserts `invalid_input` for a malformed call on a path-scoped connection; if one does, it is asserting the old ordering and must be re-pointed at a resolvable slug.
      Verified: the two `invalid_input` tests (`memory-tools.test.ts:1189,1197`) use `fakeContext(projectA)`, whose `requestedSlug` IS `projectA.slug` and whose `project` is populated — a RESOLVABLE path-scoped connection. They pass unchanged, and a new control test pins that ordering (`unresolvable-slug.test.ts`, "still invalid_input on a resolvable slug").
- [x] 3.3 Confirm the 16 `requireScope` call sites need no change (all already wrapped — see the table in `design.md` §Context). Do not touch them.
      Verified: all 16 are untouched apart from `requireScope` itself now forwarding `deps` into `assertAuthorized` (task 4.3).
- [x] 3.4 Confirm `handleSave` (`memory-tools.ts:786`) still short-circuits before the resolver via the `isPathScoped()` guards at `:764`/`:775`, so its error contract is byte-identical apart from the added `suggestedSlugs[]`.
      Verified. Both guards still precede the resolver call. The `:775` guard now reads `ctx.requestedSlug !== null` instead of `isPathScoped()` — the same predicate by definition (`_shared.ts::isPathScoped` is `getRequestContext().requestedSlug !== null`), narrowed inline so the slug is typed non-null without a cast. Code and message are unchanged; only `suggestedSlugs` is added. `memory-tools.test.ts`'s `scope_locked` / `project_required` / `project_not_found` save assertions all pass untouched.

## 4. Error messages (D5, D6)

- [x] 4.1 Rewrite the `scope_locked` message at `memory-tools.ts:766-771`: name the bound project (still required by `mcp-api` strict isolation), state that user-wide memory is not reachable on this connection (matching `instructions.ts:33`), drop the "open a separate MCP connection at '/mcp'" sentence, and name a remedy the agent or operator can apply.
- [x] 4.2 Add the optional `deps` parameter to `assertAuthorized` (`_shared.ts:91-100`) and append the remedy when: the target scope is global, the check failed, the connection is not path-scoped, and the token is pinned to exactly one project. Name `project.use({slug})` and the token's project slug (fall back to its id when `deps.projects` is unavailable).
      **Decision `design.md` did not settle:** D6 says the pin comes from `getRequestContext()`, "which carries `token.projectId`". It does not, in practice — the only production creation path (`apps/server/src/dashboard/tokens.ts:208`) passes `projectId: null` for every token, including project-scoped ones, so a `token.projectId`-based condition would never fire. The pin is therefore derived from the token SCOPE STRING via a new `pinnedProjectId()` next to `isAuthorized` in `services/tokens.ts` — the same grammar `isAuthorized` already parses, kept in one place rather than duplicated into `mcp/`.
      Noted while there, out of scope and NOT changed: that same dashboard path writes `project:<slug>` into the scope while `isAuthorized` compares the segment against a project **id** (a ULID), so dashboard-minted project-scoped tokens do not authorize their own project. Pre-existing, unrelated to this change; the remedy hint falls back to printing the raw segment when it resolves to no project, so it stays truthful either way.
- [x] 4.3 Pass `deps` at every production call site of `assertAuthorized` that has it: `requireScope` (`_shared.ts:108`), plus the direct callers in `memory-tools.ts`, `observability-tools.ts`, `prompt-tools.ts`, `project-tools.ts`, `session-tools.ts`.
- [x] 4.4 Verify the unchanged half: a `read:*` token denied a write still gets a message with no `project.use` suggestion (there is no pin to activate). This is the control for 4.2 — without it the hint could be appended unconditionally and every test would still pass.

## 5. Tests

- [x] 5.1 **Invert, do not delete**, `describe('KNOWN DEFECT #302 — an unresolvable path slug resolves to the global scope')` at `apps/server/src/mcp/memory-tools.test.ts:1768`. Rename the block, replace `expect(memories.some((m) => m.scope === 'global')).toBe(true)` with an assertion that the response is the structured `project_not_found` error, and rewrite the comment to record what is now guaranteed (its current comment says: "This test SHOULD FAIL once #302 is fixed — invert it there rather than deleting it.").
- [x] 5.2 Add the `memory.get` half the characterization test never covered: a global memory id fetched from `/mcp/<unresolvable>` returns `project_not_found` and no `content`.
- [x] 5.3 Add the two write cases found by probe: `memory.capture_passive` with a well-formed Key Learnings section, and `memory.save_prompt`, each refused on `/mcp/<unresolvable>` with **no row inserted** (assert the `memory` / `prompts` row count is unchanged, not merely that the response is an error).
- [x] 5.4 Add the `suggestedSlugs` assertion: with a project `rembric` present, a tool call on `/mcp/rembic` returns `suggestedSlugs` containing `rembric`.
- [x] 5.5 Add the recovery test: on `/mcp/no-such-project`, `project.current`, `project.list`, `memory.about` and `project.use({slug: 'no-such-project', autocreate: true})` all succeed, and a scope-resolving tool issued **after** that `project.use` operates in the new project's scope. (`authenticate` runs per request — `http.ts:317` — so `ctx.project` is populated on the next call.)
- [x] 5.6 Add the enumerating guard (D4) — drive the registered MCP tool list, invoke every tool on an unresolvable-slug context with a `*` token and minimally valid arguments, and assert each response is the structured `project_not_found` envelope or is on an explicit, commented exemption list (`memory.about`, `project.use`, `project.list`, `project.current`). Place it where the applier judges best; `apps/server/src/test/invariants.test.ts` is the natural home if it can reach the built server, otherwise a dedicated `apps/server/src/mcp/unresolvable-slug.test.ts`.
      Placed in the dedicated `apps/server/src/mcp/unresolvable-slug.test.ts`: the guard needs a live `McpServer` plus every service, which `invariants.test.ts` (a grep/lexical + migration-shape suite) does not construct. It drives a real SDK `Client` over `InMemoryTransport`, so `listTools()` supplies the tool list and every call passes through the registered zod schemas — the boundary a real caller uses, not the handler directly (which is what made `memory.save` look like a leak when probed in isolation). Its first assertion is `advertised === classified`, so a tool registered later fails the test until it is put on the refusal table or the exemption list. 23 tools: 19 refused, 4 exempt.
- [x] 5.7 Add the message tests: `scope_locked` contains the slug, does not match `/separate MCP connection|second connection/`, and states user-wide memory is unreachable; the project-pinned `forbidden` message names `project.use` and the slug; the `read:*` control message does not.
- [x] 5.8 Add a cross-surface test asserting the `scope_locked` message and the path-scoped `buildInstructions` variant do not disagree about whether user-wide memory is reachable.
- [x] 5.9 Re-run the pre-existing scope tests untouched: `apps/server/src/mcp/authorization.test.ts`, `session-scope-resolution.test.ts`, `project-suggestion-pending.test.ts`, `project-suggestion-gate.test.ts`, `roots-discovery.test.ts`. Any failure here is a regression in resolvable-slug behaviour, not an expected update.
      All five green (72 tests). **One test in `session-scope-resolution.test.ts` DID have to be updated** — `'ctx.requestedSlug set + ctx.project null → returns global, ignores router pin'` (`:314`). It is a second characterization of the same defect: its stated intent ("MUST NOT fall back to the stale router entry — that would silently leak data from a different project") is now served more strongly by the refusal, but it asserted the global fallback as the mechanism (`expect(payload.scope).toBe('global')`). Re-pointed at `project_not_found`, keeping the no-leak assertion. This is NOT the failure mode 5.9 warns about: the pre-existing test for resolvable-slug behaviour immediately below it (`'ctx.project set (path-scoped, valid slug) → uses ctx.project regardless of router'`) is the control and passes untouched. Its module docstring's "3. SCOPE_GLOBAL → no resolution" was corrected in the same pass.

## 6. Mutation check (a guard is not covered until its test fails without it)

- [x] 6.1 Mutate the refusal away and confirm the tests naming it go red:
      `node scripts/mutate.mjs --file apps/server/src/mcp/_shared.ts --spec src/mcp/memory-tools.test.ts --mutation '<the throw statement>' --with 'return { scope: SCOPE_GLOBAL, project: null };'`
      Expected: 5.1–5.3 and 5.6 red. A green run means the guard is untested — that is the finding, not a pass.
      **CAUGHT.** `--spec src/mcp/unresolvable-slug.test.ts` → caught by 23 (5.2 both `memory.get` cases, both 5.3 write cases, both 5.4 `suggestedSlugs` cases, and 17 of the 19 enumerated tools). `--spec src/mcp/memory-tools.test.ts` → caught by 1 (the inverted 5.1). `memory.save` and `memory.session_start` are correctly absent from the caught set: each has its own explicit pre-resolver guard, so this mutation cannot reach them — 6.3 catches both instead.
- [x] 6.2 Mutate the condition rather than the body — weaken `ctx.requestedSlug !== null` to `false` — and confirm the same tests go red, so the test is pinned to the condition and not merely to the presence of a throw.
      **CAUGHT by the same 23.** (`--with 'if (false) throw unresolvableSlugError(ctx.requestedSlug ?? "", deps.projects);'` — the `?? ""` only keeps it compiling; the branch is dead.)
- [x] 6.3 Mutate the `errToMcp` payload forwarding away (D2) and confirm 5.4 goes red. Without this the `suggestedSlugs` requirement is claimed but unproven.
      **CAUGHT by 21** — both 5.4 cases plus all 19 enumerated tools (each asserts `suggestedSlugs` is an array). This is the mutation that covers `memory.save` and `memory.session_start`, whose refusals route their payload through `errToMcp` rather than the resolver.
- [x] 6.4 Mutate the `assertAuthorized` remedy condition (drop the "token pinned to exactly one project" clause) and confirm 4.4's control goes red — otherwise the hint is being appended unconditionally and nothing notices.
      **CAUGHT by 1** — 4.4's `read:*` control. Per the house rule, the other two clauses were then mutated separately:

      - drop `ctx.requestedSlug !== null` (path-less) → **NOT CAUGHT initially.** Fixed by strengthening, not by dropping: added a `project.current` case on `/mcp/<unresolvable>` with a project-pinned token, where `project.use({slug})` is rejected outright by `project-tools.ts` whenever the slug differs from the path slug, so the hint would name an unreachable remedy. Re-run → **CAUGHT by 1**.
      - drop `scope.kind !== 'global'` (global target) → **NOT CAUGHT initially**, and the first attempt at a control did not discriminate either (a path-scoped connection short-circuits on the other clause, so the mutant behaved identically). Fixed with a path-LESS connection carrying a router pin to project A and a token pinned to project B: the denial names B's project target, and activating A would not make it succeed. Re-run → **CAUGHT by 1**.

- [x] 6.5 Record each mutation's outcome in the PR description. A mutation that reddens nothing must be resolved by strengthening the test, never by dropping the mutation.
      Six mutations run, all recorded above. Two initially reddened nothing; both were resolved by adding a discriminating test, neither by weakening or dropping a mutation.

## 7. Specs and docs

- [x] 7.1 Apply the `mcp-api` delta (`specs/mcp-api/spec.md` in this change). It modifies four requirements and adds one; the `include_global` modification exists solely to delete the caveat at `openspec/specs/mcp-api/spec.md:2178` — verify that sentence is gone after the archive merge.
      The merge itself is the archive phase's step; `check:spec-provenance` correctly requires this change to be archived before `openspec/specs/` moves, so `openspec/specs/mcp-api/spec.md` is deliberately untouched here. Pre-flighted for a clean merge: all four MODIFIED `### Requirement:` headers match the live spec byte-for-byte and appear exactly once each; the ADDED header appears zero times. Confirmed the delta's replacement text for the `include_global` requirement drops the whole `:2178` caveat ("A path slug that does not resolve … until it is fixed, the isolation guarantees above hold for a path-scoped connection whose slug resolves"), and that `:47`'s `projects.findOrCreate(slug)` plus `:52`'s "creating it if needed" are both replaced. **Archiver must re-verify the `:2178` sentence is gone post-merge.**
      **The delta was amended during apply** (see 3.1's deviation): the shared-resolver requirement asserted "The shared resolver SHALL be the single place that decides an unresolvable path slug is unusable", which the implementation makes false — `memory.save` (pre-existing) and `memory.session_start` (this change) must guard before a scope exists to assert against. Merging that sentence would have published a statement the code contradicts on day one. Replaced with the accurate obligation (same behaviour, plus "every such site SHALL build its message and `suggestedSlugs[]` from one shared constructor"), and a `memory.session_start` scenario was added to the strict-isolation requirement.
- [x] 7.2 Confirm no `projects`, `memory`, `http-api`, `sessions` or `auth` delta is needed (reasons in `proposal.md` §Capabilities). If implementation reveals one, stop and amend the proposal rather than editing `openspec/specs/` directly.
      Confirmed, and re-checked against the `session_start` deviation specifically, since that is the one place implementation moved outside the proposal's file list. `sessions/spec.md:273` governs a different message (`session-tools.ts:139`, the mismatched-`project`-arg `scope_locked`), which is untouched. `proposal.md` §Capabilities already pre-reasoned this exact case: "`memory.session_start` on an unresolvable slug does change (it will refuse instead of creating a global session), but that is the shared-resolver requirement in `mcp-api`, not a sessions-contract change." No `openspec/specs/` file was edited.
- [x] 7.3 `docs/troubleshooting.md:124` — "Open a second connection at `/mcp` for user-wide writes." is the retracted claim; correct it.
- [x] 7.4 `docs/agents.md:19` — extend the `/mcp/<slug>` bullet to say an unresolvable slug is refused rather than silently global.
- [x] 7.5 Optional (Open Question 1): add a `code: project_not_found` section to `docs/troubleshooting.md` naming the three remedies.
      Taken. The failure mode is reachable through the documented setup (a hand-authored, often committed `.rembric`) and produces one error code across every memory tool, which is exactly what a troubleshooting entry is for. Added with the three remedies, the `suggestedSlugs[]` hint, the four tools that keep working, and the committed-`.rembric` cause.
- [x] 7.6 Draft the release-note line for the behaviour transition: a deployment whose `.rembric` names a non-existent slug goes from silently reading/writing user-wide memory to `project_not_found`, with the three remedies. This is the one user-visible change and it must not ship unannounced.
      Done, but NOT as a hand-written draft here. The transition is announced by the `BREAKING CHANGE:` footer on the fix commit, which release-please renders into the `server` CHANGELOG on its own — a second copy in this file would only rot. Operator-facing detail (the three remedies, `suggestedSlugs[]`, the committed-`.rembric` cause) lives in `docs/troubleshooting.md` → `code: project_not_found`.
      Note the commit says `fix(mcp):`, not `fix(mcp)!`: this repo's commitlint rejects the `!` marker outright (`fix(mcp)!: x` alone fails `subject-empty`/`type-empty`), so the footer is the only way to mark a release breaking here.

## 8. Verification

- [x] 8.1 `pnpm run typecheck` — clean (`tsc --noEmit`, exit 0).
- [x] 8.2 `pnpm run lint` — clean (`eslint .`, exit 0, no output).
- [x] 8.3 `pnpm test` — full suite, including `apps/server/src/test/invariants.test.ts`.
      Green. Root `pnpm test`: `Test Files 120 passed | 1 skipped (121)`, `Tests 2171 passed | 10 skipped (2181)`, plus the Hermes python suite `Ran 72 tests … OK`. `apps/server` `pnpm vitest run` (the superset, which also collects `../plugin/test/*`): `Test Files 122 passed | 1 skipped (123)`, `Tests 2237 passed | 10 skipped (2247)` — +1 file and +40 tests vs. the pre-change baseline of 122 files / 2207 tests. `invariants.test.ts` passes and was NOT edited by this change (its +78 lines are pre-existing `includeGlobal` work for a different issue; verified its diff contains zero occurrences of `unresolvable` / `project_not_found` / `suggestedSlugs`).
- [x] 8.4 `pnpm run eval` is **not required**: no retrieval ranking, scoring, embedding or query construction is touched, and the harness runs against resolvable scopes. Run it once anyway as a no-regression control and record that the score is unchanged; if it moves, something outside this change's stated blast radius moved with it.
      Run on BOTH sides rather than asserted, since "unchanged" needs a before figure: the nine source files were stashed, eval run, then restored (`typecheck` + the new suite re-run green afterwards to confirm the restore). Byte-identical on both sides:
      `hybrid P@8=0.156 R@8=1.000 MRR@8=0.783 tokens=412 abstainFP=1.00 overAbstain=0.00`
      `grep P@8=0.148 R@8=0.938 MRR@8=0.710 tokens=490 abstainFP=1.00 overAbstain=0.00`
      `memory-md-dump P@8=0.094 R@8=0.656 MRR@8=0.295 tokens=516 abstainFP=1.00 overAbstain=0.00`
      Consistent with the diff: no file under `services/memory.ts`, `embeddings/`, or `db/repositories/memory-repository.ts` is touched.

## 9. Docker smoke against pre-existing seeded data (mandatory — MCP + production behaviour)

- [x] 9.1 Bring up the dev stack: `pnpm run dev:docker:up`. Note it wipes and reseeds (`seed-dev --reset`) on every boot — take the smoke against the seeded corpus, and do not run it against a corpus you care about. `chown -R 10001:10001 data-dev` first if the container dies with `SQLITE_CANTOPEN`.
      Up and healthy on `127.0.0.1:8788`. `chown -R 10001:10001 data-dev` applied first. Mounts verified per the smoke skill's §2 (the compose project name is global across worktrees, so this is the only way to catch attaching to another stack's container): `/root/rembric/data-dev -> /data`, `/root/rembric/apps/server/src -> /app/apps/server/src`. Also verified the fix is the code actually running, not a stale image — `docker exec rembric-dev grep -n unresolvableSlugError /app/apps/server/src/mcp/_shared.ts` returns the throw at `:69`.
- [x] 9.2 Record the seeded baseline before probing: the seeded project slug(s) and the count of `scope='global'` memories, from `/dashboard/memories`. The before/after global count is what proves no write leaked.
      Seeded slug: `demo` (the only project; `DEMO_SLUG` in `seed-dev.ts`). Baseline read host-side off the bind-mounted `data-dev/data.db` (the container ships no `sqlite3`): 35 `scope='project'` memories, **0 `scope='global'`**, 0 prompts, 0 global-scope sessions.
      **The seeded corpus has no global memories at all, which would have made 9.5 vacuous** ("a before/after match over an empty set proves nothing"). Seeded one deliberately first, through the legitimate path — `memory.save({scope:'global'})` on the path-LESS `/mcp` — giving a working baseline of 1 global memory plus a concrete id to probe `memory.get` with.
- [x] 9.3 **Control (must pass):** connect an MCP client at `/mcp/<a real seeded slug>` with the seeded token and confirm `memory.search` returns that project's rows and `memory.capture_passive` writes into that project. A probe without this cannot distinguish a real fix from a broken connection.
      PASS. `/mcp/demo`: `memory.search` → 6 rows, 0 of them `scope='global'`; `memory.capture_passive` → `saved=2`, and the project count moved 35 → 37 while the global count stayed at 1.
- [x] 9.4 **Probe:** connect at `/mcp/<slug-that-does-not-exist>` with the same token and confirm every one of `memory.search`, `memory.get(<a global memory id from 9.2>)`, `memory.context`, `memory.capture_passive`, `memory.save_prompt`, `memory.session_start` returns `isError: true` with `code: 'project_not_found'` and a `suggestedSlugs` array.
      All six PASS on `/mcp/no-such-project-xyz` with the `*` admin token: `isError=true`, `code='project_not_found'`, `suggestedSlugs` present as an array. Also asserted the `memory.get` refusal body contains none of the global memory's content.
      `suggestedSlugs` came back `[]` there — correct, since `no-such-project-xyz` is beyond `findSimilarSlugs`'s max edit distance from `demo`, but it means that run alone does not prove the field ever populates. Re-probed with near-misses: `/mcp/demoo` → `["demo"]`, `/mcp/dem` → `["demo"]`, and the resolvable control `/mcp/demo` → succeeds with 6 rows.
- [x] 9.5 Re-read the global memory count from 9.2 and assert it is **unchanged** after 9.4. Assert it is non-zero at both ends — a before/after match over an empty set proves nothing.
      PASS, non-zero at both ends: global memories `1 -> 1`, prompts `0 -> 0`, global-scope sessions `0 -> 0` across the whole six-call probe. The pre-fix behaviour would have moved all three (capture_passive +2 memories, save_prompt +1 prompt, session_start +1 session).
- [x] 9.6 **Recovery:** from the same unresolvable connection call `project.use({slug: <the same slug>, autocreate: true})`, then re-issue `memory.search`, and confirm it now succeeds in the new (empty) project rather than refusing.
      PASS on `/mcp/recoverable-smoke-slug`: refuses first (`project_not_found`), then `project.current` / `project.list` / `memory.about` all succeed on the refusing connection, then `project.use({slug, autocreate: true})` → `created: true`, then `memory.search` succeeds with 0 rows (the new empty project) instead of refusing.
- [x] 9.7 Confirm the boot itself is clean on the populated DB: no migration runs (this change adds none), `memory.doctor` reports `integrity: ok`, and `/dashboard` renders. Then tear the stack down.
      PASS. 29 migration files on disk, 29 rows in `_migrations` — nothing pending, and this change adds none. Restarted the container against the already-migrated DB: healthy in ~15s, still exactly 29 applied, zero `[error]` / `FOREIGN KEY` / `unhandled` lines in the logs. `memory.doctor` → `db.integrity='ok'`, `warnings: []`. `/dashboard/login` → 200, `/dashboard` unauthenticated → 302 redirect, `/healthz` → `{"ok":true,"version":"0.25.2"}`. Torn down with `docker compose -f docker-compose.yml -f docker-compose.dev.yml down --remove-orphans`.
- [x] 9.8 Probe the real client path once, not just the handler: run the smoke through `rembric-bridge.mjs` with a `.rembric` naming a non-existent slug, so the measurement covers the zod schemas and the transport the shipped plugin actually uses.
      PASS, through the shipped path end to end: `rembric-bridge.mjs` → `npx mcp-remote` → stdio → HTTP → the server, driven by a real SDK `Client`. With `.rembric` = `PROJECT_SLUG=demoo` the bridge path-scopes to `/mcp/demoo` and `memory.search`, `memory.save` and `memory.capture_passive` all return `isError=true`, `code='project_not_found'`, `suggestedSlugs=["demo"]`, while `project.list` stays reachable. **Control on the same bridge and token with `.rembric` = `PROJECT_SLUG=demo`: all four calls succeed** — so the refusal is the slug, not a broken bridge. This is the run that covers the zod schemas, and it is why the "omitted scope writes to global" reading of `memory.save` was a probe artifact: no real caller can bypass `memorySaveSchema`'s `.default('project')`.

## 10. Deferred / explicitly rejected (do not silently lose)

- [x] 10.1 **Rejected here:** resolving a project-pinned token on path-less `/mcp` to its own project (issue #302's primary proposal). Reasons in `design.md` §Rejected. If it is revived, it needs its own change: it moves the `project_required` contract and interacts with `pendingSuggestionGate`.
      Honoured: no behaviour change. A project-pinned token on path-less `/mcp` still resolves to global and is still denied with `code: 'forbidden'` — only the message gained the remedy (D6, #302's own "Alternative, cheaper"). Asserted by test, including the `read:*` control that no hint appears where there is no pin.
- [x] 10.2 **Rejected here:** implementing `projects.findOrCreate` to make `mcp-api:47` true. Reasons in D7 — it would mint projects from URLs before any write authorization, and it contradicts `projects/spec.md:113`.
      Honoured: `grep -rn findOrCreate apps/server/src` → still zero hits. The delta corrects the spec to `findBySlug` instead, and the "creating it if needed" scenario with it. Verified on the real stack that a path slug never mints a row: after probing `/mcp/no-such-project-xyz` and `/mcp/demoo`, `projects` still held only `demo`.
- [x] 10.3 **Left open:** closing the gap that lets `.rembric` name a slug no project has (a bridge pre-flight or an installer step). Open Question 2. Four-client plugin change, out of scope; does not reduce the server's obligation to refuse.
      Left open as specified — `apps/plugin/` is untouched (`git status` shows no file under it modified). The 9.8 bridge probe measured the gap without closing it: the bridge path-scopes to whatever `.rembric` says and does not interpret the error code, so the operator sees a clear `project_not_found` with `suggestedSlugs` rather than a silent widening. `docs/troubleshooting.md` now names the committed-`.rembric` cause, which is the cheap mitigation until the question is decided.
- [x] 10.4 **Not attempted:** re-scoping global rows a previously-leaking connection already wrote. Append-only forbids it and no heuristic separates them from legitimate user-wide memories.
      Not attempted. The diff contains no `DELETE`, no `UPDATE` of `content`, and no write to `scope` — `invariants.test.ts`'s append-only checks pass unchanged. The release-note draft (7.6) tells operators where to find such rows (`scope=global` + capture window in the dashboard) rather than moving them.
