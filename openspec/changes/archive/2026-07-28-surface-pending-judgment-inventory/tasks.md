## 0. Before-picture, measured

- [x] 0.1 Reproduce the blind spot against a real instance, not a fixture: record `memory.doctor`'s `review.pendingJudgments`, then `memory.context`'s `pendingJudgments.length`, then judge every surfaced pair and re-read both. The observation this change exists for is that the second number reaches 0 while the first is still large. Record all four numbers. → `measurements/before.md` §0.1. Doctor 52 · stats total 0 · context list 0. The judge-and-re-read loop is the run already recorded in memory `01KYMP1X83872XZRMFYRE9RJXH` (52 → 5,5,5,0 → doctor 37) and is not repeated, because repeating it means judging real pairs for a measurement already taken.
- [x] 0.2 Confirm `countPendingInScope` returns the same number `memory.doctor` reports, for the same scope. If it does not, the discrepancy is the real defect and this change is premature. → **It does not, and the reason is by design.** `memory.doctor` reports `adminCountByStatus('pending')`, server-wide (`bootstrap.ts:563`, a documented spec exception); `memory.stats` reports the scoped `countPendingInScope`. Full argument for why the change is nevertheless not premature — the gap is structural, provable from `relations-repository.ts:376` + `memory-tools.ts:1299-1306` without reference to any count — in `measurements/before.md` §0.2. The naming collision is carried to §6.3 rather than silently absorbed.
- [x] 0.3 Record the exact line numbers of the asymmetry so review can see it in one look: where `pendingJudgments` is declared and computed, and where `needsReviewTotal` is declared and computed. → `measurements/before.md` §0.3.

## 1. The total

- [x] 1.1 Add `pendingJudgmentsTotal` to `memory.context`'s output schema and return `countPendingInScope(scope)`. Do NOT derive it from the returned array's length — that is the misleading number this change corrects. → `memory-tools.ts` (`contextOutput`, `handleContext`).
- [x] 1.2 Test that the total is the FULL scoped count while the list is a page: seed more pending pairs than the page size and assert `pendingJudgments.length < pendingJudgmentsTotal`. **Mutate it** — return `pendingJudgments.length` as the total and confirm the test fails, because a test that only checks the field exists would pass against exactly the bug being fixed. → `mcp/context-pending-judgments.test.ts`. Mutation `pendingJudgmentsTotal = pendingJudgments.length` run: **4 tests fail**.
- [x] 1.3 Assert the total is SCOPED: a pending pair in another project must not raise it. Cross-scope leakage in a count is the failure mode this repo's scope invariant exists for. → two tests, one per mode (default and `judgments: 50`).

## 2. The size, and the age filter

- [x] 2.1 Add `judgments` to the input schema, clamped like its siblings. Choose the clamp against the sibling maxima (100 / 50 / 25) and record WHY the chosen value, not just the value — Open Question 1. → **50**, matching `prompts`. A `pendingJudgments[]` entry carries two `CONTEXT_SNIPPET_CHARS` snippets plus two titles, so it costs about twice a `recentMemories` row: 50 entries land near what `memories: 100` — the largest sibling budget — already buys, so the ceiling adds no new worst case to the session-start path. It also clears the low tens, which the 52-deep queue in `measurements/before.md` rules out as a bound. Recorded at the constant (`PENDING_JUDGMENTS_MAX`).
- [x] 2.2 Parameterise `listPendingOlderThanInScope` so the cutoff can be skipped, rather than adding a sibling method. One predicate, two modes; the `sessionHasContent` precedent. → renamed to `listPendingInScope` with `cutoffMs: number | null`; the name no longer claims a filter it may not apply, and there is still exactly one SQL builder.
- [x] 2.3 Wire it: no `judgments` → current behaviour byte-for-byte, including the age filter and the current page size. With `judgments: N` → up to N pairs, age filter lifted. → the omitted-argument path still passes `cutoffMs: pendingCutoff` and `limit: 5` (`PENDING_JUDGMENTS_DEFAULT`).
- [x] 2.4 Test the default is UNCHANGED. Seed a pair younger than the cutoff and assert a default call does not return it — otherwise this change silently makes the session-start channel noisier, which is the thing D2 rejects.
- [x] 2.5 Test that `judgments: N` DOES return the young pair. **Mutate** the cutoff-skip and confirm this fails; without it the parameter would look implemented while changing nothing observable. → mutation `cutoffMs: pendingCutoff` (always filtered) run: **5 tests fail**.
- [x] 2.6 Test the clamp holds, and that asking for more than exists returns what exists rather than erroring. → plus `judgments: 0` (empty list, total still reported) and `judgments: 999` (clamped, `clamped: true`, not rejected).
- [x] 2.7 Confirm the returned pairs still carry both endpoints' content and both titles, so a caller can actually JUDGE from the response without a second read. A list of ids would be a worse surface than the aged channel it extends. → asserted on an un-aged row specifically, since that is the row the aged path never produced.

## 3. Description

- [x] 3.1 Document the parameter AND the total in `memory.context`'s description. State that a size lifts the age filter — a caller cannot guess that. → `server.ts`; backed by a `tools/list` assertion in `mcp-integration.test.ts` so the clause cannot be dropped silently, and by a scenario in the delta.
- [x] 3.2 Measure the description's length against `DESCRIPTION_MAX_LENGTH` and record it with the remaining headroom. If it does not fit, cut prose from that description; do NOT raise the constant. → **1231 / 1900 characters, 669 of headroom.** The constant is untouched.
- [x] 3.3 Do NOT touch `instructions.ts`. It already says `resolve candidates[] with memory.judge`, and it sits at 965/1000 — spending its headroom on a lever this change does not measure is out of scope (proposal, Not in scope). → not touched.

## 4. Specs

- [x] 4.1 Apply the `mcp-api` delta. Verify the scenario count against the published requirement BEFORE archiving; the last two changes each had the archiver catch dropped scenarios. → the change shipped with an EMPTY `specs/mcp-api/` directory, so the delta was authored here rather than applied. It MODIFIES "The MCP server MUST expose three research tools", copied whole: the published requirement carries **16** scenarios, the delta carries **22** — the 16 originals (two reworded: the bootstrap snapshot, and "exposes only aged pendings" now qualified with "by default") plus 6 added. `openspec validate --strict` green.
- [x] 4.2 Grep `openspec/specs/` for `pendingJudgments` and reconcile every hit — `consolidation` and `sessions` both mention it, and either may describe the channel in terms this change makes false. → 9 hits across three files, none made false. `consolidation/spec.md:122` ("after `JUDGMENT_ORPHAN_AFTER_MS` the pending relation SHALL appear in `pendingJudgments[]`") still holds: the default channel is unchanged, and the change only ADDS a second way to reach a row before that point. `sessions/spec.md:757-764` (every context text field through one `snippet()` bound) still holds: inventory rows go through the same `.map()`. `mcp-api/spec.md:255,264-265` (source/target titles on every entry) still holds and is now re-asserted for un-aged rows by task 2.7. `mcp-api/spec.md:653,659` (`memory.doctor`'s server-wide `review.pendingJudgments`, `memory.stats`'s scoped `pendingJudgmentsTotal`) are untouched — see §6.3.

## 5. Verify

- [x] 5.1 `pnpm run typecheck`, `pnpm run lint`, full suite. → typecheck clean, eslint clean, prettier clean; **1843 passed / 10 skipped across 113 files**.
- [x] 5.2 Drive it against a real instance and finish the job the before-picture could not: read the total, ask for that many, judge them, and confirm both the list and the total reach 0. That is the acceptance test for this change, and it is the thing that was impossible on 2026-07-28. → run against `pnpm run dev:docker:up`; transcript in `measurements/acceptance.md`. Run on the dev stack rather than the author's instance because that instance's `rembric` scope now holds 0 pending pairs (`measurements/before.md` §0.2), so it cannot exercise a drain.
- [x] 5.3 Confirm no client changed: `git diff --stat apps/plugin/` empty. → empty.

## 6. Deferred, recorded

- [ ] 6.1 The accumulation RATE is not addressed: up to five candidates per `memory.save`, closed five at a time. Drainability without a rate change means the backlog returns. The two levers are a lower fan-out or a harder save-time instruction; both change model behaviour and need their own before/after evidence.
- [ ] 6.2 Whether the default page size should change now that a total is visible — Open Question 2. Left at 5: with the total beside it the page size is no longer load-bearing as a signal, and raising it would charge every session start for an inventory only some callers want.
- [ ] 6.3 `memory.doctor.review.pendingJudgments` is server-wide while `memory.stats.pendingJudgmentsTotal` is scoped, and both are printed as "pending judgments". The split is deliberate and specified (`mcp-api/spec.md:653`, `bootstrap.ts:555-557`), but it misled this change's own author into reading a cross-scope number as a scoped backlog (`measurements/before.md` §0.2). That is evidence about the LABELLING, not the queue, so it belongs to its own change — the candidate fix is naming, not re-scoping the doctor.
