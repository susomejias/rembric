## 1. Preserve the working tree and read the contract

- [x] 1.1 Confirm the pre-existing, unrelated local modification in `apps/plugin/scripts/stop-nudge.sh` (whitespace-only re-indentation of the `case "$COUNT"` arms, around `:45-47`) is still present, and do NOT revert it, re-indent it back, or describe it as part of this change; `git diff apps/plugin/scripts/stop-nudge.sh` must end up showing that hunk plus your guard hunk and nothing else.
- [x] 1.2 Read both delta specs in this change folder before editing code — `specs/plugin-session-protocol/spec.md` renames the requirement and adds three scenarios, `specs/claude-code-plugin/spec.md` retracts the "cannot hold a turn open" justification. The code must satisfy them, not the other way round.
- [x] 1.3 Record the baseline: `cd apps/server && pnpm vitest run ../plugin/test/stop-nudge.test.ts` passes with 16 tests before any edit.

## 2. Shared loop-guard extractor in `_api.sh`

- [x] 2.1 Add `rembric_stop_hook_active_from_stdin_json` to `apps/plugin/scripts/_api.sh`, beside the other stdin extractors (after `rembric_transcript_path_from_stdin_json`, `:254-259`). Body: return early on empty input; when `jq` is on `PATH`, `jq -r 'if .stop_hook_active == true then "true" elif .stop_hook_active == false then "false" else "" end'`; otherwise `sed -n 's/.*"stop_hook_active"[[:space:]]*:[[:space:]]*\(true\|false\).*/\1/p' | head -n1`. This mirrors `rembric_session_ensure`'s `.created` handling at `:131-135` — do not invent a different shape.
- [x] 2.2 At most ONE comment line on the function, stating the non-obvious fact only: callers MUST treat an empty result as `false`. No banner, no restatement of the signature, no reference to this change or issue.
- [x] 2.3 Verify the extractor against nine payloads, once with `jq` present and once with a `PATH` that has no `jq`, and confirm both arms agree: `stop_hook_active:true` → `true`; `false` → `false`; key absent → empty; `null` → empty; unparseable stdin → empty; empty stdin → empty; and the three adversarial payloads whose `last_assistant_message` quotes the flag with the opposite value → the TOP-LEVEL value in every case.
- [x] 2.4 Do NOT add a Codex- or Claude-specific variant, and do not touch `_transcript.sh`. One helper serves both `Stop`-registering clients (`plugin-session-protocol` D4 in `design.md`).

## 3. The guard in `stop-nudge.sh`

- [x] 3.1 Insert the guard as a single line immediately after the `_emit_nothing` function definition closes (`:34`) and BEFORE the configuration gate comment at `:36`: `[ "$(rembric_stop_hook_active_from_stdin_json "$INPUT")" = "true" ] && _emit_nothing`. The `[ cond ] && _emit_nothing` idiom is already proven safe in this file under `set -u` + `trap 'exit 0' ERR` (`:61`, `:68`).
- [x] 3.2 At most one comment line above it, naming the host fact that is invisible from the code: the host delivers this hook's `additionalContext` by continuing the turn, so firing again on that continuation is the loop. Rationale belongs in the spec, not here.
- [x] 3.3 Nothing else in the script moves: the config gate, the counter peek, the transcript resolution, the 32 MiB ceiling, `RBR_NUDGE_MAX_FACTS_CHARS`, the tail-keeping slice and the emitted `hookSpecificOutput` shape are all unchanged.
- [x] 3.4 `bash -n apps/plugin/scripts/stop-nudge.sh` and `bash -n apps/plugin/scripts/_api.sh` are clean, and `shellcheck` reports nothing new for either file.

## 4. Tests, with controls and a mutation gate

- [x] 4.1 Extend the `stdin()` helper in `apps/plugin/test/stop-nudge.test.ts` (`:91-96`) to accept an optional `stopHookActive` argument that is omitted from the payload when not supplied — the "field absent" case must stay reachable.
- [x] 4.2 Add: firing turn (via the existing `advanceToFiringTurn`, which drives the real `prompt-nudge.sh`) with `stop_hook_active: true` → `''` for the default client, and `'{}'` for `codex-cli`.
- [x] 4.3 Add the two controls in the same file, so a green guard test cannot be a broken probe: the same firing turn with `stop_hook_active: false` → still emits the reminder; with the field ABSENT → still emits the reminder.
- [x] 4.4 Add a source-order assertion: in `apps/plugin/scripts/stop-nudge.sh`, the index of the guard line is lower than the index of `rembric_transcript_path_from_stdin_json` and of `rembric_session_facts_raw`. This is what stops a later edit putting the guard behind the parse the host waits on.
- [x] 4.5 Reword the comment on the existing `never emits an interrupting decision` test (`:114`, "this must never be able to hold a turn open") — its premise is measured false. Keep every assertion in that test unchanged (`"decision"`, `block`, `"continue"` are still never emitted).
- [x] 4.6 `cd apps/server && pnpm vitest run ../plugin/test/stop-nudge.test.ts` passes with the new count (16 baseline + the tests added above). Actual: 21/21 (16 baseline + 5 new).
- [x] 4.7 Mutation gate — the guard is not covered until this reddens: `node scripts/mutate.mjs --file apps/plugin/scripts/stop-nudge.sh --spec ../plugin/test/stop-nudge.test.ts --mutation '[ "$(rembric_stop_hook_active_from_stdin_json "$INPUT")" = "true" ] && _emit_nothing' --with ''`. It must report the mutation as red; a green run means the tests prove nothing and 4.2/4.3 have to be fixed, not the mutation. RED, caught by 2: `yields once the host reports the turn was already continued to satisfy it` and `decides the loop guard before it resolves or parses the transcript`. File restored byte-identically.
- [x] 4.8 Second mutation, to prove the fail-open direction is actually asserted: invert the comparison (`= "true"` → `!= "true"`) and confirm the controls in 4.3 go red rather than only the guard test. RED, caught by 8 (including both 4.3 controls: `control: the same firing turn with stop_hook_active false still fires` and `control: the same firing turn with the flag absent still fires`). File restored byte-identically.

Deviation note (honest, evidence-based): the `'{}' for codex-cli` half of 4.2 (`yields for codex-cli too, emitting the empty object it always requires`) does NOT provide unique mutation coverage of the guard. Measured: `_rembric_facts_raw_codex_cli` does not exist in `_transcript.sh` (only `_rembric_facts_raw_claude_code` is defined; dispatch is by `declare -F "_rembric_facts_raw_${parser}"`), so `rembric_session_facts_raw` always returns empty for the `codex_cli` parser and `stop-nudge.sh` already reaches its own `_emit_nothing` at the "empty RAW" check regardless of the guard. Verified against the pristine (pre-change, `HEAD`) script: run as `codex-cli`, at a firing cadence turn, with a real tool-using transcript and NO `stop_hook_active` field at all, it already emits `{}` today. The test is kept because it correctly documents the real output contract (matches the spec scenario's "or exactly `{}` on the client whose host requires a JSON object"), but the mutation gates above are caught entirely by the default-client and source-order tests — this is a pre-existing, out-of-scope gap (no Codex facts extractor), not a defect introduced or masked by this change.

## 5. Documentation

- [x] 5.1 Add ONE bullet to `.agents/skills/rembric-plugin-development/references/per-client-gotchas.md` under "All shell-hook clients (Claude + Codex)": a `Stop` hook's `hookSpecificOutput.additionalContext` is delivered by CONTINUING the turn on Claude Code (measured 2.1.232), both hosts send `stop_hook_active` in the `Stop` payload, and any model-facing `Stop` output MUST return silence while that flag is true.
- [x] 5.2 Do NOT name or document the host's block-cap environment variable anywhere — it is a host-side workaround that outlives this bug, and documenting it invites operators to hide the next one.
- [x] 5.3 No change to `docs/agents.md`, `README.md`, or any installer surface: nothing about install, distribution or credentials is involved.

## 6. Focused verification

- [x] 6.1 `pnpm run typecheck` and `pnpm run lint` clean (the changed `.ts` test file is in scope; the shell scripts are not). Both clean.
- [x] 6.2 `pnpm test` green from the repo root, including `apps/plugin/test/hook-manifests.test.ts` (the ordered `(script, async)` pair on `Stop` at `:79-89` must still hold) and `apps/server/src/test/invariants.test.ts` (the rubric-surface list at `:1297-1303` and the tail-keeping trimmer at `:1337` both name `stop-nudge.sh`). Full run: `Test Files 145 passed | 1 skipped (146)`, `Tests 2788 passed | 10 skipped (2798)`, plus the Hermes Python suite `Ran 91 tests ... OK`. `hook-manifests.test.ts` 25/25 and `invariants.test.ts` 96/96 both passed.
- [x] 6.3 Confirm `apps/plugin/test/nudge-fixtures.json` is byte-unchanged — `endOfTurnRubric` (`:16`) is pinned and this change does not touch emitted text. `git diff`/`git status --short` on the file: no output, byte-unchanged.
- [x] 6.4 Reproduce the measurement the spec now states, and record the actual numbers in the PR: with the counter driven to a cadence point and an ~8 MB transcript, hook wall-clock per invocation with `stop_hook_active: true` must fall from ~790 ms (pre-change) to single-digit ms (post-change), and the emitted payload from ~2.7 KB to 0 bytes (`{}` for `codex-cli`). One instrument, named: per-invocation hook wall-clock, not an end-to-end turn timing.

  Reproduced on this machine, same instrument (per-invocation hook wall-clock via `date +%s%N` around a single `bash stop-nudge.sh` call, real counter driven to the cadence point via `prompt-nudge.sh`, synthetic 8,767,203-byte / 20,685-line tool-edit transcript, 3 runs each, `stop_hook_active: true`):
  - Pre-change (`git show HEAD:apps/plugin/scripts/stop-nudge.sh`, guard absent): 499 ms / 491 ms / 531 ms, payload 2727 bytes every run (the flag is ignored).
  - Post-change (guard present): 6 ms / 5 ms / 5 ms, payload 0 bytes.
  - `codex-cli` client, post-change: 5 ms, output `{}`.
    Absolute pre-change number (≈500 ms) differs from the spec's stated ≈790 ms (different machine), but the direction and order of magnitude match, and the post-change floor (single-digit ms, 0 bytes / `{}`) matches exactly.

## 7. Real end-to-end smoke (operator-assisted — `/opsx:apply` must pause here)

- [x] 7.1 Bring up the dev stack with pre-existing seeded data: `pnpm run dev:docker:up` (chown `data-dev` to `10001:10001` first if it refuses with `SQLITE_CANTOPEN`). Up healthy on `http://127.0.0.1:8788`, `demo-writer` token captured from the boot banner.
- [x] 7.2 In a real Claude Code session against that stack, drive the session past a cadence point with at least one tool-using turn, and confirm from the transcript that the reminder appears exactly ONCE at that turn and that no "A hook blocked the turn from ending N consecutive times" warning is surfaced. This is the issue-#343 reproduction, so run it once on the pre-change plugin to see the warning and once after — a post-change-only run cannot distinguish the fix from a session that never hit the cadence. Ran both arms against real `claude -p` 2.1.232 (isolated `CLAUDE_CONFIG_DIR`, hooks registered from this repo's scripts, counter pre-seeded to 9, haiku model). Pre-change arm (scripts from `git HEAD`): the reminder re-fired on 141 consecutive host continuations (`stop_hook_active: true` on every one, ~1.15 KB each) over 10 minutes until an external timeout killed the session — the host's block cap never engaged, because the model answered every continuation with an `mcp__rembric__memory_session_summary` tool call (verified from the nested transcript: 141 such calls) and a tool-using response resets the host's CONSECUTIVE-blocks counter. The cap the issue's warning names only rescues tool-less turns; a model that obeys the reminder loops unboundedly. Post-change arm (working tree): exactly 2 `stop-nudge.sh` invocations at the cadence turn — first `stop_hook_active=false` emitted the 1075-byte reminder, second `stop_hook_active=true` emitted 0 bytes — turn completed in 13 s, no warning.
- [x] 7.3 Confirm the two `Stop` entries still do their jobs on the same run: the session row's raw `summary`/`title` continue to update (that is `stop-sync.sh`), and a `memory.session_summary` written by the model still lands as a curated version row. Post-change session row: `agent=claude-code`, `status=ended`, raw `summary` 510 chars with the six `##` headings, `title="e2e-343-fix command execution"`, and `session_summary_versions` has 1 row for the session.
- [ ] 7.4 Repeat 7.2 under Codex CLI far enough to confirm the guard does not break the existing behaviour there, whatever that behaviour turns out to be (see 8.1) — a silent `{}` is an acceptable outcome; a hook failure is not. NOT locally drivable: Codex hook trust requires interactive `/hooks` approval (see `rembric-plugin-development` e2e walkthrough, "When local e2e is not possible"), and driving a real Codex with live credentials risks reaching production data. Unit-level coverage stands in: the `codex-cli` guard test asserts `{}` on `stop_hook_active: true`, and `stop-nudge.sh` under `codex-cli` already emitted `{}` unconditionally pre-change (no codex facts extractor exists), so the guard cannot regress it. Manual verification remains open.

## 8. Deferred and explicitly rejected — do not silently drop

- [ ] 8.1 Open a separate issue for the adjacent finding, with the doc quotes rather than a paraphrase: Codex's documented `Stop` output contract is "Common output fields" plus `decision`/`reason`, and `additionalContext` is documented for `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse` and `SubagentStart` but NOT for `Stop` — while `openspec/specs/codex-distribution/spec.md:355-359` asserts that channel for the second `Stop` entry. Unverified against a running Codex. Do NOT expand this change to cover it.
- [ ] 8.2 No `SubagentStop` entry is added, because neither manifest declares one. Note in the PR that the host sends the same flag on that event, so the same guard applies the day such an entry is added.
- [ ] 8.3 Rejected and not to be revived inside this change: a private per-turn statefile or transcript-derived continuation counter, moving the reminder to another `Stop` channel, withdrawing the end-of-turn reminder, and raising the host's block cap (see `design.md` D1).
- [ ] 8.4 PR title and description in English, Conventional Commits (`fix(plugin): …`), hooks never bypassed. Reference issue #343 and state plainly that the change also retracts a spec claim this repo published.
- [ ] 8.5 Note for the archive phase (verified by running `openspec archive` against a scratch copy of `openspec/`): the merge applies cleanly as 1 renamed + 2 modified with no scenario dropped, but because the requirement is RENAMED the archiver re-appends it at the END of `openspec/specs/plugin-session-protocol/spec.md` instead of keeping its original position, and it normalises blank lines around `## Requirements`. Both are the tool's behaviour, not a lost requirement — do not hand-restore the position; do run prettier on the merged specs.
