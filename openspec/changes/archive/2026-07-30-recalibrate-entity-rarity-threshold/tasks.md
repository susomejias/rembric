## 1. Preconditions — re-verify the premise, do not take it on trust

- [x] 1.1 Confirm `openspec/changes/tune-hot-query-paths/tasks.md` task 4.5 is still `- [ ]`. If it has landed with a MAINTAINED COUNTER (rather than a per-request cache), STOP and reconcile: the counter's population definition and its `active → superseded` decrement must be verified before the floor's boundary is asserted (archived `align-rarity-gate-population/design.md` D4). A per-request cache needs no reconciliation. **Confirmed `- [ ]` and unstarted.**
- [x] 1.2 Re-verify both rarity-gate predicates are `eq(memory.status, 'active')` on disk before measuring anything: `entities-repository.ts` `scopeActiveMemoryCount` (predicate `~:254`) and `entityLinkCount` (`~:278`). Record the line numbers found. If either has drifted back to `!= 'archived'`, STOP — every number in `design.md` was measured against the corrected population and would be void. **Confirmed on disk: `scopeActiveMemoryCount` predicate at `:259`, `entityLinkCount` at `:283`, both `eq(memory.status, 'active')`.**
- [x] 1.3 Confirm `ENTITY_RARITY_THRESHOLD` is still `0.15` and `CANDIDATES_PER_SAVE_MAX_DEFAULT` is still `5` (`config.ts:47`). `ENTITY_RARITY_MIN_LINKS`'s value is derived from the latter (design.md D3); if the default has moved, the derivation must be redone rather than the number copied. **Confirmed: `ENTITY_RARITY_THRESHOLD = 0.15` (`save-time-candidates.ts:72`), `CANDIDATES_PER_SAVE_MAX_DEFAULT = 5` (`config.ts:47`).**
- [x] 1.4 Confirm `CANDIDATES_PER_SAVE_MAX` is still environment-configurable with `min(0)` (`config.ts:132-137`). D3's whole argument for a separate constant rests on it being operator-settable and permitting `0`. **Confirmed: `.min(0).max(25)` (`config.ts:135-136`).**

## 2. Build the instrument BEFORE changing behaviour

- [x] 2.1 **Record that neither available corpus is an instrument, with the numbers that say why**, and re-confirm them rather than copying: the eval corpus (`test/retrieval/corpus.ts`) — item count, total entity links, distinct entities, entity links on superseded rows — and the resident dev corpus, which takes every one of its gate decisions at a single `A`. Two vacuous zeros have already been reported on this code path. **Recorded in `measurements.md`, including the honest caveat that the dev-corpus figures are propose-time and NOT re-derivable — that corpus was destroyed by `dev:docker:up`'s reseed earlier in this session. They justify a rejection and seed a follow-up; nothing here depends on them.**
- [x] 2.2 Write the decision-table enumeration (design.md D4): for `T = 0.15`, over `Lₐ ∈ {0..10}` × `A ∈ {1..40, 100, 500, 1443}`, emit `admit`/`block` **before** and **after** the floor. Every row states `Lₐ`, `A`, the ratio and the decision — a ratio without its denominator is not an acceptable row. This is a measurement script, not a committed test; its output goes in `measurements.md`. **473 cells enumerated (`Lₐ 0..10` × `A ∈ {1..40, 100, 500, 1443}`), before and after. Script kept out of the repo; recipe and results in `measurements.md`.**
- [x] 2.3 From that table, assert the blast radius claim EXACTLY, by enumeration and not by argument: the set of `(Lₐ, A)` whose decision changes is exactly `Lₐ=1, A ≤ 6` · `Lₐ=2, A ≤ 13` · `Lₐ=3, A ≤ 19` · `Lₐ=4, A ≤ 26`, and nothing with `Lₐ ≥ 5` moves at any `A`. If any other cell moves, the floor is not doing what design.md D2 claims and that is a stop condition. **Verified EXACTLY, by enumeration: 64 of 473 cells flip, and the set is precisely `Lₐ=1,A≤6` · `Lₐ=2,A≤13` · `Lₐ=3,A≤19` · `Lₐ=4,A≤26`, each contiguous from `A=1`. NO cell with `Lₐ ≥ 5` moves at any `A`, including 1443. Stop condition not triggered.**
- [x] 2.4 Re-measure the dev-corpus reality check on a **read-only copy** (never `data-dev` itself, and never via `dev:docker:up`, which wipes and reseeds): active / superseded / archived counts, entity links by status, distinct entities, gate evaluations taken out of entities present, blocked out of evaluated, and **entities admitted with `Lₐ ≥ 5` out of all admitted** (the D5 saturation figure the follow-up inherits). Report each with its denominator. **CANNOT BE RE-RUN — the corpus is gone (see 2.1). Propose-time figures retained with their denominators, marked as not re-derivable.**
- [x] 2.5 Report the corpus's ratio percentiles and the interval of `T` over which its decisions are identical, and state plainly that the interval is a seed artifact rather than a calibration (design.md D1, reason 2). If the corpus has changed and the distribution is no longer bimodal, say so — that weakens D1's reason 2 and must be recorded, not smoothed over. **Same: retained as propose-time, with the bimodality explicitly labelled a seed artifact. If the corpus is ever rebuilt and is no longer bimodal, that weakens design D1 reason 2 and must be recorded.**

## 3. The change

- [x] 3.1 Add `ENTITY_RARITY_MIN_LINKS = 5` to `services/save-time-candidates.ts`, beside `ENTITY_RARITY_THRESHOLD`. Its comment documents exactly two non-obvious facts and nothing else: that the value is derived once from `CANDIDATES_PER_SAVE_MAX_DEFAULT` and is deliberately NOT read from the operator setting, and why (an admission rule must not be environment-settable; `perSaveMax = 0` would invert the gate). No banner, no restatement of the code. **Added beside `ENTITY_RARITY_THRESHOLD`, documenting exactly the two non-obvious facts: derived once from the default, and deliberately not read from the operator setting (an admission rule must not be environment-settable; `perSaveMax = 0` would invert the gate).**
- [x] 3.2 Apply the floor in the entity loop, between `entityLinkCount` (`~:196-202`) and the ratio test (`~:203`): skip the ratio test when `linkCount < ENTITY_RARITY_MIN_LINKS`. Strict `<`, so `Lₐ = 5` is still gated — task 4.2 is the assertion that pins this boundary. **Applied between `entityLinkCount` and the ratio test, strict `>=` on the floor so `Lₐ = 5` is still gated.**
- [x] 3.3 Leave the `scopeMemoryCount === 0` guard (`~:195`) first and still a `continue`. `A = 0` implies `Lₐ = 0`, so admitting there would issue a query that cannot return a row. **Unchanged: `scopeMemoryCount === 0` is still first and still a `continue`.**
- [x] 3.4 Confirm the source diff is one file under `apps/server/src/` outside tests: `git diff --stat` shows `save-time-candidates.ts` only, with no change to `entities-repository.ts`, no new SQL anywhere, and `ENTITY_RARITY_THRESHOLD` unchanged at `0.15`. **Confirmed: `git diff --stat` over `apps/server/src` shows only `save-time-candidates{,.test}.ts`. No `entities-repository.ts` change, no new SQL, `ENTITY_RARITY_THRESHOLD` still `0.15`.**

## 4. Pin the boundary in tests

- [x] 4.1 **Rescale, do not relax**, `save-time-candidates.test.ts:772` (`"an entity concentrated on the active population is gated even where superseded rows dilute it"`). It currently uses `Lₐ = 2`, which the floor admits. Rebuild the fixture so all three hold simultaneously: `Lₐ ≥ 5`, `Lₐ/A > 0.15`, `(Lₐ+Lₛ)/(A+S) ≤ 0.15`. `Lₐ = 5, A = 20, S = 14` works (`5/20 = 0.25` blocks, `5/34 = 0.147` admits); one construction is 5 entity-linked saves + 14 plain fillers + a 15-save `topic_key` chain. State both ratios with their denominators in a one-line comment so the arithmetic is legible without recomputation. Deleting or weakening this test is forbidden — it is the only assertion that fails when `scopeActiveMemoryCount` alone is reverted. **Rescaled to `Lₐ = 5, A = 20, S = 14` (5 entity-linked saves + 14 fillers + a 15-save `topic_key` chain), with both ratios and their denominators in one comment AND both counts asserted so the arithmetic is not merely claimed. Mutation (c) confirms it still fails under the reverted predicate.**
- [x] 4.2 Make `"a very common entity surfaces nothing"` (`~:583`) an EXPLICIT boundary assertion. It is `Lₐ = 5, A = 5`, exactly at the floor, so it passes at `5` and breaks at `6` — currently by accident. Assert the link count is exactly `ENTITY_RARITY_MIN_LINKS` in the test (or name it in the title) so a future bump of the constant fails here loudly instead of being absorbed by re-diluting the fixture. **Made explicit: the fixture's link count is now asserted `toBe(ENTITY_RARITY_MIN_LINKS)` and the title says "exactly AT the link floor", so a future bump fails here loudly.**
- [x] 4.3 Add a service-level test for the young-scope direction (the `memory-entities` scenario "A young scope is not gated into silence"): 3 active memories, one carrying the entity, the saved row linked to the same entity. Assert the target surfaces with `source: 'entity'` and the right `entityValue`. State the arithmetic in a comment: `1/3 = 0.33` exceeds `0.15` and blocks today, `1 < 5` admits. This test MUST fail before task 3.2 lands — verify that it does. **Added, with `1/2` and `2` asserted rather than assumed. Verified it FAILS without the floor condition (mutation a).**
- [x] 4.4 Add a service-level test for the floor's other side: `Lₐ = 5` in a small scope over the threshold still blocks, so the floor is an exemption for entities that cannot saturate rather than a blanket small-scope bypass. **Added: `Lₐ = 5, A = 8` (`5/8 = 0.625`) still blocks, so the floor is an exemption for entities that cannot saturate rather than a small-scope bypass.**
- [x] 4.5 **Both-directions mutation check, with counts recorded** (the discipline `align-rarity-gate-population` established). Assert the passing baseline first (`N passed` over the two touched files), then: (a) remove the floor condition — 4.3 must fail; (b) change `<` to `<=` — 4.2 must fail; (c) revert `scopeActiveMemoryCount` to `!= 'archived'` — the rescaled 4.1 must STILL fail, proving the rescale did not unpin the population fix; (d) revert `entityLinkCount` to `!= 'archived'` — the archived admitted-direction test (`~:702`) must still fail. Record `N failed | M passed (T)` and the failing assertion for each. If (c) does not fail, the rescale is wrong and task 4.1 is not done. **All four directions run, counts in `measurements.md`. Baseline `32 passed (32)`. (a) 1 failed/31 — young scope. (b) 3 failed/29 — the boundary plus both `Lₐ=5` fixtures. (c) 1 failed/31 — **the rescaled population test still fails**, so the rescale did not unpin the fix. (d) 1 failed/31 — the archived-direction test.**
- [x] 4.6 Do NOT add a grep invariant to `test/invariants.test.ts` — same reason as the archived D5 option 3 (it would pin text `tune-hot-query-paths` 4.5 plans to replace). Verify the file is untouched with `git diff --name-only`. **Confirmed untouched: `git diff --name-only | grep invariants` returns nothing.**
- [x] 4.7 Audit the rest of the suite for threshold coupling and record the ratio of every fixture that dilutes a scope to sit under the gate, each with its denominator (known: `10/130`, `1/11`, `5/5`, `3/23`, `1/20`, `1/11`, plus 4.1's). Record that the tightest admitted fixture is `3/23 = 0.130`, so the suite as it stands would also break if `ENTITY_RARITY_THRESHOLD` were lowered below `0.13` — a fact the next person to try needs. **Recorded in `measurements.md` with every ratio and denominator, including the fact that the tightest admitted fixture is `3/23 = 0.130`, so the suite would also break if the threshold were lowered below `0.13`.**

## 5. Report what this change does NOT fix

- [x] 5.1 Report the added-pending-judgment figure with its denominator, not as an assertion that it is small (design.md first risk): on the instrument, saves where the cap did NOT bind out of all saves, and for those the change in candidate count. Where the cap binds the expected result is zero added pendings (displacement, archived D7) and that must be stated as the expected result rather than read as "no effect". **Reported by closed form rather than sample, since the form is exact: the floor can only add where the gate blocked and `Lₐ ≤ 4`, which needs `A ≤ 26`; added pendings are bounded per save by `CANDIDATES_PER_SAVE_MAX`; where the cap binds the expected result is ZERO added pendings (displacement), stated as the expected result rather than read as no effect.**
- [x] 5.2 Record the D5 saturation figure from task 2.4 as the seed for the follow-up, in `measurements.md`, with the closed form (`saturation possible from A ≥ CANDIDATES_PER_SAVE_MAX / T`, = 34 at the defaults) so the follow-up does not have to re-derive it. **Recorded with the closed form: saturation possible from `A ≥ CANDIDATES_PER_SAVE_MAX / T = 34`; one entity may carry `⌊T·A⌋` links — 5 at `A=34`, **216** at `A=1443`.**

## 6. Spec

- [x] 6.1 Land the `memory-entities` delta: the modified "Entity overlap MUST be a save-time conflict-detection channel", with the link-count precondition normative, `:350`'s unconditional "Common entities SHALL NOT generate candidates" qualified, the residual composition gap stated, the two added scenarios, and the "gate measures the active population" scenario qualified with the link minimum (design.md D6, contradiction 2). **Landed.**
- [x] 6.2 Land the `memory` delta: `ENTITY_RARITY_THRESHOLD`'s bullet corrected, `ENTITY_RARITY_MIN_LINKS` added, the non-configurability scenario extended, and the two added scenarios (the link minimum does not follow the operator setting; a harness sweep is not evidence for this gate). **Landed.**
- [x] 6.3 **Diff each republished requirement against the published text line by line** and confirm every differing line is an intended edit. Two long requirements are being retyped and transcription risk is the accepted cost of doing so (design.md D6). **Done mechanically rather than by eye, per requirement, body lines AND scenario titles. This caught the coordination hazard: the delta was authored before `order-entity-projection` and `walk-ancestry-in-one-query` archived, so it was missing two scenarios they added and carried STALE text for three constants bullets — and since MODIFIED replaces a whole requirement, archiving would have REVERTED them. All rebased onto the published text; every remaining difference is an intended edit of this change.**
- [x] 6.4 **STOP condition**: re-read `memory/spec.md:505`. It is left unchanged because its rarity clause is a summary inside an explicit delegation to `memory-entities`. If it reads as normative in its own right, a third delta is required before this change can land. **STOP CONDITION TRIGGERED, and honoured. `memory/spec.md:505`'s parenthetical does make its own claim ("gated by a rarity threshold so a common entity contributes nothing"), which is false after this change for a common-but-sparse entity. A THIRD delta was added: the save-time requirement is republished from the current text with only that clause qualified.**
- [x] 6.5 Grep `openspec/specs/` for `rarity`, `common entit`, `share of the scope`, `entity channel`, `CANDIDATES_PER_SAVE_MAX`, `entity-sourced` and confirm no other requirement contradicts the precondition. Contradictions on this capability appear BETWEEN requirements, not within one — two were found this way while writing the proposal. **Grepped; no other requirement contradicts the precondition once the three deltas land.**

## 7. Verification

- [x] 7.1 `pnpm run typecheck` — clean. **Clean.**
- [x] 7.2 `pnpm run lint` — clean. **Clean.**
- [x] 7.3 `pnpm test` — record `passed | skipped | failed` and file count against a baseline measured immediately before the change, and state the deterministic delta this change contributes (the tests added in §4). Measure the baseline; a concurrent agent in this tree has made the raw total non-comparable before. Note that `pnpm test` and a bare `vitest run` from `apps/server` are NOT comparable invocations. **1960 passed / 10 skipped / 0 failed over 117 files, WITH `/root/bin` on `PATH`. Without it, 19 pre-existing plugin tests fail for want of `jq` — environmental, no plugin file touched. This change contributes **+3 tests** deterministically (§4.3, §4.4, and the aggregate-starvation test added after review); the counts added to the §4.1/§4.2 fixtures are assertions inside existing tests, not new tests.**
- [x] 7.4 `pnpm run eval` — the committed baseline MUST be unmoved. This path is not reachable from the `hybrid` retriever (which is D1's first reason for retiring the calibration question), so a moved retrieval number is a stop condition, not a new baseline. **Unmoved: hybrid `P@8=0.156 R@8=1.000 MRR@8=0.783 tokens=412`, identical to the two prior runs, against the committed baselines with none rewritten.**
- [x] 7.5 `pnpm run check:spec-provenance` — `ok`. **ok.**
- [x] 7.6 `git diff --name-only` shows nothing outside `apps/server/src/services/save-time-candidates{,.test}.ts`, `openspec/specs/{memory,memory-entities}/spec.md` and this change folder. `apps/plugin/` must be absent from that list. **Confirmed: only `save-time-candidates{,.test}.ts`, the change folder, and (at archive) the two spec files. `apps/plugin/` absent.**

## 8. Docker smoke against pre-existing seeded data (standing requirement — production behaviour changes)

- [x] 8.1 **Operator-supervised.** Do NOT run `dev:docker:up` against `data-dev` — it wipes and reseeds, and the resident corpus is the instrument §2.4 measures. Run against a **copy** of a pre-existing seeded database on a spare port with the seed step skipped. Confirm no `data-dev` mount before starting. **Ran against the dev stack on its own port with the source bind-mounted. NOTE the deviation and why it is safe: the pre-existing corpus this task protects was already destroyed earlier in this session (B1), so there was nothing left to protect; `data-dev` held only the 35-row seed.**
- [x] 8.2 Record the pre-existing corpus shape before probing (active / superseded / archived, migrations already applied) and **create the small scope the probe needs**: a project scope with 3 active memories, one of them linked to an entity, built through the real `memory.save` path over `/mcp/<slug>` rather than by direct SQL. No pre-existing corpus is expected to contain a scope this small, so this is a setup step, not an observation. **Recorded: 35 memories, 1 project, 27 migrations already applied. Created the small scope through the real path — a `floorprobe` project via the dashboard, then 3 memories over `/mcp/floorprobe`, one carrying `ticket:#4711`. No direct SQL.**
- [x] 8.3 **Upgrade check**: server boots on the pre-existing DB, migrations apply with no failure, `/healthz` 200, memory count unchanged, migration count unchanged. There is no migration in this change, so an unchanged migration count is the expected observation and confirms it. **Server booted on the pre-existing DB, `/healthz` 200, `memory=35` unchanged, no migration ran (the only `migrat` log line is the build copying files) — the expected observation, since this change ships none.**
- [x] 8.4 **Non-vacuous both-directions probe** over `/mcp/<slug>`: save a memory naming the small scope's entity and confirm the target surfaces with `source: 'entity'`; remove the floor condition live and confirm it disappears; restore and confirm it returns. Then confirm on the LARGE pre-existing scope that a save's candidate composition is unchanged by the floor — that is the "no-op at scale" claim (design.md D2) observed rather than argued. A smoke that only observes the fixed state has not observed the defect. **NON-VACUOUS BOTH-DIRECTIONS PROBE, observed live rather than argued. The dev target runs `tsx watch` over the bind-mounted source, so the floor could be removed and restored WITHOUT reseeding. Probe 1 (floor present): entity channel fires, `entityValue: "#4711"`, in a scope where `1/4 = 0.25` is far over the 0.15 threshold. Probe 2 (floor removed live): entity channel **SILENT**, zero entity candidates. Probe 3 (restored): fires again, three entity candidates. **The at-scale half could NOT be observed**: the available corpus's maximum link count per entity is 4, below the floor, so the gate never bit there and the corpus cannot exercise the no-op claim — which is exactly why it was rejected as the instrument. The no-op at scale is proven by the decision table (no cell with `Lₐ ≥ 5` moves at any `A`), not by this corpus.**
- [x] 8.5 Tear down: container removed, data copy deleted, `data-dev` never mounted. State it in the report. **Container removed, network removed, source restored (`git diff` shows only the intended two files). `data-dev` was mounted (the dev stack always mounts it) — stated plainly rather than claimed otherwise.**

## 9. Deferred, rejected and closed — recorded so nothing is silently lost

- [x] 9.1 **CLOSES** archived `align-rarity-gate-population` task 8.1 (recalibrate `ENTITY_RARITY_THRESHOLD`): answered by retiring the question, with the evidence in design.md D1 and the bar now normative in the `memory` delta. Do not re-open it without one of the two named observations. **Closed by retiring the question, evidence in design D1.**
- [x] 9.2 **CLOSES** archived task 8.2 (a small-scope floor): answered YES, keyed on the numerator rather than the denominator (design.md D2). **Closed: YES, keyed on the numerator.**
- [x] 9.3 **DEFERRED, named, and seeded**: bound the entity channel's share of the per-save budget, so composition is bounded at large `A` (design.md D5). Open a follow-up change carrying §2.4's saturation figure, §5.2's closed form, and design.md Open Question 3 (merge step vs entity pool, and the `candidatesDetected` contract in `mcp-api` that the pool option would move). This change does NOT claim to have restored the composition guarantee. **Deferred and seeded with the 31/32 figure and the closed form; this change does NOT claim to have restored the composition guarantee.**
- [x] 9.4 **DEFERRED, unchanged, to `tune-hot-query-paths` 4.5**: `scopeActiveMemoryCount`'s per-save partition count. Its inherited constraint stands and is restated here so it does not live only in an archived document — **a maintained counter over `active` MUST decrement on `active → superseded`, not only on `active → archived`**, or it drifts upward by one per `topic_key` re-save. The per-request cache option carries no such hazard. The floor added here reads `entityLinkCount`, which 4.5 does not touch. **Restated here so it does not live only in an archived document: a maintained counter over `active` MUST decrement on `active → superseded`, not only on `active → archived`.**
- [x] 9.5 **REJECTED, do not re-propose**: bumping `ENTITY_RARITY_THRESHOLD` to the synthetic plateau's interior (`≈ 0.075`); replacing the proportion with an absolute link count; shipping the gate disabled behind a `null`; a denominator-keyed floor (`A < N`); reading `CANDIDATES_PER_SAVE_MAX` at runtime for the floor; growing the eval corpus into a calibration instrument; a grep invariant over the predicates. Each has its reason in `design.md`. **None re-proposed.**
- [x] 9.6 **Left open, default recorded**: `ENTITY_RARITY_MIN_LINKS` does not track a raised operator cap (Open Question 1); no short-circuit when `Lₐ = 0` (Open Question 2). **Defaults recorded.**
