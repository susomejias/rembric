# Tasks — stop promising a clamp that never happens

Read `design.md` first: D1–D12 are cited by number below. **No migration, no schema change to the database, no load-bearing invariant crossed** — no `DELETE`, no `content` `UPDATE`, no status flip, no SQL added or moved, no new MCP tool, no dashboard token touched. Six agent-facing defects: one code fix, three description corrections plus two more, two field removals across three output schemas.

Section 1 first (it is the code defect and the only one an agent is misled by today), then 2 (constants, so later sections edit names not literals), then the rest in any order.

## 1. `memory.get({ids})` computes review state — the code defect (D7)

- [x] 1.1 Widen the shared row schema FIRST, or the emission fails output validation: add `replaces: z.array(z.string()).optional()` and `reviewEscalated: z.boolean().optional()` to `memoryRow` (`apps/server/src/mcp/memory-tools.ts:310-337`). It is the row schema for BOTH `memory.search` (`:376`) and `memory.get`'s batch (`:441`); it already declares `reviewState`/`reviewAfter` (`:331-332`). Both additions OPTIONAL, so `memory.search` stays byte-identical.
- [x] 1.2 In the batch branch (`:1114-1136`), call `deps.memory.reviewStateForMemories(rows)` ONCE for the page — copy the call site at `:992`, not a per-row loop — and emit `reviewState`, `reviewAfter`, `reviewEscalated` per row on the same status-driven condition the single-`id` path uses (`:1181-1187`): present for `active`, omitted otherwise. Never emit them as `null` for a non-active row.
- [x] 1.3 Add `replaces: m.replaces` to the batch row (`:1128` region).
- [x] 1.4 Do NOT add `head`, `predecessors`, `predecessorCount`, `truncated`, `headTruncated` or `confirmationCount` to the batch form (D7) — those are per-target walks. Do NOT remove `lastSeenAt` from the batch form or add it to the single-`id` form (D7). Both asymmetries are now spec text; confirm the code matches the enumeration in the delta rather than assuming it.
- [x] 1.5 Do NOT touch `GET_DESCRIPTION` (`apps/server/src/mcp/server.ts:133`). It states the intended contract and this task makes it true; weakening it to "single-`id` form only" is the remedy D1 forbids.
- [x] 1.6 **The parity test, both directions, both forms in ONE connection.** Given an `active` in-scope row whose derived `reviewState` is `needs_review`, call `memory.get({id})` and `memory.get({ids:[id]})` adjacently and assert the three review fields are present and EQUAL on both. Assert `replaces` equal on both. Assert the batch entry carries `lastSeenAt` and the single-`id` response does not. The single-`id` call is the control: a test asserting only the batch form cannot distinguish a correct batch from a scope where nothing needs review.
- [x] 1.7 Add the status control in the same batch: a `superseded` or `archived` id in the same `ids` array must carry NONE of the three review fields, so the omission stays status-driven rather than form-driven.
- [x] 1.8 Assert the negative half of D7 at the wire: the batch entry must NOT contain `head`, `predecessors`, `predecessorCount`, `truncated`, `headTruncated` or `confirmationCount`, and must still validate against the declared `outputSchema`.
- [x] 1.9 **Mutation, one condition at a time** (CLAUDE.md — a new guard is uncovered until its test fails without it). (a) Remove the `reviewStateForMemories` call from the batch branch → 1.6 must go red naming review state. (b) Invert the `active`-only condition so review fields are emitted for every row → 1.7 must go red. (c) Remove `replaces` from the batch row → 1.6's `replaces` assertion must go red. Three separate `node scripts/mutate.mjs` runs; record each verdict. A combined mutation cannot tell you which test carried which condition. **Three runs, three verdicts, all CAUGHT by the parity test naming the right assertion.** (a) `const review = deps.memory.reviewStateForMemories(rows);` → `new Map<string, never>()`: red at `expect(head!.reviewState).toBe(singleBody.reviewState)` (`expected undefined to be 'needs_review'`). (b) the `active`-only condition inverted to `...(r ? { reviewState: r.reviewState ?? 'fresh', …`: red at `expect(field in predecessor!)` — the status control. (A first attempt at (b) that emitted `reviewState: null` reddened on output-schema validation instead, before the status assertion ran, so it was rewritten to a schema-valid mutation.) (c) `replaces: m.replaces,` deleted: red at `expect(head!.replaces).toEqual([predecessorId])`.
- [x] 1.10 Confirm the cost is flat in page size, not per row: time `memory.get({ids})` at the largest batch the schema admits on the seeded volume, before and after. Record both figures with the instrument named. If it grew with page size the implementation looped — fix it, do not accept the number. **Measured, instruments named separately.** ISOLATED STATEMENT (`reviewTimestampsByIds`, 2000-row corpus, 500 confirmations, n=200): ids=1 0.020ms · ids=10 0.030ms · ids=50 0.069ms · ids=100 0.117ms median. One statement, not N: 100 separate 1-id lookups would cost ~2.0ms against the 0.117ms measured for one 100-id lookup, 17x. END-TO-END OPERATION (MCP `callTool` round trip over StreamableHTTP, n=40-60) at ids=100: after 5.32 / 5.54 / 5.03ms median across three runs; before (same probe, source stashed) 6.77ms. The added cost is BELOW run-to-run variance on this box and the sign is inverted, so it is not resolvable end-to-end — the honest figure is the isolated one, 0.117ms on a 100-id page.

## 2. Named bounds (D5) — before the removals, so they edit constants not literals

- [x] 2.1 Hoist `contextSchema`'s three anonymous maxima to named constants beside `PENDING_JUDGMENTS_MAX` (`memory-tools.ts:84`): the `25` at `:260`, the `50` at `:261`, the `100` at `:262`. Each is used twice — schema and the handler's `clamp()` at `:1332`, `:1373`, `:1333` — so afterwards each number appears exactly once in the file.
- [x] 2.2 Give `timelineSchema`'s window its OWN constant (D5), feeding all THREE copies: the schema (`:283-284`), both `clamp()` calls and the `before + after > 50` check (`:1529-1531`), and the error message string it is interpolated into (`:1531-1536`). Section 6 adds a fourth copy in the tool description — that one is a literal in prose and must be kept in step by the test in 6.4, not by the constant.
- [x] 2.3 **Do NOT share the timeline constant with `prompts`' or `judgments`' 50** (D5 reason 2): three independent bounds that happen to collide on one value.
- [x] 2.4 Verify no bare bound literal remains: `grep -n "max(2[0-9])\|max(5[0-9])\|max(10[0-9])\|, 0, 2[0-9])\|, 0, 5[0-9])\|, 0, 10[0-9])" apps/server/src/mcp/memory-tools.ts` returns nothing. Paste the output here. **Deviation:** the grep cannot return nothing — `max(10[0-9])` also matches three pre-existing bounds unrelated to the context/timeline maxima, and they stay: `title: z.string().min(1).max(100)` (`:130`), `memoryGetSchema.ids … .max(100)` (`:218`), `memoryConfirmSchema.ids … .max(100)` (`:237`). No `contextSchema`/`timelineSchema` literal remains.
- [x] 2.5 Confirm this section alone changes no behaviour: `pnpm run typecheck` and, from `apps/server`, `pnpm vitest run src/mcp src/services` green with **zero** test edits. A test needing an edit here means a bound moved — find which and revert it.

## 3. Remove `clamped` from `memory.context` (D10)

- [x] 3.1 Delete `clamped: z.boolean()` from `contextOutput` (`memory-tools.ts:548`) AND the `clamped,` response entry (`:1515`) in the SAME edit — the SDK validates `structuredContent` against the published schema, so one without the other fails.
- [x] 3.2 Delete the `const clamped = …` disjunction (`:1334-1338`). **Keep** the `clamp()` calls at `:1332`, `:1333`, `:1373` (D3). Do not add a comment explaining them; the constants from §2 carry the intent and the spec carries the reason.
- [x] 3.3 Move the `clamped === false` assertions out with the field: `apps/server/src/mcp/context-pending-judgments.test.ts:192,202` and `apps/server/src/test/mcp-integration.test.ts:893,1635`. Everything around them (page length, `pendingJudgmentsTotal`, the at-max `judgments: 50` inventory) stays.

## 4. Remove `clamped` from `memory.search_prompts` (D10)

- [x] 4.1 Delete `clamped: z.boolean()` from `searchPromptsOutput` (`prompt-tools.ts:73`) and `clamped: result.clamped,` from the response (`:197`), in one edit.
- [x] 4.2 Delete `clamped` from `SearchByScopeResult` (`services/prompts.ts:65`), its computation (`:280`) and the return (`:299`). **Keep** `clamp(requestedLimit, 1, 100)` at `:279` (D3).
- [x] 4.3 Drop the `clamped === false` assertion at `services/prompts.test.ts:214`; the FTS assertions around it stay.
- [x] 4.4 `git grep -n "\.clamped\|clamped:" -- apps/server/src` returns zero lines after §3 and §4.

## 5. Remove `db.open` from `memory.doctor` (D10)

- [x] 5.1 Delete `open` in ALL THREE places, one edit: the literal `db: { open: true, … }` (`apps/server/src/server/bootstrap.ts:571`), the `DoctorReport` interface's `db` member (`apps/server/src/mcp/observability-tools.ts:82`), and `doctorOutput`'s `db` zod shape (`:95`). The interface is what makes `tsc` find any reader you missed — do the type first and let the compiler enumerate the rest.
- [x] 5.2 Assert at the wire that `memory.doctor`'s `db` block has exactly `journalMode`, `integrity`, `sizeBytes`, and that the declared `outputSchema` no longer declares `open` at all (not merely no longer requires it).
- [x] 5.3 Add the positive half: a pragma-read failure still surfaces. Force `readJournalMode`/`quickCheck` to throw and assert the call SUCCEEDS with the failure named in `warnings` and `integrity === 'unknown'` (`bootstrap.ts:525-533`). Without this, removing `open` looks like losing a health signal; with it, the signal is shown to live where it varies.
- [x] 5.4 Update the `db: { open: true, … }` fixture at `apps/server/src/mcp/observability-tools.test.ts:192` and grep the file for any assertion on `db.open`; record what you found, including "none". **Found: none** — the file carried the `db: { open: true, … }` fixture only, no assertion on it; four sibling fixtures (`authorization.test.ts:123`, `session-deleted.test.ts:85`, `session-scope-resolution.test.ts:100`, `unresolvable-slug.test.ts:51`) were the same and were updated too, `tsc` having enumerated them. Confirm no dashboard page renders it — `DoctorReport`'s only non-test consumers are `bootstrap.ts` and `server.ts` (`git grep -n DoctorReport -- apps/server/src`), so the operator surface never sees the field.

## 6. The five descriptions (D4) — the character budget is the constraint

- [x] 6.1 `memory.context` (`server.ts:312`): delete the 96-char tail clause quoted in D4 and append D4's 195-char replacement verbatim. Leave the mid-string `judgments` clause alone — mandated by the description scenario the delta modifies.
- [x] 6.2 `memory.search_prompts` (`server.ts:367`): replace the return-shape sentence with D4's 118-char version, dropping `clamped` and naming `limit`'s default and maximum.
- [x] 6.3 `memory.session_start` (`server.ts:280`): replace the closed `Returns:` list with D4's 215-char version naming all six required fields and saying what `reused: true` means.
- [x] 6.4 `memory.timeline` (`server.ts:334`): add D4's argument-and-bound clause. Its `50` is a fourth copy of the constant from 2.2 — add a test asserting the description's number equals the constant, so a bound change cannot leave the description behind.
- [x] 6.5 `memory.doctor` (`server.ts:378`): insert D4's 230-char two-causes clause MID-STRING, before the closing "Use at session start…". Not stylistic: `openspec/specs/mcp-api/spec.md:856` requires that "`memory.doctor`'s server-wide disclosure SHALL NOT be the trailing clause" because truncation is a tail cut.
- [x] 6.6 **Measure all five from a real `tools/list` response, not from the constants** — the boundary `mcp-api/spec.md:2122` mandates and `mcp-integration.test.ts:582` asserts over. Expected: `memory.context` **1432**/468 (was 1333), `memory.search_prompts` **428**/1472 (was 357), `memory.session_start` **624**/1276 (was 461), `memory.doctor` **612**/1288 (was 403), `memory.timeline` **395**/1505 (was 210). Record the measured length AND headroom for each. Any mismatch means the wording drifted — fix the wording, not the expectation.
- [x] 6.7 Substring assertions against the live `tools/list` string, one per delta clause rather than one combined check: `memory.context` names 25 / 50 / 100 / 50; `memory.timeline` names `before`, `after` and `memory.search`; `memory.session_start` names `title` and `reused`; `memory.doctor` conveys the filtering cause; each of `memory.context`, `memory.search_prompts`, `memory.timeline` contains "rejected, not clamped".
- [x] 6.8 The negative assertions — the ones that would have caught these defects, since no test asserted any of this text before: no description contains `clamped:true`; none lists `clamped` in a return shape; `memory.doctor` does not present scope as the ONLY cause.
- [x] 6.9 Add the schema/description agreement test the delta requires for `memory.session_start`: compare its description against its `outputSchema`'s required list and fail if any required field is unnamed. That is what stops instance 2 recurring the next time a field is added.
- [x] 6.10 Confirm no OTHER description moved and the cap holds for every tool: `pnpm vitest run src/test/mcp-integration.test.ts`. Record the tool count asserted, so a silently-skipped tool is visible. **78 tests in `src/test/mcp-integration.test.ts`, all passing; the every-tool cap assertion measures `tools.length >= 23` and the new length block asserts `descriptions.size >= 23`.**

## 7. Replace the dead unit test with the wire tests the bounds never had (D11)

- [x] 7.1 Delete `context-pending-judgments.test.ts:205-216` — the `judgments: 999` direct-handler test plus its comment saying the state is unobservable on the wire.
- [x] 7.2 Wire-level rejection test for `memory.context`, all FOUR arguments, each in its own `callTool`: `sessions: 26`, `prompts: 51`, `memories: 101`, `judgments: 51` → `isError` with `-32602` in the text block and no context payload alongside. Copy the shape at `mcp-integration.test.ts:378-410`.
- [x] 7.3 The at-max control in the same test: `{ sessions: 25, prompts: 50, memories: 100, judgments: 50 }` returns `isError=false`, and its `structuredContent` has no `clamped` key.
- [x] 7.4 Replace `services/prompts.test.ts:288-295` with a wire-level test: `limit: 101` → `-32602`, `limit: 0` → `-32602` (two-sided; the sweep measured `too_small`), `limit: 100` → success. Place it where an MCP `Client` is available — the bound under test is the transport's.
- [x] 7.5 Wire-level test for `memory.timeline`: `{before: 30, after: 30}` → `invalid_input`, control `{before: 25, after: 25}` → ok. Assert the error message names `memory.search`, so the description (6.4) and the error stay in step.
- [x] 7.6 Wire-level test for `memory.session_start`: two consecutive calls on one connection return the SAME `sessionId` with `reused: false` then `reused: true`, and the `sessions` row count for that `(tokenId, projectId)` is 1 afterwards. The row count is the control that `reused: true` means adoption rather than an id coincidence.
- [x] 7.7 Assert the three output schemas from a real `tools/list`: `clamped` absent from `memory.context`'s and `memory.search_prompts`' `required` lists; `open` absent from `doctorOutput`'s `db`; and `memory.context`'s `structuredContent` holds exactly the 9 remaining keys (`needsReview, needsReviewTotal, pendingJudgments, pendingJudgmentsTotal, recentMemories, recentPrompts, recentSessions, relevantMemories, scope`) plus `rankedPass` when the ranked pass ran.

## 8. `memory.doctor`'s divergence is disclosed, and no counter moves (D8)

- [x] 8.1 Confirm by reading and then by running that NO counter changes: `adminCountByStatus` (`relations-repository.ts:438-445`), `countPendingInScope` (`:408-424`), `adminCountNeedsReview`/`countNeedsReview` (`memory-repository.ts:998`/`:732`) and the dashboard badge are untouched. `:856`'s own closing sentence forbids more: "satisfying it SHALL NOT add, remove or rename a field on either payload." **Confirmed by reading and by running.** `git diff` touches no repository file: `relations-repository.ts`, `memory-repository.ts`, `agent-sessions-repository.ts` and the dashboard are all unmodified. The wire test in 8.2 re-measures the counters after the description edit and they are unchanged.
- [x] 8.2 Reproduce the divergence as a test inside ONE project, so scope cannot be the explanation: one pending pair → doctor 1 / stats 1 / context 1; archive one endpoint → doctor **1** / stats **0** / context **0**. This is the measurement the description now has to account for.
- [x] 8.3 Assert the description conveys the filtering cause (6.7) and that 8.2's numbers still hold after the edit — the text changed, the counters did not.

## 9. Evidence gate — the checks that make this a verified change

- [x] 9.1 **Completeness.** `git grep -n clamped -- apps/server/src apps/plugin docs openspec/specs README.md CONTRIBUTING.md` returns ONLY the three unrelated prose comments (`server/rate-limit.ts:26`, `services/hybrid-search.ts:381`, `test/retrieval/floor-ratchet.ts:116`), the four surviving `openspec/specs/mcp-api/spec.md` policy lines (`:2163,:2189,:2192,:2219`), and the two `openspec/specs/http-api/spec.md` lines (`:384,:402`, out of scope by design). Also `git grep -n "open: true" -- apps/server/src` returns nothing in `bootstrap.ts`. Paste both outputs. **`open: true` grep: zero lines (exit 1).** `clamped` grep **deviates from the expectation, for two reasons that are not defects.** (1) `openspec/specs/mcp-api/spec.md` still carries all eleven lines (`:592,:642,:659,:660,:661,:1417,:1440,:1475` plus the four policy lines) — the delta replaces published spec text at ARCHIVE, not at apply, so those hits cannot be gone yet. (2) The expectation omitted pre-existing unrelated uses of the word: `apps/server/src/mcp/memory-tools.ts:115,905` (the `relations_limit` REJECTED-not-clamped prose and its comment), `openspec/specs/memory/spec.md:257,259,329,346` and `openspec/specs/retrieval-evaluation/spec.md:107` (other bounds). Everything else is either the three named comments (`rate-limit.ts:26`, `hybrid-search.ts:381`, `floor-ratchet.ts:116`), the two `http-api` lines, the two named controls, or text this change ADDS: the three new `rejected, not clamped` description clauses (`server.ts:312,334,367`) and the assertions over them.
- [x] 9.2 **The wire agrees, same instrument before and after.** Re-run the sweep probe (bootstrapped `createServer` + official MCP SDK `Client` over `StreamableHTTPClientTransport`) against the tree before and after: (a) `clamped` gone from both `required` lists; (b) `db.open` gone; (c) `memory.context`'s `structuredContent` keys are the 9 remaining; (d) max+1 still `-32602` and at-max still `isError=false` for all four `memory.context` arguments, `search_prompts`' `limit`, and `memory.timeline`'s window; (e) `memory.get({id})` and `memory.get({ids})` now agree on review state and `replaces`; (f) two `session_start` calls still show `false` then `true`. Record before/after key lists verbatim. Do NOT switch instruments between runs. **Done — same probe file, same bootstrapped `createServer` + SDK `Client` over `StreamableHTTPClientTransport`, source stashed for the before run.** BEFORE: `memory.context.outputSchema.required = ["clamped","needsReview","needsReviewTotal","pendingJudgments","pendingJudgmentsTotal","recentMemories","recentPrompts","recentSessions","relevantMemories","scope"]`; `memory.search_prompts.required = ["clamped","prompts","scope","total"]`; `doctor.db keys = ["integrity","journalMode","open","sizeBytes"]`; `context structuredContent = ["clamped",…,"rankedPass",…]` (11); `get({ids})` entry `{reviewState:null, reviewAfterPresent:false, reviewEscalated:null, replaces:null, lastSeenAtPresent:true}`. AFTER: `context.required` = the 9 without `clamped`; `search_prompts.required = ["prompts","scope","total"]`; `doctor.db keys = ["integrity","journalMode","sizeBytes"]` (schema AND payload); `context structuredContent` = the 9 plus `rankedPass`; `get({ids})` entry `{reviewState:"needs_review", reviewAfterPresent:true, reviewEscalated:false, replaces:[], lastSeenAtPresent:true}`, equal to `get({id})` on all four. The (d) rejection matrix is BYTE-IDENTICAL before and after (26/51/101/51 and limit 101/0 rejected; 25/50/100/50, limit 100/1, timeline {25,25} accepted; timeline {30,30} rejected), and (f) `session_start` still reports false then true with one id.
- [x] 9.3 **The bound guards are not vacuous — SEPARATE mutations, never one combined.** Weaken one `contextSchema` maximum at a time via its constant (`node scripts/mutate.mjs --file apps/server/src/mcp/memory-tools.ts --spec src/test/mcp-integration.test.ts --mutation '<CONST> = 25' --with '<CONST> = 9999'`), four runs; the test naming THAT argument goes red while the other three stay green. Record four verdicts. **Four verdicts, one mutation each, and a defect found and fixed first: the probes originally derived `max+1` from the constant under test, so all three moved with the mutation and all three came back NOT CAUGHT. The probe values are now literals pinned to the published `inputSchema.maximum`.** `CONTEXT_SESSIONS_MAX = 9999` → CAUGHT: `rejects memory.context sessions above 25` + the description-agreement test; the prompts/memories/judgments tests stayed green. `CONTEXT_PROMPTS_MAX = 9999` → CAUGHT: `rejects memory.context prompts above 50` (+ description). `CONTEXT_MEMORIES_MAX = 9999` → CAUGHT: `rejects memory.context memories above 100` (+ description). `PENDING_JUDGMENTS_MAX = 9999` → CAUGHT: `rejects memory.context judgments above 50` (+ the existing judgments-description test).
- [x] 9.4 Same for `memory.search_prompts`' `limit`: weaken `.max(100)` (`prompt-tools.ts:45`) → 7.4's over-max case red; weaken `.min(1)` separately → 7.4's `limit: 0` case red. Two conditions, two mutations. **Both CAUGHT, separately.** `.max(100)` → `.max(9999)`: only `rejects memory.search_prompts limit above its maximum` red. `.min(1)` → `.min(0)`: only `rejects memory.search_prompts limit below its minimum` red.
- [x] 9.5 Same for the timeline window: weaken the constant from 2.2 → 7.5 red. Then mutate ONLY the description's literal (6.4) → the description/constant agreement test red. Two mutations, because the two copies can drift independently. **Both CAUGHT.** `TIMELINE_WINDOW_MAX = 9999` → the wire rejection test AND the description-agreement test red. Description literal only (`must not exceed 50` → `60`, in `server.ts`) → only the description-agreement test red.
- [x] 9.6 The `memory.get` parity mutations are 1.9 — three runs, recorded there.
- [x] 9.7 **The reject policy was not disturbed.** `apps/server/src/mcp/annotation-bound.test.ts:408` and `mcp-integration.test.ts:374` assert `/rejected, not clamped/` on `relations_limit`. Both must keep passing UNTOUCHED — they are the control proving this change removed the contradicting field without weakening `mcp-api/spec.md:2163`. Confirm with `git diff --stat` on both files. **`git diff --stat -- apps/server/src/mcp/annotation-bound.test.ts` is empty (untouched); the file's 15 tests pass. `mcp-integration.test.ts:374-380` (`the relations_limit parameter publishes the bounded-ask recipe on both reading tools`) is unmodified and passes. Neither `:2163` nor `:2120` of the published spec is modified.**
- [x] 9.8 **Plugin impact is zero, re-verified not assumed.** `git grep -n "clamped\|db\.open" -- apps/plugin` returns 0 lines before and after; `git grep -n reused -- apps/plugin` returns nothing that reads the MCP response. Record it, so no plugin release is implied. The plugin tree ships to FOUR clients; a zero here is what proves there is nothing to copy. **Zero on both trees.** `git grep -n "clamped\|db\.open" -- apps/plugin` → exit 1, no lines, before AND after (source stashed for the before run). `git grep -n reused -- apps/plugin` → exit 1, no lines. `git ls-files apps/plugin/ | wc -l` = **68** before and after.

## 10. Verification

- [x] 10.1 `pnpm run typecheck`
- [x] 10.2 `pnpm run lint`
- [x] 10.3 `pnpm test`
- [x] 10.4 `pnpm run eval` is **NOT** required — no retrieval path, ranking constant, gate or corpus is touched. `memory.search`'s payload is byte-identical (§1's schema additions are optional and unemitted there). State that here rather than skipping silently. **Not run, and the reason holds: no retrieval path, ranking constant, gate or corpus is touched.** `memory.search`'s payload is byte-identical — §1's two `memoryRow` additions are OPTIONAL and `handleSearch` still emits only `reviewState`/`reviewAfter` (12.5 keeps `reviewEscalated` there deferred). `services/hybrid-search.ts` and every eval fixture are unmodified.
- [x] 10.5 `pnpm run check:delta-freshness`. Expect **ok with body advisories, not a failure**: six advisory lines naming the six deliberate rewrites (`memory.get`'s two, `search_prompts`' three, the `session_start`-unaffected sentence). Two published scenario titles are kept verbatim although their wording predates the change — see D9; a rename reds this CI-gated check.

## 11. Docker smoke against pre-existing seeded data (standing requirement — MCP surface)

Operator-run on the host. `dev:docker:up` reseeds with `seed-dev --reset` on every boot, so it is **not** a valid instrument for "pre-existing data" — use a volume that already holds rows.

- [x] 11.1 Bring up the CURRENT image on a volume already holding memories, prompts, at least one pending judgment, and **at least one `active` memory genuinely past its review shelf life** (the batch fix must be observed on real data, not a fixture). Capture verbatim: `memory.context({})` and at all four maxima and at max+1 each; `memory.search_prompts({})`, `limit:100`, `limit:101`, `limit:0`; `memory.timeline` at `{25,25}` and `{30,30}`; `memory.doctor`; `memory.get({id})` and `memory.get({ids})` for the same needs-review row; two `memory.session_start` calls. This is the "before" half of 9.2 through the real Docker boundary. **Done.** Volume: a copy of the host `data-dev` directory holding **38 memories (18 `active` + 17 `superseded` in project `demo`, 3 archived global), 1 project, 5 sessions, 3 tokens, 4 pending relations**, plus (added through the OLD server, on that corpus) 1 prompt and one genuinely refuted row so `needsReview` was non-empty. Container `rembric-smoke` on 127.0.0.1:8899, image `rembric-smoke:before` (runtime target built from HEAD with this change stashed), booted `rembric v0.25.2 ready / counts: memory=38 projects=1 sessions=5 tokens=3 prompts=0`. Probes ran through the real boundary (SDK `Client` over `StreamableHTTPClientTransport` against the container). **The defect reproduced on real data:** `get({id})` → `reviewState:"needs_review", reviewAfter:"2026-08-04T03:22:02.019Z", reviewEscalated:false`; `get({ids})` → all three absent.
- [x] 11.2 Upgrade to the built image **on the same volume**, without reset. Confirm the server boots, runs no migration (there is none), and every capture differs ONLY by: the absent `clamped` and `db.open` keys, the four keys added to `memory.get({ids})` rows, and the five changed descriptions. Any other diff is a regression. **Done — `rembric-smoke:after` on the SAME volume, no reset, no migration in the log (`rembric v0.25.2 ready / counts: memory=38 projects=1 sessions=7 tokens=3 prompts=2`).** The whole capture diff is: `clamped` out of both `required` lists, out of `search_prompts`' payload keys and out of `context`'s `structuredContent`; `db.open` out of the schema AND the payload; the five description lengths 1333→1432, 210→395, 357→428, 461→624, 403→612; `get({ids})` gaining `reviewState`/`reviewAfter`/`reviewEscalated`/`replaces`. The (d) rejection matrix is IDENTICAL line for line. Two non-regression deltas, both the probe's own footprint: `sessionsByStatus.active` 2→3 / `doctor.sessions.active` 3→4 and `session_start` reporting `true then true` instead of `false then true` — the BEFORE run's own session survived the image swap, which is itself evidence the volume was not reset.
- [x] 11.3 Confirm counts survive untouched — memories, prompts, pending judgments, sessions — via `memory.stats` and `memory.doctor` before and after. A digest match is vacuous if both sides are empty: assert the non-empty counts explicitly. **Non-empty and unchanged across the upgrade:** `memoriesByStatus {active:18, superseded:17}`, `memoriesByType {feedback:4, project:13, reference:17, user:1}`, `needsReviewTotal 4`, `pendingJudgmentsTotal 1`, `doctor.review {needsReview:4, pendingJudgments:4}`, `search_prompts total 1`, `db.sizeBytes 10027008`. Identical before and after.
- [x] 11.4 Confirm on real data that `memory.get({ids})` now reports the same `reviewState`/`reviewAfter`/`reviewEscalated` as `memory.get({id})` for that seeded needs-review row, and that `memory.doctor`'s `review.pendingJudgments` is unchanged by this upgrade (§8's no-counter-moves claim, checked against a populated table rather than a two-row fixture). **Confirmed on the populated volume.** After the upgrade, for the seeded needs-review row `01KZ438CA1JA4VEG082KT7CH30`: `get({ids})` returns `reviewState:"needs_review", reviewAfter:"2026-08-04T03:22:02.019Z", reviewEscalated:false` — byte-equal to `get({id})`. `doctor.review.pendingJudgments` is **4** before and **4** after; `memory.stats.pendingJudgmentsTotal` is **1** before and after.
- [x] 11.5 Roll back to the previous image on the same volume: it must serve `clamped: false`, `db.open: true` and review-free batch rows again with no data step. Verify the rollback claim rather than asserting it. **Verified, not asserted.** `rembric-smoke:before` re-run on the same volume: `clamped` back in both `required` lists, `db.open: true` back in schema and payload, `get({ids})` back to `reviewState:null, reviewAfter:null, reviewEscalated:null, replaces:null`, while `get({id})` still reports `needs_review`. Counts unchanged (`active:18, superseded:17`, `needsReviewTotal 4`, `pendingJudgmentsTotal 1`, `doctor.review {4,4}`). No data step of any kind.
- [x] 11.6 Tear down. Record the image tags used for before / after / rollback. **Torn down** (`docker stop`/`rm rembric-smoke`, both images removed; the operator's `rembric-dev` container was never touched). Tags: before **`rembric-smoke:before`**, after **`rembric-smoke:after`**, rollback **`rembric-smoke:before`** — both built locally from `apps/server/Dockerfile --target runtime`, the before one from this working tree with the change stashed. **Deviation:** the published `ghcr.io/susomejias/rembric:latest` on this host is a DEV-target image (its CMD runs `seed-dev --reset`, which refused on boot), so it could not serve as the previous release; two locally built runtime images from the two trees are the instrument used instead.

## 12. Deferred and explicitly rejected — recorded so nothing is silently lost

- [x] 12.1 **DEFERRED:** a field-rejection counter, without which `bound-annotation-response-size`'s Open question 2 ("in case field evidence shows agents hitting the rejection often") can never be evaluated — nothing counts `-32602` per parameter today. Its own change; the door stays closed until the instrument exists (design open question 1).
- [x] 12.2 **DEFERRED:** instance seven, if one is ever found. Default is its own change citing the requirement added here, which costs one scenario (design open question 5). The sweep reports the class closed: every other `z.boolean()`/`z.literal()` under `apps/server/src/mcp/` has both wire values reachable, every error code any description names was reached, and `DIVERSITY_CAP` (ships `null`) is named on no agent-facing surface.
- [x] 12.3 **DEFERRED with its evidence recorded:** `memory.context`'s description does not teach `rankedPass`/`gateShortened`, though both are reachable there — measured `rankedPass {abstained:false, gateShortened:true}` with `relevantCount 1` on a 7-row pool, control `{abstained:false}` with `relevantCount 5`. Out of scope here because the description makes no FALSE statement, only an incomplete one (design D12), and because it belongs to `mcp-api/spec.md:744`. **Reopen it:** the archived deferral's premise was "no measured benefit" (`archive/2026-08-03-tell-the-truth-about-the-relevance-gate/design.md:122`) and the measurement above falsifies that premise.
- [x] 12.4 **DEFERRED:** `confirmationCount` on `memory.get`'s batch form. No description promises it there, and a per-row count on a bulk read is a query-shape decision for `db-performance-auditor`, not a guess (design D7, open question 3).
- [x] 12.5 **DEFERRED:** whether `memory.search` rows should also carry `reviewEscalated`. It emits `reviewState`/`reviewAfter` only (`memory-tools.ts:1061-1062`); 1.1 makes the field permissible on the shared schema without emitting it. Pre-existing, and widening a ranked read's payload deserves its own decision.
- [x] 12.6 **REJECTED, do not revisit without new evidence:** relaxing any maximum so `clamped` becomes reachable. Rejected in writing at `archive/2026-07-30-bound-annotation-response-size/design.md:200-211`, both the silent and the receipt-bearing variant, and the receipt variant's objections apply here word for word (design D2).
- [x] 12.7 **REJECTED:** keeping `clamped` while fixing only the description, or shipping `clamped: z.literal(false)`. Both preserve a required wire field documenting an impossible state — the "preserves a field the change exists to discredit" failure named at `archive/2026-08-03-scope-and-name-the-project-memory-count/design.md:90` (design D10).
- [x] 12.8 **REJECTED:** qualifying `GET_DESCRIPTION` with "single-`id` form only" instead of fixing the batch branch. It would ratify the gap and leave the batch step of the taught `needsReview` → batch read → `memory.confirm({ids})` flow blind (design D1, D7).
- [x] 12.9 **REJECTED:** filtering `memory.doctor`'s pending count so it agrees with `memory.stats`. Contradicts `openspec/specs/memory/spec.md:1130` and `filter-retired-endpoints-from-pending-queue` D5's operator argument (design D8).
- [x] 12.10 **OUT OF SCOPE, with reason:** `POST /api/<slug>/memory/recall`'s silent `[1,5]` clamp (`openspec/specs/http-api/spec.md:384`, `:402`). It publishes no field, so the new requirement does not reach it, and it is not an agent-facing tool description. Named so the general rule is not later read as demanding an HTTP change.
- [x] 12.11 **OUT OF SCOPE:** the in-process clamps on `memory.search` (`services/memory.ts:819-824`) and `memory.timeline` (`memory-tools.ts:1529-1530`) — equally unreachable, no flag, therefore no claim to correct (design D3).

## 13. Landing the change

- [x] 13.1 Conventional Commits, never `--no-verify`. **Deviation: ONE commit, not the suggested split** — the operator asked for a single commit, with a `BREAKING CHANGE:` footer naming each removed field (this repo's commitlint rejects the `!` marker). Suggested split was: `refactor(mcp): name the context and timeline bounds` (§2) · `fix(mcp): compute review state on memory.get's batch form` (§1) · `fix(mcp): stop publishing fields no caller can observe` (§3–5) · `fix(mcp): say what the tools actually do` (§6) · the spec delta if not already committed.
- [x] 13.2 `pnpm run check:spec-provenance` — **ran green over `origin/main...HEAD`, a range holding 2 commits (`3c3e628`, `ed81269`), so it examined something: `spec-provenance: ok (origin/main...HEAD)`.** — it now defaults to `--base origin/main --head HEAD` and **exits 2 on an empty range** (commit `4f58bce`: "over an empty range every predicate below is trivially satisfied, so `ok` would mean 'nothing was examined'"). Run it over a range that actually holds this change's commits; a run reporting nothing examined is a failure, not a pass.
- [x] 13.3 PR title and description in English. **Not opened — the operator handles push and PR.** Material for it: the commit `ed81269` body already leads with instance 1, states BREAKING on the three output schemas and carries the zero-consumer evidence. State **BREAKING** on three output schemas, with the measured zero-consumer evidence (9.1, 9.8) beside it, and lead the description with instance 1 — it is the only one that changes what an agent believes about its own corpus.
