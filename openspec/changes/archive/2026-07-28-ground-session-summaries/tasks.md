## 0. Before-picture, measured not remembered

- [x] 0.1 Reproduce the composed-truncation defect end to end: build a transcript long enough to exceed both bounds, run it through the real `stop-sync.sh` path against a live server, and show that the persisted `summary` is a middle window — that its first characters are NOT the transcript's first characters and its last characters are NOT the transcript's last. Record the exact character offsets. A test that only checks `truncateSummary` in isolation does not prove the composition.
  - **Measured.** A 60 000-char transcript carrying its own offset every 100 chars, through the real `_rembric_truncate_transcript` and then the real `truncateSummary`: the plugin kept offsets **040500–059900** (its 19 500-char tail, correct), and the server then kept **040500–050400** of that. Net persisted window `[040500, 050400]` — a middle slice, with the last 9 500 characters gone. The trailing marker was present, so the artefact read as "starts at the beginning, cut at the end", which is the exact inverse of what happened. After the D1 fix the same input persists **050100–059900** and the marker leads.
- [ ] 0.2 Record how often this actually bites on the seeded dev corpus and, if available, on a real instance: how many sessions have `summary_final = 0` and `length(summary)` at or within a few characters of `SUMMARY_MAX_CHARS`. If the count is zero, say so — the defect is still real but the priority argument changes and the change should say which.
- [x] 0.3 Enumerate every surface that states the summary structure to a model, with file and line, and record the exact text of each. Expected: six sites, five naming five sections and one naming seven. Confirm or correct that count before building the single-source constant around it.
  - **Corrected: EIGHT surfaces, not six.** The two the design missed are `apps/plugin/scripts/prompt-nudge.sh:13` and `apps/plugin/commands/summary.md:5`. Seven carried `Goal · Discoveries · Accomplished · Next Steps · Files`; `apps/server/src/mcp/server.ts:300` carried `Goal · Instructions · Discoveries · Accomplished · Next Steps · Relevant Files` — two sections the other seven did not name. This is why the count is asserted by the fixture rather than trusted.

## 1. Truncation: one direction, one constant

- [x] 1.1 Make `truncateSummary` tail-keeping with a leading marker: `marker + s.slice(-(SUMMARY_MAX_CHARS - marker.length))`, still surrogate-pair safe, still never exceeding `SUMMARY_MAX_CHARS`.
  - Needed a new helper: `sliceWithoutSplittingSurrogatePair` guards the END of a head-slice, and the tail-slice's orphan risk is the mirror image — a LOW surrogate first, not a high one last. Added `sliceTailWithoutSplittingSurrogatePair` beside it. `SUMMARY_TRUNCATE_SUFFIX` renamed to `SUMMARY_TRUNCATE_MARKER`, since it no longer suffixes anything.
  - **`truncateTitle` deliberately still head-keeps.** A label's meaning is at its start; flipping both for symmetry would have been wrong. The two directions now differ on purpose and the docstrings say why.
- [x] 1.2 Test the boundary arithmetic directly: exactly `SUMMARY_MAX_CHARS` is unchanged; `+1` yields a marked result of exactly `SUMMARY_MAX_CHARS`; the result's tail equals the input's tail; a surrogate pair at the cut point is not split. **Mutate each assertion** — restore head-keeping and confirm the tail assertion fails, because an assertion on length alone passes under both directions.
  - **Done, and the mutation confirmed it.** Added "keeps the END of the text and discards the beginning" (a `HEAD-MARKER … TAIL-MARKER` fixture) and flipped the emoji test to the low-surrogate direction. Restoring head-keeping fails **exactly** that one new assertion out of 82 — the pre-existing length and marker-presence assertions passed under both directions, which is why they were not sufficient. Six call sites that asserted `endsWith('…[truncated]')` updated to `startsWith`.
- [x] 1.3 ~~Derive the plugin's wire bound from the server constant. It SHALL NOT exceed the server cap.~~ **Task text corrected — this was the wrong call and the delta spec was amended with it.** `_transcript.sh:33-40` states its own rationale for exceeding the cap and the rationale is sound: a client cannot know a given server's cap at runtime, so a plugin hard-bounded to one version's value would silently under-deliver against a server whose cap is higher, and the server is meant to be the only authoritative trimmer. Forcing the plugin to cut exactly at the cap would also require duplicating the truncation marker into bash, Python and TS — four copies of one string, which is the duplication pattern this repo keeps getting burned by.
  - **The defect was the DIRECTION, not the size.** Two successive tail-cuts are idempotent (the result is the last `min(bounds)` chars of the original); a tail-cut followed by a head-cut is what produced the middle window. So `RBR_TRANSCRIPT_MAX_CHARS` stays above the cap, its misleading comment is corrected, and the missing guard — that every client trims the SAME SIDE the server does — becomes 1.4.
- [x] 1.4 Add an invariant test asserting every client-side trim keeps the same SIDE as the server's. Asserting the two _numbers_ agree would have been the wrong guard — it would have failed on a correct tree and passed on the broken one, since the numbers disagreed harmlessly while the sides disagreed fatally.
  - Added `invariants.test.ts::"summary truncation keeps the same side in every layer"`. One entry per language pinned to that language's real tail idiom, each asserting the tail form is present AND the head form is absent: bash `${out: -$RBR_TRANSCRIPT_MAX_CHARS}`, opencode `body.slice(body.length - MAX_TRANSCRIPT_CHARS)`, Hermes `transcript[-_SUMMARY_MAX_CHARS:]`. Plus one asserting `truncateTitle` still keeps the HEAD, so the deliberate asymmetry is pinned rather than looking like an oversight.
  - **Mutation-checked in both directions**: flipping the server to head-keep fails the server assertion; flipping Hermes to `transcript[:_SUMMARY_MAX_CHARS]` fails its own. My first version of the guard used one regex narrow enough to miss opencode's `slice(body.length - N)` form — it failed on a correct tree, which is the other way a guard can be wrong.
- [x] 1.5 Confirm no migration is needed and say why in the task: truncation is a write-time transform and stored rows are never rewritten. Rows written before this change keep their head-slice — which is the reason the marker's position must distinguish the two.
  - Confirmed: `truncateSummary` is called by the HTTP layer on the way in, and no code path rewrites a stored `summary`. A pre-existing head-truncated row keeps its trailing marker; a row written from now on carries a leading one, so the two are distinguishable on inspection without a migration.

## 2. The fallback carries facts, not a slice

- [x] 2.1 Add the deterministic extraction beside the existing per-parser transcript seam (`rembric_format_transcript_<parser>`), NOT as a new parallel seam. Emit: files created/written/edited, commands run with failures identified, tools used, final exchange.
  - **Claude Code done** (`rembric_extract_facts_claude_code`, beside the existing `_jq`/`_fallback` seam). Verified against a real 3610-tool-call transcript. Codex CLI, and the no-jq bash fallback, are NOT done — the extraction returns empty without `jq`, which degrades to today's behaviour rather than failing.
  - **Discovery that shaped it: the current formatter DISCARDS exactly the material the facts come from.** `_rembric_format_transcript_claude_code_jq` selects only `user`/`assistant` text, so every `tool_use` and `tool_result` — the paths, the commands, the exit statuses — is thrown away. The raw path was never a lossy summary of the session; it was a summary of the conversation about the session.
  - Two jq passes, not one slurp: a `tool_result` arrives AFTER the `tool_use` it reports on, so a single streaming pass cannot mark a command failed when it sees it, and slurping a large transcript into memory inside a hook is worse than a second read.
- [x] 2.2 Do not include diffs. Record the reason in the task rather than only in the design, so a later reader does not re-add them: the cap cannot hold them and git already has them.
  - Held. Beyond diffs, a second bound had to be added that the task did not anticipate: a per-call LISTING of one real session measured **343 KB against a 10 KB cap**, with 837 file lines covering 206 distinct paths — as bloated as the transcript it replaces. The output is now aggregated (distinct files, distinct failures, counts), which took the same session to **6.8 KB**. Every bound reports what it dropped, because a cap that truncates silently reads as "this is everything".
- [x] 2.3 Fixture-test the extraction against a committed transcript sample per parser. Assert the failed command is identified AS failed — an extraction that lists commands without their status is the version of this that looks right and is useless.
  - `test/facts-fixture.claude-code.jsonl` + `test/extract-facts.test.ts`, 9 assertions. **Mutation-checked**: collapsing the failed/succeeded branch to one marker fails two of them.
  - Also asserts a file that was only READ is not reported as touched — the distinction between reading and changing is the whole point of the list.
- [x] 2.4 Prove the traceability property is real, not asserted: every emitted line must correspond to a transcript event. Test it by feeding a transcript and asserting the output contains no path and no command absent from the input.
  - Asserted by exclusion: every emitted path must appear in the input transcript.
- [x] 2.5 Prove the truncation-degradation property: take an extraction longer than the cap, truncate it through the real helper, and assert the surviving text is still a well-formed fact list — not a fragment beginning mid-record.
  - Asserted through the WIRING rather than the helper: a 400-tool-call transcript posts a body ≤ 10 000 chars that still reads `files touched (400 distinct)` and still names what it dropped. Aggregation is what makes this hold — the pre-aggregation form was 34× the cap.
- [x] 2.6 Unparseable transcript → previous behaviour, exit 0, no malformed body. Test the failure path explicitly; a fallback that throws is worse than no fallback.
  - Unparseable, missing and empty transcripts all exit 0 with no output. Also covered: no `jq` on PATH returns empty rather than erroring.

- [x] 2.7 **Wire it.** Task 2.1 as written stopped at "add the extraction", which would have left a tested function nothing called. `stop-sync.sh` now sends the facts as the fallback body and degrades to the conversation slice when the parser has no extraction or `jq` is absent.
  - **The facts REPLACE the slice rather than prefixing it**, and that follows from D1: truncation keeps the TAIL, so anything prefixed is what gets cut first. Prefixing the facts would have put the highest-value content in the position most likely to be discarded. Replacing also required the extraction to carry the final exchange, so it can stand alone — added as `last request:` / `last reply:`, bounded at 800 chars each.
  - Five wiring tests in `stop-sync.test.ts`, driving the real script against the existing fake server. **Mutation-checked**: blanking the facts call fails three of them. The pre-existing 10 tests still pass unchanged, because their fixture is conversation-only and therefore exercises the degrade path — which is the regression guarantee, not a coincidence.
  - **Two of my own new tests were vacuous and are recorded as such.** One asserted the no-jq degrade through the wiring, but emptying `PATH` removes `curl` too, so no request ever arrived and the loop asserted nothing. The other asserted the codex-cli degrade with a Claude-shaped transcript, which that parser cannot read — the absence of a POST proved nothing about the degrade. Both moved to the helper level where the condition can be created in isolation; the no-jq one additionally needs an ABSOLUTE `/bin/bash`, or an emptied `PATH` stops node finding the interpreter and the test fails to spawn rather than exercising anything.

## 3. The end-of-turn reminder

- [x] 3.1 ~~Answer Open Question 1 from the host contract~~ **Answered: the loop-guard flag is `stop_hook_active`, a boolean, true when the current stop is happening because a previous hook blocked.** Recorded because it is the fact that would have been guessed otherwise. **It is moot** — the operator chose non-interrupting feedback over blocking, so there is no loop to guard.
- [x] 3.2 Split the end-of-turn wiring into the existing async raw-sync entry plus a new synchronous entry. Do not make the raw sync synchronous — it is a side effect and must not delay the turn.
  - `stop-nudge.sh`, a second Stop entry with NO `async`. The existing `stop-sync.sh` entry stays async.
- [x] 3.3 Gate the new entry on the SAME per-session turn counter `prompt-nudge.sh` already uses (`rembric_turn_count`) at the SAME cadence. Do not introduce a second counter, a second key, or a second `_EVERY` constant: two independently-tuned cadences for one obligation is two things to keep in step, and the repo already learned that lesson with the nudge strings.
  - Reuses `rembric-turnnudge` at `SUMMARY_NUDGE_EVERY=10`, via a NEW `rembric_turn_count_peek`. The existing helper APPENDS a byte on every call, so calling it from a second hook would have doubled the count per turn and silently HALVED every cadence keyed on it. Guarded by a test that reads the counter file's size before and after.
  - **The reminder MOVED rather than being added.** There is no GET endpoint to ask whether a session already has a curated summary — only `POST /summary` and `POST /end` — so "do not fire when curated" is not implementable without new HTTP surface. Adding a second reminder at the same cadence would have been pure duplication; moving the every-10 one from `UserPromptSubmit` to `Stop` is strictly better with no extra noise. `prompt-nudge.sh` keeps only the turn-1 firing, which is protocol rather than a reminder about work already done.
- [x] 3.4 On a firing turn, make one bounded request asking whether a curated summary is owed. Emit the host's **non-interrupting** feedback carrying the canonical structure plus the extracted facts. Never emit an interrupting decision — assert that in a test, because the field name for blocking is one keystroke from the field name for feedback.
  - `hookSpecificOutput.additionalContext` only. Asserted directly (`not.toContain('"decision"')`) **and mutation-checked** by making the script emit a blocking decision: 3 tests fail.
  - **My first attempt at that mutation silently did nothing** — shell quoting meant the replacement never applied and the run reported 0 failures, which reads identically to "the test does not discriminate". Re-run with an assertion that the mutation target existed.
- [x] 3.5 Skip the request entirely on non-firing turns. The cheapest request is the one not made, and this is the difference between one round trip per ten turns and one per turn.
  - Non-firing turns exit before reading the transcript at all.
- [ ] 3.6 Implement fail-open and test every branch separately: non-2xx, timeout, unparseable response, missing configuration, **unreadable turn counter**, unexpected error. Each exits 0 with no output. **Mutate the handler to fail closed and confirm each test fails.** Note the counter branch specifically: `prompt-nudge.sh` chose to go silent rather than nudge every turn when the counter is unreadable, and this entry must make the same choice for the same reason.
- [x] 3.7 Budget the added end-of-turn latency, state the budget before measuring, and measure it on a firing turn and a non-firing one separately.
  - The cost MOVED with the reminder, and needed a bound the plan did not anticipate: a ~250-byte nudge at the start of the turn became a facts payload at the end, which on a real session is ~7 KB of INJECTED context per cadence point. Bounded at `RBR_NUDGE_MAX_FACTS_CHARS=1800`, tail-kept for the same reason the server tail-keeps, and it says `…[earlier facts omitted]` when it cuts. The stored fallback keeps its 10 000 — different consumer, different budget, same split as the rubric's two forms.
  - `UserPromptSubmit`'s own per-turn output DROPPED from >720 to 542 bytes at a divergent turn. Its budget test was retargeted to a RANGE rather than relaxed to a ceiling, so moving the reminder back fails it.
- [x] 3.8 Wire the same entry for every client whose host exposes an end-of-turn event, and record per client whether it does. Parity only where it needs no per-host logic beyond the existing seams — where a host has no such event, record the absence rather than emulate it.
  - Claude Code and Codex CLI both wired. opencode and Hermes are in-process with no end-of-turn event; they keep firing the reminder themselves at the SAME cadence number, so the constant stays in lock-step across all four while the firing point does not. The lock-step fixture was repointed at `stop-nudge.sh` for `SUMMARY_NUDGE_EVERY` and left on `prompt-nudge.sh` for `SAVE_NUDGE_EVERY`.

- [x] 3.9 **Existing tests that broke, and why each break was the right one.** Nine failures across four files, every one a real consequence rather than a flake. `hook-manifests.test.ts` pinned "the Stop handler is async" and saw `[true, undefined]` — that assertion was **strengthened** to an ordered pair of (script, async) rather than relaxed, because getting either entry's async flag wrong silently disables it. `prompt-nudge.test.ts` asserted BOTH nudges at turn 10 and now asserts the save nudge ALONE, as an exact ordered list. The cadence lock-step fixtures were repointed. The `nudge-fixtures.json` rubric strings were regenerated through the JSON parser, because the file stores `·` as `\u00b7` and a literal replacement silently matched nothing.

## 4. One rubric, one source

- [x] 4.1 Define the canonical structure once as an exported constant, with the enriched sections: goal, work accomplished, decisions and their reasons, what was verified and how, what was left unfinished and why, files.
  - `apps/server/src/mcp/summary-rubric.ts`. TWO forms, because the surfaces have different budgets: `SUMMARY_SECTIONS` (75 chars, the section list) for nudges and the tool description, and `SUMMARY_RUBRIC` (1040 chars, what each section is for and why) for the end-of-turn payload, which has no budget. The enriched sections are `Goal · Accomplished · Decisions+why · Verified+how · Unfinished+why · Files`.
- [x] 4.2 Point every surface from 0.3 at the constant. Include `post-compact.sh` — a compaction is exactly when the structure matters most and it currently carries its own copy.
  - All seven other surfaces repointed. TypeScript interpolates the constant; bash, Python and markdown carry the text and are held to it by the fixture, per the repo's cross-language rule. The second Hermes site turned out to be a byte-mirror of the whole server block (its own comment says so), so the two prose cuts below had to be mirrored there as well.
- [x] 4.3 Keep the MCP tool description terse and re-assert it against `DESCRIPTION_MAX_LENGTH`. Record the measured length and the remaining headroom. If the terse pointer does not fit, cut prose elsewhere in that description rather than raising the constant — the constant exists because the host truncates above it.
  - **The budget bit, and it was the instructions block rather than the tool description.** The enriched list took the unscoped block to **1022 against its 1000 cap**. Per this task, prose was cut instead of raising the constant: `Trivial? Skip.` (redundant — `did real work happen?` already carries the skip condition) and `each description has exact mechanics` (informational, not directive; the model reads descriptions anyway). Measured after: **965/1000 unscoped (35 headroom)**, 866/1000 path-scoped (134). Tightening the list itself from `Decisions (+why)` to `Decisions+why` saved a further 8.
  - First attempt landed at **999/1000 — one character of headroom**, which is a coincidence rather than a budget, so the second prose cut was made deliberately to buy real room.
- [x] 4.4 Add the divergence fixture: enumerate the surfaces, assert each carries text derived from the constant, and assert the enumeration itself is complete by counting call sites. **Mutate one surface's text and confirm the fixture fails**; a fixture that only checks the constant exists would have passed against all six divergent copies.
  - `invariants.test.ts::"the session-summary rubric has one source"`, three assertions: every surface carries the canonical list (accepting interpolation for TS and split literals for Python), no surface still names a superseded section, and **the enumeration itself is complete** — derived from `git grep`, so adding a ninth surface fails here.
  - **Mutation-checked both ways.** Reverting `post-compact.sh` to the old list fails two of the three. Adding a new tracked surface not in the list fails the completeness check and names the file. The first attempt at the second mutation was invalid — the rogue file was untracked, and `git grep` only sees tracked files, so it passed for the wrong reason.

## 5. Delegated work — DEFERRED, and the requirement pulled with it

- [x] 5.0 **Section 5 is deferred to its own change, and the `Delegated work MUST reach the parent session's record` requirement was REMOVED from the `plugin-session-protocol` delta rather than shipped unimplemented.** Publishing a requirement no code honours is the exact failure this repository keeps finding in its own archives; the delta now covers only the end-of-turn reminder, which is implemented and tested.
  - **The gap is real and measured**: the parent transcript contains no subagent turns (no `isSidechain` entries; 124 `Agent` tool calls and not one of their file edits), so a session that delegated its work shows `tool: Agent` and nothing about what changed.
  - **Also verified, because it would have been a defect I introduced**: plugin-level `Stop` hooks fire ONLY on the main agent's completion. The host's "for subagents, `Stop` hooks are automatically converted to `SubagentStop`" applies to hooks declared in subagent frontmatter, not to plugin-level ones — so `stop-nudge.sh` does not fire mid-work when a subagent finishes.
  - **Why deferred rather than done**: the subagent's facts have to reach the PARENT's record, and `POST /summary` overwrites rather than appends. That leaves either a new HTTP append endpoint (an `http-api` delta) or client-side accumulation in a per-session temp file. The second is not unreasonable — `rembric_turn_count` already establishes exactly that pattern — but it introduces a statefile whose lifecycle nothing currently owns, and the operator asked for parity only where it needs no complex per-client logic. It deserves its own change with that decision made deliberately.

## 6. Specs and archive

- [x] 6.1 Apply the `sessions` delta. Both MODIFIED requirements reproduce every published scenario — 5 on the structure requirement and 7 on the cap requirement. **Verify that count against the published file before archiving**: the previous change had four scenarios silently dropped by its delta and the archiver caught it, after that change's own task had claimed nothing was lost.
  - Applied by the archiver. It refused the first attempt: the handler count lives in the requirement's own HEADER, so raising it to nine RENAMES the requirement and a MODIFIED block cannot match. Restructured as ADDED + REMOVED with a `Reason`/`Migration` pair, the same shape `audit-plugin-spec-integrity` used when it moved that count from seven to eight.
- [x] 6.2 Apply the `plugin-session-protocol` delta (ADDED only, so no published scenario is at risk).
  - Applied. Scenario counts verified against the published file BEFORE archiving this time, which is why nothing was dropped — unlike the previous change, where the archiver caught four.
- [x] 6.3 Grep `openspec/specs/` for `Goal · Discoveries`, `Goal /`, `truncat`, and `19500`, and reconcile every hit. Any spec that states the head-truncation behaviour becomes false the moment this lands.
  - **Six false statements found across four capabilities, and two of them were in MY OWN delta.** The delta claimed the reminder "SHALL NOT fire when the session already carries a curated summary" with a scenario for it — never implemented, because no read endpoint exists — and its fail-open clause listed "non-2xx, timeout, unparseable response" for a hook that makes NO request at all. Both corrected to what the code does, with the missing endpoint recorded as the reason and a follow-up named.
  - `claude-code-plugin`: "eight handler entries" → nine; "the unified per-turn save+summary nudge" → save only; the post-compact block naming the old rubric; and **"[the Stop hook] never emits `hookSpecificOutput.additionalContext` or any other model-facing output"**, which cites the `proactive-save-nudges` decision that declined `Stop`. That decision is **narrowed, not reversed**, and the delta says why: forced-continuation is a property of the BLOCKING decision, not of the event, so a non-interrupting reminder is outside its scope.
  - `claude-code-plugin` (nudge requirement): the summary line fired at `count == 1 OR count % 10 == 0`; now `count == 1` only, with a scenario asserting `SUMMARY_NUDGE_EVERY` is not even declared there so the cadence has one owner.
  - `codex-distribution`: "seven handler entries" → eight, and "the same **single** script Claude Code's `Stop` hook invokes" → the first of two entries.
  - `plugin-session-protocol`: the instructions-block requirement restated the old section list; now points at the single source and says why the three `+why`/`+how` sections exist.
  - `docs/agents.md`: "seven handler entries, since `SessionStart` and `UserPromptSubmit` each declare two" → eight, since `Stop` now declares two as well.
- [x] 6.4 Re-check `mcp-api`'s truncation-ceiling requirement still reads true after 4.3 changes the description, and reconcile it in the same commit if it does not.
  - Still true: `DESCRIPTION_MAX_LENGTH` is unchanged and the description shrank rather than grew. The budget that actually bound was the 1000-char instructions block, and prose was cut there instead of raising anything (see 4.3).

## 7. Verify

- [x] 7.1 `pnpm run typecheck`, `pnpm run lint`, full test suite.
  - `pnpm run typecheck`, `pnpm run lint`, **1825 server tests + 72 Hermes tests**, all green.
- [x] 7.2 Real end-to-end against the dev stack, driving the actual hook scripts rather than calling the helpers: a session that writes a curated summary must not be interrupted; a session that does not must be interrupted exactly once; a stopped server must not interrupt at all. Testing the helpers in isolation does not exercise the wiring, and the wiring is where the `async: true` defect lived.
  - **Driven against a real dev stack, reading the row back out of `data-dev/data.db`.** A tool-bearing transcript through the real `stop-sync.sh` persisted `SESSION FACTS`, the failed `pnpm run lint`, the edited path, and the final exchange, with `summary_final = 0`. The real `stop-nudge.sh` emitted 857 bytes of `hookSpecificOutput.additionalContext` carrying both the rubric and the facts, with no blocking key.
  - **Precedence verified end to end**: a `final:true` curated summary written first SURVIVES a subsequent raw sync — the facts do not overwrite it.
- [x] 7.3 Confirm the disabled/unconfigured path is inert: with no server URL or token configured, every new hook entry exits 0 and writes nothing.
  - **The e2e found a real gap a unit test had not.** `stop-nudge.sh` reminded even with `REMBRIC_SERVER_URL`/`REMBRIC_API_TOKEN` unset, because it makes no request and so never checked configuration — telling the model to call a tool that is not reachable. The delta REQUIRED this ("any missing configuration ... SHALL exit successfully and produce no output") and the code did not do it: the third time in this change my own spec text outran the implementation. Gated and covered by a test whose comment says it came from the e2e.
  - Re-verified against the real hook: 0 bytes unconfigured, 857 configured. `stop-sync.sh` unconfigured exits 0.
- [x] 7.4 Run the `rembric-plugin-development` e2e walkthrough — this change touches `apps/plugin/` hook manifests for more than one client.
  - `pnpm run e2e:installer`: 57 tests green. The hook manifests are what the installer installs, and this change adds an entry to both.

## 8. Deferred, recorded so it is not lost

- [ ] 8.1 Surfacing uncurated summaries in `memory.context` is **not** in this change. It becomes defensible once the fallback is structured facts rather than a dump, but it needs its own measurement of the effect on context size. Open it as a follow-up citing this change's extraction format.
- [ ] 8.2 Server-side model summarisation of a session that ended raw is **not** in this change. The consolidation sweep is contractually deterministic with no LLM and no cron; introducing one is a decision about cost and latency that belongs to its own change.
- [ ] 8.3 Keeping both ends of an over-cap summary and eliding the middle is **rejected for now**, not forgotten: it needs a policy for splitting the budget and produces a discontinuity that is harder to read and to assert than a clean tail. Revisit only if 0.2's measurement shows the opening carries something the extraction does not already surface.
- [ ] 8.4 Raising `SUMMARY_MAX_CHARS` is **rejected** on the grounds recorded in design D2. If real sessions still truncate after §2 lands, that measurement is the trigger to reconsider — and the constant is one line, with no migration.

- [x] 9.1 **Post-archive sweep for stale documentation, requested explicitly.** The first grep found three leftovers; each fix surfaced more, and it took five passes to converge. Final tally beyond the six already recorded in 6.3: `plugin-session-protocol` carried BOTH handler counts plus `the Stop handler SHALL declare "async": true` (now an ordered statement about two entries with opposite obligations); `claude-code-plugin` had a SECOND requirement asserting eight entries, and my own delta had left the old section list sitting beside the new "carried verbatim from the single source" sentence, so the published text contradicted itself; `opencode-plugin`, `hermes-agent-plugin` (twice) and `mcp-api` all restated the old list; `docs/agents.md` twice; and `hook-manifests.test.ts` had a test TITLE reading "carries exactly eight handler entries" while asserting nine.
  - Editing `mcp-api`, `hermes-agent-plugin` and `opencode-plugin` meant three capabilities were touched with no paired delta, which this repo's own provenance guard rejects. Deltas were added to the archive folder for each, and the pairing was verified by diffing the edited-capability set against the arriving-delta set rather than by assuming it.
  - `openspec/specs/sessions/spec.md:71` was deliberately left: the old list appears there as an arbitrary sample VALUE inside a `writeSummary` scenario, not as a statement about the rubric.
- [x] 9.2 **CI caught a test that read its premise from the environment.** Adding the configuration gate in 7.3 broke three `stop-nudge.test.ts` cases in CI while they passed locally: the helper spread `process.env`, and this development shell had `REMBRIC_SERVER_URL`/`REMBRIC_API_TOKEN` exported from earlier e2e work. CI has neither, so the gate fired and the tests that expect the reminder got empty output. The two vars are now set EXPLICITLY in the helper, and the whole suite was re-run with `env -u` stripping them — 1826 green. `stop-sync.test.ts` and `rembric-bridge.test.ts` already declared theirs, which is why only the new file was affected.
  - This is the fifth thing in this change that passed for the wrong reason, and the only one a local run could not have found: the others were vacuous assertions or mutations that never applied, all detectable here, while this one required an environment that did not carry my exports.

## 10. Review pass (`/simplify` + adversarial `/code-review`)

- [x] 10.1 **CRITICAL, fixed: the fallback could be made to FABRICATE facts, which disproved the delta's traceability guarantee.** `input.command` was sanitised; `file_path`, `name` and `tool_use_id` were not, and the render layer parses a `KIND<TAB>VALUE` newline-delimited stream. So a `file_path` of `"/repo/ok.ts\nX\tsudo rm -rf /"` — a name a model chooses — emitted a synthetic `X` record and the output read `commands: 1 run, 1 failed / failed commands: - sudo rm -rf /` on a transcript with no `Bash` call at all. Same via a tool `name`, and a `tool_use_id` of `"q1\nq2"` marked two unrelated commands failed. The fabricated text was BOTH stored as the session summary AND injected into the next turn's context under "Ground it in these extracted facts rather than recollection". Fixed with a jq `flat` that strips `[\n\r\t]` from every value reaching the stream. Three tests, **mutation-checked**: removing `flat` fails four.
  - The pre-existing traceability test could not have caught it — it only inspected lines whose fact `startsWith('/')`, so fabricated COMMANDS were outside its reach.
- [x] 10.2 **`\t` in a `sed` BRE is a GNU extension, so the tools and files sections VANISHED on macOS.** `s/^T\t//p` and `s/^F\t//p` read `\t` as a literal `t` under BSD sed, and this file's own header sets a BSD-portability bar. A previous pass had converted only the `X` sites. All now `$'…'`.
- [x] 10.3 **PRIVACY, fixed: `<private>` spans reached the injected reminder unredacted.** The reminder bypasses `_rembric_truncate_transcript` (the stored path's redaction point) because it uses its own tighter bound, so commands, file paths AND the final exchange all leaked. A first pass redacted only the exchange; the whole rendered payload is now redacted. The STORED path was already safe. Test mutation-checked.
- [x] 10.4 **The Codex reminder could never fire.** Its manifest entry was the only one of eight carrying `REMBRIC_SERVER_URL="${CLAUDE_PLUGIN_OPTION_SERVER_URL}"` — a Claude-Code placeholder that expands to EMPTY under Codex, so the prefix OVERWROTE the ambient value and the configuration guard then exited. The prefix is gone; the entry inherits the ambient env like Codex's others. CI locked in a permanently inert hook plus a wasted process spawn per turn.
- [x] 10.5 **`SUMMARY_RUBRIC` was dead code and no surface carried the long form.** Zero importers; the text that actually shipped was hand-written a second time in `stop-nudge.sh` with nothing comparing them. Moved to `nudge-fixtures.json` — the mechanism this repo already uses for cross-language nudge text — the TS constant deleted, and a lock-step fixture added. Mutation-checked.
- [x] 10.6 **The reminder asserted "the session has no curated summary" without checking.** The signal was already in the stream: the MCP call appears as an ordinary `tool_use`, so `rembric_facts_show_summary_written` costs nothing. No endpoint needed — which also retires the reason the requirement was pulled.
- [x] 10.7 **Six extra jq passes on the synchronous end-of-turn path.** `has_work` re-parsed to answer what the stream already said. The tagged stream is now exported (`rembric_session_facts_raw`) and `stop-nudge.sh` parses once. Dead `[ -z "$FACTS" ]` branch removed with it.
- [x] 10.8 **Turn 1 delivered the reminder twice** (`prompt-nudge.sh` as protocol, `stop-nudge.sh` at the cadence). `stop-nudge.sh` no longer fires at turn 1, and a test pins it.
- [x] 10.9 **`commands: N run, M failed` counted occurrences above a deduplicated list** — a command retried twelve times read as twelve failures over one bullet. Now distinct, relabelled `N distinct failed`, with a test.
- [x] 10.10 **My own truncation guard shipped with a hole.** `stop-nudge.sh` was a fourth trimmer the invariant did not enumerate, so flipping it to a head-cut passed CI on day one. Added and mutation-checked.
- [x] 10.11 ~28 restated / duplicated-rationale comment lines cut across five files, and `claude-code-plugin`'s "the cadence has exactly one owner" corrected — three owners remain (bash plus the two in-process clients), pinned equal by a fixture.

- [ ] 10.12 **OPEN — the `has one source` fixture does not discriminate.** Its Python branch tests six section substrings INDEPENDENTLY, so reverse order, scattered prose, EXTRA sections, and a wrong separator all pass. The reviewer constructed six divergences and the fixture accepted every one — including "extra sections added", which is the exact drift mode its own comment cites as its reason to exist. It would not have caught the bug it was written for. Needs an ordered/exact match, not membership.
- [ ] 10.13 **OPEN — no size guard on the synchronous hook.** Measured 1.36 s at 20 MB and **22.6 s at 213 MB**, on a path that blocks the turn. Amortised 1-in-10 by the cadence, but the firing turn stalls. Needs a byte-size ceiling that skips extraction above it.
- [ ] 10.14 **OPEN — a command that failed and was then fixed still reads as failed.** `pnpm test` failing then passing renders `failed commands: - pnpm test`, the opposite of the session's final state, which is the one fact a "Verified+how" handoff most depends on. The later success is in the stream and is discarded.
- [ ] 10.15 **OPEN — the `tools (N distinct)` count is off by one against its own list** when a `tool_use` has an empty name (the count filters blanks, the list does not), and renders a stray leading `, `. Also `tools:` is misleading by construction: `Write`/`Edit`/`Bash` never emit a `T` record, so a session using all of them reports `tools: Read`.
- [ ] 10.16 **OPEN — false statements still in the archived docs.** `proposal.md` claims the plugin's wire bound "is derived from the server cap and cuts at a record boundary" — neither is true, and a later task in this same folder argues the opposite deliberately. `design.md` D4 and its risk table still describe "one bounded request with a short timeout" and "never when a curated summary already exists"; D6 still states the subagent event "appends" facts as a shipped decision while `proposal.md` records it as deferred; D5 still claims every surface carries the long form. And `stop-nudge.sh`'s own comment says "every other hook skips on this" — `prompt-nudge.sh` has no env guard at all.
