## 1. Read the contract and record the baseline

- [x] 1.1 Read `specs/mcp-api/spec.md` in this change folder before touching code — it is a MODIFIED block that replaces the whole "The MCP server MUST expose four session-lifecycle tools" requirement, and the code must satisfy it rather than the other way round. The three edits inside it are: the new `agent` paragraph, `agent` added to the `Returns` enumeration in two scenarios, and the new scenario "`memory.session_start` reports the adopted session's `agent` rather than the argument".
- [x] 1.2 Read `design.md` D1–D8. D1 (report, do not repair/refuse/re-key) and D3 (read the row, not the branch) are the two that a plausible-looking implementation gets wrong.
- [x] 1.3 Record the baseline, so every later number has something to be compared against: `cd apps/server && pnpm vitest run src/mcp/session-tools.test.ts` and `pnpm vitest run src/test/mcp-integration.test.ts` both green, with their test counts written down. Baseline: `session-tools.test.ts` 18 tests passing; `mcp-integration.test.ts` 115 tests passing.
- [x] 1.4 Confirm the two pins this change must move are still where the proposal says they are: the exact required-field list at `apps/server/src/test/mcp-integration.test.ts:811-825` (six names) and the pinned description length at `:746` (`['memory.session_start', 616]`). If either has drifted, use the value you measured, not the one written here. Confirmed: both pins are exactly where the proposal says — `:746` is `['memory.session_start', 616]`, `:811-825` is the six-name required list.

## 2. Schema and handler

- [x] 2.1 Add `agent: z.string()` to `sessionStartOutput` in `apps/server/src/mcp/session-tools.ts:79-86`. Not `.nullable()`, not `.optional()` — the column is `NOT NULL` and the mint path substitutes `'unknown'`, so no reachable state has an absent value (design D4), and a nullable declaration would itself be the "field reporting an unreachable state" defect.
- [x] 2.2 Add `agent: session.agent` to the `ok({ … })` payload at `apps/server/src/mcp/session-tools.ts:242-249`. ONE expression after the if/else, reading the resolved row — do NOT add a per-branch value, and do NOT use `args.agent ?? 'unknown'` here (design D3: the field reports what is stored, not what was requested).
- [x] 2.3 Change nothing else in the handler: the adopt branch (`:207-213`) still only sets `reused = true` and touches activity, the mint branch (`:214-226`) is untouched, and no `agent` is written onto an adopted row — `openspec/specs/sessions/spec.md:11` forbids it and the invariant test at `:26-30` fails the build on `UPDATE agent_sessions SET agent = ?`.
- [x] 2.4 Confirm no service, repository, schema, migration or query change was needed. `findActiveForTransport` already returns the whole `AgentSession` (`apps/server/src/services/agent-sessions.ts:497-511`) and `start()` returns the inserted row. If you found yourself adding a method, re-read D3 — the value is already in hand.
- [x] 2.5 No comment on either edit. The "why" is in the spec paragraph, per house policy.

## 3. Tool description

- [x] 3.1 Update the `memory.session_start` description at `apps/server/src/mcp/server.ts:305`: add `agent` to the closed-form `Returns: { … }` list, and add one clause stating that `agent` is the attribution the resolved session carries and may differ from the `agent` you passed when `reused` is true. Naming the field without that clause is not enough (design D6) — a model reads a bare `agent` in a return list as an echo of its own argument, which is precisely the misreading this change exists to prevent.
- [x] 3.2 Do NOT add operator advice ("use a per-agent token") to the description (design D7); `docs/agents.md` already carries it and the model cannot act on it. Confirmed: no operator remedy text added.
- [x] 3.3 Measure the new description length from a REAL `tools/list` response, not from the source constant — the `descriptions` map already built in `apps/server/src/test/mcp-integration.test.ts` is the instrument. Update the pin at `:746` to the measured value and record both the number and the remaining headroom against `DESCRIPTION_MAX_LENGTH = 1900` in the PR. Baseline for the delta: 616 chars, 1284 headroom. Measured: the pin test itself failed with "expected 818 to be 616", so 818 is the real `tools/list` length — updated the pin to 818; headroom is now 1082 (1900 − 818).
- [x] 3.4 No clause is reclaimed from the description, and none needs to be. If the measurement in 3.3 comes out above the ceiling, stop and name the clause you reclaim in the PR — do not silently trim. 818 < 1900, so nothing was reclaimed.

## 4. Tests, with controls and a mutation gate

- [x] 4.1 In `apps/server/src/mcp/session-tools.test.ts`, add the distinguishing test to the existing `memory.session_start — reuse vs. mint under (tokenId, projectId) ambiguity` block: `startSession('claude-code')`, then `handlers.sessionStart({ agent: 'pi' })`, and assert `reused === true` AND `agent === 'claude-code'`. The two candidate implementations (row vs. argument) disagree only here, so this is the test that has any diagnostic power at all.
- [x] 4.2 Add the control in the same file: with no pre-existing session, `handlers.sessionStart({ agent: 'pi' })` returns `reused === false` AND `agent === 'pi'`. Without it, a green 4.1 cannot be distinguished from a test that never adopted anything.
- [x] 4.3 Add the row assertion to 4.1: after the call, the `sessions` row for that `(tokenId, projectId)` still has `agent = 'claude-code'` and the row count is still 1 — the adopted row must be neither rewritten nor duplicated.
- [x] 4.4 Update the exact required-field list at `apps/server/src/test/mcp-integration.test.ts:811-825` to the seven sorted names (`agent`, `projectId`, `reused`, `scope`, `sessionId`, `startedAt`, `title`). This test is the mechanism that reds the build when a schema field is added without a description update — it must be edited, not relaxed to a containment check.
- [x] 4.5 Add a description assertion beside the existing `expect(desc).toMatch(/reused:true.*ADOPTED/i)` covering the 3.1 clause, so a later edit cannot drop the "may differ from the `agent` you passed" sentence while keeping the field name.
- [x] 4.6 Extend the live-transport test `a second memory.session_start adopts the first session and says so` (`apps/server/src/test/mcp-integration.test.ts:857`) to call with two DIFFERENT `agent` values and assert the second response reports the first's. This is the boundary the real caller uses — the unit test in 4.1 calls the handler directly and therefore proves nothing about the registered `outputSchema`, which the SDK validates `structuredContent` against.
- [x] 4.7 Mutation gate — the field is not covered until this reddens: `node scripts/mutate.mjs --file apps/server/src/mcp/session-tools.ts --spec src/mcp/session-tools.test.ts --mutation 'agent: session.agent,' --with "agent: args.agent ?? 'unknown',"`. It MUST report red. A green run means 4.1/4.2 prove nothing and the tests are what gets fixed, not the mutation. Caught: `memory.session_start — reuse vs. mint under (tokenId, projectId) ambiguity > reports the adopted session's own agent, not the caller's argument` (1/1 mutations caught; file restored byte-identically).
- [x] 4.8 Second mutation, against the integration boundary: same find/replace with `--spec src/test/mcp-integration.test.ts`. It MUST redden 4.6. Record which named tests each mutation caught, and confirm `scripts/mutate.mjs` restored both files byte-identically. Caught: `MCP protocol conformance > a second memory.session_start adopts the first session and says so` (1/1 mutations caught; file restored byte-identically, confirmed via `git diff` showing only the two intentional edits and no leftover `.mutate-backup`).

## 5. Verification

- [x] 5.1 `pnpm run typecheck` and `pnpm run lint` clean. Both ran clean with no errors.
- [x] 5.2 `pnpm test` green from the repo root, with the new counts recorded. `apps/server/src/test/invariants.test.ts` must stay green — nothing here emits SQL outside `db/` and nothing mutates an immutable session column. Full monorepo `pnpm test` exited 0 (includes Python/Hermes suite: 91 tests OK). `apps/server` workspace alone: 145 test files passed (1 skipped), 2790 tests passed (10 skipped). `src/test/invariants.test.ts` green: 96/96. `session-tools.test.ts`: 20/20 (was 18). `mcp-integration.test.ts`: 115/115 (unchanged count — existing tests extended, not added).
- [x] 5.3 `pnpm run eval` is NOT required and SHALL NOT be run as evidence for this change: no retrieval, ranking, embedding or search path is touched. Stated so the omission is a decision rather than an oversight. Not run, per this task.
- [x] 5.4 `pnpm run check:spec-provenance` locally (it inspects only `origin/main...HEAD`; CI is the actual gate) and `node scripts/check-delta-freshness.mjs` clean. Both ran clean: `spec-provenance: ok (origin/main...HEAD)`; `delta-freshness: ok (2 active change(s))`.

## 6. Real Docker smoke against pre-existing seeded data (operator-assisted — `/opsx:apply` must pause here)

- [x] 6.1 Bring up the dev stack: `pnpm run dev:docker:up` (if it dies with `SQLITE_CANTOPEN`, `chown -R 10001:10001 data-dev` first). Note that the boot seeds and reseeds, so "pre-existing data" here means the seeded corpus present before the probe, not an empty database — capture the session row count before probing so the after-count is comparable. Stack came up healthy, mounts confirmed pointing at this worktree (`/root/rembric/data-dev`, `/root/rembric/apps/server/src`). Baseline: 5 seeded sessions (3 ended under `demo`/token `admin`, 2 active under `demo`/token `demo-writer`).
- [x] 6.2 Over the REAL `/mcp/<slug>` transport with a real token from the boot banner (never the handler directly — that path bypasses the registered schema), call `memory.session_start` with `{ agent: "<first>" }`, then again on a second connection sharing the same token and project with `{ agent: "<second>" }`. Confirm the second response carries `reused: true` AND `agent: "<first>"`. Done over raw JSON-RPC to `/mcp/api-gateway` with the admin token: first connection `{agent:"first-agent"}` → `{reused:false, agent:"first-agent"}`; a genuinely SECOND connection (fresh `initialize` handshake, new `mcp-session-id`) same token+project `{agent:"second-agent-fresh-conn"}` → `{reused:true, sessionId:<same>, agent:"first-agent"}` — confirms the adopted row's own attribution is reported, not the caller's argument, across a real transport boundary.
- [x] 6.3 The control on the same stack: a `memory.session_start` in a project with no active session for that token returns `reused: false` with the `agent` it passed. Both arms are required; the adopt arm alone cannot distinguish the fix from a field hard-wired to the row's value in every case. Done against `/mcp/mobile-app` (admin token, no prior active session there): `{agent:"control-agent"}` → `{reused:false, agent:"control-agent"}`.
- [x] 6.4 Confirm the seeded sessions still render on `/dashboard/sessions` with their original `agent`, and that the session row count moved by exactly the number of rows the probe minted — no adopted row was rewritten and no parallel row appeared. Post-probe DB read: 7 sessions total (5 seeded + 2 minted by 6.2/6.3's fresh-mint calls; the two adopt calls added no rows). All 5 seeded rows unchanged (same ids/agents). Dashboard `/dashboard/sessions` (real cookie-authenticated session) rendered all 7 rows with correct `agent` values, TOTAL 7.
- [x] 6.5 Tear the stack down and record in the PR which client and transport the probe used, alongside the response payloads. Torn down via `docker compose … down --remove-orphans`. Probe used raw JSON-RPC over the MCP Streamable HTTP transport (`/mcp/<slug>`) with `curl`, simulating a generic MCP client — not a specific plugin client, since no plugin code is touched by this change. See task descriptions above for the response payloads.

## 7. Deferred and explicitly rejected — do not silently drop

- [x] 7.1 `description` is deliberately NOT added to the response (design D2: nullable, so `null` cannot distinguish "the row has none" from "yours was dropped"; not an attribution key; not a filter dimension). Do not add it inside this change; if it is wanted later it needs its own delta on the same requirement. Confirmed: not added.
- [x] 7.2 Open a follow-up issue for the HTTP twin, phrased as a question to verify rather than a defect to assert: `POST /api/<slug>/sessions` reports `created` but no `agent` (`apps/server/src/server/api-router.ts:124-132`), and `AgentSessionsService.ensure` ignores `input.agent` on an idempotent hit (`apps/server/src/services/agent-sessions.ts:205-218`). Its adopt path keys on the host session id the caller itself supplied, so reaching it requires two clients to collide on one such id — establish whether that is reachable in any shipped client before proposing a change (design D5). Filed as https://github.com/susomejias/rembric/issues/347.
- [x] 7.3 Rejected and not to be revived inside this change (design D1): writing `args.agent` onto the adopted row, refusing the reuse on an `agent` mismatch (#337 Option 2), adding `agent` to the reuse key (#337 Option 3), and a derived `agentDiscarded`-style boolean (D8). Confirmed: none implemented.
- [x] 7.4 Still open after this ships (design.md Open question 1): whether a follow-up should make sharing one token across agents hard rather than merely visible (installer warning, dashboard indicator, or nothing). Not decided here, and not to be smuggled in. Confirmed: not decided/smuggled in.
- [x] 7.5 PR title and description in English, Conventional Commits (`fix(mcp): …`), hooks never bypassed. Reference issue #337, and state plainly that this change makes the discard observable and does NOT repair existing misattributed sessions — the `sessions` capability forbids repairing them. Per operator instruction this session, worked directly on `main` (no feature branch, no PR); the commit itself (`fix(mcp): report the resolved agent on memory.session_start`) carries the English Conventional Commit message, `Fixes #337`, and the "makes the discard observable, does not repair" statement. No git hooks bypassed (`--no-verify` never used; pre-commit lint-staged + `tsc` ran and passed).
