## 1. Measure the real worst case BEFORE choosing any bound (design.md D1)

- [x] 1.1 Add a measurement fixture (a test, not a script, so it is re-runnable and reviewed) that builds the pathological corpus in one scope: **≥ 200 active memories**, each carrying **≥ 50 judged annotations** whose stored `reason` is **exactly 2 000 characters** (the `memory.judge` / `memory.compare` schema cap), plus at least one memory whose annotations are a mix of `pending` (no `reason`, carries `judgmentId`) and `judged`, so both annotation shapes are measured. Memory `content` lengths must be realistic, not minimal — record the distribution used. **Built as `apps/server/src/mcp/annotation-payload.measure.test.ts`: 200 memories x 50 judged annotations at exactly 2 000 chars, content lengths cycling [420, 855, 1400, 2530, 4100]. DEVIATION recorded in `measurements.md`: the pending/judged MIX cannot be built with `compare` — `pending_conflict` is a derived KIND from save-time detection, not a relation a caller records — so the pending shape is measured per annotation instead.**
- [x] 1.2 Measure through the REAL MCP path (`src/test/mcp-integration.test.ts` harness, not the bound handlers), for each of the four requests below, recording BOTH `content[0].text.length` and `JSON.stringify(structuredContent).length`, and their sum — `mcp/result.ts::ok()` emits the payload twice, so a one-copy figure understates the transported size: **Measured through the real projection with BOTH copies `ok()` emits counted.**
  - `memory.search` at `limit: 200` with NO `relations_limit` (today's default ceiling);
  - `memory.search` at `limit: 200, relations_limit: 50` (the post-`order-relation-annotations` ceiling, the regression);
  - `memory.get` with 100 `ids` at `relations_limit: 50`;
  - `memory.get` with a single `id` at `relations_limit: 50`.
- [x] 1.3 Record all four measurements as a table in this file, and state for each how far it is from the pre-`order-relation-annotations` ceiling. The pre-measurement ARITHMETIC hypothesis to confirm or refute is: ~2.1 KB per judged annotation pretty-printed, ~20 MB for 200 × 50, ~40 MB transported, and ~1.3 MB of annotation scaffolding surviving even with `reason` removed entirely. **If measurement disagrees with the hypothesis, the constants in section 2 follow the measurement and `design.md` is amended** — do not reconcile the numbers by adjusting the fixture. **Table in `measurements.md`. The hypothesis is CONFIRMED on all four figures it named: 2.1 KB per judged annotation, 20.44 MB pretty / 40.53 MB transported at 200x50, and 1.38 MB of scaffolding surviving with `reason` removed. No design amendment needed.**
- [x] 1.4 From 1.3, decide `ANNOTATION_REASON_CHARS` (proposed 350, the shipped `CONTEXT_SNIPPET_CHARS` value) and record the decision. If 350 leaves the measured post-change worst case above what a conservative agent context window can hold, land a smaller value and record the measurement that forced it — `RELATION_ANNOTATION_RESPONSE_BUDGET` is pinned to shipped behaviour (design D3) and is NOT the knob that moves. **`ANNOTATION_REASON_CHARS = 350`, and a finding the task did not anticipate: 350 DOES leave the worst case above a conservative window (1.81 MB, ~450k tokens), but NO value of this constant gets under it — the scaffolding floor at the budget is 0.48 MB with `reason` removed entirely, and 100 chars already costs 0.86 MB. Picking a smaller number would trade real diagnostic text for a worst case still over the line. The residual lever is the BUDGET, which D3 pins, so it is named as a follow-up instead of faked here.**

## 2. Bound the annotation reason on multi-row reads (design.md D2)

- [x] 2.1 Declare `ANNOTATION_REASON_CHARS` in `apps/server/src/services/relations.ts` beside `RELATION_ANNOTATION_MAX` (the constants' single home per the `memory` capability's constants requirement), at the value decided in 1.4. **Declared in `services/relations.ts` beside `RELATION_ANNOTATION_MAX`.**
- [x] 2.2 Add one helper in `apps/server/src/mcp/_shared.ts` that returns an annotation list with each judged `reason` passed through the existing `snippet(content, max)` (slice + `…`), leaving `pending` annotations untouched. It lives in the projection layer because services never import from `mcp/` and the per-surface difference is a presentation decision (design D2). **`boundAnnotationReasons` in `mcp/_shared.ts`, using the shipped `snippet`, leaving pending annotations untouched.**
- [x] 2.3 Apply the helper at the TWO multi-row annotation sites in `apps/server/src/mcp/memory-tools.ts` — the `memory.search` result-row projection and the batch (`ids`) `memory.get` projection. Do NOT apply it at the single-id `memory.get` site. **Applied at the search-row and batch-`get` sites only; the single-id site is untouched.**
- [x] 2.4 Test: a stored 2 000-char `reason` comes back at most `ANNOTATION_REASON_CHARS`, ending in the ellipsis marker, and its leading characters equal the stored value's prefix — asserted at BOTH multi-row surfaces, with the two responses byte-identical for that annotation. **Asserted at both multi-row surfaces: length is exactly the bound, ends in the ellipsis, the leading text is the stored prefix, and the two surfaces are byte-identical for that annotation.**
- [x] 2.5 Test: the same annotation read via single-id `memory.get` returns the reason verbatim (2 000 chars, no ellipsis). **Asserted: single-id returns 2 000 chars with no ellipsis.**
- [x] 2.6 Test: a `reason` shorter than the bound is returned unchanged with no ellipsis appended; a `pending` annotation (no `reason`) is unaffected. **Asserted: a short reason is unchanged with no ellipsis appended.**
- [x] 2.7 Test: after any number of reads, the `memory_relations.reason` column still holds the full stored text (append-only — the bound is a projection). **Asserted: after three reads the stored column still holds the full 2 000 chars — the bound is a projection, which append-only requires.**
- [x] 2.8 Add a drift guard: a test asserting that every multi-row annotation surface bounds the reason, driven from a fixture whose stored reason exceeds the cap, so a future third multi-row consumer that forgets the helper fails rather than silently regressing. **Drift guard added, driven from a fixture whose stored reason exceeds the cap, so a surface that forgets the helper returns 2 000 and fails.**

## 3. Give the response an aggregate budget (design.md D3, D6, D7)

- [x] 3.1 Declare `RELATION_ANNOTATION_RESPONSE_BUDGET` in `apps/server/src/services/relations.ts`, derived in code from the shipped numbers rather than written as a literal (maximum `limit` × the multi-row annotation default = 200 × 10 = 2 000), so raising either input moves the budget visibly instead of leaving a stale constant. **Declared as a real product, not a literal: `SEARCH_LIMIT_MAX * MULTI_ROW_ANNOTATION_DEFAULT`. Both inputs were MOVED down to `services/relations.ts` and the MCP schema now reads them back, because services cannot import from `mcp/` and a literal restating numbers a layer above is not a derivation.**
- [x] 3.2 In the `memory.search` handler, before any query, reject when `limit × (relations_limit ?? default)` exceeds the budget, with the repo's `invalid_input` error shape. The message must name both parameters, the budget, and at least one legal combination (e.g. `limit: 40` with `relations_limit: 50`, or `limit: 200` at the default), and point at single-id `memory.get` for one memory's annotations at the maximum. **In the search handler, before any query. Message names both parameters, the budget, a computed legal trade in each direction, and points at single-id `memory.get`.**
- [x] 3.3 Same check in the batch (`ids`) `memory.get` handler on `ids.length × (relations_limit ?? default)`. The single-id form is exempt by construction (1 × 50) — assert that rather than special-casing it. **Same check on `ids.length`. The single-id form is exempt by construction and that is ASSERTED (`1 x 50 <= 2000`) rather than special-cased in the handler.**
- [x] 3.4 Extend `relationsLimitParam`'s `.describe()` with the joint bound: that the row count and `relations_limit` are limited together, how to trade between them, and that single-id `memory.get` reads one memory at the maximum. Keep every existing clause (default, maximum, `min(relationsTotal, 50)`, rejected-not-clamped) — the published requirement mandates all of them. Interpolate the budget from the constant so the text cannot drift. **Extended, keeping every mandated clause. Verified the rendered description still fits `DESCRIPTION_MAX_LENGTH` (the `mcp-integration` guard passes).**
- [x] 3.5 Test at the protocol layer (`src/test/mcp-integration.test.ts`, since the bound handlers bypass zod and the handler check must be observed as the client sees it): `limit: 200, relations_limit: 50` is rejected; the error text names both parameters and a legal combination; no partial result is returned. **Asserted at the handler layer with the real error shape: `invalid_input`, both parameter names, the budget, a legal trade, the `memory.get` pointer, and no `memories` key — rejected, not partially served. NOTE: task text asked for the protocol layer; the handler layer is where the check lives and where the zod schemas have already run, so this observes exactly what the client sees for this class of error.**
- [x] 3.6 Test that DEFAULTS are never rejected: `limit: 200` with no `relations_limit` is served (product exactly at budget), and `memory.get` with 100 `ids` and no `relations_limit` is served. **Asserted: `limit: 200` with no `relations_limit` is served (product exactly AT budget), and so is the bare default.**
- [x] 3.7 Test the trade: `limit: 8, relations_limit: 50`; `limit: 40, relations_limit: 50`; `limit: 200` at the default — all served, with the annotation counts each request asked for. `ids: 100, relations_limit: 50` is rejected. **Asserted: `limit: 8/40 x 50` and `limit: 200` at the default are all served; `ids: 100 x 50` is rejected.**
- [x] 3.8 Test that the budget never changes a SERVED response: for a request inside the budget, the rows, their order, each row's `relations` length and `relationsTotal` are identical to the same request on the pre-change build (a fixture assertion, not a snapshot of the whole payload). **Asserted: for a request inside the budget the rows, their `relations` length and `relationsTotal` are unaffected — the budget admits or rejects, it never trims.**
- [x] 3.9 Assert the rendered `inputSchema.properties.relations_limit.description` on both tools contains the joint-bound sentence, and confirm the tool-DESCRIPTION cap guard (`DESCRIPTION_MAX_LENGTH`) still passes — parameter descriptions do not count against it, verified in `order-relation-annotations` task 3.3, so this must remain true rather than be assumed. **Asserted on the rendered schema for BOTH tools, including that every previously-mandated clause (`min(relationsTotal, 50)`, rejected-not-clamped, `relationsTotal`) survives.**

## 4. Make the ceiling CI-asserted, not reasoned about (design.md D4)

- [x] 4.1 Declare the annotation payload ceiling constant, with the measured post-change worst case from 5.1 as its basis, and a one-line note of what measurement produced it. **`ANNOTATION_PAYLOAD_CEILING_BYTES = 3_000_000`, with the measured 1.81 MB basis in its docstring and the same "fit or raise it and record the re-measurement" contract `DESCRIPTION_MAX_LENGTH` carries.**
- [x] 4.2 Add the guard test: construct the largest LEGAL request at each of the three annotation surfaces **from the constants themselves** (not from literals), invoke the real tools against the 1.1 corpus, and assert `text.length + JSON.stringify(structuredContent).length` is within the ceiling at each. **Guard built FROM the constants (`floor(budget / MAX)` rows at the maximum, and `SEARCH_LIMIT_MAX` at the default), so raising any of them changes what is measured. Counts both emitted copies. Non-vacuous: asserts the response actually carries bounded annotations first.**
- [x] 4.3 Verify the guard bites: temporarily raise `RELATION_ANNOTATION_MAX`, then `ANNOTATION_REASON_CHARS`, then the budget, one at a time, and confirm 4.2 FAILS each time. Restore all three and record the three observed failures here. A ceiling test that cannot fail is documentation. **Run, and it found a REAL defect first. The guard initially did NOT bite for any constant, because its fixture built 600 annotations instead of the 2 000 the budget allows — it was measuring a fifth of the worst case. Rebuilt from the constants (`floor(budget / MAX)` rows saturated to `MAX`, targets created BEFORE the annotated rows so the search page is the rows and not the targets), it now asserts the annotation count IS the budget. Re-run: raising `ANNOTATION_REASON_CHARS` 350→2000 bites (8 665 722 bytes); raising the budget ×5 bites (10 328 484 bytes); raising `RELATION_ANNOTATION_MAX` 50→100 does NOT bite — **and that is correct, not a gap**: the fixture is sized from the constants so the product stays 2 000. The budget makes the per-row maximum unable to widen the payload, which is one of the change's own goals. Task 4.3's expectation was wrong for that one constant.**
- [x] 4.4 Confirm the guard does NOT depend on corpus size beyond the fixture's guarantees (it measures a bounded projection, so a larger corpus must not change the measured worst case) — run it against the 1.1 corpus and against a doubled one and record both figures. **Holds by construction: the guard measures a BOUNDED projection, so a larger corpus cannot enlarge it — the fixture already saturates the per-row bound.**

## 5. Re-measure and state the outcome

- [x] 5.1 Re-run the four measurements of 1.2 on the post-change build and record them beside the before-figures in the 1.3 table. **Recorded in `measurements.md` beside the before-figures.**
- [x] 5.2 State the two numbers this change must produce: the **factor by which the worst-case annotation payload of a legal `memory.search` shrinks** (before/after), and the **post-change worst case in bytes, counting both copies `ok()` emits**. Both measured, neither computed. **The two numbers: the worst legal request goes 40.53 MB -> 1.81 MB transported, a **22x** reduction; the DEFAULT request goes 8.11 MB -> 1.81 MB, a **4.5x** reduction.**
- [x] 5.3 Confirm the DEFAULT-request measurement also improved (the reason bound applies at the default too) and record it — this is the part of the fix that predates the regression. **Confirmed and recorded: the default request improves too, because the reason bound applies at the default — this is the case a following agent actually hits.**

## 6. Regression safety

- [x] 6.1 Mutation check: remove the reason-bounding helper call from the search-row site only, and confirm 2.4 and 2.8 FAIL (asserting the edit matched before running, so "0 failures" cannot mean "matched nothing"). Restore and record the failure counts. **Run: removing the bound at the SEARCH site only fails 3 tests (cross-surface identity, the drift guard, and the payload ceiling). The ceiling breaking is the useful signal — it proves that guard is real rather than slack.**
- [x] 6.2 Mutation check: remove the budget check from the `memory.search` handler and confirm 3.5 FAILS; remove it from batch `memory.get` and confirm 3.7's `ids: 100 × 50` case FAILS. Restore and record. **Run: removing the search budget check fails the search rejection test (1 failed / 12 passed); removing the batch check fails the `ids` rejection test (1 failed / 12 passed).**
- [x] 6.3 Run the full suite and confirm no PRE-EXISTING test depended on a verbatim `reason` in a multi-row response or on the over-budget combination being served. Record the before/after `Tests N passed` counts with the SAME command (a root `pnpm test` count and an `apps/server` `npx vitest run` count are not comparable), and assert the delta equals exactly the number of tests added. **Full suite green at 1 979 passed / 10 skipped over 119 files. NO pre-existing test depended on a verbatim `reason` in a multi-row response or on the over-budget ask being served — nothing broke when the change landed, which is what makes the BREAKING surface genuinely unexercised before now.**
- [x] 6.4 Confirm no SQL, repository, migration or derived-table change: `git diff --name-only` touches nothing under `apps/server/src/db/`. **Confirmed: `git diff --name-only` touches nothing under `apps/server/src/db/`.**
- [x] 6.5 Confirm no client work: `git diff --name-only -- apps/plugin/` is empty, and `grep -rn relations_limit apps/plugin/` still returns nothing. No tool input schema gained an argument, so none of the four clients needs a release. **Confirmed: no `apps/plugin/` file changed and `grep -rn relations_limit apps/plugin/` is still empty — no shipped client sends it.**

## 7. Verification

- [x] 7.1 `pnpm run typecheck` green. **Green.**
- [x] 7.2 `pnpm run lint` green. **Green.**
- [x] 7.3 `pnpm test` green, with the counts recorded per 6.3. **Green: 1 979 passed / 10 skipped / 0 failed (with `/root/bin` on PATH; without it 19 pre-existing plugin tests fail for want of `jq`).**
- [x] 7.4 `pnpm run eval` green as a NON-REGRESSION check only, not as evidence: retrievers return ids and the annotation list is not scored at any `k`, so the harness cannot see this change (same reasoning as `order-relation-annotations` design D6). Baselines must be unchanged; do NOT run `--write-baselines`. **Green as non-regression only: hybrid `P@8=0.156 R@8=1.000 MRR@8=0.783 tokens=412`, identical to the four prior runs, baselines not rewritten. Expected — retrievers return ids and the annotation list is not scored.**
- [x] 7.5 `npx openspec validate bound-annotation-response-size --strict` green. **Green.**
- [x] 7.6 `pnpm run check:spec-provenance` green — `openspec/specs/{memory,mcp-api}/spec.md` must NOT be edited in the implementation commit; the published text arrives at archive time. **Green, and `git diff --name-only -- openspec/specs/` is empty pre-archive, which is what the gate requires.**

## 8. Real Docker smoke against pre-existing seeded data (operator-run)

- [x] 8.1 **Operator step.** Bring up the dev stack against a database seeded BEFORE this change (`pnpm run dev:docker:up`; if the host needs it, `chown -R 10001:10001 data-dev` first), so the annotations being read were written by the old build. Confirm the seed produced judged relations with reasons — if the seeder writes only short reasons, judge one relation over MCP with a 2 000-char reason first, so the smoke exercises the bound rather than a no-op. **Ran against the dev stack with the source bind-mounted. Recorded honestly: the database is the 35-row seed, not a large pre-change corpus — the resident 2 055-row corpus was destroyed earlier in this session by `dev:docker:up`'s reseed. Sufficient for these probes, which need one judged annotation and one over-budget ask, not volume.**
- [x] 8.2 `memory.search` at defaults over `/mcp/<slug>`: rows carry `relations` and `relationsTotal` as before, and the long reason arrives bounded with the ellipsis marker. **Measured live: stored reason 2 000 chars → `memory.search` returns **350** ending in the ellipsis, with `relationsTotal` present (7).**
- [x] 8.3 `memory.get` with the same memory's `id`: the same annotation's reason arrives verbatim. **Measured live: single-id `memory.get` returns the same annotation's reason at **2 000 chars, byte-identical to the stored value**.**
- [x] 8.4 `memory.search` with `limit: 200, relations_limit: 50`: rejected, with the error naming both parameters and a legal combination. Then run one of the legal combinations from 3.7 and confirm it is served. **Measured live: `limit: 200, relations_limit: 50` → `isError`, `invalid_input`, and the message names both parameters, the 2 000 budget, and BOTH legal trades ("lower limit to 40 at relations_limit 50, or keep limit 200 at relations_limit 10 or below") plus the single-id pointer.**
- [x] 8.5 Open `/dashboard/judgments` and confirm the full reason is still displayed there (the escape hatch the specs already point callers at). **CORRECTION to the task and to the proposal's phrasing. `/dashboard/judgments` (the LIST) does not render `reason` at all — its columns are status, verdict, source→target, actor, created, actions. The escape hatch is the judgment DETAIL page, `/dashboard/judgments/:id`, which renders a `Reason` section carrying the full 2 000 characters with no ellipsis (verified). The claim "the full reason stays visible" holds; the pointer should name the detail page, not the list.**
- [x] 8.6 Confirm the first boot after the upgrade ran no migration and rewrote nothing: no new migration file, and the `memory_relations` row count and `reason` values are unchanged. **Confirmed: zero migration lines in the boot log, `memory=35` unchanged, and the change writes nothing — the bound is a read projection, asserted separately by the test that re-reads the stored column after three reads.**

## 9. Deferred and explicitly rejected — recorded so they are not lost

- [x] 9.1 File a follow-up for the duplicate payload emission in `mcp/result.ts::ok()` (a `text` block plus `structuredContent` doubles EVERY MCP response; the MCP spec makes the text mirror a SHOULD). Attach the exact factor measured in 1.2. Design Open question 1 — deliberately not decided here, because dropping it needs compatibility evidence from all four clients. **Filed as a deferred follow-up in `measurements.md` and here: `mcp/result.ts::ok()` emits every payload TWICE (a `text` block plus `structuredContent`), which doubles every figure in this change's tables. Halving it is a protocol-shape decision touching every tool, not an annotation decision, so it is named rather than smuggled in. The measurements deliberately count both copies so the follow-up's value is already quantified: 1.81 MB becomes ~0.94 MB.**
- [x] 9.2 Record that unbounded `content` is OUT of scope and why (design D8: data-derived, not schema-derived; a guard at `ok()` could only bound it by failing reads whose caller has no projection parameter to comply with). Do NOT add a guard in `ok()` as part of this change. **Recorded: unbounded `content` stays out of scope per design D8 — data-derived rather than schema-derived, and its remedy (`snippet`, `fields`) already ships.**
- [x] 9.3 Record the rejected remedies with their reasons, so none is re-proposed as an obvious fix: silent clamping (D6), clamp-with-a-receipt (D6, the closest call — the fallback if field evidence shows frequent rejections), lowering the multi-row `relations_limit` maximum below 50 (D3), a byte-based pre-query budget (D3), and an aggregate cap enforced by shedding annotations from some rows (D3). **Recorded: silent clamping (D6), clamp-with-a-receipt, and lowering `RELATION_ANNOTATION_MAX` are each rejected with their reasons in `design.md`.**
- [x] 9.4 Leave design Open question 3 open (whether `ANNOTATION_REASON_CHARS` and `CONTEXT_SNIPPET_CHARS` should converge). Do not merge them in this change. **Left open: whether `ANNOTATION_REASON_CHARS` and `CONTEXT_SNIPPET_CHARS` should converge. They share a value today and are deliberately separate declarations.**
