## 1. Plugin bug fixes (Claude Code / Codex CLI)

- [x] 1.1 Fix `apps/plugin/commands/recall.md` to call `memory.search({query: '$ARGUMENTS', limit: 5})` (was `q:`); verify manually that a `/rembric:recall <keyword>` invocation returns keyword-matched results, not the recency listing.
- [x] 1.2 Fix `apps/plugin/commands/context.md` to drop the unsupported `limit` argument from its `memory.context(...)` call.
- [x] 1.3 Update `apps/plugin/scripts/prompt-search.sh` to read the hook's stdin JSON `prompt` field and apply the existing recall-intent keyword regex itself before proceeding, so it self-filters identically on Claude Code (where the declared matcher already filters) and Codex (whose dispatcher does not consult `UserPromptSubmit` matchers).
- [x] 1.4 Add a `unit test / fixture check (co-located `\*.test.ts`or a shell-level check under the plugin's existing test harness) asserting`prompt-search.sh` exits without emitting the nudge for a non-matching prompt and emits it for a matching one, independent of any matcher.

## 2. Server bug fixes and existing spec-gap closure

- [x] 2.1 Fix `apps/server/src/mcp/errors.ts`'s `errToMcp`: non-`DomainError` exceptions produce a generic message + a generated error id in the MCP response, and the full error (with the same id) is logged server-side via the new logger from task 2.2 — never returned to the client. Add/update a test asserting the client-facing message never contains the raw `err.message`/stack.
- [x] 2.2 Add a small leveled logger module honoring the already-parsed `config.logLevel` (`apps/server/src/config.ts`), and replace the ad-hoc `console.error` calls in `bootstrap.ts` and the `errToMcp` path (task 2.1) with it. Add a test asserting a log below the configured level is suppressed and one at/above it is emitted.
- [x] 2.3 Fix `apps/server/src/dashboard/memories.ts`'s FTS search branch to apply scope/status/type filters BEFORE the page slice, not after; add a test with more matching rows than one page that asserts a full page is returned when enough matches exist.
- [x] 2.4 Implement `include_global` in the project-scoped branch of `memory.search`: widen the lexical branch's `WHERE` clause to `(scope='project' AND project_id=:pid) OR (scope='global')` when `include_global=true`, and run the dense/kNN branch against both the project and global partitions, unioning candidates before the existing RRF step. Add/update tests matching the two scenarios already in `openspec/specs/memory/spec.md` (project+globals when requested; globals-only search never returns project rows).
- [x] 2.5 Wire the dashboard's vendored `htmx.min.js` into the SSR shell (`apps/server/src/dashboard/templates.ts::shell()`) via a `<script src>` tag, and add the `hx-*` attributes the spec already describes to the memories filter form (via new `filtersBar`/`viewHead` opts), the `data-confirm` modal rebind, and the timestamp-upgrade script's `htmx:afterSwap` re-run — so filtering updates without a full page reload. Unit-tested (rendered HTML asserts `hx-get`/`hx-target`/`hx-select`/`hx-push-url`/`hx-swap-oob`); full-browser verification against `pnpm run dev:docker:up` deferred to the task 9.4 e2e smoke pass.
- [x] 2.6 Raise `PAGE_SIZE` from 10 to 50 in `apps/server/src/dashboard/components.ts`; update any test fixture that hardcoded the old page size.
- [x] 2.7 Render predecessor content snapshots (not just title/status/timestamp) in the memory detail view's history-chain visualization (`apps/server/src/dashboard/memories.ts`).
- [x] 2.8 Correct `.agents/skills/rembric-plugin-development/references/per-client-gotchas.md`: remove the stale `plugin_hooks`-feature-flag guidance for Codex (removed upstream in `codex-cli` 0.142.3+) and the incorrect `codex plugin update/enable/disable` subcommands (real verbs: `add`/`list`/`remove`/`marketplace{add,list,upgrade,remove}`).

## 3. Search ranking: post-fusion boost and relation expansion

- [x] 3.1 Implement the post-fusion boost in `apps/server/src/services/hybrid-search.ts`: a compile-time-constant `boost(candidate)` function of `confirmationCount`, time-since-`last_seen_at`, and `type`, clamped to `[0.7, 1.4]`, applied as `finalScore = rrfScore * boost` before truncating to `limit`. Applies only to the text-query branch, never the no-query chronological listing.
- [x] 3.2 Add tests: a heavily-confirmed, recently-seen memory outranks a stale unconfirmed one with a close raw RRF score; the boost never pulls in a memory absent from the fused pool; the no-query listing path is unaffected.
- [x] 3.3 Add an optional `include_relations` boolean param to `memory.search`'s schema (`apps/server/src/mcp/memory-tools.ts`) and service layer (`apps/server/src/services/memory.ts`): when true, after the primary result set is computed, resolve each result's `supersedes`/`superseded_by`/`conflicts_with` relation targets not already present, fetch them (respecting scope isolation), and return them in a separate `expanded` array (each entry tagged `expandedFrom`/`relationKind`), capped at 5 entries, never counted against `limit`.
- [x] 3.4 Add tests matching the three new scenarios in `openspec/specs/memory/spec.md`: co-surfacing a `superseded_by` head; the 5-entry cap; `expanded` is absent when `include_relations` is omitted/false.

## 4. Dashboard quick-wins

- [x] 4.1 Add a `last_seen_at` row (always shown, via the shared timestamp helper) to the memory detail metadata block (`apps/server/src/dashboard/memories.ts`).
- [x] 4.2 Add forward "Superseded by" navigation to the memory detail view for `status='superseded'` memories, reusing the existing `findSuccessorId` repository method; render nothing for non-superseded memories or unresolved successors.
- [x] 4.3 Add a `needs_review` count badge to the `MEMORIES` sidebar nav entry (`apps/server/src/dashboard/components.ts` / sidebar template), reusing the existing `adminCountNeedsReview` repository read and mirroring the `JUDGMENTS` badge's rendering pattern; omit the badge entirely when the count is 0. Do NOT add a new Overview stat card. (Note: computed on all three memories.ts routes for consistency, not just Overview like the pre-existing Judgments badge — a real, low-risk improvement over the pattern being mirrored.)
- [x] 4.4 Add a "PAGE X OF Y" indicator next to the existing PREV/NEXT pager on `/dashboard/memories`, `/dashboard/sessions`, `/dashboard/prompts`, and `/dashboard/judgments`, computed from each view's existing true filtered total and the shared `PAGE_SIZE` constant (minimum "PAGE 1 OF 1" on an empty result set).
- [x] 4.5 Add a fifth "Backup database" region to `/dashboard/maintenance` (admin-scope only, matching the existing three purge cards' gating): a `data-confirm-tone="warn"` form that triggers an online snapshot via the existing `diagnostics.vacuumInto` mechanism, then offers the resulting file for authenticated download; display the most recent on-demand backup's timestamp/size if one exists.
- [x] 4.6 Add tests: sidebar badge renders/omits correctly by count; superseded-by link renders only for superseded memories; page-count indicator matches filtered totals including the zero-rows case; a non-admin token does not see the backup card; a triggered backup produces a downloadable, well-formed snapshot.

## 5. Hermes: per-turn recall + heartbeat + context gating

- [x] 5.1 Add `POST /api/<slug>/memory/recall` to `apps/server/src/server/api-router.ts`: same `authenticate({pathSlug})` gate as the existing session routes, body `{query, limit?}` (limit clamped `[1,5]`, default 5), delegating to `MemoryService.search()` (project scope only, `include_global` unset), returning `{ok, memories: [{id,title,snippet}], formatted}`. Add tests for the scenarios in `openspec/specs/http-api/spec.md` (success, empty result, limit clamping, missing-query rejection, auth/scope error parity with the existing session routes).
- [x] 5.2 Implement `queue_prefetch` in `apps/plugin/.hermes-plugin/__init__.py`: POST to the new recall endpoint with a 3s timeout, cache the `formatted` string keyed by session id on success, leave the prior cache entry untouched on failure (silent stderr diagnostic).
- [x] 5.3 Implement `prefetch` to return the cached `formatted` string for the session (or `""` if no cache entry exists yet) — no network call.
- [x] 5.4 ~~Fire one best-effort `queue_prefetch("", session_id=...)` from `initialize()`~~ — dropped during implementation: Hermes calls `prefetch` with the REAL first user message before `queue_prefetch` ever runs (queue_prefetch only warms the NEXT turn), so `initialize()` has no real query to warm with; an empty-string call would also be rejected by the new endpoint's `query.min(1)`. Turn 1 is empty by construction (documented, at-most-one-turn-behind, matching the same tradeoff already accepted elsewhere in this codebase).
- [x] 5.5 Implement `sync_turn`: increment a per-session in-provider turn counter; every 5th call, POST a heartbeat to the existing `/api/<slug>/sessions/<id>/summary` route with the accumulated transcript; no-op on the other 4 calls beyond incrementing.
- [x] 5.6 Gate the session-creation POST in `initialize()` on `kwargs.get("agent_context", "primary")` — skip the POST (but still cache/register) when the value is an explicit non-primary context (`subagent`/`cron`/`flush`).
- [x] 5.7 Update the `system_prompt_block` comment to stop asserting Hermes "exposes no per-turn hook" now that `prefetch`/`queue_prefetch` provide one (the returned nudge text itself never mentioned hooks, so no content/char-cap risk); keep the SAVE/RECALL/SUMMARIZE guidance and the ≤1000-char budget.
- [x] 5.8 Add/update Python-side tests for all of the above matching the new scenarios in `openspec/specs/hermes-agent-plugin/spec.md`. Also added `prefetch`/`queue_prefetch`/`sync_turn` to `plugin.yaml`'s `hooks:` list (per the documented-but-uncertain gating behavior on that array) and rewrote the now-stale `test_memory_touching_methods_issue_zero_http` test whose premise (queue_prefetch/sync_turn are zero-HTTP) no longer holds.

## 6. Codex: compact-matcher parity + spec-drift reconciliation

- [x] 6.1 Add a second `SessionStart` matcher group for `"compact"` to `apps/plugin/hooks/hooks.codex.json`, invoking the existing `${CLAUDE_PLUGIN_ROOT}/scripts/post-compact.sh` (same script Claude Code's `SessionStart(compact)` hook uses). Also scoped the pre-existing default `SessionStart` entry to `matcher: "startup|resume|clear"` (mirroring `hooks.json`'s Claude Code pattern) so the two groups are mutually exclusive instead of double-firing on a compact resume.
- [x] 6.2 Verified `post-compact.sh` is already client-agnostic (uses the same shared `_api.sh` stdin-parsing helpers as `session-start.sh`, which already serves both clients) — updated its stale header comment claiming Codex lacks `SessionStart matcher:"compact"` support.
- [x] 6.3 Updated `docs/agents.md`'s Codex section: dropped the `codex features enable plugin_hooks` step (flag removed upstream) and retitled the section to just the `/hooks` per-hook trust-review step, per the corrected `codex-distribution/spec.md`.

## 7. opencode: event-registration fix (bug)

- [x] 7.1 In `apps/plugin/.opencode-plugin/plugin.ts`, remove the top-level `"message.updated"` and `"session.idle"` Hooks object keys and add `if (event.type === "message.updated")` / `if (event.type === "session.idle")` branches to the existing `event` dispatcher, preserving each branch's existing internal logic (sub-agent filtering, debounce, idempotent id-keyed replacement) adapted to the Event union's payload shape (`event.properties.info` for `message.updated`, `event.properties.sessionID` for `session.idle` — different from the old top-level (input, output) split). Added 5 new unit tests exercising both branches (accumulate, streaming-idempotent replace, ignore non-assistant/unknown-session, debounced flush, no-op for unregistered session) — all 41 tests in `plugin.test.ts` pass.
- [x] 7.2 No existing invariant test enumerated the plugin's Hooks keys (checked `apps/server/src/test/invariants.test.ts` and `plugin.test.ts` — neither referenced `'message.updated'`/`'session.idle'` as top-level keys before this change, consistent with them never having been invoked). Nothing needed updating; the `PluginReturn` type itself now only declares `event`, `chat.message`, `experimental.session.compacting` (+ `shell.env` under Plan B).
- [ ] 7.3 **Operator-gated / mandatory e2e — NOT run in this session**: `opencode` is not installed in this environment, so the live walkthrough against `pnpm run dev:docker:up` with a real opencode session could not be performed here. Run the `rembric-plugin-development` skill's e2e walkthrough — drive at least one assistant turn and one idle period, and confirm via the dashboard that the session's summary reflects the assistant's response (not just the user's prompt) — before merging. This is the load-bearing verification for Decision 6 in `design.md`, which is based on static analysis of opencode's types/source plus comprehensive unit tests, not a prior live run.

## 8. Bridge version handshake

- [x] 8.1 Add a `MIN_SERVER_VERSION` constant and a startup `GET ${REMBRIC_SERVER_URL}/healthz` check (2s timeout, reusing the existing bearer token) to `apps/plugin/bin/rembric-bridge.mjs`, printing exactly one stderr line on an outdated server and silently skipping the check on any request failure; `mcp-remote` spawning proceeds unchanged in every case.
- [x] 8.2 No existing unit-test harness for the bridge — added one (`apps/plugin/test/rembric-bridge.test.ts`) spawning the real bridge as a subprocess against a local `/healthz` stub with a shadowed `npx` (so no real network/MCP session is needed): covers outdated-server warning, up-to-date/newer-server silence, and healthz-unreachable silence with the connection still proceeding. Also added `fetch`/`AbortSignal` to the `apps/plugin/bin/**/*.mjs` ESLint globals (previously missing, now legitimately needed).

## 9. Validation

- [x] 9.1 Run `pnpm run typecheck` and `pnpm run lint` clean.
- [x] 9.2 Run `pnpm test` clean, including all new/updated tests from sections 1-8.
- [x] 9.3 Run `openspec validate --strict` against this change and fix any reported issues.
- [x] 9.4 Ran the `rembric-smoke-tests` playbook against `pnpm run dev:docker:up` — ALL PASS: (1) `/api/demo/memory/recall` happy/clamp/400/401/404 + `<memory-context>` formatting; (2) MCP `memory.search` from `/mcp/demo` without `include_global` returns project-only, with `include_global:true` blends the global rows in (ranked first on exact match); (3) shell loads `htmx.min.js` + filter form carries `hx-get/hx-target/hx-select/hx-push-url` + `#memories-list`/`hx-swap-oob` meta (note: the vendored htmx file is the repo's documented 896-byte no-op placeholder — full no-reload behavior activates when an operator vendors the real file, per pre-existing policy; forms degrade to plain GET meanwhile); (4) Maintenance backup card → CSRF POST → 22.7MB `VACUUM INTO` snapshot on disk → Last-backup line + authenticated download of a valid SQLite file (`integrity_check` ok, 37 memory rows); (5) MEMORIES sidebar badge shows `3`, matching the 3 seeded needs_review rows. Stack torn down after.
- [ ] 9.5 Re-run the opencode e2e walkthrough from task 7.3 as part of the final pre-merge pass — **operator-gated: opencode is not installed in this environment** (see task 7.3).
