## 1. The section parser and merge (pure, no service, no SQL)

- [x] 1.1 Create `apps/server/src/services/summary-sections.ts` exporting a parse and a merge function over Markdown level-2 sections, per design D3–D6: a section starts at `^##[ \t]+<non-empty>$`, extends to the next such line, `###`+ is body, lines inside a triple-backtick or `~~~` fence are never headings, `\n` and `\r\n` both split lines, text before the first heading is a section with an empty key, keys are trimmed + lower-cased, repeated keys concatenate in document order at the first occurrence. No reflow: the merged output reuses source lines verbatim.
- [x] 1.2 Create the co-located `apps/server/src/services/summary-sections.test.ts` covering every scenario of `sessions`::"A curated session-summary write MUST be merged section-wise with the stored summary" at the function level — partial update, outright section replacement, appended new heading, stored-order preservation against a reordering write, case/whitespace-insensitive matching, `## ` inside a fence staying body text, empty body storing an empty section, duplicate stored heading concatenated once, and a merge that reproduces the stored bytes exactly.
- [x] 1.3 Assert in the same test file that the merge is a pure function: no `Date`, no repository, no `db` import. `pnpm vitest run apps/server/src/services/summary-sections.test.ts` is green.

## 2. Wiring the merge into the single precedence site

- [x] 2.1 In `apps/server/src/services/agent-sessions.ts`, compute the merge inside `precedenceSet` (`:792-804`) — the single site the published "One site" rule names — gated on: incoming `final: true` with a `summary`, stored `summary IS NOT NULL` AND `summary_final = 1`, and precedence saying the value will be stored (design D2).
- [x] 2.2 Confirm by test that `writeTerminalFields` (`:257-278`) still returns a terminal curated row unchanged for a late flat-paragraph write and for a late over-cap write — no `invalid_input`, no mutation. This is the behaviour D2(3) protects and it is the easiest one to break.
- [x] 2.3 Reject a curated write carrying zero `##` headings against a stored summary that carries at least one, with `DomainError('invalid_input', …)` whose message names the missing `##` section and the canonical structure. No mutation, `summary_final` not lifted.
- [x] 2.4 Add the second `SUMMARY_MAX_CHARS` check on the merged document, reading the same constant as `assertSummaryWithinCap` (`:75`), immediately before the merged value enters the update `set`. Leave the argument check at `:339`/`:386` exactly where it is.
- [x] 2.5 Make the merged-overflow error actionable: the message states the merged length, contains the decimal string of `SUMMARY_MAX_CHARS`, names `memory.session_get`, and says to condense and resend. Nothing is truncated on this path — `truncateSummary` is not called on a merged document.
- [x] 2.6 Extend `apps/server/src/services/agent-sessions.test.ts` with the service-level scenarios from both new `sessions` requirements plus the two new cap scenarios (within-cap argument whose merge overflows; condensed full rewrite always accepted against a `SUMMARY_MAX_CHARS`-sized stored value).
- [x] 2.7 Confirm `end()` merges identically to `writeSummary()` — one test per method for the same partial-write case, so the two paths cannot diverge.

## 3. Prove every new guard is actually covered (mutation, not inspection)

Run each with `node scripts/mutate.mjs --file <path> --spec <test path> --mutation '<find>' --with '<replace>'`. A mutation that reddens NOTHING is the finding: write the missing test before moving on.

- [x] 3.1 Drop the stored-side `summary_final` condition (merge against a raw body) — the "first curated write over a raw body replaces it whole" test must go red.
- [x] 3.2 Drop the incoming `final: true` condition — the "`final: false` per-turn sync never merges" test must go red. **Deviation**: ran as specified (`node scripts/mutate.mjs --file apps/server/src/services/agent-sessions.ts --spec src/services/agent-sessions.test.ts -t "curated summary section-wise merge" --mutation 'incomingFinal &&\n      incomingSummary !== undefined &&\n      existing.summaryFinal &&' --with 'incomingSummary !== undefined &&\n      existing.summaryFinal &&'`) and it came back NOT CAUGHT. Proved this is not a missing test: within the surrounding `if (summary.changed)` gate, `applyPrecedence` already returns `changed:false` for any `final:false` write against an already-`summaryFinal` column, so whenever `existing.summaryFinal` is true and `summary.changed` is true, `incomingFinal` is true by construction — the explicit `incomingFinal &&` conjunct in `isCuratedMerge` was unreachable dead code, not an uncovered guard. Removed it (with a comment recording why) instead of writing a test that could never fail; D2(1) is still enforced, just via the enclosing precedence gate rather than a redundant local check.
- [x] 3.3 Delete the zero-heading rejection — the flat-paragraph-against-sectioned test must go red, and the flat-paragraph-against-unsectioned test must stay GREEN (the control that proves the rule is conditioned on the stored value, not on the write's layout).
- [x] 3.4 Delete the merged cap check — the within-cap-argument/over-cap-merge test must go red.
- [x] 3.5 Disable fence awareness in the parser — the fenced-`## Goal` test must go red.
- [x] 3.6 Remove key case-folding — the `## files` / `## Files` test must go red.
- [x] 3.7 Order the merged document by the write instead of the stored value — the stored-order test must go red.
- [x] 3.8 Make an empty section body remove the heading — the empty-body test must go red.
- [x] 3.9 Move the merge so it runs before the terminal discard — task 2.2's test must go red.

## 4. Model-facing text, inside the caps that already exist

- [x] 4.1 Add a second exported constant to `apps/server/src/mcp/summary-rubric.ts` carrying the one canonical merge sentence (both halves: sections sent replace, sections omitted stay). `SUMMARY_SECTIONS` is NOT modified — verify with `git diff` that its bytes are untouched.
- [x] 4.2 Rewrite the `memory.session_summary` description (`apps/server/src/mcp/server.ts:326`) to carry the ten facts plus the `title` clause required by the modified `mcp-api` requirement, interpolating the new constant. Measured target ≈1683 of `DESCRIPTION_MAX_LENGTH` 1900 (it was 1295 before this change); record the emitted `tools/list` length in the PR description.
- [x] 4.3 Rewrite the `SUMMARIZE` clause of `BASE` in `apps/server/src/mcp/instructions.ts` to carry the merge sentence, reclaiming the block's opening line (which carries no published obligation) to stay under the unchanged 1000-character cap. Do NOT add the `title` guidance here — measured at 1039/1000.
- [x] 4.4 Record the emitted lengths of BOTH instruction variants. Baseline on `origin/main`: 978 unscoped, 961 scoped. Target: ≤1000 with at least 10 characters of margin (a measured configuration exists at 978 unscoped: a 56-character merge sentence with the opening line trimmed 67→38). **Measured on this tree**: unscoped 977/1000 (margin 23), scoped 960/1000 (margin 40), via `buildInstructions` directly — `SUMMARY_MERGE_RULE` is 59 chars ("The `##` sections you send REPLACE; the ones you omit STAY.") and the opening line was trimmed from 67 to 51 chars ("Rembric — persistent memory. Use tools proactively.", dropping "across sessions"). `memory.session_summary` description measured 1800/1900 via a real `tools/list` response (was 1295 before this change).
- [x] 4.5 Mirror the new `BASE` byte-for-byte into `apps/plugin/.hermes-plugin/__init__.py::system_prompt_block`. This is the only plugin file this change touches.
- [x] 4.6 Verify NO other model-facing surface moved: `git status` shows no change to `apps/plugin/test/nudge-fixtures.json`, `apps/plugin/scripts/*.sh`, `apps/plugin/commands/summary.md`, `apps/plugin/bin/rembric-plugin-core.mjs` or `apps/plugin/.opencode-plugin/plugin.ts` (design D12 — the nudge surfaces belong to the follow-up change).

## 5. Tests over the text surfaces

- [x] 5.1 Extend `apps/server/src/mcp/instructions.test.ts`: both variants carry the merge sentence from the shared constant, both stay ≤`INSTRUCTIONS_MAX_LENGTH`, and every existing assertion (`Before ending`, `if you lack prior detail`, `memory.about`, `10000`, `title`, the six headings, no `global`) still passes.
- [x] 5.2 Extend `apps/server/src/test/mcp-integration.test.ts` for the `memory.session_summary` description read from a real `tools/list`: both halves of the merge sentence, the partial-write instruction, condense-never-delete with the `none` escape hatch, the over-cap-merge statement, the `title` discouragement, and `DESCRIPTION_MAX_LENGTH`.
- [x] 5.3 Add the shared-source assertion required by the modified `mcp-api` requirement: the rendered instructions and the emitted description both contain the same merge sentence, from the same constant.
- [x] 5.4 Run `pnpm vitest run apps/server/src/test/invariants.test.ts` and confirm `"the session-summary rubric has one source"` and its completeness enumeration still pass unchanged — the new constant lives in `summary-rubric.ts`, which that enumeration excludes.
- [x] 5.5 Run the Hermes pinning test explicitly (`apps/plugin/.hermes-plugin/tests/test_system_prompt_block.py`). A TypeScript-only run will not catch a drifted Python mirror.

## 6. Verification

- [x] 6.1 `pnpm run typecheck`
- [x] 6.2 `pnpm run lint`
- [x] 6.3 `pnpm test` — 148 files / 2863 passed, 10 pre-existing skips (benchmarks/example, unrelated); Hermes plugin suite 90/90 passed. Exit 0.
- [x] 6.4 `pnpm run eval` is NOT required — retrieval is untouched. Record that decision rather than skipping it silently. **Recorded**: this change touches session-summary writes and model-facing text only; no ranking, embedding, FTS or hybrid-search code path is touched, so `pnpm run eval` was not run.
- [x] 6.5 Real Docker smoke against pre-existing seeded data (`pnpm run dev:docker:up`; data-dev already owned by 10001:10001, no chown needed). Mount verified pointing at this worktree (`/root/rembric` → `/data`, `/root/rembric/apps/server/src` → `/app/apps/server/src`). Ran against a fresh MCP session (`memory.session_start` over `/mcp/demo`) rather than a pre-existing row, to keep the arms independent of seeded content:

  | Caso                                                                                     | Esperado                                                                            | Resultado                                                                                                                                                                                                                                                                                                                                                                                                                               |
  | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | (a) six-section curated write                                                            | `ok:true`, full doc stored                                                          | Stored verbatim; confirmed via `tools/list`/response and later via DB read                                                                                                                                                                                                                                                                                                                                                              |
  | (b) partial write, only `## Files`                                                       | other five sections byte-identical, only Files changes                              | Response summary: `## Goal\nShip the merge feature\n## Accomplished\nWrote the parser\n## Decisions+why\nUsed section-wise merge\n## Verified+how\nRan vitest\n## Unfinished+why\nDocker smoke pending\n## Files\napps/server/src/services/summary-sections.ts, apps/server/src/services/agent-sessions.ts` — five sections untouched, Files updated                                                                                    |
  | (c) flat paragraph vs. sectioned stored value                                            | `invalid_input` naming missing `##` section, no mutation                            | `{"ok":false,"code":"invalid_input","message":"sessions: summary has no ## section, but the stored summary already uses the canonical ## Markdown structure (## Goal, ## Accomplished, ## Decisions+why, ## Verified+how, ## Unfinished+why, ## Files) — include at least one ## heading to merge, or call memory.session_get to read what is stored"}`; DB read after the call showed the six-section doc unchanged, `summary_final=1` |
  | (d) merged overflow (well-formed `## Risks` section pushing the merge past 10 000 chars) | `invalid_input` naming the merged length + cap + condense action, nothing truncated | `{"ok":false,"code":"invalid_input","message":"sessions: merged summary would be 10228 characters, exceeding the 10000-character cap — condense the ## sections and resend; read the stored summary with memory.session_get first"}`; DB read showed `length(summary)=268`, i.e. still the pre-overflow doc, no truncated variant landed                                                                                                |
  | (e) condensed full six-section rewrite                                                   | accepted even though the pre-overflow doc was already near the cap                  | `ok:true`, stored exactly as the condensed six-section document — the rejection in (d) did not wedge the session                                                                                                                                                                                                                                                                                                                        |
  | (f) dashboard session detail                                                             | renders the merged document as six `<h2>`/`<p>` pairs                               | `GET /dashboard/sessions/<id>` (cookie auth via `POST /dashboard/login` with the admin token) rendered `<h2>Goal</h2><p>Condensed goal</p>…` through all six headings, plus a "Summary History (3)" section showing v1/v2/v3 (the two rejected writes correctly appended nothing)                                                                                                                                                       |

- [x] 6.6 Confirm on the same stack that a `final: false` HTTP transcript sync still replaces exactly as before (the raw per-turn path must be untouched) — this is the control that makes 6.5(b) a fact about the curated path. Created a session purely over HTTP (`POST /api/demo/sessions`) so it started with no curated summary, then two `POST /api/demo/sessions/<id>/summary` calls with `final:false`: turn 1 stored `"raw turn 1 text with a ## looking line"` (deliberately containing a `##`-shaped line), turn 2 stored `"raw turn 2 text, completely different, no relation to turn 1"`. The second response's `summary` was turn 2's text alone — no trace of turn 1, no section-wise composition despite the fake heading — confirming the raw per-turn path is unaffected by the merge and still replaces wholesale.
- [x] 6.7 Confirm the merge never reaches a `memory` row: no new `memory`, `memory_fts`, `memory_vec` or entity-table write appears in the smoke (append-only memory is not in play here, and this proves it). Read `data-dev/data.db` directly (host-side `better-sqlite3`, container has no `sqlite3`): `memory` and `memory_fts` both held 35 rows, matching the boot log's `[bootstrap] counts: memory=35 …` exactly — unchanged by every `memory.session_summary` call in 6.5/6.6, curated and raw alike. `memory_vec` is only reachable from inside the running server process (`vec0` extension), not queryable from the host directly; no code path in `agent-sessions.ts`/`summary-sections.ts` touches it regardless (grep-enforced SQL confinement to `db/`).

## 7. Deferred and rejected, recorded so they are not lost

- [x] 7.1 **For the PR description**: the nudge surfaces (`postCompact`, `endOfTurnRubric`, `commands/summary.md`, the opencode compaction handler) still frame the write as a whole-document replacement. This is fail-safe under the merge — a complete document replaces every section, so the outcome they describe is still the outcome they get — and rewriting them belongs to the nudge change that owns their byte budgets (design D12). Confirmed untouched this session: `git status` shows no change under `apps/plugin/` except `.hermes-plugin/__init__.py`.
- [x] 7.2 **For the PR description**: `session_summary_versions` was deliberately untouched. The only edit to its requirement (`sessions/spec.md`, "Every curated session-summary write MUST append a version row in the same transaction") is the correction of the now-false `exactly ONE cap check` scenario — renamed and reworded to name the merged value and the second check — plus a new scenario ("A merged write versions the merged document, not the argument") and its "A partial write that changes nothing appends no version row" companion. No new argument rests on the table's existence; the table's own mechanics (`insertSummaryVersion`, `latestSummaryVersion`, byte-identical-skip, `memory.session_get({limit})`) are all pre-existing and untouched by this change.
- [x] 7.3 **For the PR description**: two open questions from `design.md` are explicitly deferred, defaults taken as written there — (1) the merged-overflow error does NOT report per-section character counts, only the total, the cap and the action (revisit only if a real session is observed failing the retry); (2) the six canonical headings do NOT become mandatory on a first curated write — free-form storage remains accepted for a session with no sectioned summary yet (`sessions`, "Free-form summary storage remains accepted"), deliberately left to a later change with its own evidence.
- [ ] 7.4 Conventional Commits throughout; hooks never bypassed.
