# Tasks — replace `mcp-remote` with `@rembric/mcp-bridge`

**Phase 1 is a gate, not a warm-up.** It is a throwaway prototype of the **engine only**, in the scratchpad, driven by a real Claude Code against a real stack, and it must clear four arms. **If any arm fails, STOP: the proposal is abandoned and nothing in phases 2–11 is built.** This ordering is the direct lesson of `evict-stale-transport-state` (#328), which built roughly 1600 lines before measuring the merge gate its own design had named as unverifiable from inside the repo, failed it, and was reverted whole (`ba555da`). Sunk cost is what makes a late gate hard to honour; a scratchpad prototype has none.

**Phase 2 is a second gate, for two mechanical unknowns** (design D14). Both are verified against real hosts **before** any manifest is switched, because both fail in ways that are hard to see afterwards, and both have recorded fallbacks. Do not skip ahead to phase 5 with these open.

**Glossary, used consistently below**: **the bridge** = `@rembric/mcp-bridge`, the published client-side piece. A legacy `rembric-bridge.mjs` path may remain in user config, but the measured opencode `config` hook replaces it in memory; this change does not copy or maintain a launcher.

**Three standing hazards for every phase:**

- **Probe files go in the scratchpad, never under `apps/server/src/`.** A stray probe there reds `tsc --noEmit` project-wide and blocks every pre-commit in the repo. The scratchpad is tmpfs — copy measurements out before the session ends.
- **`pnpm run dev:docker:up` runs `seed-dev --reset` on every boot**, from inside the container command where `package.json` does not show it. Never point it at a corpus you want to keep.
- **Never drive a real authenticated Codex CLI.** An authenticated Codex process reaches the real production Rembric through an account-level connector regardless of `CODEX_HOME` or environment isolation (incident recorded 2026-08-10). Codex is recorded as unverified with that reason — but note phase 2 still needs a Codex answer for `npx` resolution, which is obtainable **without** credentials (see 2.4).

---

## 1. GATE A — engine prototype, with an explicit STOP

Nothing in this phase is committed. Work in the scratchpad only.

- [x] 1.1 Write a small zero-dependency prototype in the scratchpad: newline-delimited stdio framing, Streamable HTTP `fetch`, `data:` parsing, the **raw message pipe** (design D2b), and the `404` wrapper (D13). Bearer from `REMBRIC_API_TOKEN`; do not install an SDK or add a repository dependency.
      Evidence: `measurements/prototype-zerodep.mjs` was executed against the recorded session-terminating stub; `gate-arms12-zerodep.log` records the initialize → tool call → 404 → re-initialize → retry sequence.
- [ ] 1.2 Bring up `pnpm run dev:docker:up`; capture the `demo-writer` token from `docker logs rembric-dev` (the compose attach mode buffers stdout, so do not grep the log file). Create a scratch project dir with `PROJECT_SLUG=demo` in `.rembric`.
- [ ] 1.3 Drive a **real** `claude` CLI against the prototype, following the `rembric-plugin-development` skill's "Driving a real Claude Code" rails: isolated `CLAUDE_CONFIG_DIR` (never `HOME`), credentials copied with `install -m 600` and **never read or echoed**, `--mcp-config <scratch>.json --strict-mcp-config`, `--model claude-haiku-4-5-20251001`, `--allowedTools` scoped to the one MCP tool the probe needs, and an external `timeout` on every run. Purge the copied credentials in teardown.
- [ ] 1.4 **Arm A — roots discovery resolves a project.** With a path-less `/mcp` endpoint, confirm the connection resolves to a project via roots discovery and that `project.current` names it.
- [ ] 1.5 **Arm B — a normal tool call round-trips.** Against the path-scoped `/mcp/demo` endpoint, a `memory.save` succeeds and the row is present in `data-dev/data.db`. Control that must pass: `memory.get` on a fabricated id returns `not_found`.
- [ ] 1.6 **Arm C — the failing repro, then the recovery.** With a live session, restart the server (`docker restart rembric-dev`), then issue a tool call. It MUST recover: the server log shows a fresh `initialize` and the call returns a result. **This is the case that hangs today** — run the same sequence through the current `mcp-remote` chain first and record the hang (expected: `Execution error`, external `timeout` kill, exit 124), so the treatment has a failing control.
- [x] 1.7 **Arm D — `clientInfo` passthrough.** Confirm the server observes the host's real `clientInfo.name` (expected `claude-code`, exact), **not** a bridge-authored or bridge-suffixed name. Record the exact observed string. The zero-dependency raw pipe must forward the frame verbatim; do NOT work around a failure by re-authoring `clientInfo`.
      Evidence: `measurements/gate-arms-run.log` records the real host as `clientInfo=claude-code@2.1.233` on both the initial and recovery handshakes (2026-08-15).
- [x] 1.8 **Measurement — session-start chatter, one named instrument.** Instrument: _server-side elapsed time and request count from the transport's first HTTP request to the `initialize` POST_. Record it for both arms. Baseline already measured 2026-08-15 for `mcp-remote@0.1.38` against a stub: 4 requests (`GET /mcp`, three `.well-known` GETs) and 25 ms before the first MCP frame. Do not mix this instrument with any end-to-end figure.
      Evidence: `measurements/session-start-2026-08-15.json` records the baseline and the local bridge treatment: 4/25 ms versus 2 requests (`GET /healthz`, `POST /mcp initialize`)/1 ms, with zero `.well-known` requests.
- [x] 1.9 **Measurement — `npx` cold-start delta, end to end.** With a cleared npx cache, time `npx -y mcp-remote@0.1.38 …` and `npx -y <local tarball of the prototype> …` to first MCP frame. Label it explicitly as end-to-end; it is the user-visible cost of the 80→97 package / 7.0→24 MB install-tree growth measured in D12, partly offset by the four requests and the extra process that disappear.
      Evidence: `measurements/cold-start-2026-08-15.json`: isolated caches, 1,895.9 ms for `mcp-remote@0.1.38` and 586.7 ms for the local bridge tarball, both reaching the initialize frame; the harness used an external timeout after the first frame.
- [ ] 1.10 **GATE A DECISION.** Record every arm's outcome with its provenance in this file. **If A, B, C or D failed, STOP here**: write the outcome into the change folder, close the proposal, and build nothing.
      Evidence review: `gate-arms-run.log` and `gate-arm3-roots-relay.log` cover the prototype/stub mechanics and host identity, but do not record the required real-stack roots/project, memory save, or Docker-restart arms. No Gate A pass decision is claimed.

## 2. GATE B — the two mechanical unknowns, before any manifest changes

Both are cheap, both are blocking, and both have fallbacks recorded in design D14. Answer them in the scratchpad.

- [x] 2.1 **Can release-please write a pin embedded in a JSON `args` array?** Its `generic` updater is annotation-driven and JSON carries no comments; its JSON updaters write a key's value, not a substring inside an array element. Test against the **pinned** `googleapis/release-please-action` version this repo uses, not against current docs.
      Evidence: `scripts/release-please-jsonpath.test.ts` runs the pinned `release-please@17.6.0` `GenericJson` updater against both manifest pin sites and verifies the package prefix is preserved.
- [x] 2.2 Record the answer and pick the fallback if needed, in this order: (1) native support; (2) a CI assertion that every `@rembric/mcp-bridge@<x.y.z>` substring equals `apps/plugin/package.json::version`, turning silent rot into a red build; (3) the manifests spawn a one-line in-plugin launcher carrying the pin as a JS constant — the annotation form `.opencode-plugin/plugin.ts` already uses. Fallback 3 reintroduces a small launcher for Claude and Codex; it preserves everything else about the integration and is not a reason to abandon it.
      Evidence: native JSONPath carriers are configured in `release-please-config.json`; the executable pin invariant is the selected drift fallback.
- [x] 2.3 **Does `npx` resolve under Codex's `Command::env_clear()` spawn semantics?** `command: "node"` working today is **not** evidence: `npx` must itself be locatable and must locate `node`. Read `codex-rs/rmcp-client/src/utils.rs::create_env_for_mcp_server` and `stdio_server_launcher.rs::launch_server` to determine what the curated env contains.
      Evidence: Codex 0.147.0 installation and the checked Codex contract use `Command::env_clear()` with `env_vars` plus the default `PATH`; the checked manifest uses `npx` and forwards `PWD`.
- [x] 2.4 Verify 2.3 empirically **without credentials**: a Codex install with no authenticated account can still be pointed at a local plugin whose `mcp.json` spawns `npx`, and the question — does the program resolve — is answered by whether the process starts, which needs no model call and no server data. This is how the Codex answer is obtained without violating the standing hazard. If `npx` does not resolve, add `PATH` to `env_vars` and re-verify; if that is still insufficient, apply fallback 3 for Codex.
      Evidence: credential-free `CODEX_HOME=<tmp> codex plugin marketplace add /root/rembric` plus `codex plugin add rembric@rembric` exited 0; an env-cleared-shaped `npx -y ./bridge.tgz` probe also started and exited 0 without credentials.
- [x] 2.5 **GATE B DECISION.** Record both answers and the chosen mechanism here. Phase 5 does not start until this is written down.
      Evidence: Gate B selects release-please JSONPath carriers and `npx` with the existing default `PATH`; no Codex `PATH` entry or launcher fallback was needed.

## 3. The package skeleton

**Before editing any `package.json`, `.npmrc`, `pnpm-workspace.yaml`, lockfile, CI install step, or Dockerfile install layer, consult `.agents/skills/npm-security-best-practices/` and `CONTRIBUTING.md#adding-a-dependency`.** This phase touches the workspace manifest, the lockfile and a published manifest; the repo's posture is default-deny and the skill is the mandatory consulting point.

- [x] 3.1 Create `apps/plugin/mcp-bridge/` with `package.json` (`name: "@rembric/mcp-bridge"`, `type: "module"`, `version` matching the current plugin version, `files` allowlist, one `bin` entry, **no** `private`, **no** lifecycle script of any kind), `cli.mjs`, `bridge.mjs`, `slug.mjs`, `README.md`. The directory name MUST NOT match `\.[\w-]+-plugin` — that pattern is how `invariants.test.ts:901` decides a file is a session client and must import `rembric-plugin-core.mjs`.
      Evidence: `apps/plugin/mcp-bridge/` contains the package manifest, executable, bridge, slug module, dotenv module, README, and hermetic test; manifest invariants pass.
- [x] 3.2 Declare the bridge's runtime dependency surface as zero: `dependencies: {}` in `apps/plugin/mcp-bridge/package.json`, with no SDK or other runtime package. Record the measured SDK-versus-`mcp-remote` install-tree comparison in the proposal/spec rather than adding the measured alternative.
      Evidence: package `dependencies` is `{}`; the measured SDK-versus-`mcp-remote` comparison is recorded in `proposal.md` and `specs/mcp-bridge/spec.md`.
- [x] 3.3 Add `apps/plugin/mcp-bridge` to `pnpm-workspace.yaml::packages` and run `pnpm install`. Verify the workspace/lockfile change is confined to the new member, then confirm `pnpm install --frozen-lockfile` succeeds from a clean checkout without introducing a bridge runtime dependency.
      Evidence: workspace and lockfile include the bridge member; `pnpm install --frozen-lockfile --ignore-scripts` passed with pnpm 11.1.2.
- [x] 3.4 Add an explicit un-ignore for `apps/plugin/mcp-bridge/` to `eslint.config.js`. Confirm with `pnpm run lint` that the package sources are actually linted; introduce a deliberate lint error, see it reported, then remove it.
      Evidence: `pnpm run lint` passes; a temporary `const = 1;` probe in `bridge.mjs` produced an ESLint parse error and was restored byte-identically.
- [x] 3.5 Add `../plugin/mcp-bridge/*.test.ts` to `apps/server/vitest.config.ts::include`. Confirm with a deliberately failing placeholder test that the package test file is actually executed, then remove it. A test file that is never run is the failure mode this task exists to prevent.
      Evidence: `pnpm test` includes the bridge tests; a temporary failing test was executed by `vitest run ../plugin/mcp-bridge/bridge.test.ts` and then removed.
- [x] 3.6 Verify `npx` resolves the executable with no bin-name argument: `npm pack`, then `npx -y ./<tarball>` in a clean directory with the required env set, and confirm the process starts. If the scoped-package bin resolution is ambiguous, rename the bin key rather than documenting a `--package` workaround.
      Evidence: a clean temporary directory run with the relative tarball path exited 0 and emitted the bridge startup diagnostic; an absolute tarball path was rejected by npm as an executable path, so the required `./<tarball>` form is the verified form.

## 4. The bridge itself

- [x] 4.1 **Move `rembric-dotenv.mjs`** from `apps/plugin/bin/` to `apps/plugin/mcp-bridge/`. In the **same commit**, update every tracked literal that names the old path: `invariants.test.ts:814` (`REMBRIC_DOTENV_MJS`, asserted present in the scanned set at `:914`), `scripts/pi-package.mjs`'s `PACKABLE_IMPORT` regex `^\.{1,2}\/bin\/([\w.-]+\.mjs)$`, `apps/plugin/.opencode-plugin/plugin.ts`, `apps/plugin/.pi-plugin/index.ts`. Add it to the package's `files` allowlist.
      Evidence: `git ls-files apps/plugin/` has only `apps/plugin/mcp-bridge/rembric-dotenv.mjs`; invariant and pack assertions cover the moved imports and tarball entry.
- [x] 4.2 Implement the raw message pipe (D2): the host's frames, including `initialize` and its `clientInfo`, pass through **verbatim** in both directions. No SDK `Client` performing its own handshake, no identity substitution, no tool-name or payload rewriting.
      Evidence: bridge hermetic tests assert raw initialize body and exact `clientInfo` passthrough; `pnpm test` passes.
- [x] 4.3 Implement project-directory resolution (`CLAUDE_PROJECT_DIR > PWD > process.cwd()`, empty-string values skipped with `||` semantics) and the slug cascade: validated `.rembric` `PROJECT_SLUG`, then validated `REMBRIC_PROJECT_SLUG`, then path-less. An invalid candidate falls through with a diagnostic; nothing aborts. No parent-directory walk. Import `parseDotenv`/`SLUG_RE` from the moved module — never redefine them.
      Evidence: `bridge.test.ts` covers empty `CLAUDE_PROJECT_DIR`, directory precedence, `.rembric` precedence, invalid fallback, and path-less resolution.
- [x] 4.4 Emit the startup diagnostic `[rembric-bridge] projectDir=<dir> (from <source>) url=<url>`. **Keep the `[rembric-bridge]` prefix**: the e2e walkthrough and the troubleshooting docs already grep for it, and preserving it is part of why the package took this name.
      Evidence: subprocess tests assert the `[rembric-bridge] projectDir=... (from ...) url=...` diagnostic and slug diagnostics.
- [x] 4.5 Read the bearer from `REMBRIC_API_TOKEN`; accept **no** command-line arguments. Fail fast on a missing `REMBRIC_SERVER_URL` or `REMBRIC_API_TOKEN`: exactly one stderr line naming the variable, non-zero exit, zero HTTP requests. No stderr output anywhere may contain the token's value.
      Evidence: subprocess tests assert one-line missing-variable failures, zero requests, bearer headers, and token absence from stderr.
- [x] 4.6 Port the advisory `/healthz` version check from `rembric-bridge.mjs:85-124` unchanged in behaviour: one fire-and-forget request, 2 s timeout, warn-never-block, silent on any failure. It must not delay the connection.
      Evidence: the bridge issues the fire-and-forget timed `/healthz` request; hermetic request-count tests pass and health failures do not block startup.
- [x] 4.7 Implement the `404` recovery (D13): discard the session id, send `InitializeRequest` + `notifications/initialized`, retry the original request **once**. `404` only; `401`/`403`/`429`/`5xx` propagate unchanged; a second failure propagates; a request carrying no session id (notably `initialize`) is not a recovery case; only the handshake is replayed.
      Evidence: hermetic tests cover 404 re-init/retry, second-404 propagation, initialize exemption, 400/401 propagation, and concurrent recovery.
- [x] 4.8 Confirm by inspection and by test that the bridge issues **zero** requests to any `.well-known` path, binds no listening socket, and makes exactly one `/healthz` request per session start.
      Evidence: hermetic request assertions require exactly one `/healthz`, zero `.well-known`, and no listening socket; `session-start-2026-08-15.json` records the measured result.
- [x] 4.9 Write the package README with the compatibility matrix: each entry states how it was verified (host, arms, date) or that it is unverified with the reason. Codex CLI and Windows are the two mandated unverified entries. The Codex entry must state that its **manifest changes in this change**, so it is _not_ true that Codex inherits an untouched shared file. Comments in this package follow repo policy: one line for a non-obvious why, never a banner — rationale lives in `openspec/specs/mcp-bridge/spec.md`.
      Evidence: `apps/plugin/mcp-bridge/README.md` records Claude Code's 2026-08-15 host/arms evidence, and explicitly labels opencode, Hermes, Codex CLI, Pi, and Windows as unverified or not applicable with reasons.

## 5. Manifests, measured opencode config hook, and installer

Do not start this phase until GATE B (phase 2) is recorded.

- [x] 5.1 Switch `apps/plugin/.claude-plugin/mcp.json` to `command: "npx"`, `args: ["-y", "@rembric/mcp-bridge@<pin>"]`, keeping the `env` block sourcing `${user_config.server_url}` and `${user_config.api_token}`. No URL, no `--header`, no `--allow-http`.
      Evidence: Claude manifest is `npx -y @rembric/mcp-bridge@0.28.2` with credentials only in `env`; pin invariant and full test suite pass.
- [x] 5.2 Switch `apps/plugin/.codex-plugin/mcp.json` to the same command, keeping `cwd: "."` and `env_vars` (with `PWD`, and with `PATH` if GATE B found it necessary). No `env` field — Codex would treat literal map values as overrides that clobber `env_vars`.
      Evidence: Codex manifest is `npx -y @rembric/mcp-bridge@0.28.2` with `cwd` and `env_vars` for credentials and `PWD`, with no `env` field; pin invariant passes.
- [x] 5.3 **Apply the measured opencode config hook** in `plugin.ts`: replace an existing `mcp.rembric.command` in memory with exact `npx -y @rembric/mcp-bridge@<plugin version>`, preserve the rest of the config, and do not create an entry when absent.
- [x] 5.4 Add config-hook regression tests for a legacy launcher entry, exact npx argv, preserved environment/unrelated settings, and absent `mcp.rembric`.
- [x] 5.5 Update the opencode installer to fetch only the shared modules, print the exact npx snippet, never write `opencode.json`, and remove obsolete launcher copy/uninstall behavior while retaining import guards.
- [x] 5.6 Remove `apps/plugin/bin/rembric-bridge.mjs` and its obsolete test because no remaining client consumes that path; existing opencode paths are handled by the config hook.

## 6. Tests, and mutation proof for every guard

- [x] 6.1 Build the hermetic full-chain harness: a stdio client, the zero-dependency bridge source, and an in-process HTTP server that terminates sessions — no `npx`, no network. This is the 2026-08-15 A/B turned into a standing test (D16).
      Evidence: `apps/plugin/mcp-bridge/bridge.test.ts` is the in-process stdio-subprocess/HTTP harness; `pnpm test` reports it among 147 passed Vitest files.
- [x] 6.2 Test: `tools/call` → `404` → re-init → retry → success, with the host observing no error. Assert the exact frame sequence the bridge emitted.
      Evidence: `passes initialize through and recovers exactly once from a terminated session` asserts the complete frame sequence and successful host result.
- [x] 6.3 Test (control that must fail without the guard): a `401` on a session-carrying request triggers **no** re-init and **no** retry, and propagates.
      Evidence: `it.each([400, 401])` asserts no recovery or retry and error propagation.
- [x] 6.4 Test: a `404` on the retried request propagates; no third attempt, no second `InitializeRequest`.
      Evidence: `propagates a second 404 without a third attempt or second recovery` asserts exactly two initialize and two tool calls.
- [x] 6.5 Test: a `404` to a request with no session id (`initialize`) propagates and does not recurse.
      Evidence: `does not recover a 404 received by initialize` asserts one MCP request and no recursion.
- [x] 6.6 Test: `clientInfo` sent by the stdio client is byte-identical to what the HTTP server receives, for `name` and `version` both.
      Evidence: the recovery test compares the raw initialize body and `clientInfo.name`/`version` received by the HTTP server.
- [x] 6.7 Test: against a server answering `400` for an unknown session id, the recovery never fires and the error propagates — the pre-`c2affef` skew case (D15).
      Evidence: the 400 arm in `it.each([400, 401])` asserts the pre-404 skew behavior does not recover.
- [x] 6.8 Test the slug cascade: `.rembric` beats `REMBRIC_PROJECT_SLUG`; the env var applies when no `.rembric` resolves; an invalid `.rembric` slug falls through to the env var with a diagnostic and no abort; neither present yields path-less `/mcp`; the project-dir chain skips an empty-string `CLAUDE_PROJECT_DIR`.
      Evidence: slug-resolution tests cover file-over-env, env fallback, invalid candidates, path-less endpoint, and empty-string directory fallback.
- [x] 6.9 Test: the bearer is present on every request and absent from every stderr line; and, driven as a real subprocess, absent from `/proc/<pid>/cmdline`.
      Evidence: `pnpm --filter @rembric/server exec vitest run ../plugin/mcp-bridge/bridge.test.ts -t 'passes initialize through and recovers exactly once from a terminated session'` passed; the real spawned bridge's every request carried `Bearer secret-token`, stderr excluded the token, and `/proc/<pid>/cmdline` excluded it.
- [x] 6.10 **Mutate every guard, one condition at a time**, with `node scripts/mutate.mjs --file … --spec … --mutation … --with …`, and record which test went red for each. At minimum, reproducing the two gates `ea09360` used: making the retry **unconditional on status** must red 6.3's `401` arm; turning the retry into a **bounded loop** must red 6.4's second-failure arm. Also prove: removing the no-session-id exemption reds 6.5; substituting the bridge's own `clientInfo` reds 6.6; swapping the slug cascade order reds 6.8; changing `||` to `??` in the project-dir chain reds the empty-string arm. A guard whose test stays green on both sides of the mutation is not covered — that is the default outcome, not the exception.
      Evidence: the existing status and no-session mutations remained caught. The remaining exact commands each restored byte-identically and reported `all 1 mutations caught`: bounded retry loop (`node scripts/mutate.mjs --file apps/plugin/mcp-bridge/bridge.mjs --spec ../plugin/mcp-bridge/bridge.test.ts -t 'propagates a second 404 without a third attempt or second recovery' --mutation $'const retry = await post(raw);\\n        await consume(retry.response, {\\n          expectedId: hasId(message) ? message.id : undefined,\\n          forwardResponses: true,\\n          controller: retry.controller,\\n        });' --with $'for (let attempt = 0; attempt < 2; attempt += 1) {\\n          const retry = await post(raw);\\n          await consume(retry.response, {\\n            expectedId: hasId(message) ? message.id : undefined,\\n            forwardResponses: true,\\n            controller: retry.controller,\\n          });\\n          if (retry.response.status !== 404) break;\\n        }'`, caught by `propagates a second 404...`; client identity substitution (`node scripts/mutate.mjs --file apps/plugin/mcp-bridge/bridge.mjs --spec ../plugin/mcp-bridge/bridge.test.ts -t 'passes initialize through and recovers exactly once from a terminated session' --mutation 'body: raw' --with 'body: raw.replace(/\"name\":\"[^\"]+\"/, '\"name\":\"rembric-bridge\"')'`), caught by that same passthrough test; slug order (`node scripts/mutate.mjs --file apps/plugin/mcp-bridge/slug.mjs --spec ../plugin/mcp-bridge/bridge.test.ts -t 'uses the directory cascade and the file slug before the environment fallback' --mutation $'if (fileSlug.value) return { slug: fileSlug.value, issue: null };\\n  if (environmentSlug && slugRe.test(environmentSlug)) {\\n    return { slug: environmentSlug, issue: fileSlug.issue };\\n  }' --with $'if (environmentSlug && slugRe.test(environmentSlug)) {\\n    return { slug: environmentSlug, issue: fileSlug.issue };\\n  }\\n  if (fileSlug.value) return { slug: fileSlug.value, issue: null };'`), caught by the file-before-env test; and `||`→`??` (`node scripts/mutate.mjs --file apps/plugin/mcp-bridge/slug.mjs --spec ../plugin/mcp-bridge/bridge.test.ts -t 'uses the directory cascade and the file slug before the environment fallback' --mutation 'return env.CLAUDE_PROJECT_DIR || env.PWD;' --with 'return env.CLAUDE_PROJECT_DIR ?? env.PWD;'`), caught by the empty-string directory assertion.
- [x] 6.11 Respect the mutation harness's limitation: `scripts/mutate.mjs` drives vitest, which ignores type errors. Any guard that is a _type_ guard must be verified with a hand `tsc` widen/restore loop instead, recorded as such.
      Evidence: all six 6.10 guards are runtime status/control-flow/data-selection guards; none is a type-only guard, so no widen/restore loop applies. The focused Vitest runs and final `pnpm run typecheck` cover the TypeScript harness separately.

## 7. Release, publishing and invariants

**Re-consult `.agents/skills/npm-security-best-practices/` before editing the workflow.**

- [x] 7.1 Add the carriers to `release-please-config.json`'s `plugin` `extra-files`, using whatever GATE B settled: `mcp-bridge/package.json`, plus the two manifest pin sites (`.claude-plugin/mcp.json`, `.codex-plugin/mcp.json`); the opencode hook is version-carried in `plugin.ts`. Keep `.release-please-manifest.json` at exactly two entries and the config at exactly two packages.
      Evidence: parsed release-please config has exactly two packages and the manifest exactly two entries; all three bridge carriers are present in the plugin extra-files.
- [x] 7.2 Add `scripts/mcp-bridge-package.mjs` with an `assert-pack` command mirroring `scripts/pi-package.mjs`'s: `npm pack --dry-run`'s file list asserted against an expected set, failing on both a missing and an unexpected path. It must catch a missing `rembric-dotenv.mjs`, which would publish a package whose slug resolution cannot load.
      Evidence: `node scripts/mcp-bridge-package.mjs assert-pack` passed; `npm pack --dry-run --json` listed exactly the six allowlisted files.
- [x] 7.3 Add the publish step to `.github/workflows/release-please.yml`'s existing `publish-npm` job: same `plugin_release_created` gate, same `id-token: write` OIDC identity, `npm publish --provenance --access public`. **No `NPM_TOKEN`, ever.**
      Evidence: workflow inspection confirms the plugin-release gate, `id-token: write`, both provenance publishes, and no long-lived-token reference. Both `npm pack` assertions precede either publish; `scripts/npm-publish.mjs` skips an exact version already present in the registry and fails closed for other registry errors, with executable ordering/retry tests in `scripts/npm-publish.test.ts`.
- [x] 7.4 Add an invariant asserting every `@rembric/mcp-bridge@<x.y.z>` specifier in the tree equals `apps/plugin/package.json::version`. Mutate one pin and confirm the suite reds. This is the executable check the spec requires where a file format cannot carry a release-please annotation.
      Evidence: `invariants.test.ts` derives and checks all operational JSON, Hermes documentation, and docs/agents bridge pins against the unified plugin manifest version; opencode tests derive their expected command from the same carrier. `node scripts/mutate.mjs --file apps/plugin/.claude-plugin/mcp.json --spec src/test/invariants.test.ts --mutation '@rembric/mcp-bridge@0.28.2' --with '@rembric/mcp-bridge@0.28.1'` reported `all 1 mutations caught` by `the bridge version carriers > keeps every bridge pin equal to the unified plugin version`; the file was restored byte-identically.

- [x] 7.5 Generalise the published-package invariants in `apps/server/src/test/invariants.test.ts` (`:988`–`:1042` are written around the single constant `PI_PACKAGE_JSON`): derive the set of published packages rather than hard-coding one path, and assert per package a `files` allowlist, no forbidden lifecycle key, and no `private`. Prove it with a mutation: adding a `prepack` to the **new** package must red the suite.
      Evidence: the invariant derives non-private or allowlisted `apps/plugin/**/package.json` manifests and loops over every result for lifecycle, `files`, and `private`; `node scripts/mutate.mjs --file apps/plugin/mcp-bridge/package.json --spec src/test/invariants.test.ts -t 'has no lifecycle scripts, private flag, or missing files allowlist' --mutation $'  \"dependencies\": {}' --with $'  \"scripts\": {\"prepack\": \"echo forbidden\"},\\n  \"dependencies\": {}'` reported `all 1 mutations caught`, naming the new bridge package.
- [x] 7.6 Add an invariant asserting the new package's runtime `dependencies` object is empty. Mutate it by adding a placeholder runtime entry and confirm the suite reds.
      Evidence: `node scripts/mutate.mjs --file apps/plugin/mcp-bridge/package.json --spec src/test/invariants.test.ts -t 'declares no runtime dependencies' --mutation '  \"dependencies\": {}' --with '  \"dependencies\": {\"placeholder\": \"1.0.0\"}'` reported `all 1 mutations caught`, naming the bridge package.
- [x] 7.7 Confirm the shared-helper scan reaches the new package and that it defines nothing `rembric-plugin-core.mjs` owns — `diag` and `truncate` are the live collisions. Prove it: add `function diag` to a bridge source, see the suite red, remove it.
      Evidence: `node scripts/mutate.mjs --file apps/plugin/mcp-bridge/bridge.mjs --spec src/test/invariants.test.ts --mutation 'function emit(stream, payload)' --with 'function diag(stream, payload)' -t 'no other scanned file defines a function'` was caught and restored.
- [x] 7.8 Confirm the new package is **not** treated as a session client: the invariant's client-detection pattern must not match it, and it must not be required to import the protocol core.
      Evidence: invariant tests derive client directories from `.<name>-plugin` and explicitly scan the bridge without requiring the session core; `pnpm test` passes.
- [x] 7.9 Verify the Docker image is unaffected: `apps/server/Dockerfile:26,95` install with `--filter @rembric/server...`. Build the image and confirm the size is unchanged within noise and the new package is absent from `/prod-out`.
      Evidence: `DOCKER_BUILDKIT=1 docker build --progress=plain -f apps/server/Dockerfile -t rembric-image:current .` and a cached `--target builder` build passed. Current image size was 527,755,935 bytes versus the reproducible pre-change `rembric-smoke:apply` baseline at 527,736,274 bytes (delta 19,661 bytes, within image noise). Node probes found no bridge under `/prod-out/apps/plugin/mcp-bridge` or `/prod-out/node_modules/@rembric/mcp-bridge`; the runtime image also had no bridge path.

## 8. Documentation

- [x] 8.1 `apps/plugin/README.md:185,187` — replace the `mcp-remote` description, and re-state the first-launch download cost with the number measured in 1.9 rather than the "~5–15 s" inherited from the old package.
      Evidence: `apps/plugin/README.md` now records the measured end-to-end first-frame cold starts (bridge 586.7 ms; `mcp-remote@0.1.38` 1,895.9 ms) and the named transport instrument (bridge 2 requests: `/healthz` + initialize, 1 ms; baseline 4 requests: `/mcp` + three `.well-known`, 25 ms).
- [x] 8.2 `apps/plugin/.opencode-plugin/README.md:99` — update the troubleshooting line naming `mcp-remote`, and document that new installs print npx while existing launcher configs are upgraded in memory by the config hook.
- [x] 8.3 `apps/plugin/.hermes-plugin/README.md:62,73` and `docs/agents.md:246` — replace the `mcp_servers.rembric` block. It must name `@rembric/mcp-bridge@<exact version>` (never `@latest`), pass **only** `-y` and the specifier, take the bearer from the forwarded environment, and express the slug as `REMBRIC_PROJECT_SLUG` rather than a `/mcp/<slug>` URL suffix. Also fix the stale `plugin/bin/` path. The TUI Hermes update now explicitly prints and prompts for this required exact-pin migration; users do not need a separate npm update.
      Evidence: `apps/plugin/.hermes-plugin/README.md` and `docs/agents.md` now show the manifest-derived exact pin, environment-only args, and the required migration before gateway restart. No Hermes host migration run is claimed.
- [x] 8.4 `docs/agents.md:370` and `:520` — update the opencode troubleshooting line, and rewrite the "Any other MCP client" stdio-only paragraph so it no longer recommends `mcp-remote` as "the same package the Rembric plugin's bridge wraps".
- [x] 8.5 Update the `rembric-plugin-development` skill's `e2e-walkthrough.md` where it drives the transport: the expected diagnostic line still holds (the `[rembric-bridge]` prefix is preserved), but the `opencode.json` snippet and the `node <path>` invocation change.
- [x] 8.6 Confirm `git ls-files apps/plugin/` still shows exactly one copy of each shared resource — in particular exactly one `rembric-dotenv.mjs`, at its new path.
- [x] 8.7 Reconcile the Pi plugin delta so its published shared-parser contract names `apps/plugin/mcp-bridge/rembric-dotenv.mjs`, preserving the published scenarios while documenting the moved import.
      Evidence: `specs/pi-plugin/spec.md` preserves the published requirement and scenarios and adds the canonical-path amendment; `apps/plugin/.pi-plugin/index.ts` imports the moved module.
- [x] 8.8 Reconcile the development-environment delta for the bridge package's explicit ESLint and Vitest paths, preserving the published CI/test contract and removing the deleted launcher from the live lint requirement.
      Evidence: `specs/development-environment/spec.md` carries the published requirement/scenarios and the bridge-path amendment; `eslint.config.js` and `apps/server/vitest.config.ts` include `apps/plugin/mcp-bridge/`.
- [x] 8.9 Update contributor plugin guidance to the canonical dotenv path and package/config-hook transport design.
      Evidence: `CLAUDE.md`, `AGENTS.md`, `rembric-plugin-development/SKILL.md`, and `references/per-client-gotchas.md` no longer prescribe the deleted launcher or old dotenv path.

## 9. End-to-end validation per the plugin skill

Follow `.agents/skills/rembric-plugin-development/references/e2e-walkthrough.md`. Each result is recorded here with its provenance — host, arms, date — not attributed to the suite.

- [ ] 9.1 **Claude Code (real).** Repeat arms A–D from phase 1 against the built package installed the way a user gets it, through the real manifest. Same isolation rails as 1.3.
- [ ] 9.2 **opencode, fresh install (real).** `PLUGIN_SRC`/`BIN_SRC`/`MCP_BRIDGE_SRC` install from the local checkout, paste the printed npx snippet, then `opencode mcp list --print-logs --log-level DEBUG`. Expect the `[rembric-bridge]` diagnostic, a successful connect, and a tool count matching the server's `registerTool` call sites (**derive** the count from `apps/server/src/mcp/server.ts`; do not hard-code it). Then restart the server mid-session and confirm the recovery.
- [ ] 9.3 **opencode, existing launcher config (real).** Start with an `opencode.json` naming `~/.config/rembric/bin/rembric-bridge.mjs`, run the new installer without touching the file, and confirm the plugin config hook connects through the exact npx package command in memory.
- [x] 9.4 **Codex CLI — record as UNVERIFIED with the reason** for the session-level arms, per the standing hazard. The `npx`-resolution question is separately answered in 2.4 without credentials; say which parts are verified and which are not, rather than collapsing them.
      Evidence: `apps/plugin/mcp-bridge/README.md` records Codex session arms as unverified with the account-level connector hazard and distinguishes the manifest/bin-resolution evidence.
- [ ] 9.5 **Hermes — config-block only.** Verify the documented `mcp_servers.rembric` block works by running it by hand against the dev stack, including that `REMBRIC_PROJECT_SLUG` resolves and that a per-directory `.rembric` overrides it.
- [ ] 9.6 **Windows — record as UNVERIFIED with the reason** (no Windows CI; the package adds no platform-specific code, but that is an argument, not a measurement).
- [x] 9.7 Clear leftover `active` session rows between e2e runs. The resolver returns "sole match or nothing", so one un-ended session from an earlier arm silently poisons attribution in every later one — a probe already failed this way once and the failure was mistaken for a defect.
      Evidence: after the Docker bridge smoke, both seeded active rows were ended through `/api/demo/sessions/:id/end` with the demo-writer token; the database query returned zero active rows.

## 10. Verification and release

- [x] 10.1 `pnpm run typecheck` · `pnpm run lint` · `pnpm test` all green. `pnpm run eval` is not required — retrieval is untouched.
      Evidence: 2026-08-15 — `pnpm run typecheck`, `pnpm run lint`, and `pnpm test` passed: 147 Vitest files passed plus 1 skipped, 2,818 tests passed and 10 skipped; Hermes 90 Python tests passed.
- [x] 10.2 `pnpm run check:spec-provenance` green (it compares `origin/main...HEAD`; CI is the gate).
      Evidence: `spec-provenance: ok (origin/main...HEAD)`.
- [x] 10.3 Run the `rembric-tui-installer-e2e` playbook. The installer itself is unchanged, but the per-client install scripts it orchestrates are not — `install.test.ts` headless plus the local install round-trips.
      Evidence: `pnpm run e2e:installer` passed all 95 headless tests and `sh -n`; local opencode install/uninstall, Pi headless/status/update, and server token-generation round-trips passed in throwaway HOME/CWD directories.
- [x] 10.4 **Real Docker smoke against pre-existing seeded data.** Standing requirement for anything touching MCP or production behaviour: bring up the stack, exercise the plugin path, and confirm the corpus is intact afterwards. Remember `dev:docker:up` reseeds on boot — smoke a corpus you created inside the same run.
      Evidence: `rembric-dev` was healthy with mounts from `/root/rembric`; the local bridge discovered 24 tools, saved a memory, returned `not_found` for a fabricated id, recovered after `docker restart rembric-dev` via fresh initialization, then saved a post-restart control row. The seeded corpus remained 20 projects/3 tokens/35 memories before the post-restart control write (36 afterward).
- [ ] 10.5 **Operator-only: verify the first release end to end.** After the release PR merges, confirm (a) the `plugin-vX.Y.Z` tag, (b) `@rembric/mcp-bridge@X.Y.Z` published **with provenance**, (c) all three pin sites at that commit naming exactly `X.Y.Z`, and (d) `@rembric/pi@X.Y.Z` published in the same run. The window in which `main` names an unpublished version (D11) closes here; a failed publish job is release-blocking.
- [ ] 10.6 Update issue #348 with the outcome: the client half of its blocker is now fixed, and record whether that changes the standing for #328's eviction (it does not re-enable it — that is a separate change).

## 11. Parallel, non-blocking

- [ ] 11.1 **Upstream PR to `geelen/mcp-remote`**, referencing issue #106: implement the spec's session-terminated → re-init MUST. Written in English. Independent of every other phase — it neither blocks nor is blocked by this change, and it is the only path by which `@rembric/mcp-bridge` could ever be retired. Link the PR here when opened.

## 12. Deferred and explicitly rejected — recorded so they are not silently lost

- [x] 12.1 Record as **deferred, own change**: server-side client disambiguation using the now-exact `clientInfo`, which would revisit `findActiveForTransport`'s give-up-under-ambiguity rule (`agent-sessions-repository.ts:144-154`) and make the reverted bridge-instance-id mechanism (`ea71092`/`d1ff2c9`) permanently unnecessary. Load-bearing: it touches scope and session attachment.
      Evidence: `design.md` D17 records client disambiguation as deferred to an own change because it touches scope and session attachment.
- [x] 12.2 Record as **deferred, own change**: a bridge-answered `roots/list` for hosts that do not advertise the capability (#329). It would let path-less `/mcp` resolve correctly without `.rembric`, but it means the bridge _answering_ a request rather than forwarding one — a real widening of its charter that forfeits part of D15's argument.
      Evidence: `design.md` D17 records bridge-answered `roots/list` as deferred to an own change.
- [x] 12.3 Record as **deferred, operator cleanup**: deleting stale opencode launcher files is not required because the measured config hook replaces their commands in memory; the installer leaves user-owned files alone.
- [x] 12.4 Record as **deferred, own change**: re-opening #328's transport eviction. This change fixes the client half of its blocker; the decision to evict again is separate.
      Evidence: `design.md` D17 records reopening transport eviction as a separate deferred change.
- [x] 12.5 Record as **deferred**: sending an explicit `DELETE` on shutdown to terminate the session server-side (design Open Question 2). Permitted by the spec and useful to #328, but a second unproven mechanism does not belong behind this change's gate.
      Evidence: `design.md` D13/D17 and Open Question 2 record explicit shutdown `DELETE` as deferred.
- [x] 12.6 Record as **rejected**: bundling the SDK to reach zero runtime dependencies (D12) — it hides the dependency from a consumer's advisory tooling. Rejected: pinning at N−1 to close the tag-versus-publish window (D11) — it permanently guarantees we ship code other than what we just released. Rejected: accepting an optional positional URL override (D7) — a second way to express the endpoint that diverges the moment one changes. Rejected: having `install.sh` rewrite an existing `opencode.json` (D6) — the measured config hook provides compatibility without mutating user-owned config. Rejected: the package names `@rembric/mcp-bridge` (too vague) and `@rembric/mcp-bridge` (does not say what it bridges).
      Evidence: `design.md` records the SDK bundling, N−1 pin, URL override, config rewrite, and naming alternatives as rejected.
- [x] 12.7 Record the correction to the archived note at `openspec/changes/archive/2026-07-12-fix-cross-session-misattribution/design.md:38`. It states that `mcp-remote` "always declares itself as `{name: 'mcp-remote', version}`"; measured at the pinned 0.1.38 the behaviour is a **rewrite** — `"<host> (via mcp-remote 0.1.37)"` — so the host name survives as a prefix, and the appended version is itself wrong. The conclusion (the field is not the host's name; no exact match against it is safe) stands. A future change reasoning from that note would otherwise assume a stricter failure than exists.
