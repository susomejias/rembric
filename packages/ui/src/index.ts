// Placeholder entry point. This package will hold the shared React components
// extracted from the server dashboard during the Next.js port; it has no
// dependents yet, so this single export only keeps the module non-empty.
export const PACKAGE_NAME = '@rembric/ui';
