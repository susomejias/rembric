# Pi subagents

The project version-controls the subagent definitions in `subagents/`, but model routing is personal. Copy the template and choose the provider, models, and effort available to your account:

```bash
cp .pi/subagents.example.json .pi/subagents.json
```

`subagents.json` is ignored by Git. Restart Pi or run `/reload` after changing it.
